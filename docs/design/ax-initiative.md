# The AX initiative — Accessibility-driven ops: state, evidence, and the forward plan

Status: **ACTIVE ROADMAP** (written 2026-07-15, pre-compaction — this document is the durable plan; a fresh agent picks up HERE). Companions: [ui-vector.md](ui-vector.md) (shipped architecture), [../lab/uic1-certification.md](../lab/uic1-certification.md) (certification verdicts + the AX addressing catalog + the full Repeat-dialog field map), [../lab/axvm1-accessibility.md](../lab/axvm1-accessibility.md) (the VM grant recipe + background/lock verdicts), [../lab/ui-certification-runbook.md](../lab/ui-certification-runbook.md).

## Where things stand (2026-07-15)

Seven ui-vector ops shipped (PR #137), certified in-VM by UIC1 (PR #140): **lab-certified** — `todo.make-repeating`, `todo.reschedule-repeat`, `todo.pause-repeat`, `todo.resume-repeat`, `todo.convert-to-project`. **Blocked/uncertified** — `todo.stop-repeat` (the Stop popover is card-only; the card opens ONLY via mouse double-click; AXPress/Return fail) and `heading.convert-to-project` (`things:///show?id=<heading>` does not select headings). Both refuse fail-closed. Rule vocabulary is deliberately minimal (frequency + interval); the FULL dialog field map (day-of-week, monthly `last`/nth-weekday, yearly month, after-completion, Ends bounds, reminders, deadline/start-earlier) is captured in uic1-certification.md and ready to build against.

## Mike's rulings (2026-07-15)

1. **The project-view `…` menu is the expected unblocker for repeating-PROJECT ops** (his screenshots): `things:///show?id=<project>` opens the project view, whose title-adjacent `…` button opens a menu carrying **Repeat…** (plus Complete/When/Tags/Deadline/Move/Duplicate/Delete/Share). No sidebar row selection needed. Probe it (UIC2) before building.
2. **Create-repeating-project = a two-step emulation**: create a plain project via the quiet vectors, then promote it via `…` ▸ Repeat…. (There is no File ▸ New Repeating Project — UIC1.) Low usage expected; build for completeness.
3. **`stop-repeat` may simply be DROPPED** rather than fought for — especially given the crash below. Decide after UIC2.
4. **CRASH REPORT (Mike, live GUI, 2026-07-15 — unconfirmed, probe it)**: promote a project to repeating via `…` ▸ Repeat…, then `…` ▸ (repeat bar?) Stop + confirm, then attempt to SELECT the demoted project → **Things crashes**. Reproduce in a VM, capture the `.ips` from DiagnosticReports, characterize (crash on selection only? does the demoted project survive relaunch? data loss?), and file in oddities §7 (crash catalog) for the CC report.
5. **Sidebar AREA reorder is the next AX target** (project sidebar order already shipped via the bounce protocol; area order has NO automation spelling — P6/O13). Edge case to harden: a long sidebar SCROLLS — probe how scrolling interacts with drag simulation (AXScrollToVisible before drag; auto-scroll when the drop target is beyond the viewport).

## Inventory: locale reliance (UIC1 evidence)

Nearly ALL leaf actions are **title-pinned English** today: every menu item (Items menu, Repeat submenu, context menus, presumably the `…` menu) shares the generic `performCommand:` identifier; Repeat-dialog controls carry only volatile `_NS:NNN` numbers (the interval field's id even changes with the frequency). Locale-INDEPENDENT: the confirm-sheet buttons (`action-button-1` — the only semantic AXIdentifiers found), structural addressing (roles, `window 1`/`sheet 1`, index-in-parent), and the menu-bar top-level ids (`items`/`file`/`edit` — but not their leaves). Doctrine: **pin the app to English on the deployment machine** (`defaults write com.culturedcode.ThingsMac AppleLanguages -array en`) + the recipe canary's proven fail-closed refusal under locale drift (German negative test, UIC1-c). Optional follow-up: an explicit preflight locale check in the driver.

## Inventory: background operation (what works while Things is NOT frontmost)

| Tier | Mechanism | Evidence |
|---|---|---|
| **Proven background + under-lock** | menu-bar AX presses | AXVM1-d: pressed with Finder frontmost (focus stayed on Finder) and under a real `sysadminctl` lock |
| **Proven background + no focus steal (dialog only)** | Repeat **dialog sheet** drive (select-popup, interval, OK, confirm-sheet `action-button-1`) | UIC2-a: drove weekly + OK with Finder frontmost, frontmost stayed Finder |
| **Proven background + no focus steal (project row-select → make-repeating)** | project **`AXSelectedRows` set** (settable pure-AX) → Items ▸ Repeat… press → Repeat editor drive | UIC4-a/b: selected the project row + drove make-repeating with Finder frontmost, frontmost stayed Finder; backgrounded the editor DETACHES to a top-level `AXUnknown` window (dual-form addressing). `project.make-repeating` joins the least-disruptive backgroundable tier — a better profile than the reschedule/pause/resume ops (still mouse-synthesis) |
| **Foreground-bound (resolved by UIC2)** | the `…`-button press + `…`-menu / repeat-bar **popover** item selection | UIC2-a: the `…` button is **not an AX node**; menu/popover items are AX-readable but **inert to `AXPress`** → need a synthetic MOUSE click (screen-coordinate, visible-bound). `open -g things:///show?id=<project>` reveals+selects **without** activating (frontmost stayed Finder) |
| **Foreground-bound, PERMANENTLY** | anything needing synthetic MOUSE: card double-click (todo.stop), drag-reorder (areas/tags) | ~~the native helper may change this tier: `CGEventPostToPid` posts to the process, not to focus — background plausible~~ **FALSIFIED (NATIVE1, 2026-07-15): `CGEventPostToPid` is INERT for Things' mouse hit-testing; the working path is the global HID event tap (`CGEventPost(kCGHIDEventTap)`), which is foreground+unlocked-only (no background, no under-lock). Mouse synthesis stays the most-disruptive tier.** |

## The plan — three probe campaigns, then builds

### UIC2 (VM campaign) — ✅ DONE 2026-07-15 ([lab/uic2-project-menu.md](../lab/uic2-project-menu.md))
(a) **Project `…`-menu discovery + drive** — ✅ **feasible, but HYBRID not pure-AX.** The `…` **button is not an AX node** (drawn inside the title text-area frame; the Items menu bar has no Repeat for a shown project; `AXShowMenu` on the title gives a text-edit menu). The `…` menu opens only by a **synthetic mouse click**; once open its items are AX-**readable** (by `description`: Complete Project/When/Add Tags/Add Deadline/Move/**Repeat…**/Duplicate/Delete/Share) with resolvable `AXPosition` **but INERT to `AXPress`** — actuation needs a synthetic click. The Repeat **dialog** it opens is a **sheet, byte-identical to the to-do dialog** (freq `_NS:116`, OK `_NS:164`) and **fully AX-drivable, backgrounded, no focus steal**. All four project ops DB-verified: **make** (new template `fu`=256 + instance, identity replacement), **reschedule** (repeat-bar ▸ Change…, `fu` 256→8, identity preserved), **pause/resume** (repeat-bar ▸ Pause↔Resume). Repeating-project reschedule/pause/resume/**stop** live in an **always-visible repeat bar** in the project view — **no card double-click** (unlike to-dos). `open -g` reveals+selects **without activating** (frontmost stayed Finder). `…`/popover navigation is **foreground-bound** (synthetic mouse); the dialog sheet is backgroundable.
(b) **Two-step create-repeating-project** — ✅ validated (`things:///add-project` → promote via (a) → template + spawned instance).
(c) **CRASH1** — ✅ **REPRODUCED, deterministic 2/2.** Stop-repeat a project → select the demoted project → `EXC_BREAKPOINT`/SIGTRAP (Swift trap, `ThingsModel`). **Transient** (gone after relaunch), **no data loss**; crash is on the SELECT not the Stop. Two `.ips` banked; oddities §7 **C5**. → any `project.stop-repeat` op must NOT reveal the demoted result.
(d) **`todo.stop-repeat` re-check** — ✅ **no new surface** (Items ▸ Repeat still = Reschedule/Pause/Show Latest, no Stop; the project repeat bar has no to-do analog — to-do Stop stays card-double-click-only). **Recommend DROP** (ruling #3).
**Net for the builds:** project repeat ops are buildable but depend on a **mouse-synthesis primitive (NATIVE1)** to open+navigate the custom `…` menu / repeat-bar popover (target the AX-resolved item positions, never `AXPress`); the dialog half rides the existing AX machinery. `project.make-repeating` additionally needs the `…` **button** position, which has no AX node (title-length-dependent) — the reschedule/pause/resume/stop ops all use the AX-resolvable repeat bar instead.

### AXDRAG1 (VM campaign) — ✅ DONE 2026-07-15 ([lab/axdrag1-reorder.md](../lab/axdrag1-reorder.md))
Hardened the NATIVE1 area-drag into an arbitrary-rank recipe and scoped tag reorder. **Verdicts:** **(a) precision placement WORKS** both directions incl. to-first/to-last (DB-verified via `TMArea."index"`). Drop geometry — sidebar is 40px/slot (h24 row + h16 spacer); **upward drags use static target coords, downward drags subtract one slot-height** because lifting the source collapses its slot (lower rows shift up 40px). Index rewrite: first drag renumbers the WHOLE list (sparse/irregular); each later drag reassigns ONE index (intermediate, not exact midpoint) and **may renumber a neighbour rather than the dragged row** → undo captures the full order. **(b)** scroll-wheel synthesis (`CGEventCreateScrollWheelEvent`→HID tap) reveals off-viewport rows; the sidebar **AXScrollBar `AXValue` (0–1) is readable** so scroll distance is computable. **(c) mid-drag auto-scroll works bidirectionally** but is time-based → prefer pre-scroll for a deterministic landing (full list exceeds one viewport). **(d)** areas can't nest (no structural hazard on a mis-drop — only wrong rank); **Escape mid-drag aborts cleanly** (the op's abort primitive). **(e) TAG reorder FEASIBLE** (Window▸Tags AX menu press; `AXDialog`; `TMTag."index"`+`parent`): **drop-on-centre re-parents, drop-on-boundary reorders** — a mis-aimed drop silently NESTS, and tag names are not AX-exposed (address by position). **(f) reversible** via pre-rank capture + drag-back (order restores; index values don't). Note the divergence from NATIVE1: AppleScript-seeded sidebar rows carry an EMPTY `AXDescription` — identity is via descendant `AXStaticText`. This was a PROBE campaign; the production `area.reorder-sidebar`/`tag.reorder` ops are not built.

### NATIVE1 (spike; gates AXDRAG1's drag path and todo.stop) — ✅ DONE 2026-07-15 ([lab/native1-spike.md](../lab/native1-spike.md))
The row-selection/mouse-synthesis primitive. ~~Try in order: (1) **JXA ObjC bridge** ... (2) a small compiled Swift helper.~~ **VERDICT: Tier 1 (JXA ObjC bridge) wins — no Swift helper.** `osascript -l JavaScript` resolves AX frames (`AXUIElementCopyAttributeValue` for `AXPosition`/`AXSize` via `ObjC.castRefToObject` + `CFCopyDescription`) and synthesizes mouse via `CGEventCreateMouseEvent`. **Double-click → the card opens** (repeat bar reachable → `todo.stop-repeat` unblocked). **Drag → an area reorders** (writes `TMArea."index"`, 0 → −448). BUT ~~`CGEventPostToPid`~~ is inert for Things — the working post is `CGEventPost(kCGHIDEventTap)`, which is **foreground+unlocked only** (backgrounded: NO; under-lock: NO — opposite of the AX menu path). **TCC: the Accessibility grant alone authorizes CGEvent posting** (no second consent class; no PostEvent/ListenEvent row created). `AXScrollToVisible` UNAVAILABLE on rows (−25206) → AXDRAG1 scroll edge case needs synthesized scroll-wheel events.

### Builds (after probes; each rides the existing pipeline/gating/certification machinery)
1. **Repeating-project ops** — ✅ **BUILT + LAB-CERTIFIED 2026-07-15** for `project.reschedule-repeat` / `project.pause-repeat` / `project.resume-repeat` ([uic3-build-certification.md](../lab/uic3-build-certification.md), UIC3-b). Rode the new **mouse-synthesis driver primitive** (`click-element`: resolve an element's AX frame via the NATIVE1 JXA bridge → HID click at its center, with a post-click assertion and Escape-abort; static mouse targets canaried like AX presses). The repeat bar = `text area 2` of the header cell; the popover is a **separate `AXUnknown` window** (UIC3 discovery corrected the provisional `pop over 1`), items clicked at their AX-resolved frames; the Repeat sheet stays pure-AX. DB deltas match UIC2-a (pause sets paused + clears next; resume round-trips; reschedule `fu` 256→8, identity preserved). **`project.make-repeating` + the two-step create composite are ~~BLOCKED-ON-RULING~~ PATH-IDENTIFIED (PURE-AX)** — **UIC4 (2026-07-15) corrected UIC3-a**: a project **does** render as a **selectable content row** in the area/Someday views, `AXSelectedRows` is **settable by pure AX** (`AXUIElementIsAttributeSettable` → YES; set returns `kAXErrorSuccess`, selection lands), and with the project so selected **Items ▸ Repeat… is present and enabled**. So make-repeating is drivable **100% pure-AX** (`select-row → Items ▸ Repeat… → sheet`), **background-capable with no focus steal** — a *better* profile than the reschedule/pause/resume ops above (which still ride the mouse-synthesis `…`/repeat-bar popover). DB-verified: identity replacement, new template `fu`=256 + spawned instance, **area preserved** (`start` normalized to the repeating bucket). The one taxonomy gap: an area-less **anytime** project renders as a *header* in the Anytime view (no row) → reach it via a **right-click NSMenu** (real menu, keyboard-typeahead, foreground-bound) or a **cleanup-free Someday coercion** (`update-project?when=someday` quiet vector) then the pure-AX path. See [uic4-project-selection.md](../lab/uic4-project-selection.md). ✅ **BUILT + LAB-CERTIFIED 2026-07-15 (UIC5, all 7 cases a–g + gating DB-verified through the production CLI):** `project.make-repeating` rides a new **`select-row`** driver primitive (walk the content table's rows, `select` each row, read back `name of selected to dos` — coordinate-free, readback-verified) + **dual-form dialog addressing** (`pathCandidates`: attached sheet OR detached `AXUnknown` window). UIC5 corrected two provisional paths: the select-row primitive uses the row **`select` action** (the table `AXSelectedRows` *attribute-set* is a silent no-op via System Events — UIC4-a's settability was proven with the ObjC-bridge NSArray set, a different API) and the detached editor's interval field nests in **group 1** like the sheet. The `runMakeRepeatingProject` orchestrator does taxonomy dispatch (area/someday direct; anytime coerced to Someday first) and refuses fail-closed via `H-PROJECT-REPEAT` (already-repeating / trashed / logged / duplicate-title row ambiguity). The composite **`project.create-repeating`** (`runCreateRepeatingProject`) is the two-step `project.add`→promote (seeded Someday or an area; non-atomic — the create persists if the promote refuses). The right-click NSMenu was NOT used (AX-invisible; keyboard-typeahead-only, foreground-bound) — the coercion path is preferred.
2. **Full rule vocabulary** for make/reschedule (to-do AND project): build directly against the UIC1 dialog field map — day-of-week toggles, monthly `last`/nth-weekday, yearly month, after-completion mode, Ends bounds, reminders checkbox, deadline/start-earlier offset. Extend the op params + reversibility notes (a richer vocabulary makes reschedule-undo *more* faithful — revisit its irreversible classification). *(Not started.)*
3. **`area.reorder-sidebar` + `tag.reorder`** — ✅ **AREA OP BUILT + LAB-CERTIFIED 2026-07-15** ([axdrag2-reorder-certification.md](../lab/axdrag2-reorder-certification.md), AXDRAG2-c): `area.reorder-sidebar` (CLI `things area reorder-sidebar`, MCP `move_area_in_sidebar`) rides the standard pipeline (ordering delta on a new `area-index` key, full-order pre-capture undo, H-UI-DRIVE two-key gate). Ladder per the amended ruling: rung 1 (pre-scroll + one drag) + rung 3 (multi-hop floor: per-hop DB asserts, strict-progress guard w/ one retry, derived hop cap, benign positional abort) certified; **rung 2 (scroll-while-held) built + probe-certified (AXDRAG2-a GO: mid-drag wheel delivery AND fresh mid-drag AX frames, both directions) but ships DISABLED** — held travel ≳1.5 viewports triggers the sidebar **AX-mirror ghost bug** (rows lose their name or vanish from the AX tree until relaunch; oddities §9; `THINGS_UI_DRAG_LADDER=held-scroll` re-enables for lab work). AXDRAG2-b (chevron, record-only): exists as a persistent `Source Toggle Template` AXImage node but exposes no AX actions. **`tag.reorder` BLOCKED-ON-JUDGMENT (AXDRAG2-d — do not ship)**: tag rows expose no names (placeholder text only) AND the DB→row mapping is unverifiable whenever `TMTag."index"` ties exist — which is the NORM (every never-dragged tag sits at 0, and unlike areas there is no safe way to materialize tag indexes without an already-aimable reorder); a mis-aim silently re-parents (AXDRAG1-e) with no reliable positional recovery. Unblockers: a name handle on tag rows, or a validated tie-break oracle for the Tags-window order.
4. **Drop the stop family** — ✅ **DONE 2026-07-15**: `todo.stop-repeat` REMOVED from the catalog/CLI/MCP/manifest (shipped blocked-uncertified in #137, never worked — no reachable to-do surface, UIC2-d). **No `project.stop-repeat` built**: the project Stop works but stopping-then-selecting the demoted project crashes Things (CRASH1 / oddities §7 C5); parked, and any future build must never auto-reveal the demoted result.

## Standing constraints (unchanged)
Two-key gating (`ui.enabled` + `--dangerously-drive-gui`), fail-closed everywhere (canary preflight, wait-for-element, no coordinates in production code — mouse synthesis via NATIVE1 targets AX-RESOLVED element positions, never guessed pixels), certification manifest discipline, VM lab per the AXVM1 grant recipe, English locale pin on targets, prod DB untouchable.
