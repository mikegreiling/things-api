# Decisions register — rulings that are settled, so they stay settled

One line per ruling: **date · ruling · evidence**. This register exists to prevent relitigating decisions that were already made and paid for — a "no" is as expensive to re-argue as a "yes". **Append-only in spirit**: add a ruling when one is made; supersede (never silently delete) an entry only when a later ruling explicitly overturns it, naming the reversal.

**Completed WORK is not recorded here — git is the archive.** This file holds *decisions* (what will NOT be built, which shape won, which knob is canonical), not shipped features (those live in CHANGELOG + the living docs). The short-horizon queue is [up-next.md](../up-next.md); long-horizon strategy is [roadmap.md](../roadmap.md).

## Product / capability rulings

- **2026-08-19 · CLI-as-sole-interface is skill doctrine.** An agent with the things-cli skill installed must never construct raw `things:///` URLs, AppleScript, or Shortcuts invocations (reads or writes) — routing around a CLI refusal/failure bypasses validation, verification, undo/audit, and the session gates, and is nearly always a reportable bug in disguise. Encoded in SKILL.md ("The CLI is the only interface") + references/errors.md; the acknowledgment-flag system is the only sanctioned override path. (Maintainer ruling after observing a live agent improvising raw calls.)
- **2026-08-15 · Fixed-repeat first occurrence (issue #476) — DRIVE the "Next:" field, do not refuse. SUPERSEDES the same-day ANCH1 refusal ruling below.** ANCH2's live census found the Repeat dialog's "Next:" first-occurrence field (ANCH1 missed it — its census failed and it fell back to the UIC6 field map). The field is editable and honored, so the promote verbs DRIVE it with the requested first occurrence (source `when` for make-repeating / `--when` for add-repeating / reschedule) and derive the weekly weekday from it; post-drive they VERIFY the landed first occurrence fail-closed. `H-REPEAT-ANCHOR` is DELETED. Monthly/yearly phase is now honored too (Next drive is frequency-agnostic). `set-datetime` targets its control deterministically (Next/Ends/reminder), which also fixes §8v and makes `--reminder` drivable (UIC6-g retracted). Evidence: [lab/anch2-next-field.md](../lab/anch2-next-field.md).
- **~~2026-08-15 · Fixed-repeat wrong-phase anchor (issue #476) — FAIL-CLOSED REFUSAL (branch 3).~~ SUPERSEDED 2026-08-15 by the Next-drive ruling above (ANCH2).** ANCH1 believed there was no first-occurrence control and no correction vector, so the promote verbs refused (`H-REPEAT-ANCHOR`) a wrong-phase interval>1 date. That premise was false (the "Next:" field exists and is drivable); the refusal is replaced by driving Next. Kept here as the reversed record. Evidence: [lab/anch1-repeat-anchor.md](../lab/anch1-repeat-anchor.md) → corrected by [lab/anch2-next-field.md](../lab/anch2-next-field.md).
- **2026-08-15 · Repeat exhaustion is a CURSOR signal, and the decoded count is the immutable TOTAL (RRX1).** `--ends-after N` writes an IMMUTABLE `rc=N` that never decrements; the app counts spawns in `rt1_instanceCreationCount` and ends the series by clearing the next-occurrence cursor. So `templateStatus` reads exhaustion from the cursor (a FIXED template with no next occurrence is `ended`; after-completion cursor-NULL stays `waiting`), and the decoded field is `occurrenceCount` (the configured total, not "remaining"). The over-count of a partway ends-after PROJECTION (horizon > 1, off by the already-spawned count because the pure rule math has no `icCount`) is LEFT as a documented best-effort limitation — NOT worth threading `icCount` through the read model/wire. Evidence: [lab/rrx1-rc-and-reminder.md](../lab/rrx1-rc-and-reminder.md).
- **2026-08-15 · `todo.clear-dated-reminder` REFUSES a repeating template — the refusal stands (RRX1, reason corrected).** On 3.22.12 a template's rule reminder IS a real `reminderTime` column value, but NO automation surface clears it in place (Shortcuts no-op / AS 301 / URL crash — DB-verified), so the op refuses a repeating template outright (`H-REPEAT-SCHEDULE`, "change it in the app's repeat editor"). This corrects RCLEAR's mechanism (it relied on `H-NO-REMINDER` firing while the column was NULL on 3.22.11); the conclusion (no in-place clear) is unchanged. Evidence: [lab/rrx1-rc-and-reminder.md](../lab/rrx1-rc-and-reminder.md); corrects RCLEAR (oddities §8b).
- **2026-08-15 · After-completion + ANY end bound — REFUSE.** After-completion mode doesn't expose the Ends control the fixed schedule does: BOTH `ends:on-date` (drive dies looking for "on date") AND `ends:after` (drive silently fails to set the count) fail to apply (ANCH1-B FIX4). `assertRepeatRule` refuses `afterCompletion` + any non-`never` ends before any GUI side effect; no-end (the default) is fine. Whether after-completion has a reachable Ends control at a different recipe index (→ un-refuse + recipe fix) is an unprobed follow-up (up-next). Evidence: [lab/anch1-repeat-anchor.md](../lab/anch1-repeat-anchor.md) (issue #476 item 5).
- **2026-07-09 · Headings are ALWAYS first-class — there is NO flatten/dual mode.** The flatten case rested on "headings barely work without Shortcuts" + "their index makes flat reads incoherent"; both premises fell (AppleScript unblocked rename/archive/delete/reorder + placement; v0.6.0 hid `index`/`todayIndex`). Reads stay heading-aware; only `heading.add`-in-an-existing-project is capability-gated. Evidence: [lab/heading-research.md](../lab/heading-research.md).
- **2026-07-09 · AppleScript project-schedule vector — DOCUMENT-ONLY, do not wire.** `schedule to do id <PROJECT>` works via AppleScript (P14-A3), but URL `update-project?when=` is validated and sufficient; a second vector adds compile/matrix surface with zero new capability. Evidence: capability-matrix project-update row.
- **2026-07-14 · Repeating-to-dos hide-flag in `upcoming` — RULED NO.** The `── Repeating To-Dos ──` section stays always-on; the "leftover budget" idea was already the shipped `--limit` behavior. Evidence: CHANGELOG (v0.8.0 window work).
- **2026-07-15 · Tag reorder — ABANDONED by decision (not blocked).** Risky/potentially destructive (silent re-parent hazard, unverifiable nameless positional rows + ubiquitous `TMTag."index"` ties) for little utility; the bar to reopen is a name handle or a validated tie-break oracle. Evidence: [lab/taglab-probes.md](../lab/taglab-probes.md) (TAGORD1), [lab/axdrag2-reorder-certification.md](../lab/axdrag2-reorder-certification.md).
- **2026-07-15 · Drag-ladder rung 2 (scroll-while-held) — SHELVED.** The production ladder stays rung 1 + rung 3; the oddities §9 AX-mirror blanking bug blinds the closed-loop verification the doctrine requires. Revisit only if CC fixes the mirror bug, a non-relaunch heal is found, or a certified travel cap lands. Evidence: [design/ax-initiative.md](ax-initiative.md) build item 3, [lab/axdrag2-reorder-certification.md](../lab/axdrag2-reorder-certification.md).
- **2026-07-15 · `todo.stop-repeat` DROPPED; no `project.stop-repeat`.** The to-do Stop popover is card-only (opens only via mouse double-click, no menu/AX surface — UIC2-d); the project Stop works but stop-then-select CRASHES Things (oddities §7 C5). Evidence: [design/ax-initiative.md](ax-initiative.md), [lab/uic2-project-menu.md](../lab/uic2-project-menu.md).
- **2026-07-23 · Anytime aggregate reorder (`reorder to dos in list "Anytime"`) — DO NOT SHIP.** Non-deterministic (five identical calls → five non-converging orders) AND destructive (strips `area` → NULL on every loose to-do touched, oddities §9c). Area'd loose anytime rows reorder cleanly via the AREA scope; area-less ones use the ANYBNC bounce. Evidence: [lab/anyord-results.md](../lab/anyord-results.md).
- **2026-07-23 · SYNCLAT post-write sync-nudge — NOT JUSTIFIED.** Every write vector advances the sender's `BSSyncronyMetadata` last-attempt signal within ~2–3 s (Things syncs on-change, no periodic heartbeat); the minutes-of-latency Mike saw is receiver-side (no APNs push-wake in a VM). No no-push bug to mitigate. Open residue (on-hardware repro of the sender-side stale panel) PARKED INDEFINITELY 2026-08-14 (see the wait-and-see ruling below). Evidence: [lab/synclat-results.md](../lab/synclat-results.md).
- **2026-08-14 · SYNCLAT on-hardware repro — PARKED INDEFINITELY (wait-and-see, maintainer).** The sender-side stale-panel observation Mike originally reported has not recurred since; the sync-trigger model is otherwise fully characterized (writes push within ~2–3 s; cross-device latency is the RECEIVER'S pull/APNs cadence, not a sender delay — SYNCLAT + SYNC2B). No further probing is scheduled — revisit ONLY if the maintainer observes the stale panel again on real hardware. The sync-trigger model itself is documented in the skill + contracts references. Evidence: [lab/synclat-results.md](../lab/synclat-results.md), [lab/sync2b-durable-account.md](../lab/sync2b-durable-account.md).
- **2026-07-26 · `TMTombstone` deletion-tracking — DON'T BUILD.** `TMTombstone` is written only for `leavesTombstone=1` rows (repeating-template lineage), NEVER ordinary user deletions, which propagate tombstone-lessly via the Syncrony change-log; a `changeKind:"deleted"` feed on it would silently miss every normal deletion. If deletion tracking is ever needed: a scoped member-set snapshot-diff (the watch primitive). Evidence: [lab/tomb1-results.md](../lab/tomb1-results.md).
- **2026-07-31 · `today ok` / banner-acknowledge — WILL NOT IMPLEMENT.** The banner OK's in-place `start 2→1` (+ `startDate:=deadline`) is not URL-reproducible byte-faithfully (BANNERACK), the OK is not AX-reachable (custom NSView), and it is purely presentational (provisional rows are already full Today members, honestly modeled by the wire `provisional` field + TTY `•` pip); a clamp-to-today approximation was explicitly REJECTED. Revisit only if CC exposes an acknowledge surface. Evidence: [lab/banner1-research.md](../lab/banner1-research.md), [lab/sit3-arrival-evening-lists.md](../lab/sit3-arrival-evening-lists.md).
- **2026-08-11 · `todaySince` (a "time in Today" read axis) — SCRAPPED.** A GUI cross-cohort drag ADOPTS the drop-neighbour's entry cohort (single-row `tiRef` write, older or newer), so `todayIndexReferenceDate` is not a faithful "entered Today" timestamp and cannot back a duration signal. Evidence: [lab/todwire-partial-wires-today.md](../lab/todwire-partial-wires-today.md) (TODWIRE §9y).
- **2026-08-11 · Promote-to-repeating REIMPLEMENTED as promote-via-clone; first-class clone ops exposed (maintainer).** `make-repeating` becomes `clone(X) → native-promote(the clone) → trash(X)` — recoverable (X survives in trash) and deterministic (the RSIM fate lottery lands on a disposable row; laws confirmed backdating-inert on minted clones, CLONE verdict B). `todo.clone` / `project.clone` ship standalone (2026-08-12), and one add→promote composite closes §0.2/§0.3 (`todo.add-repeating`, full-vocab `project.add-repeating`). Design of record: [promote-via-clone.md](promote-via-clone.md); evidence: [lab/clone-fidelity-and-template-trash.md](../lab/clone-fidelity-and-template-trash.md).
- **2026-08-13 · Promote undo = automated trash-both + restore (RATIFIED, maintainer).** Undo of a `make-repeating`/`add-repeating` promote trashes BOTH the minted template and its spawned current instance (no native cascade exists — we find and trash both), then, for `make-repeating`, restores the trashed original X. SERDEL validated the trash-both (a cursor-cleared template = zero spawns, GUI Trash renders sanely, GUI Put Back can even revive it) and that Things has NO delete-confirmation dialog — the GUI-native series removal IS trash-both. The trash legs route through a SANCTIONED-INTERNAL path (raw-AS `delete` behind the `H-REPEAT-SCHEDULE` guard); the guard's PUBLIC refusal on `todo delete`/`project delete` of a template stays untouched. Un-deleting a template stays unsupported headlessly (undo-of-undo = GUI Put Back only) — disclosed on the record/result. Evidence: [lab/serdel-series-removal.md](../lab/serdel-series-removal.md) (S1/S2), [promote-via-clone.md](promote-via-clone.md) §4.
- **2026-08-13 · Nested-repeater refusal, no `--flatten` (RATIFIED, maintainer).** `project.clone` (and thus clone-then-promote) refuses a project containing a nested repeating template, fail-closed, naming the child; NO acknowledgement-flag escape in v1 (deliberately stricter than the GUI's silent flatten — "more correct", per the advisories doctrine of honest refusals over machinery). Evidence: [promote-via-clone.md](promote-via-clone.md) §2/§6b, `src/write/clone.ts` (A6 refusal).
- **2026-08-13 · Symmetric umd-restore on undo (RATIFIED, maintainer).** Undo restores modification dates ONLY for operations that ran with `--preserve-modified` (whose `ok` audit records carry `preModDates`): after its mutation legs, undo appends best-effort AS `set modification date` restore legs to the captured pre-write umd values (disclosed in the result, never fatal). Default operations' undos remain honest umd bumps (a normal timeline entry). Evidence: `src/write/preserve-modified.ts` (#460), [promote-via-clone.md](promote-via-clone.md) §4.
- **2026-08-13 · Template-direct clone via re-promote (RATIFIED direction; IMPLEMENTED 2026-08-14).** Cloning a repeating template directly = clone its content as a PLAIN item + native-promote the clone with the SOURCE's decoded rule (a NEW series identity; no instances cloned; one instance spawns immediately per the one-instance-on-create law); REFUSE when the decoded rule is not expressible in our promote vocabulary (`ruleToInverseParams` boundary — the SAME mapping the reschedule undo rides) or undecodable; PAUSED source clones UNPAUSED with disclosure; undo = trash-both. `todo.clone`/`project.clone`'s template-source branch routes through `promote-clone.ts` `cloneTemplateViaRepromote` (ui-gated). Also refined `sameTitleRowCount` (pre-state.ts) to exclude hidden template rows from the promote's row-select ambiguity check (a clone keeps the source template's title+area; the source template is an invisible row). Result = the add-repeating contract; recorded/undone as `*.add-repeating`. VM certification queued (up-next). Evidence: [promote-via-clone.md](promote-via-clone.md) §6(d); `test/engine/write-promote-clone.test.ts`.
- **2026-08-15 · Checklist writes on a repeating to-do TEMPLATE (issue #479) — SHIP NO FIX; the silent no-op is version-specific to 3.22.14.** CHKT1 reproduced the exact #479 command on the golden (3.22.12) and it SUCCEEDS: `todo checklist <template> --item …` (url-scheme `todo.replace-checklist`) lands the items, verifies `ok`, and propagates them to the next spawned instance with per-item state reset. So the `H-REPEAT-SCHEDULE` allow-list copy ("checklist replacement remain allowed on templates") is CONFIRMED truthful on the golden, not falsified — no guard/matrix/simulator change is correct here (a preflight refusal would regress a working, verified 3.22.12 feature and fail `lab:regress`). The reported `verify-failed:silent-noop` is a suspected Things **3.22.14** regression in the URL `update?checklist-items=` template handler (same guarded-vs-unguarded template split as oddity §2i's `deadline=` drop), UNVERIFIED under any golden. Census: url-scheme replace AND append (`append-checklist-items=`, additive) both work on templates; AppleScript has no checklist class (A30); Shortcuts `set-detail Checklist` is a silent no-op (can't set). On 3.22.14 the shipped fail-closed verify already degrades safely (clear error, no bad state). Deferred to a maintainer GOLDEN DECISION: advance the golden and re-run CHKT1; IF confirmed there, the fix is branch-3 (extend `H-REPEAT-SCHEDULE` to block checklist ops on templates) + correct the copy. Evidence: [lab/chkt1-template-checklist.md](../lab/chkt1-template-checklist.md).
- **2026-08-24 · The repeat ↻ rides OUTSIDE the box, and BOTH halves of a series are marked (Mike, from Things 3.23 GUI screenshots).** The 2026-07-13 deltas 2/3 seated ↻ inside the checkbox (`[↻]`/`(↻)`) arguing GUI parity — parity with the pre-3.23 GUI, which 3.23 changed. The row language now mirrors the new one: a TEMPLATE keeps an ordinary `[ ]` / `blue("( )")` and wears a **blue ↻ as the first element after the box**, ahead of the date/state chip and the title; a spawned INSTANCE — previously rendered indistinguishably from a plain to-do — wears a **dim ↻ at the head of the post-title marker cluster**, before `◷ ≡ ≔`. One shared `REPEAT_MARK`, two wrappers (`repeatTemplateMark`/`repeatInstanceMark`), no third ↻ minted anywhere. The template branches stay in `todoBox`/`projectCircle` solely to defeat the someday fall-through (templates are stored `start=someday`). Nothing threaded through the read layer: instance-ness was already carried (`repeating.isInstance`/`templateUuid` off the `rt1_repeatingTemplate` FK) and already on the wire (presence-keyed `instanceOf`, RS6) — only the TTY was blind to it. Supersedes render-language deltas 2 and 3, which are marked as superseded in place. Evidence: [render-language.md](render-language.md) § The repeat pair.

- **2026-08-25 · CGRD1 — the AX-scrutiny doctrine's three guards, SHIPPED and certified.** The posture is the same-date private-API scrutiny ruling (#594, under *Process / workflow rulings*); this is what it costs in code, settled so the shape is not re-argued per drive. #589 fixed one wrong address; these close the error CLASS behind it — *a state-dependent AX tree addressed positionally, verified only against its own address*. (1) **no bare positional field addressing** — every `<class> <N>` selector in the ui vector either carries a `// positional-ok:` marker naming the MEASURED reason it is safe or does not ship, enforced by a source scan (`test/unit/positional-addressing.test.ts`) whose marker must sit on the offending line or the comment block directly above it; the one value-bearing survivor (`text field 1` of the dialog shell, the start-days-earlier offset) was CONVERTED to a label-row address, and the interval discriminator was hardened from a negative rule ("not on the `Ends:` row") to a POSITIVE `Every`-row match with a uniqueness fallback. (2) **A PRE-COMMIT full-dialog audit** — every setter's own read-back is self-referential, so before the OK press the drive re-reads EVERY control it set through that control's own discriminated address and aborts fail-closed naming each mismatch; the audit plan is derived from the recipe's own step list, so a driven control cannot be omitted from it. (3) **Unexplained-delta detection** — post-drive, the DECODED rule is diffed pre versus post over the RRF1 vocabulary and every changed field must be attributable to a requested field or to an explicitly mapped CO-MOVER carrying its law (frequency→anchor, `--when`→derived anchor, deadline↔start-offset, any cadence change→cursor); an unattributable one is the new `verify-failed:collateral` error suffix, which states that the requested change DID land, names the field with both values, and tells the caller not to retry. The attribution map is EXHAUSTIVE over `RepeatRule` per the #491 structural doctrine, so a new rule field breaks compilation until classified; `paused` and the rule's storage-format `version` are classified `independent` — nothing can explain them away. Scope: reschedule-repeat (to-do + project) only — make/add-repeating MINT the rule, so there is no pre-state to diff. Evidence: [lab/cgrd1-precommit-audit.md](../lab/cgrd1-precommit-audit.md) (16 assertions, 0 failures, golden-v4 / Things 3.23).

## Grammar / contract rulings

- **2026-07-26 · `add` is the canonical create-verb across the operation catalog.** `heading.create` → `heading.add`, `project.create-repeating` → `project.add-repeating`; `make-`/`convert-` stay DISTINCT as transform verbs, the delete family is untouched. Evidence: CHANGELOG (verb harmonization).
- **2026-07-27 · ALPHA-CONTRACT clean-break doctrine — no compat machinery pre-1.0.** The whole contract (TS API, CLI, JSON, MCP, audit records, error codes) is alpha until v1.0; breaking changes are made cleanly, NO shims/alias maps/deprecation windows, and agents must not add or propose them. Flips at v1.0 (teardown tracked in the [roadmap](../roadmap.md) v1.0 checklist). Evidence: AGENTS.md (Conventions), [design/architecture.md](architecture.md) (Alpha contract).
- **2026-07-27 · Heading selection is title-or-uuid — NO index/ordinal addressing.** A heading is itself a reorder target, so an index silently re-targets after any reorder (the unanimous multi-model "reindex hazard"); the checklist `--index` precedent deliberately does NOT transfer. Heading uuids STAY in read payloads (dup disambiguation + script robustness + titleless records). Evidence: [design/cli-vocabulary-study.md](cli-vocabulary-study.md), [design/heading-demotion-and-move.md](heading-demotion-and-move.md).
- **2026-07-27 · `--detach` REMOVED — one spelling per kind.** The detach family is `--no-heading`/`--loose` (to-dos) and `--no-area` (projects); the overloaded `--detach` (which conflated stripping a heading vs stripping a project) is gone. Evidence: [design/heading-demotion-and-move.md](heading-demotion-and-move.md) §4/§5.
- **2026-08-05 · Anchor vocabulary is `--start` / `--end` / `--before <ref>` / `--after <ref>`.** Ratified over the earlier `--first`/`--last` spelling; `--end` is retained alongside `--start` (MCP `reorder` likewise takes `start`/`end`/`before`/`after`), plus `--in <token>` for axis disambiguation. Bare (no position) assembles the block at the earliest movee's slot. Evidence: CHANGELOG (0.15.0 move/reorder vocabulary), [design/heading-demotion-and-move.md](heading-demotion-and-move.md) §4.
- **2026-08-11 · Non-axis orderings are query VIEWS, not a `--sort-by` reorder flag.** A named ordering (deadline order, etc.) ships as its own read view whose identity IS the ordering and is declared NON-SCOPE (`--in <view>` refused) — the `deadlines` view precedent (`deadline ASC`, one comparator shared with `upcoming`). Reorder axes stay the container/bucket scopes only. Evidence: [design/read-shape-doctrine.md](read-shape-doctrine.md) (deadlines addendum), CHANGELOG (`things deadlines`).
- **2026-08-11 · Symbolic-when UPDATE verify is an arrived-date predicate; `evening` stays exact date-equality.** `when: today` on an UPDATE asserts `startDate != null && startDate <= today` (an already-arrived row is not a mismatch); adds and explicit ISO dates keep exact equality; `when: evening` is unchanged, date-anchored to today exactly (the date-anchored evening law is correct as written). Evidence: PR #440, [research/project-update-today-verify-mismatch.md](../research/project-update-today-verify-mismatch.md), `test/engine/write-when-today-verify.test.ts`.
- **~2026-08 · Bare-noun routing of a TAG name (`things <tag>` → a filtered view) — REJECTED; explicit-only remains the open question.** Bare-noun sugar already arbitrates commands/views/areas/projects/to-dos; routing a tag name adds a collision surface where a rename in the user's Things could silently flip which view renders, and a tag uniquely expands to a FILTERED VIEW rather than a single-entity show. The still-open sliver — an explicit sigil (`things @errand`) vs no tag shorthand at all — lives in up-next. Evidence: up-next tag-shorthand item's own reasoning (2026-08-02). *(The bare-noun rejection was not independently pinned to a dated PR in committed docs — see the restructure report.)*

- **2026-08-13 · `make-repeating` / `add-repeating` are REFUSED as batch legs (promote compounds, not atomic ops).** A batch line with any of `todo.make-repeating` / `project.make-repeating` / `todo.add-repeating` / `project.add-repeating` refuses per-line, naming the standalone command — the multi-leg clone|add → GUI-promote → trash compound does not compose inside a batch. The old batch path ran the destructive native promote; it is deleted per ALPHA-CONTRACT (no partial/destructive fallback). Evidence: [design/promote-via-clone.md](promote-via-clone.md) §3, `src/write/batch.ts`, CHANGELOG.
- **2026-08-13 · Deleting a repeating TEMPLATE is ALLOWED with disclosure (the delete arm of `H-REPEAT-SCHEDULE` is lifted).** Our template-trash is byte-identical to the GUI's own Edit ▸ Delete (SERDEL S1) and is human-recoverable via the app's Trash ▸ Put Back (cursor restored, series resumes — SERDEL S2/S3). Like other headlessly-irreversible commands (`log-now`), a public `todo/project delete` of a template returns `ok` with NO undo token plus warnings disclosing (a) the series stops generating, (b) its live instances are left in place (count + the current instance uuid), (c) headless restore is unavailable (Put Back only). A `todo/project restore` of a trashed template keeps refusing, but with a categorical Put-Back message (replacing the raw wrapped AS-301 `verify-failed:silent-noop`). `H-REPEAT-SCHEDULE` stays for the OTHER template hazards (URL schedule writes, move, status); the `internalSeriesRemoval` exemption flag (#464) is deleted (public delete needs no exemption). Evidence: [lab/serdel-series-removal.md](../lab/serdel-series-removal.md) S1–S3, `src/write/{guards,pipeline,undo}.ts`.
- **2026-08-14 · Placement-restore after promote — WILL NOT IMPLEMENT.** The GUI's own native promote makes no effort to restore the spawned instance's prior position either — it lands at the app-native placement, and that GUI-parity behavior is accepted. The promote result still DISCLOSES the native landing (the placement warning stays); no automated reorder-restore leg will be built. Evidence: [design/promote-via-clone.md](promote-via-clone.md) §3.
- **2026-08-14 · `things batch` performs a STATIC preflight — any statically-invalid line refuses the WHOLE batch before anything runs.** A line checkable with no DB/app state (torn/non-object line, missing/unknown op, a `make-repeating`/`add-repeating` compound, non-object params, malformed `opId`, a `$ref` naming a `tempId` no EARLIER line declares — unknown or forward — or a duplicate `tempId`) rejects the entire batch up front, enumerating every offending line and dispatching nothing (dry-run runs the same pass). Only RUNTIME failures (guard block, verify-failed, unsupported vector, a ref to a leg that bound nothing) keep per-line semantics. Rationale: a structurally broken script is a single authoring error, so it fails as a unit rather than half-applying. Evidence: `src/write/batch.ts` (`preflightBatch`), `test/engine/write-batch.test.ts`.
- **2026-08-14 · Stop-on-failure is the DEFAULT for `things batch`; `--continue-on-error` opts into proceed-past.** A runtime per-line failure HALTS the batch (later lines reported not-run); the summary states the not-run count and whether a verbatim resubmission is safe to resume (safe when every committed line carried an `opId`; otherwise it names the non-idempotent indices). The old opt-in `failFast` flag is DELETED outright (ALPHA-CONTRACT — no alias). Rationale: half-running a script past an unexpected failure is the more dangerous default; resuming is made safe by per-line `opId`s. Evidence: `src/write/batch.ts`, `src/cli/commands/writes.ts`, `src/mcp/server.ts`.
- **2026-08-14 · `undo --last`/`--by` SKIP already-undone records so repeated undo walks progressively deeper.** Selection derives the undone set from the trail (inverse records: `undoOf` linkage, actor `undo:*`, result ok) and excludes those records, so `--last N` always means "the N newest not-yet-undone mutations" and `undo --last N` run twice advances into older history instead of re-selecting the same records (whose inverses would trip the out-of-band precondition). IRREVERSIBLE records are NOT skipped — they still COUNT toward N and are reported every pass (reaching silently past an irreversible record to undo something older would be a dangerous surprise). `--txn` is unchanged (surgical; loud already-undone error). Evidence: `src/write/undo.ts` (`selectUndoTargets`/`undoneTokens`), `test/unit/undo-plan.test.ts`.
- **2026-08-14 · Row-shaping rules RENUMBERED R6–R13 → RS1–RS8 (maintainer).** The per-row emission rules (RS1 no-redundant-ancestry, RS2 detail tiers, RS3 search provenance, RS4 universal item-DTO reshapes, RS5 `stage`, RS6 repeating template/instance split, RS7 `when`, RS8 provisional/deadline-pull) were numbered R6–R13, sharing digits with the read-shape doctrine's structural R1–R9 (the standing collision the #469 note flagged). They now carry an **RS** prefix (contiguous, offset-preserving), ending the collision; the doctrine's structural R1–R9 keep their numbers. ALPHA-CONTRACT: no alias table — every reference in LIVING text/code/tests was swept to RS; history (CHANGELOG, `docs/lab/` evidence, dated change-notes) was left as-written. Evidence: this change; [read-shape-doctrine.md](read-shape-doctrine.md) §2 note, [contracts.md](contracts.md) / [../contract.md](../contract.md).
- **2026-08-14 · RS rules drift-locked between the two contract docs (maintainer).** The RS1–RS8 rules are stated in near-duplicate prose in [../contract.md](../contract.md) (consumer covenant) and [contracts.md](contracts.md) (design contract). A unit test (`test/unit/rs-rule-drift.test.ts`, modeled on the banned-vocab lock) extracts the `**RS<n> — …**` bold headers from both and asserts the SAME RS number-set (contiguous) and a byte-identical one-line summary per rule in each file — so the two can't silently diverge. The lock covers the number-set + summary line only; bodies may differ in depth. Evidence: `test/unit/rs-rule-drift.test.ts`.
- **2026-08-23 · The to-do/project UPDATE vocabulary has ONE registry — the #491 exhaustive-map doctrine applied to its widest case; `TodoUpdateParams`/`ProjectUpdateParams` are one shared `UpdateFields`.** The seven update fields were hand-enumerated in SIX consumers (both CLI verbs, both MCP branches, the URL compile, the expected-delta assert, plus the undo inverse) — the exact arrangement #491's structural doctrine (2026-08-17, below) forbids. They now derive from `src/write/update-fields.ts`: an exhaustive `Record<keyof UpdateFields, …>` whose every field carries a wire leg, an assert leg and an undo leg (or a written skip — `reminder` has no URL parameter of its own; it rides the `when` value's `@time` suffix), and a second exhaustive map over the CONSUMER vocabulary (the same fields plus the two `clear-*` flags and the resolution timestamps) that both surfaces call as `buildUpdatePatch`, which also owns every mutually-exclusive pair. **Sub-ruling: a contradictory flag pair is REFUSED, never silently resolved** — `--deadline` + `--clear-deadline` (CLI accepted both and cleared) and an `@time` suffix + `--clear-reminder` (the suffix's reminder was parsed then discarded) join the pairs MCP already refused; the two surfaces are now identical by construction, including the `@time` sugar on MCP `update kind:"project"`. Semantic backstop: the #491 discrimination property test (`test/unit/update-fields.test.ts`). **Same sweep, the promote orchestrators:** `src/write/promote-clone.ts`'s hand-rebuilt bags are gone too — the rule/add split of an add-repeating request is one exhaustive key map (`splitAddRepeatingRule`, repeat-rule.ts), the seed carries the whole add vocabulary by spread, and `ruleParamsFor` is a spread with exactly one deliberately re-derived field (`next` — deadline-shifted, and absent on an after-completion dialog). That file had already lost a field twice this way (#549 `next`, YANCH1 #493 the deadline pair). Audited-and-left: `project.add` asserts nothing about its seeded children (queued in up-next — an assertion needs probe evidence about the verify window first).

## Scope rulings

- **(standing) · Archived/"ignored" tags, emoji-stripping opt-in, pseudo-archived areas — SHELVED INDEFINITELY.** First-class support is not planned; revisit only if real usage surfaces a need. The leading-symbol-significant rule already protects emoji-prefixed archival tags for free (v0.6.0). Evidence: roadmap history (shelved item 4).

- **2026-08-13 · `--preserve-modified` is SYNC-SAFE; the "UNSYNCED-only" caveat is retired (SYNC2B).** A hand-written past `userModificationDate` propagates to peer devices AND survives the sync round-trip in both directions — Things Cloud treats `umd` as ordinary per-attribute synced data, not a monotonic sync clock, and the accompanying content edit rides the sync change-log independent of `umd` — so the flag's timeline-silent effect holds on every device. The sole residual (a genuinely concurrent same-row edit elsewhere re-bumps `umd` via the per-attribute max-merge) fails safe (the edit resurfaces, never silently hidden). **No `doctor`-driven sync-state guard is added** — the flag is safe to point at a synced production store. Evidence: [docs/lab/sync2b-durable-account.md](../lab/sync2b-durable-account.md) (SY-2/SY-2M).
- **2026-08-16 · `add-repeating` AUTO-TRASHES its own seed when the promote leg fails (issue #480).** The add→promote legs are not atomic: the seed persists if the promote no-ops. Because the seed is OUR artifact — recreatable verbatim from the command args — and the Trash is recoverable, a failed promote now auto-trashes the seed inside the same txn and discloses it with a `restore` remediation; if the auto-trash itself FAILS, the result carries the seed's REAL, resolvable uuid with a working `things <kind> delete <uuid>` remediation (never a buried, non-actionable uuid — the #480 second bug). This is the single ruling that makes the failure path's cleanup honest regardless of app version. Also ruled: a to-do promote drive must POSITIVELY assert an eligible selection (uuid-precise `id of selected to dos` + menu `AXEnabled`) before the menu press, so a disabled-menu no-op fails early + named rather than as an opaque dialog-wait timeout; and `add-repeating --reminder` drives the Repeat dialog's reminder so the base reminder rides the SERIES (the dialog conversion otherwise drops the seed's one-off reminder). Evidence: [docs/lab/adr1-add-repeating-reveal.md](../lab/adr1-add-repeating-reveal.md).
- **2026-08-16 · The #480 `add-repeating` silent-noop is a suspected Things 3.22.14 behavior change, not a golden bug (STOP-and-report).** The full repro matrix (bare/+area/+tag/+area+tag/+reminder/full issue combo) all PASS on golden-v2 / 3.22.12 — the reveal selects the row and `Items ▸ Repeat…` is enabled on every surface — so no root-cause reveal fix is shippable from the golden (a change gated on a working surface would fail the golden's own `lab:regress`). Same 3.22.14 split as #479/CHKT1. The maintainer owns the golden-advance decision; the shipped eligibility assertion is exactly the diagnostic that will characterize the failure if a 3.22.14 golden later confirms it. Evidence: [docs/lab/adr1-add-repeating-reveal.md](../lab/adr1-add-repeating-reveal.md).

## Process / workflow rulings

- **2026-08-14 · No doctrine tombstones — a superseded design doc is synthesized into its living successor and DELETED (maintainer).** When a design doctrine is revised, the old document is not kept as a "SUPERSEDED/REVERTED" tombstone alongside the new one: the still-governing content is folded into the living successor (which takes the natural, unversioned name) and the old file is deleted. Git is the archive — the lineage narration (what superseded what, when it was reverted) does not belong in the living tree. First application: the read-shape doctrine v1 + v2 files were merged into the single living [read-shape-doctrine.md](read-shape-doctrine.md) and the v1/v2 tombstones deleted. Evidence: this docs-recert sweep (PR `mg/docs-recert-sweep`).
- **2026-08-12 · GitHub issues adopted as INTAKE-ONLY for bug reports and feature requests (maintainer).** The repo's GitHub issue tracker is the intake surface (so other coding agents that drive the CLI daily can file via `gh issue create` instead of PRing docs/up-next.md); it is NEVER the archive. Durable state stays in-repo: accepted items graduate into [up-next.md](../up-next.md) with an issue link, rulings land here, evidence lands in `docs/lab/`, and the issue closes with a pointer. The tracker is PUBLIC, so every intake surface (issue forms, the dev-checkout skill CTA) loudly requires synthetic/redacted repro data — never real task titles, notes, or project/area names. Evidence: `.github/ISSUE_TEMPLATE/`, `install-skill` dev CTA.
- **2026-08-13 · ONE durable Things Cloud lab account, minted once and never burned (maintainer, sync-probe unblock).** Sync probes need a live Things Cloud account; earlier campaigns (TOMB1/SYNC2/SYNCLAT) each minted-then-BURNED a per-run account. The maintainer explicitly does NOT want Cultured Code seeing account churn, so a SINGLE durable throwaway account now serves ALL present and future sync probes — created once in-VM (mail.tm one-time code + random password, no Apple ID), kept alive at teardown. Credentials + the account's `BSSyncronyMetadata` coordinates live ONLY in gitignored `lab/artifacts/sync-durable-account/` (the repo is public). Future sync runs use the LOGIN path, never re-registration. NEVER point anything at the maintainer's real account/production DB. Evidence: [docs/lab/sync2b-durable-account.md](../lab/sync2b-durable-account.md).
- **2026-08-14 · Durable-account #2 (one-time accidental-loss replacement) + durable artifacts go to the PRIMARY checkout by absolute path.** Durable account #1 was orphaned when the provisioning agent wrote its gitignored creds only under an ISOLATED worktree's `lab/artifacts/` and the orchestrator's worktree cleanup destroyed them (gitignored files don't travel with a merge); #1 was never burned server-side (idle, lapses with its trial). A fresh durable account #2 was minted 2026-08-14 as the authorized ONE-TIME replacement (accidental loss, not churn — the no-churn doctrine stands). **Root-cause rule:** any lab artifact a *future* run depends on (account credentials above all) is written to the PRIMARY checkout's gitignored `lab/artifacts/` by ABSOLUTE path, never only to an agent worktree's copy. Evidence: [docs/lab/sync3-dedupe-tiebreak.md](../lab/sync3-dedupe-tiebreak.md) (account-provenance note), [docs/lab/harness.md](../lab/harness.md) "Durable artifacts".

- **2026-08-16 · Golden advanced to `things-lab-golden-v3` / Things 3.22.14 (behavioral-only); the reported #479/#480 3.22.14 regressions do NOT reproduce in-lab, so NO version-gated fix ships.** 3.22.14 is schema + sdef byte-identical to 3.22.12/3.22.11 (fingerprint `sha256:784bd2f6…`, DB v26; sdef `1b675233…`), so v3 was built by the DRIFT-1 in-place swap (clone v2, `ditto -xk` the app zip, warm-up, re-fingerprint) — no baseline, no simulator-chain re-model. `lab:regress` ran GREEN with zero flips (schedule-class crashes U12/R09 still reproduce — guards stay); the repeat-rule blob corpus decodes byte-identical with `rrv=4` unchanged. CRUCIALLY, re-running `research-chkt1.sh` (#479) and `research-adr1.sh` (#480) under v3 showed BOTH reported field regressions do NOT reproduce in a clean airgapped 3.22.14 clone: checklist-on-template works + propagates; the add-repeating full matrix all passes with the Repeat dialog appearing and Next/reminder driven. The version tuple alone does not explain the host reports (candidate differentiators: active Things Cloud sync, production-DB scale/window-state, MAS vs direct build channel — all unprobed here). **Ruling:** do not ship the speculative #479 checklist refusal or a #480 reveal fix (either would break a working, verified feature and fail `lab:regress`); the shipped fail-closed verify + the #483 eligibility assertion/auto-trash already degrade safely. v2 retained as the certified fallback. Evidence: [docs/lab/gv3-certification.md](../lab/gv3-certification.md).

- **2026-08-16 · The #480 TRUE root cause is SESSION STATE, not app version: dialog-class ui ops get a session-reachability gate (refuse-on-locked, relocate-on-wrong-Space) + honest modal-clearance cleanup (SESSGATE).** Found live on the maintainer's host: a **locked screen or full-screen Space** makes System Events enumerate ZERO windows for every process, so a dialog-class op's Repeat/confirm sheet opens on an AX-unreachable window — the dialog-wait times out, the stuck modal blocks AppleScript mutations app-wide (the #480 auto-trash silent-noop), and the AX-blind Escape cleanup falsely claims "confirmed gone". This is exactly the SESSION-STATE divergence the golden-v3 ruling above could not explain — the earlier "does not reproduce on clean 3.22.14" stands, now explained. **Two-branch gate ruling** (`src/write/vectors/session-reachability.ts`, probed after the reveal, before any press; and pre-seed in the promote orchestrators): (1) **LOCKED signature** (Things AX=0 AND every process AX=0) → **refuse** (`blocked`, exit 4, zero mutation) with behavioral remediation (unlock / leave full-screen); the promote orchestrators refuse this BEFORE seeding so no orphan row is created. (2) **WRONG-SPACE signature** (Things AX=0, other processes AX>0) → do NOT refuse: run the AppleScript `close window 1` + `reopen` + `activate` relocation inside the drive, re-probe closed-loop, proceed, and DISCLOSE the relocation in `warnings[]`; only if relocation fails to yield a reachable window does it then block with the Space remediation. (3) **REACHABLE** → proceed (works backgrounded too — the detached-window dialog form). Gated ops are precisely the sheet-opening dialog-class recipes (`needsWindowReachability`); pure menu-item ops (pause/resume) work under lock (AXVM1) and stay ungated. **Cleanup ruling:** the post-failure cleanup never claims an unconfirmed dismissal — when AX-blind it runs the same proven `close window 1` + `reopen` maneuver (app-level, no Accessibility) that clears a stuck sheet and unblocks the app-wide AppleScript-mutation freeze, which is what makes the seed auto-trash / clone-rollback legs land. Evidence: [docs/lab/sessgate-session-reachability.md](../lab/sessgate-session-reachability.md); live host 2026-08-16; oddities §9cc/§9dd.

- **2026-08-17 · A write against a CLOSED Things auto-launches-and-readies by default (opt-out `auto-launch`), never silently no-ops (issue #486).** A command dispatched into a not-running Things is accepted but dropped during the app's startup window — the reported `verify-failed:silent-noop`. **Ruling:** every real-transport write runs an app-running preflight (the ONE `pgrep -x Things3` shape shared with `doctor`/sync-health, `automation-probe.ts` — no second process-check form); a closed app is **background-launched (`open -g`, tier 1) and waited on CLOSED-LOOP for launch-readiness** before dispatch, with the launch disclosed in `warnings[]`. The DEFAULT is auto-launch (a daily-driver expectation: "the write just works"); a **`auto-launch false`** config key (env `THINGS_API_AUTO_LAUNCH`) flips it to a preflight **`blocked` environment refusal with ZERO dispatch** for a host that must never surface the app unbidden — chosen over silently launching everywhere so a dedicated-server operator keeps control. Readiness is a **consent-free** signal (LaunchServices launch-completion via `lsappinfo`, NOT an AppleScript probe — an AS read would drag Automation consent onto the consent-free URL path), polled past the drop window as a floor (best-effort: a residual drop is caught by verify and attributed `app-not-running`, which tells the caller to simply retry now that the app is up). Reads never launch anything. Evidence: [docs/lab/apprun1-launch-readiness.md](../lab/apprun1-launch-readiness.md).

- **2026-08-17 · #487: a GUI-driving write must never return empty output; the honest-failure contract for a slow/interrupted drive + dev-mode step-timeline trace (TRACE1).** Found live on the maintainer's 22k-task, actively-syncing production host: `make-repeating --dangerously-drive-gui --json` filled the Repeat dialog correctly but the drive ran past the caller's 30s timeout; the caller killed the node process and received EMPTY stdout, no JSON, no exit code — while the template landed moments later. Code adjudication of the hypothesis chain: (a) **no signal handler existed**, so a killed process emitted nothing — CONFIRMED; (b) the drive runs **one `osascript` per step via `execFile`**, and Node's own `timeout` timer lives in the (now-dead) parent, so the single in-flight osascript **orphans and runs to completion** unbounded, explaining the late landing — CONFIRMED structurally; (c) the stall is **aggregate**: no overall budget, ~10–12 steps each ≤ `STEP_TIMEOUT_MS` (15s) with internal retry loops, on a production DB that commits the sheet several times slower than the airgapped golden — CONFIRMED as the dominant link. The maintainer's alternate cause (post-drive polling for a future-dated, not-yet-spawned instance) is **FALSIFIED**: `discoveryOf` is instance-null-tolerant (`instanceUuid ?? null`), the verify budget is hard-capped (`verifyTimeoutMs ?? appRunning?6000:10000`, recovery 2000ms), and the live observation places the stall at/before the OK press — locked by a regression test. **Ruling (three coordinated fixes, each holding regardless of which link dominates):** (1) **Signal-safe final words** — a SIGTERM/SIGINT handler flushes a final `signal` trace entry and best-effort emits a structured `interrupted` error (new `ErrorCode`) whose contract is OUTCOME-UNCERTAIN: the in-flight UI step may still complete, so re-check with `things show <ref>` rather than assume nothing changed; SIGKILL is documented as leaving only the trace's last checkpoint. (2) **In-CLI drive watchdog** — the ui vector enforces an overall UI-drive budget (default 90s; `ui-drive-budget-ms` config / `THINGS_API_UI_DRIVE_BUDGET_MS`), checked at each step boundary (per-step execFile caps keep the boundary close); on expiry it runs the SESSGATE `clearDialog` close+reopen so no stuck modal is left, then returns a `verify-failed`/`timeout` flagged `uncertain` with the trace path — the CLI gives up FIRST, and callers are told to allow ≥120s so it always beats their timeout. (3) **Dev-mode trace** — a `-dev` build (or `trace true` config / `THINGS_API_TRACE`) writes a per-invocation JSONL timeline to `~/.local/state/things-api/trace/` (sanitized argv → pipeline stages → each osascript hop with duration/outcome → verify summary → result/exit code); overhead ≈0 when off; LOCAL-ONLY (may hold real titles) — never committed or attached to a public issue. **Orphan policy:** keep per-step osascript execution (a kill halts at a step boundary); the lone in-flight osascript is allowed to finish as an orphan (benign — e.g. a clean OK commit) and the trace/interrupt marker record where it stopped; no detached/daemonized processes are ever spawned. Evidence: [docs/lab/trace1-drive-timings.md](../lab/trace1-drive-timings.md); live host 2026-08-17.

- **2026-08-17 · #491: rule-writing verbs assert the FULL requested recurrence rule, built from an EXHAUSTIVE per-field map — never a hand-enumerated shallow subset (RRF1).** `reschedule-repeat` asserted only unit+interval, so the pre-drive idempotency check false-noop'd any reschedule that changed only the monthly anchor / weekday set / deadline offset (`ts`) / ends bound / `--when` cursor (same-frequency, same-interval), returning `ok`+"idempotent no-op" at exit 0 with no drive; the post-drive verify shared the shallow asserts, so a wrong-anchor drive would also have passed. **Ruling:** the expected-delta assert set for `reschedule-repeat` (to-do + project) AND the `make-repeating`/`add-repeating` post-drive verify is built from the COMPLETE requested vocabulary by one shared builder (`src/write/repeat-asserts.ts` `expectedRuleAssertions`), compared against the DECODED rule (`decodeRecurrenceRule` shapes) + the template deadline column, never a blob/string compare: rule type · unit · interval · calendar anchor (order-insensitive `anchorKey` over the decoded offsets) · ends bound (RRX1: ends-after→`occurrenceCount`, ends-on→`endDate`, never→both null) · `startOffsetDays` · deadline flag · and the ANCH2 first-occurrence cursor (`nextOccurrence`) when `--when` is given (reschedule only — make/add cursors follow the ANCH1 spawn law). **Requested-fields-only law:** a field the caller did not set contributes NO assertion (keeps its don't-care semantics) — so a bare `{frequency, interval}` reschedule never fails on an untouched ends/anchor; the idempotency precheck skips the drive iff EVERY requested field already holds. **Structural doctrine (maintainer-ratified, general):** a multi-consumer vocabulary must NEVER be hand-enumerated per consumer — the only acceptable shapes are an EXHAUSTIVE map (`Record<keyof …, …>` where every field is an assert-producer or an explicit skip carrying a written reason — e.g. `reminder` skipped: not stored in the rule blob) or derivation from a single registry, so a newly added field breaks compilation until consciously handled. The semantic backstop the types cannot give is a DISCRIMINATION property test (two bags sharing a footprint but differing in ≥1 value ⇒ asserts(A) UNSATISFIED against state(B)) plus coherence + mapper-completeness companions. Applied here to the assert builder AND the CLI flag→param mapper. **UIC6-l closed:** a wrong-frequency calendar-anchor CLI flag (`--frequency weekly --on-day 15`) is now REFUSED (naming the flag + its frequencies) instead of silently dropped; the flag→param mapping is itself an exhaustive map. Queued follow-up: audit other hand-enumerated vocabularies (the `todo.update` field chain) against this doctrine (up-next). Evidence: [docs/lab/rrf1-full-rule-asserts.md](../lab/rrf1-full-rule-asserts.md).

- **2026-08-17 · #493: a fixed monthly/yearly repeat DERIVES + drives its calendar anchor from the requested first occurrence; a deadlined rule anchors on the DEADLINE, so `--when` (the START) is driven as `when + start-days-earlier`.** Driving only the "Next:" field fixes the FIRST occurrence but leaves the recurring rule on the dialog default (yearly → January 1, monthly → the 1st, weekly → Sunday) — the anchor-drop ANCH2 cell (c) exhibited and accepted as "subsequent on Jan 1", which IS the #493 bug. **Ruling:** when a fixed weekly/monthly/yearly rule carries a first-occurrence date and NO explicit anchor, DERIVE the anchor from that date (weekly→weekday, monthly→day-of-month, yearly→month+day, the composeOffsets refIso law) and drive the anchor pop-ups explicitly (completing the family the weekly weekday-derivation began post-ANCH1); an explicit anchor keeps priority. A DEADLINED rule anchors on the deadline and each instance STARTS `start-days-earlier` days before it (golden-v3 probe: `start = anchor − N`), so the dialog anchor + "Next:" are driven with `when + start-days-earlier` and the app back-shifts the start to `--when`; the verify expects the START on `--when`. The shared promote helper must carry rule-level `deadline`/`start-days-earlier` (they were stripped for make-repeating — a deadlined make landed a non-deadlined series). The post-drive verify asserts the derived-or-explicit anchor (a dropped default anchor is a `verify-failed:mismatch`); `includeCursor` stays FALSE for make/add (the cursor is the first rule-ALIGNED occurrence, distinct from the driven first occurrence, which is verified via `rt1_instanceCreationStartDate`). The `set-datetime` primitive throws a NAMED error (never a raw -2700) when the target date area is absent and READS BACK the control, failing loudly on a rejected write. Skill/CLI: `--dangerously-drive-gui` gets a ≥180s timeout doctrine and an explicit "a timeout/empty/no-op is NOT success — re-read, never blind-retry, stop-and-file" rule. Evidence: [docs/lab/yanch1-derived-anchor.md](../lab/yanch1-derived-anchor.md).

- **2026-08-18 · RRD1: the Repeat dialog opens PRE-POPULATED on a reschedule, so its checkboxes converge through a deterministic closed-loop `ensure-checkbox` step, never a blind toggle; an unspecified deadline/reminder PRESERVES the pre-populated state.** Found live on the maintainer's host: `reschedule-repeat` on an already-deadlined monthly template with `--deadline --start-days-earlier 21` drove frequency→interval→anchor→"Next:" correctly, then UNCONDITIONALLY pressed "Add deadlines" — which, because the pre-populated dialog opened with the box ALREADY ticked, UNCHECKED it, hid the "start N days earlier" field it had already revealed, and collapsed the drive ("none of its expected element shapes resolved"). Fail-closed asserts (#492) correctly reported the rule untouched. **Ruling:** a dialog checkbox is driven by a new closed-loop `ensure-checkbox` primitive (read `AXValue`, press ONLY when it differs from the requested target, re-read to confirm convergence, fail closed on non-convergence — the same determinism doctrine as the interval field's read-back retry), applied to BOTH "Add deadlines" and "Add reminders". The **deadline checkbox converges BEFORE the "Next:" field is driven**: deadline mode changes what "Next:" means (it becomes the deadline date, YANCH1 #493), so the `deadlineDriveNext` shift must be computed against the CONVERGED state — a box flipped after Next was driven changes Next's meaning under an already-committed value. The checkbox reveals only a NUMBER field ("start N days earlier"), never a date area (YANCH1 census), so converging it does not perturb the ends/Next date-area targeting; the reminder checkbox stays in its ANCH2-certified position after the date drives (a freshly-checked reminder area defaults to a non-midnight 12:00, so the tod discriminator never confuses it with the midnight Next/ends pickers). **Preserve-unspecified law (extends requested-fields-only, #492):** on a reschedule, an unspecified `--deadline`/`--reminder` emits NO checkbox step, so the pre-populated state is preserved (a rule-only reschedule leaves a deadline/reminder exactly as it was); only an explicit `--deadline false` un-ticks the box, and `start-days-earlier` is driven only when given (>0). This composes with the requested-fields-only verify: an unspecified deadline is don't-care in the asserts too, so no mismatch. **Audit finding (noted, out of scope):** the weekly multi-weekday "+" add-row logic is NOT idempotent on a pre-populated multi-weekday rule (it blindly presses "+" per requested extra weekday, adding rows atop the pre-existing set, and re-drives only the FIRST weekday pop-up) — a latent reschedule gap needing its own lab cert of the weekday-row addressing, unchanged here. Evidence: [docs/lab/rrd1-checkbox-converge.md](../lab/rrd1-checkbox-converge.md).

- **2026-08-18 · DACON1: an explicit anchor disagreeing with `--when` is an OFF-RULE first occurrence to ALLOW + DISCLOSE, not a contradiction to refuse — refuse fail-closed ONLY the empirically-dishonored shape (monthly).** A concrete `--when` with an explicit calendar anchor can disagree; the Repeat dialog supports an off-schedule first occurrence (the first instance lands on `--when` verbatim, the anchor drives the recurring grid). **Ruling:** classify each such request (`repeat-anchor.ts` `assessOffRuleFirst`) — deadline-shift-aware (the anchor names the DUE date, so compare against `--when + start-days-earlier`): (a) ON-RULE → nothing; (b) HONORED off-rule (weekly, yearly — DB-verified golden-v3, [dacon1](../lab/dacon1-deadline-contradiction.md) DC1/DC3/DC4/DC5) → ALLOW and disclose BOTH halves of the landed pattern in `warnings[]` + `--dry-run`, and the reschedule verify asserts the rule anchor but NOT `cursor == --when` (an off-rule cursor is the next aligned start, DC4); (c) DISHONORED off-rule (monthly — the month row snaps "Next:" to the anchor day, DC2) → fail-closed refusal at validation with the two nearest expressible alternatives. The deadline law is UNCHANGED (`--when` = the appear/start date; deadline-mode Next drive targets `when + N`; the derived anchor from `--when` applies only when no explicit anchor is given). This supersedes the earlier "blanket anchor/when contradiction refusal" direction. Evidence: [docs/lab/dacon1-deadline-contradiction.md](../lab/dacon1-deadline-contradiction.md); oddities §9ee.
- **2026-08-18 · DACON1 poller: a GUI-drive verify returns `mismatch` EARLY when an asserted field settles on a stable wrong value (≥3 identical mismatching polls spanning ≥5s after movement), instead of burning the full budget.** A settled wrong write cannot converge by waiting (the live-host 120s never-converging reschedule poll). **Ruling:** the poller trips an early `mismatch` only on ASSERTED movement to a value that then holds unchanged for the window; ANY change resets the window (never fires while converging), it never converts a would-converge case, returns the same classification the deadline would, and is inert for budgets shorter than the window — the tripwire/timeout and silent-noop paths are untouched. (`src/write/verify/poller.ts`.)

- **2026-08-18 · DBLSPAWN1: `add-repeating`'s concrete `--deadline <date>` maps to the RULE (strip it from the seed); `make-repeating` trashes a redundant preserved FUTURE instance post-promote.** A seed carrying an item-level deadline is SRCFATE-preserved on promote as a materialized instance; when its first occurrence is FUTURE-dated the app double-books it against the template cursor (`next == icStart`, `icCount = 0`) and spawns a DUPLICATE when the date arrives — a genuine duplicate factory, DB-verified on golden-v3 (DBLSPAWN1 cells A/C), and a pure-GUI user hits it too (oddities §9ff). **Ruling — two composites, two mechanisms:** (a) **`add-repeating`** derives `start-days-earlier = deadline − when` and drives the rule's "Add deadlines" + start-offset, keeping the seed **deadline-free** (no preserve trigger, no double-book) — this ALSO fixes the deadline-mapping gap (the promote leg used to drop the rule deadline, forcing a follow-up `reschedule-repeat`), so the two-command dance is gone. It is the more GUI-parity-respectful shape (a repeating deadline is a rule property, not a one-off on the seed). Refuse fail-closed a deadline with a keyword `--when` (no concrete start to offset from) or a deadline BEFORE `--when`; after-completion is out of scope (no calendar — the seed keeps its one-off deadline). (b) **`make-repeating`** (and, as a trigger-agnostic backstop, `add-repeating`) trashes the redundant preserved instance IN-TXN only when the promote PRESERVED the source (`replacedUuid === null`) AND the instance is FUTURE-dated — the cursor mints the single real occurrence on the date; a today/past preserved instance is the legitimate current occurrence and is left untouched. Disclosed in `warnings[]`. (c) **Simulator** reconciled: the template `instanceCreationCount` follows the plan (1 iff today is the occurrence, else 0), not forced to 1 on any preserve, and the template date columns store the START (deadlined rules back-shift the driven due date) — matching DACON1 DC4 (`sr=icStart=`the start) and the cell-A double-book. Evidence: [docs/lab/dblspawn1-preserved-instance.md](../lab/dblspawn1-preserved-instance.md); oddities §9ff.

- **2026-08-18 · DBLSPAWN1 deadline/offset agreement: `todo add-repeating` accepts `--start-days-earlier N` as an alternative to a concrete `--deadline <date>`; when both are given they must AGREE, and a mismatch is refused (zero mutation).** The deadlined rule's per-occurrence lead can be named two ways — `--deadline <date>` (offset = `deadline − when`) or `--start-days-earlier N` (offset = N, deadline = `when + N`) — both mapping onto the RULE and keeping the seed deadline-free. The geometry `when/deadline/N` is OVER-DETERMINED: the dialog's start-offset is rule-global and the first occurrence's start is derived as `due − N`, so there is NO per-first-instance gap to absorb a disagreement (the decisive contrast with DACON1's off-rule-first calendar freedom, where the "Next:" field DOES hold an off-anchor first). **Ruling:** either input alone maps to the rule; when both are supplied, an exact match (`deadline − when == N`) proceeds as harmless redundancy and any mismatch is refused up front (RangeError, before any mutation) with a message naming both corrected spellings (`--start-days-earlier <D>` or `--deadline <when+N>`). `--start-days-earlier` needs a concrete `--when` and is refused with `--after-completion` (no calendar start to count back from). `project add-repeating` is untouched (its `--deadline` is the project's own item-level due date, no start-offset vocabulary). The `make-repeating`/`reschedule` boolean-`--deadline` + `--start-days-earlier` path is unaffected (the boolean form carries no date to disagree with). (`src/write/promote-clone.ts`, `src/write/operations.ts`; tests: `test/engine/write-promote-clone.test.ts`.)

- **2026-08-18 · PERF1: scope the SESSGATE session-reachability probe to the cheap discriminator and memoize a REACHABLE verdict within an invocation — refuse/relocate/disclose stays byte-identical.** The probe (three window counts → locked/wrong-Space/healthy) enumerated AX windows across every foreground process on EVERY call; near-free on the empty golden, ~8s on a busy real desktop, and a promote composite paid it TWICE (pre-seed gate + in-drive gate ≈ 15s / 53% of a 29s add-repeating on the maintainer's host, 2026-08-18). **Ruling:** (a) run the app-wide walk ONLY when `thingsAx = 0` — `thingsAx >= 1` is already "reachable" so the walk is pure waste; and when it runs, SHORT-CIRCUIT on the first window-bearing app reporting `allAx = 1` (not the sum), because `interpretReachability` only tests `allAx == 0` vs `> 0`, making every verdict byte-identical. (b) A promote composite's pre-seed and in-drive gates share a short-TTL (30s) memo (`createReachabilityCache`) that stores ONLY a `reachable` verdict — a not-reachable verdict must always re-probe fresh because the in-drive gate runs AFTER the reveal, which can legitimately turn a pre-seed "window" into "reachable" (a stale "window" would fire a spurious relocation). A mid-drive lock/Space-move inside the cached window is caught by the cleanup path's OWN independent blindness probe (`clearDialog`, deliberately not routed through the memo), so a cached reachable verdict is safe and the "refuse locked / relocate wrong-Space / disclose" behavior is unchanged. Deliverables 2 (set-datetime collect scoping) and 3 (per-step delay audit) from the PERF1 brief are DEFERRED (see up-next). (`src/write/vectors/session-reachability.ts`, `src/write/vectors/ui.ts`; evidence [docs/lab/perf1-drive-overhead.md](../lab/perf1-drive-overhead.md).)
- **2026-08-18 · PERF2: the set-datetime `AXDateTimeArea` collect is SUBTREE-SCOPED to the resolved dialog shell; per-step settle delays are trimmed ONLY where trace-measured convergence proves margin; reduced-motion animation settings are STANDING CONFIG (never baked into cert clones).** The PERF1 deferrals, landed. **(a) Collect scoping (headline).** `axSetDateTimeScript` walked the app-wide AX tree from the app ROOT at depth 16 to find the Next/Ends/reminder date areas — ~4.4s for "Next" on the busy host because the descent traverses the main window's large list. Ruling: resolve the Repeat-dialog shell FIRST (the sheet-vs-detached `AXUnknown` disjunction the recipe's `DIALOG_SHELLS`/`pathCandidates` already encode, in the SAME priority order) and collect only its subtree; when no shell resolves, fall through to the SAME named "presents N date area(s) … none is the `<target>` control" error the app-root walk threw (census counts still derivable). Byte-identical semantics: deterministic next/ends/reminder `pick`, read-back rejection detection (YANCH1), and the named error are unchanged. Lab (golden-v3): raw walk 125→26ms (~79% even on the empty golden — pure menu-bar/sidebar/toolbar traversal skipped); the "Next" drive step 619→312ms (−50%); OLD vs NEW `rt1_recurrenceRule` byte-identical on ADR1 full-combo AND Next+ends-on drive-cert. **(b) Delay audit — trim only what traces prove.** `SETTLE_AFTER_REVEAL_MS` 1500→1000 (S5a: menu repopulates in ~92ms median/116ms max on a warm running app under DEFAULT animations, a ~13× margin; menu-bar repopulation is a local UI op, not DB/sync-bound; 1000ms keeps ~8.6× the golden max as host headroom, and under-margining only ever fails-closed to a spurious drive refusal, never a bad write). `WAIT_POLL_MS` KEPT at 300 (S5b: mode-switch→control-ready ~462ms EXCEEDS the interval, so 300ms is well-sized). The set-datetime 0.2s read-back settle and the interval/popup/checkbox closed-loop retry delays are KEPT (they guard documented races / the read-back rejection safety semantic). **(c) Animation doctrine — config, not code.** Reduce Motion + `NSAutomaticWindowAnimationsEnabled false` roughly HALVE sheet present+settle (golden: ~532→~260ms, S6) but are NEVER baked into cert clones and the goldens stay untouched: documented as standing configuration for the dedicated automation host + the next golden mint, with the CERT-PARITY caveat — the cert environment must match the production host's animation state, or a trim certified under reduced motion would mask a race the default-animation host still hits. Safety semantics (read-back, named errors, checkbox convergence, watchdog, per-step checkpoints, three-state gate) untouched. (`src/write/vectors/ui.ts`; evidence [docs/lab/perf2-step-latency.md](../lab/perf2-step-latency.md).)

- **2026-08-19 · RSPA1: `reschedule-repeat` derives AND drives the calendar anchor from `--when` — the SAME derive-and-drive make/add-repeating apply, with drive-vocabulary == assert-vocabulary locked.** A `--when`-only reschedule (no explicit `--weekdays`/`--on-day`/`--yearly-month`) used to drive only the deadline-shifted "Next:" field, leaving the weekly/monthly/yearly anchor pop-ups untouched — so the reschedule silently kept the dialog's stale/default anchor while the verify (which derives the anchor from `--when` via `expectedRuleAssertions`) expected the derived placement. A live yearly reschedule failed on exactly this (drove Next=due-date/OK but NO anchor pop-up steps; verify then never converged). **Ruling:** the reschedule compile applies `deriveFixedAnchor` through a shared `reschedEffParams(params)` = `{...params, ...deriveFixedAnchor(params, deadlineDriveNext(params))}` — deriving the anchor from the deadline-adjusted DUE date (`when + startDaysEarlier`) exactly as promote-clone does upstream for make/add. Explicit anchor flags keep priority (deriveFixedAnchor returns an empty patch when an anchor is present — DACON1 off-rule semantics, incl. the monthly dishonored refusal, unchanged); a rule-only reschedule (no `--when`) derives nothing (preserve-unspecified baseline intact). Both `todo.reschedule-repeat` and `project.reschedule-repeat` expectedDelta build assertions from the SAME effective params, so the drive and the verify derive IDENTICALLY — locked by a coherence test that cross-checks the driven anchor pop-up against the asserted `repeating.rule.anchorKey`. (`src/write/commands.ts`; tests `test/engine/write-ui-vector.test.ts`; evidence [docs/lab/rspa1-reschedule-pending.md](../lab/rspa1-reschedule-pending.md).)

- **2026-08-19 · RSPA1: rescheduling a deadlined yearly template that has a PENDING materialized instance is HONORED (all three cell shapes commit) — no validation refusal is warranted; the live "2029 cursor skip" is normal-by-design.** The decisive shape YANCH1/DACON1/DBLSPAWN1 all queued (a template with no current materialized instance is not reveal-selectable) was finally driven in-lab: create a deadlined yearly series with the first occurrence tomorrow, advance +1 day to MATERIALIZE the current-occurrence instance, then reschedule. DB-verified on golden-v3 ([lab/rspa1-reschedule-pending.md](../lab/rspa1-reschedule-pending.md)): **(a)** the on-rule `--when`-only + deadline reschedule (the exact live shape) COMMITS in ~7s with the deliverable-1 anchor-derivation fix — the derived DUE anchor lands (`of=[{dy=29,mo=9}]`=Oct 30), the start back-shifts to `--when` (`sr=2028-10-16`), the deadline offset holds (`ts=-14`), the cursor is the rule-correct next start, and the pending instance is untouched (the live 120s zero-movement discard was the anchorless drive, fixed by d1). **(b)** An off-rule Next (explicit anchor ≠ `--when`) with the pending instance is HONORED, not discarded and not snapped (`ok` in ~6s, anchor `m10d16` lands); its cursor `next=2029-10-02` is the rule-correct NEXT aligned start (Oct 16 2029 − 14), which is EXACTLY the DACON1 live-host "2029 skip" — normal-by-design, not a bug. **(c)** A rule-only reschedule (no `--when`) preserves the anchor/deadline/cursor with the instance untouched (RRD1 holds with a materialized instance). **Spawn:** the re-anchored cursor never double-books the materialized occurrence (`icCount=1`, cursor at a distinct future date — the OPPOSITE of the DBLSPAWN `icCount=0` collision). **Ruling:** the pending-instance reschedule needs NO new validation refusal — the shipped fail-closed verify + poller stable-mismatch early-exit + the d1 anchor drive suffice; the zero-movement-discard hypothesis for the pending-instance interaction is FALSIFIED. One residual (RSPA1-D, up-next): the off-rule disclosure is blind to a PRESERVED deadline (mis-states the appear date; data is correct).

- **2026-08-19 · RSPA1 poller no-movement cap: NOT shipped (a generous-enough floor is unsafe against the #487 slow-commit case, and op-result already solves the capped-harness caller).** The RSPA1 live failure burned the full 120s verify on ZERO movement (the app discarded the commit). A conservative "if literally nothing — asserted fields AND tripwires — has moved after a generous floor, return `silent-noop` early" was considered to surface such a discard within the caller's window. **Ruling: do NOT add it.** The safety argument cannot be stated cleanly: TRACE1/#487 established that on a large, actively-syncing production DB the OK-press commit lands "several times slower" than the golden and "tens of seconds past a 30s timeout" — i.e. a genuinely-successful reschedule can show ZERO DB movement (not even a tripwire) for tens of seconds before the commit lands. A no-movement cap short enough to help would therefore FALSE-report `silent-noop` on a slow-but-successful commit — and unlike the SESSGATE under-margin (which fails CLOSED to a spurious refusal), a false `silent-noop` fails OPEN to a duplicate-creating blind retry (the exact harm the skill warns against). A floor generous enough to be safe (> tens of seconds) provides negligible benefit over the existing 90s watchdog / ≥120s budget. The actual caller problem (a harness killing the command) is already solved: the TRACE1 watchdog returns a structured `uncertain` timeout at the budget, and RSPA1 deliverable-3 `things op-result <op-id>` recovers the true outcome from the durable audit trail after a kill. The DACON1 stable-mismatch early-exit already surfaces the "settled on a wrong value" case within ~5s (safe because it requires asserted MOVEMENT first); pure zero-movement is precisely the case most at risk of a slow-commit false negative, so it is left to the watchdog + op-result. (`src/write/verify/poller.ts` unchanged.)

- **2026-08-19 · VMQ1: lone-Next-write commit inertness is FALSIFIED — no deterministic-dirtying step is warranted.** The live 2026-08-19 reschedule failure (a `--when`-only deadlined yearly, verify-timeout on ZERO movement) had prompted the hypothesis that a lone `AXValue` "Next:" write does not mark the Repeat dialog dirty, so OK discards it — which would require the recipe to add a deterministic dirtying interaction. **Ruling: the hypothesis is false; do NOT add a dirtying step.** DB-verified on golden-v3 ([lab/vmq1-probe-closeout.md](../lab/vmq1-probe-closeout.md) §1): a DAILY lone-Next reschedule (freq+interval re-selected to their current values — proven no-ops — and NO anchor pop-ups at all, so the Next `AXDateTimeArea` write is the SOLE value change) COMMITS (`nextOccurrence` moved); so does a yearly same-anchor lone-Next, and a deadlined yearly whose every rule field is a no-op. The app commits a Next-only change. The live failure is explained by its commit predating the #503 anchor-derivation fix (a `--when`-only yearly reschedule then SKIPPED the anchor pop-ups); post-#503 a `--when`-only reschedule always drives the derived anchor for weekly/monthly/yearly, so the exact failing drive shape is unreachable. Entry closed by falsification. (Separate, out of scope: a deadlined off-rule Next on an `icCount=0` template SNAPS to the anchor occurrence and fails CLOSED — a new probe, up-next.)

- **2026-08-19 · VMQ1: an after-completion repeat has NO Ends control at any dialog index — the `assertRepeatRule` end-bound refusal is PERMANENT (not a targeting gap).** The #476 fix refuses `afterCompletion` + any end bound; the residual asked whether after-completion actually exposes an Ends pop-up at a different recipe index (which would let the refusal be lifted). **Ruling: it does not — the refusal is permanent.** An AX census of the after-completion reschedule dialog on golden-v3 ([lab/vmq1-probe-closeout.md](../lab/vmq1-probe-closeout.md) §3) shows the sheet carries exactly TWO pop-ups — the cadence UNIT (`week`) and the FREQUENCY (`after completion`) — plus the Add-reminders/Add-deadlines checkboxes and the interval field. There is no "Ends" pop-up anywhere; the recipe's `DIALOG_ENDS = pop up button 1 of group 1` resolves in this mode to the cadence-unit pop-up (ANCH1-B FIX4 confirmed). No recipe change, no refusal lift. (`src/write/repeat-rule.ts` unchanged.)

- **2026-08-19 · β1 permission deputy (`things-deputy`): the first agent-daemon build slice is a DUMB PRIVILEGED BROKER — primitives only, never operations — shipped ahead of the phase-plan ratification at the maintainer's explicit direction.** The maintainer re-scoped mid-plan: "just a proxy for issuing AppleScript commands and reading the Things SQLite database … we shouldn't keep all of the logic in the daemon." Rulings bundled with the build (each individually revisitable): (a) **verbs are `hello`/`sql`/`osascript`/`read-file`/`locate` and nothing else** — SQL runs read-only with ATTACH denied by authorizer, one statement per request; osascript has exactly two fixed argv shapes plus a `do shell script`/`do script` refusal lint; file reads are confined to the group-container subtree; co-opting the deputy for arbitrary execution therefore requires already owning the user account (token file 0600 + same-UID peer check keep sandboxed/other-user processes out). (b) **Routing default OFF; precedence `--deputy`/`--no-deputy` flag > `THINGS_API_DEPUTY` > stored `deputy-enabled` > off; the direct path remains the contract's ground truth** and every engine/CLI test keeps running direct. (c) **Fallback is an ACTIVATION-TIME decision, never mid-operation**: enabled-but-unreachable → the whole process runs direct with one stderr notice; a deputy dying mid-request errors that request honestly (no cross-transport retry). (d) **Skew: protocol mismatch deactivates; package-version mismatch on matching protocol kickstarts once then proceeds with a notice** — dumb verbs are skew-safe by construction, which is the core reason the broker shape wins over the op-hosting daemon for this slice ("never execute across skew" applies to engine semantics, which the deputy does not carry). (e) **The database rides a DatabaseSync-shaped facade over a worker-thread sync bridge (Atomics.wait + receiveMessageOnPort)**, so the read layer is untouched and the WAL fresh-snapshot-per-statement law holds (the deputy's connection never opens a transaction); explicit `THINGS_DB`/`dbPath` targets NEVER route (the deputy only brokers the real container db). (f) **Live-binary certification is `THINGS_DEPUTY_LIVE=1`-gated and rides CI's hosted macOS runner** because the maintainer's workstation EDR (Cylance `execution_control`, score −1000, 2026-08-19) auto-quarantines every fresh build — the real-TCC churn gate (grants surviving a rebuild under the ceremony cert) remains OPEN pending an EDR exclusion or an unmanaged Mac. (`deputy/`, `src/deputy/`, PR pending; design §3a.)

- **2026-08-20 · Deputy shortcuts verb: ALL app-automation surfaces route through the broker — `shortcuts run` is restricted to the bundled `things-proxy-*` names.** The maintainer overrode the first-slice scope cut ("I want ALL permission surface areas to be handled by our helper process"): the deputy gains a `shortcuts` verb (`op:list`, and `op:run` with the vector's exact fixed argv `run <name> --input-path … --output-path …`), and both call sites route (the shortcuts write vector, availability's proxy census). **Run is name-restricted to the `things-proxy-` prefix** — the deputy is a paired tool, not a general shortcut runner; an arbitrary user shortcut can do anything, and while a same-user process could run it directly, it cannot do so AS the deputy (mirrors the osascript shell-execution lint). Remaining direct exec calls are consent-free by design: `open` (LaunchServices), `pgrep`, `lsappinfo`, `plutil` on locally-held bytes. (`deputy/src/server.swift`, `src/deputy/shortcuts-exec.ts`.)

- **2026-08-21 · AppData TCC is allow-once-per-process — the deputy splits into a pair, with a SANDBOXED READER holding the durable scoped grant (maintainer-ratified: "the sandboxed reader sidecar is probably the answer").** Live ceremony measurement: `kTCCServiceSystemPolicyAppData` rows are pinned to the granting pid + boot_uuid (`auth_value 5`); three deputy process instances produced three consent prompts; the maintainer's daily prod reads never prompt only because Ghostty holds Full Disk Access (system TCC.db, durable). The earlier "app-data grants are durable" reading is RETRACTED. Durable options were FDA (rejected as over-broad: an exploit in the helper must not be an everything-read) or the one scoped mechanism macOS offers — App Sandbox + powerbox + app-scoped security bookmark — VM-certified by SANDBOX1 ([lab/sandbox1-scoped-reader.md](../lab/sandbox1-scoped-reader.md): durable across processes/reboots/re-signed rebuilds, OS-denied outside the granted folder, WAL sqlite reads clean, socket serving from the container works; amfid refuses ad-hoc on sandboxed code; secinit refuses bare executables → the reader ships as a signed LSUIElement .app). **Rulings:** (a) file verbs ride `things-reader` when granted, else the deputy (interim, consent-stalls per process), else direct; automation verbs ride the deputy only; a present-but-ungranted reader is SKIPPED (status names the ceremony) and its file verbs refuse `not-granted` — no prompt exists in its model. (b) `things deputy grant` is the one-time interactive ceremony (panel presented BY the sandboxed process; grant survives restarts/reboots/rebuilds under the stable identity). (c) FDA stays documented as fallback, not recommendation. (d) The reader's live suite is additionally gated on a signing identity and on no production reader answering (socket collision); the granted read path is certified in the VM, where the panel can be AX-driven. (`deputy/reader/`, `src/deputy/routing.ts`, design §3b.)

- **2026-08-21 · Deputy fallback retirement: the deputy is MUTATIONS-ONLY — file verbs are reader-or-direct, never deputy (maintainer: "I don't want a convoluted multi-step fallback setup").** The deputy's file verbs (`sql`/`read-file`/`locate`) and its container access are deleted outright (Swift answers `unsupported-verb`); with them went the entire consent-stall apparatus they required — the 90s first-contact locate budget, the watch-for-the-dialog stderr notices, the stall-proof hello caching, and the deputy-side SQLite connection. Clean model: **reads = reader or direct; mutations = deputy or direct; the helpers never talk to each other and the TypeScript library is complete without both.** The shared SQLite executor stays live-certified via an unsandboxed test harness (test/deputy/helpers/sqlite-harness) since the reader's granted path is only reachable through the panel (SANDBOX1 VM rig or a human). (`deputy/src/server.swift`, `src/deputy/routing.ts`.)

- **2026-08-21 · Helper UX + distribution ratifications (maintainer: "all of your answers are satisfactory, incorporate all of this into the plan").** (a) **Auto-detect routing**: installation is the intent signal; `deputy-enabled` becomes tri-state auto|true|false (default auto = present-and-healthy ⇒ used); degradation is loud, never silent. (b) **Two processes, one possible bundle**: the reader/deputy split is forced by the platform (sandbox is per-process; sandboxed processes cannot do the automation half) and is a security feature (compromised reader = one read-only folder); packaging MAY consolidate both executables into one .app with per-executable entitlements. (c) **Distribution**: notarized prebuilt helpers ship in the npm tarball, signed at release time in CI (encrypted secrets, tag-triggered, fork-PR-safe); source+build-script stays the bring-your-own-identity path (deputy: any persistent cert incl. self-signed; reader: Apple-issued chain required — free Apple ID suffices, pending one probe cell). (d) **Names stay `reader`/`deputy`** ("the helpers" collectively). (e) **β's resident worker is a launchd NODE process** running the library engine over the helpers — zero own TCC, ledger stays engine-written, helpers stay operation-blind. (f) Socket activation + idle-exit queued as δ polish. (Design §3/§3b/§3c; up-next Helper UX/distribution batches.)

- **2026-08-22 · Single helpers bundle NOW (pre-Automation-grants), `things helpers` command surface, wholesale-owned install dir, and a separate helpers version line (maintainer-directed: "should we just go ahead and make that change now?").** (a) **Packaging pulled forward from the distribution batch**: `Things API Helper.app` (CFBundleIdentifier `com.pixelcog.things-api-helper`) houses things-deputy as its main executable and the sandboxed things-reader nested at `Contents/Helpers` with its identity UNCHANGED — timed deliberately before any deputy Automation grants existed, so the deputy's TCC identity flip (path-keyed bare Mach-O → bundle-keyed, an upgrade) cost nothing, and the maintainer's live bookmark grant survives (app-scoped bookmarks key on signing identity, not disk path). (b) **No migration logic, structurally**: `things helpers install` owns `<state>/deputy/bin/` WHOLESALE (bootout both, `rm -rf`, fresh copy, bootstrap) — erases any prior layout forever and resets the kernel's per-vnode CS cache; this is the standing contract, not a transition shim. (c) **Vocabulary**: command group `things helpers …`, config `helpers-enabled`, env `THINGS_API_HELPERS`, flags `--helpers`/`--no-helpers`; "deputy"/"reader" remain the halves' proper names; ALPHA clean break, no aliases. (d) **Helpers versioned on their OWN line** (`deputy/VERSION` ↔ `EXPECTED_HELPERS_VERSION`, drift-tested): a package release with unchanged helper sources must not nag for (or force) reinstalling a byte-equivalent bundle; the protocol version stays the hard gate. (e) **Grant UX**: the panel opens INSIDE the Things folder (accept = the only click; macOS offers no row-preselect API); the ceremony verifies a `locate` inside the granted scope and reports a wrong-folder grant loudly; the serving reader re-resolves its scope whenever the bookmark bytes change, so a corrective re-grant needs no restart. (f) **Ratified direction, queued**: `things helpers grant` grows into a FULL onboarding ceremony (fire ALL vector prompts while a human is present — deputy→Things3 + deputy→System Events Automation, Accessibility registration, Shortcuts install + first-run, URL-scheme readiness — then verify-report per vector) — merged into the `things onboard` item; and a graceful upgrade DRAIN (finish in-flight requests before exit on install/restart) rides the Helper UX batch. (`scripts/build-helpers.sh`, `src/deputy/install.ts`, `src/cli/commands/helpers.ts`; design §3/§3c.)

- **2026-08-22 · Identical-schema version-stamp bump ⇒ ship the baseline immediately; behavioral re-cert rides the queue (precedent, Things 3.23 / dbv 27).** When a Things update bumps `Meta.databaseVersion` but the observed fingerprint is byte-identical to the prior baseline AND no new extra columns appear, the new baseline ships same-day reusing the prior fingerprint (writes re-enable honestly — the schema gate measures DDL, and none moved); the `accepted-fingerprint` escape hatch is NOT used for this case. The stamp-only bump is treated as evidence of an app-internal data/behavioral migration: `certified-app-version` does NOT advance until the full drift-runbook re-certification (new golden + lab:regress + register walk) lands, and doctor's passive behavioral notice carries the signal meanwhile. (First application: `src/db/baselines/db-v27.ts`; up-next "Things 3.23 re-certification campaign".)

- **2026-08-22 · DBV27 clarification: the "identical schema" claim refines to identical TABLES/COLUMNS — the 3.23 migration's DDL delta is INDEX-ONLY and its data rewrites are real (measured).** The full sqlite_master diff against Things' own pre-update backup found two index changes (a new partial template index; the repeatingTemplate index gaining `creationDate`) plus data migration (`rt1_nextInstanceStartDate` retired, self-counting leaf counters, forward-re-anchored spawn cursors) — see [lab/dbv27-migration-diff.md](../lab/dbv27-migration-diff.md). The prior ruling STANDS unchanged (the fingerprint deliberately measures depended tables/columns; indexes and data belong to the behavioral-re-cert side), and the measured rewrites set the re-cert campaign's priorities: projection-day fix first, repeater laws first in the register walk.

- **2026-08-22 · A retired app-maintained cache is READ WHEN PRESENT and DERIVED when absent — that is live-version support, not an alpha-contract compatibility shim (precedent: the template projection day, Things 3.23 / DBV27).** When a Things update stops maintaining a column the engine depended on (`rt1_nextInstanceStartDate`, nulled library-wide by the dbv-27 migration), the answer is ONE helper that prefers the column while a supported app version still writes it and otherwise derives the value from data the app DOES maintain (here: the decoded recurrence rule + the `rt1_instanceCreationStartDate` spawn cursor, run through the existing occurrence generator — never a second rule evaluator). Reading the older shape is support for a version people are running, which the ALPHA-CONTRACT no-compatibility-machinery rule does not forbid; what it forbids is machinery for OUR own past formats. The derivation must FAIL CLOSED — an undecodable rule, an after-completion series, a paused or ended series, or a missing cursor yields `null`, and every consumer treats that exactly as it treated the NULL column (no membership, no leg, never a guessed date). (`src/model/template-projection.ts`; consumers `src/write/move.ts`, `src/write/reorder.ts`, `src/write/pre-state.ts`, and on the read side `src/read/views.ts`, `src/read/detail.ts`, `src/model/mappers.ts` — the projection inputs ride on every fetched task row (`src/read/queries.ts`), and the retired column is off the `TaskRow` type entirely so no read can reach around the helper.)

- **2026-08-23 · A private-surface capability whose DECLARATION canary cannot see its breakage is VERSION-GATED, not left to fail-closed verify (Things ≥ 3.23, the private reorder command).** GV4 measured `_private_experimental_ reorder to dos in` on 3.23 as a silent no-op — parses, exits 0, changes nothing — while `Things.sdef` still declares it, so `sdefDeclaresPrivateReorder` stays green and the planner kept PREFERRING a doomed path (verify caught it every time: exit 3, zero mutation, honest `app-behavior-change` — but only after spending the attempt). Mike's ruling: "if it no longer works and we cannot provoke it to work again, we ought to disable it for the time being" — take (a) the version gate NOW, keep (b) the behavioral canary queued as the durable defense (it is what lifts the gate). Wired as `privateReorderIsNoOp(installedVersion)` reading the version the environment tuple already captures: ≥ 3.23 ⇒ native unavailable, composing with the existing `allow-experimental` + sdef gates (any one failing disables it); an UNKNOWN version keeps prior behavior (never gate on an unreadable version); the bench simulator fence still stands in for the surface, so the bench is host-version-independent. Downstream is the existing native-unavailable behavior — the SIT7 fallbacks engage and disclose, the template-bearing day-group and unswept-resolved movee classes refuse — with the pre-dispatch canary extended so an explicit `--strategy native` and `project.move-heading` (same command, no fallback) refuse loudly instead of dispatching. Evidence: [lab/gv4-323-campaign.md](../lab/gv4-323-campaign.md) §3.1/§3.5.

- **2026-08-23 · A column that becomes a load-bearing read JOINS the depended manifest in the same breath, fingerprint change and all (`rt1_instanceCreationStartDate` + `rt1_instanceCreationCount`).** The projection-day helper (#520/#522) made the template spawn cursor and spawn tally load-bearing while they sat outside `DEPENDED_TABLES`, where their removal in a future migration would surface as a raw SQLite error mid-read rather than a drift block with remediation. Ruling: add them; the fingerprint moving is the expected cost of the manifest being the single source of truth, not a reason to defer. Both columns pre-exist in v26 and v27 identically, so the shared baseline hash moves once for both (`sha256:784bd2f6…` → `sha256:d2b7e98c…`, fixture-derived per the fixture's exact-reproduction contract) and the golden metadata is re-recorded in the same change — a manifest edit that skips that re-recording aborts `lab:run` at bootstrap.
- **2026-08-22 · A GUI recipe forks on the MEASURED dialog structure, never on the app version (precedent: the Things 3.23 Repeat dialog, RDLG2).** When an app update moves the controls a recipe addresses, the fork is decided by a probe of the live tree — here a `probe-dialog-shape` step reading which CONTROL CLASS occupies the first-occurrence row — and the recipe ships BOTH shapes (`shaped` per-step overrides for a moved index, `onlyShape` for a control whose PRIMITIVE changed). A version string is not evidence about a tree: it cannot be read before the dialog opens, it says nothing about a point release that moves either way, and it turns "which control is this?" into "which build am I on?". Two corollaries the campaign paid for: (a) each branch must be a POSITIVE match, so an unrecognized third shape refuses rather than being mistaken for the older one; (b) discriminate on the control, not on a nearby LABEL — the first probe keyed on the presence of the `Next:` static text, which Things 3.22.14 also has, and mis-drove the older dialog until the regression arm caught it. Every recipe change of this class ships with a regression arm against the previous golden, in the same run. (`src/write/vectors/ui-recipes.ts`, `src/write/vectors/ui.ts`; evidence [lab/rdlg2-323-recipe-cert.md](../lab/rdlg2-323-recipe-cert.md) §2.2.)

- **2026-08-22 · A vocabulary the APP removes is refused with the reason, never emulated (precedent: off-rule first occurrences under Things 3.23, RDLG2).** Things 3.23 replaced the Repeat dialog's free first-occurrence date field with a menu of the rule's own occurrences, so a series can no longer START off-rule. We do not approximate it (pick the nearest offered date), and we do not silently drop the request: the step fails closed naming what the dialog offers and what to do instead, the pre-1.0 contract keeps the `--when` flag unchanged, and the same request still lands on an app version that still offers it. The app-side loss is recorded in [things-app-oddities.md](../things-app-oddities.md) §11 rather than papered over in the engine.


- **2026-08-22 · A suite expectation forks on the golden's APP VERSION, and the fork must carry a POSITIVE assertion of the new behavior (`expectFrom`, Things 3.23).** 3.23 made `_private_experimental_ reorder to dos in` accepted-and-inert while still declaring it, so a flat suite expectation could only be the old law (permanently red) or the new one (un-runnable on golden-v3, the retained 3.22.14 fallback). Ruling: the suite DSL gains `ProbeSpec.expectFrom` — version-bounded expectation overrides resolved with `compareAppVersions`, i.e. the SAME comparator the shipped version gate uses, so suite and engine can never disagree about which side of a boundary a golden is on; an unparseable version falls back to the base expectation, never to an invented claim. Note the contrast with the RDLG2 ruling above: a GUI RECIPE forks on the measured tree because it can probe it before acting, whereas a SUITE expectation is a statement about a whole app build and has nothing to probe — the golden's version IS the fact. The companion `allowUnsatisfiedWaits` (a dead wire's only observable is a wait that times out) carries a hard rider: the override MUST replace the wait oracle with a positive assertion of the inertness — `deltaEmpty` for a pure cell, `fieldUnchanged` over the rows the dead leg would have moved for a mixed one. That is what makes the suite the BEHAVIORAL canary the declaration check cannot be. The rider is not theoretical: five o-suite cells were passing on 3.23 *vacuously* (their target order was the fixture's existing order, so a no-op satisfied the wait), which is exactly why "the waits stopped timing out" was rejected as the canary. Evidence: [lab/gv4-323-certification.md](../lab/gv4-323-certification.md) §1.

- **2026-08-22 · `SIMULATED_DATABASE_VERSION` tracks the app's `Meta.databaseVersion`, not the schema fingerprint (26 → 27).** The 26→27 bump is fingerprint-INVISIBLE (index-only DDL; `DB_V27.fingerprint === DB_V26.fingerprint` by construction) but the DATA semantics moved, and data semantics are what the simulator appliers model. Leaving the constant at 26 would model a generation the app no longer has and would fold two unverified generations into the next bump. Corollary: `docs/atlas/schema-v26.md` and `test/fixtures/schema-v26.sql` are NOT renamed per generation — one file per TABLE SHAPE, carrying a v27-delta section; ~15 documents link those paths and a rename is churn, not information. Evidence: [lab/gv4-323-campaign.md](../lab/gv4-323-campaign.md) §2, [lab/simfid-results.md](../lab/simfid-results.md).

- **2026-08-23 · A release that cannot sign the helpers FAILS; shipping without them requires deleting an edge, not forgetting a secret.** The signing job checks all five Apple secrets before doing anything and errors naming the missing ones, `publish` carries `needs: build-helpers`, and the job additionally refuses when the built bundle has no nested `things-reader` (the one thing an Apple-issued chain buys) or when no VALID `Developer ID Application` identity is in the keychain. The alternative — degrade quietly to an unsigned or helper-less tarball — would publish a version number that silently means something different from every other release, and the failure would surface months later on a user's machine as "grants don't survive rebuilds". Deliberately shipping without prebuilt helpers therefore costs one explicit edit to the workflow. Corollary on chain validity: `scripts/build-helpers.sh` picks its identity with `security find-identity -v` (valid identities only), so a `.p12` exported without its intermediate would select *nothing* and produce an unsigned bundle; CI imports Apple's `DeveloperIDG2CA`/`DeveloperIDCA` alongside the leaf and then asserts validity, rather than trusting the export's shape. Runbook: [release-signing.md](release-signing.md).

- **2026-08-23 · A binary artifact crossing a CI job boundary travels as a tar, and its arrival in the npm tarball is ASSERTED, not assumed (the npm11-bin-path class).** `actions/upload-artifact` does not preserve the executable bit, so a `.app` uploaded as a plain path comes back with a `0644` main executable — a bundle launchd cannot start, and nothing in the publish path would have noticed. The stapled bundle is therefore archived with `tar -czf` (`COPYFILE_DISABLE=1`, no AppleDouble litter) and unpacked on the publish runner, which then re-checks both executables and greps `npm pack --dry-run --json` for them before the version number is spent. Same reflex applied locally: `scripts/pack-smoke.sh` asserts the packed-and-installed bundle's executables are present and `+x` whenever `deputy/prebuilt/` exists, and both the workflow and pack-smoke refuse a bundle containing symlinks. Measured while building this: npm's `files` allowlist does beat `.gitignore` (the gitignored `deputy/prebuilt` packs), the space in "Things API Helper.app" survives, and modes round-trip as `0755` — all three verified with a real `npm pack`, none of them assumed.

- **2026-08-22 · `helpers-enabled` is tri-state and `auto` is the default: under `auto`, ABSENCE is silent and UNHEALTH is loud.** Installation is the intent signal, so a present-and-healthy helper is used with no ceremony — but the two ways of not routing are not the same event. A machine with no helpers installed is an ordinary machine (the direct path is the contract's ground truth and the product), so it gets no notice; a helper the host DID install and that cannot serve is the exact failure this pair exists to prevent, so it always gets one — silence there would hide the consent churn behind a "working" command. Installation is read off the installed bundle on disk, never the socket, so a stopped launchd job still counts as installed and stays loud. `true` keeps the strict reading (absence is noticed too), `false` never routes, and any other value is REFUSED by `config set` rather than coerced to off — a typo must never quietly disable routing. One consequence, accepted: a process spends at most ONE helpers notice across the routing layer and the passive CLI check (`src/deputy/notice.ts`); stacked lines about one broken helper are noise, and the first already names a remedy that resolves the state. (`src/config.ts`, `src/deputy/routing.ts`; design [agent-daemon.md](agent-daemon.md) §3c.)

- **2026-08-23 · `things helpers grant` IS the onboarding ceremony: every consent fired in one sitting, each leg gated on a prompt-free check (maintainer's directive: "Why can't we have the prompts all taken care of at the same time we register the reader?").** The reader's folder panel was only one of five consents the pair needs; the rest were being discovered by whichever command happened to hit them first, mid-workflow, days later. They are now one command: reader read grant → Automation to Things → Automation to System Events → Accessibility → bundled-shortcuts census → a verify-report per leg. Three rulings inside it. (a) **Automation is primed THROUGH the deputy's `osascript` verb** — a benign `tell application … to version`, because macOS offers no way to request Automation consent without sending a real event; sending it as the deputy is the entire point (the record attaches to the helper's stable Developer ID identity, never to the terminal), and the blocking request means the user answers the dialog in the same breath. (b) **Status is read from the deputy's own `hello`** (`axTrusted` via `AXIsProcessTrusted`, `automation.{things,systemEvents}` via `AEDeterminePermissionToAutomateTarget` with `askUserIfNeeded: false`, mapping 0/-1743/-600/else → granted/denied/not-running/unknown) — which makes the ceremony IDEMPOTENT by construction: an already-granted leg is recognized without raising anything, so a rerun on an onboarded host raises nothing and reports all-green, and `status`/`doctor` gain the same rows for free. Additive fields + a new `prime-ax` verb, NO protocol bump: an old CLI ignores them and a new CLI treats absent as unknown rather than false. (c) **`pending` exits 0.** An unanswered dialog or an unflipped Accessibility switch is human pace, not failure — the remedy is "finish it and rerun", and the ceremony resumes exactly where it stopped; only a refusal (-1743, or the OS saying no) exits nonzero. Consequence claimed and testable: on an onboarded host, first-write consent prompts are extinct. (`deputy/src/tcc.swift`, `deputy/src/server.swift`, `src/deputy/install.ts`, `src/cli/commands/helpers.ts`; design [helpers-onboarding.md](helpers-onboarding.md).)

- **2026-08-23 · A post-write verify picks its ORACLE by rule kind: an after-completion series is verified against its materialized occurrence, never the template's spawn cursor (#508).** The first-occurrence check compared `--when` against `rt1_instanceCreationStartDate` for every promote, but that column is a FIXED schedule's cursor — an after-completion template is minted with no next/reference date at all (RSIM2 / RSIM-P P4), so a correct creation failed closed 6/6 on a live library. Fail-closed is only honest when the oracle can actually see the thing being asserted: where it cannot, the right move is a different oracle (the instance row's own `startDate`), and where no oracle exists the check is SKIPPED and the create delta stands, rather than manufacturing a mismatch. Corollary applied in the same change: the DBLSPAWN1 redundant-future-instance clean-up is likewise cursor-scoped, so it exempts after-completion series — with no cursor there is no double-book, and the preserved instance it would have trashed is the series' only occurrence. Evidence: `test/engine/write-promote-clone.test.ts` (#508 cells); VM certification cell queued in [up-next.md](../up-next.md).

- **2026-08-23 · Multi-line notes come in through `--notes -` (stdin) or `$'…\n…'`, and NOTHING else: no `--notes-file <path>`, no `\n`-escape interpretation inside the plain flag.** A `--notes-file` flag would be a second spelling of `--notes - < file` — shell redirection already covers it, and every extra input path is another thing an agent has to choose between. Interpreting literal `\n` escapes inside a plain `--notes` value is worse than redundant: it makes the flag's meaning depend on whether the caller wanted a backslash, so a note that legitimately contains `\n` (a code snippet, a Windows path) silently loses it. The two shipped spellings are unambiguous — stdin carries bytes verbatim, and `$'…'` is the shell's own escape processing, visible at the call site.

- **2026-08-23 · The mutation lock has TWO scopes, and the composite scope is carried on the ASYNC CONTEXT (Mike 2026-08-18, implemented).** Per-leg remains the base case for a single mutation; a composite (one verb, several mutations) holds one lock end-to-end via `withMutationLock`, and its legs' acquisitions are reentrant no-ops. The mechanism is `AsyncLocalStorage`, not a threaded token and not a process-wide "we hold it" flag, and the choice is load-bearing rather than stylistic. A threaded token has to be forwarded through every hand-built leg-options object, and the failure mode of one missed forward is a composite deadlocking against its own holder for the full 30s wait. A process-wide flag makes the reentrancy too WIDE: two genuinely concurrent composites in one process (parallel MCP tool calls) would each read the other's hold as their own and interleave silently — the exact race being closed. The async context is the composite's own extent by construction: legs and nested composites inherit it, a sibling started outside it does not. Adopted first in the promote verbs, whose promote leg selects its row by TITLE and is therefore actively unsafe to interleave; the remaining multi-leg orchestrators are queued in [up-next.md](../up-next.md). One accepted asymmetry: a composite refused for contention writes NO audit record (the single-op path records its blocked attempt) — nothing was attempted, so there is no leg to record.

- **2026-08-23 · The daytime Today reorder carries PROJECT rows, because the caveat that barred them was measured on an app version that no longer behaves that way (VMRES1).** The `today` bounce refused a project movee on the strength of ORD-12 / SIT3 EVEPROJ: "a project's daytime `when=today` landing is mid-pack, not the front-insert the protocol needs". On Things 3.23 it front-inserts at the day cohort's `todayIndex` minimum — five re-entries, both `when=` spellings, both away-legs, and an interleaved five-row target landed 2/2 in protocol form and again through the shipped CLI. The fix is therefore a spec value, not new machinery: `today` joins `evening` and `day` on the per-type leg op, and the refusal (keyed off `legOp === "todo.update"`) lifts itself while staying in force for the index-axis bounces that still have no measured project law. The general point, since this is the second ordering caveat to expire this way: a refusal justified by a MEASUREMENT must be re-checked against the app version it is running on, not inherited — and the version-gate work that removed the native path is exactly what made this one load-bearing enough to notice. Evidence: [lab/vmres1-residuals.md](../lab/vmres1-residuals.md) §3; the superseded reading stays in [lab/gv4-323-certification.md](../lab/gv4-323-certification.md) §1.5 and the [assumption register](../reference/assumption-register.md) ORD-12 EVEORD row now carries the falsification.

- **2026-08-23 · A UI-drive step that reads app state back must prove the read describes the thing it just did — a settle alone is not the guard (VMRES1).** `axSelectRowScript` selected a table row and immediately asked Things for `name of selected to dos`. The readback lags the select, so a blank spacer row — which takes no selection at all — was credited with the PREVIOUS row's title; the loop returned success one row late and left the table with nothing selected, and the next step's wait for a selection-gated menu item timed out. It failed closed, but it made every project row-select drive after the first in a Things session fail (0/3 against 2/2 on a cold app — the shape of a race, which is what stopped it being read as app behavior). The fix is the one its sibling `axSelectHeadingRowScript` already carried: settle, then require `selected of (row i)` — the element THIS iteration acted on must itself hold the state being read — before trusting the readback. Adopted as the standing shape for readback-verified primitives: the assertion is about the element addressed, never about a global the app may still be updating. Two sibling primitives written to different standards is the failure worth generalizing from; `grep` for readbacks that assert only on app-global state when the next primitive of the same family is added. Evidence: [lab/vmres1-residuals.md](../lab/vmres1-residuals.md) §2.

- **2026-08-23 · A universal write flag reaches `batch` in BOTH shapes, and the per-line value outranks the run level.** `--preserve-modified` was single-op only, which left the natural bulk vehicle unable to do the one job the flag exists for. The shipped answer is deliberately two-sided rather than either half alone: a run-level flag applies to every line (a bulk cleanup should not have to stamp the same option onto hundreds of generated lines), and a per-line `options` key overrides it in BOTH directions (`??`, not `||` — an explicit `false` opts one line back onto the timeline). Precedence runs per-line → run-level → absent, the same shape a shell command's flags take over its environment. Nothing about the restore machinery changed: each leg takes the ordinary single-op path, so the disclosure (`preservedModified` / `preserveFailures`) and the audit record's captured pre-`umd` are per-leg exactly as they are for a single write. The generalizable rule: when a universal write flag is missing from `batch`, add it to the per-line `options` subset AND as a run-level default — the per-line half alone makes generated JSONL noisy, the run-level half alone makes a mixed submission impossible.

- **2026-08-24 · The `things helpers` surface is `status` / `restart` / `setup` / `uninstall [--revoke]`; `auto` routing requires the requisite grants; a pending setup exits NONZERO (Mike).** Three rulings in one reshape. (a) **Surface.** `install`, `grant`, and `reset` are DELETED. `install` and `grant` were always run back-to-back, so `setup` is both — an idempotent install-or-update (the install dir is owned wholesale, so update IS install) followed by the whole ceremony, with an upfront banner that sizes the sitting honestly from the prompt-free state ("about to raise N macOS consent dialogs (…) — someone must be at the screen") before anything is raised. `reset` was a `--revoke` flag wearing a command's clothes; it is now `uninstall --revoke`, leaving plain `uninstall` as the gentle, unconfirmed form that keeps grants (TCC rows are identity-keyed, so they go dormant and a later `setup` resumes them) — only revocation is confirmation-gated. NEW capability inside it: `--revoke` works on an ALREADY-uninstalled machine, because `tccutil` resolves bundle ids through LaunchServices — registering the packaged bundle first (`lsregister -f -R "<bundle>"`, MEASURED on a live host) makes both identities addressable again, and the bundle is left registered because the file really is there. With no bundle anywhere the limit is reported honestly, never as a warning. (b) **A still-outstanding setup exits nonzero** (`ExitCode.Environment`) — this REVERSES the 2026-08-23 ruling that `pending` exits 0. `pending` is still human-pace and still resumable, and the closing line says exactly where rerunning picks up; but exit 0 told an agent driving setup for an absent human that the machine was ready when it was not, and that reading is the one the exit code exists to serve. A `denied` leg names BOTH remediations (the System Settings switch and `tccutil reset AppleEvents com.pixelcog.things-api-helper` to re-arm the spent dialog) and the ceremony never clears a denial itself. (c) **No "installed but unpermissioned" auto state.** Under `auto`, installed-and-healthy no longer engages routing: a deputy without an app-control grant would relocate the consent dialog onto the helper, where nobody is watching for it — the exact ambush the pair exists to end. Each half is gated on its OWN requisite along the seams that already existed: writes on `hello.automation.things === "granted"` (`activate()`), reads on the reader's bookmark (`activateReader()`/`fileTransport()`), so a half-finished ceremony gets the half it earned. `axTrusted` and `automation.systemEvents` are NOT requisite (the UI vector is separately double-gated). A deputy too old to report its standing is not provably onboarded and fails CLOSED; mode `true` routes regardless. Dormancy is loud — one stderr notice, a `status` line, and doctor's reason all name `things helpers setup`. (`src/deputy/{install,routing}.ts`, `src/cli/commands/helpers.ts`; design [helpers-onboarding.md](helpers-onboarding.md).)

- 2026-08-24 · **Permissions doctrine RATIFIED** (docs/design/permissions-doctrine.md): consent only inside the two setup ceremonies (`things setup` / `things helpers setup [--gui]`); every other entry detects prompt-free and refuses with remediation; UI vector requires the helpers; direct path floors on host-app FDA (sub-FDA app-data class is measured pid-pinned); explicit `--db` outside the doctrine; no env-based agent sniffing (TTY selects wizard-vs-strict inside ceremonies only) · Mike, shaped in-session.

- 2026-08-24 · **Permissions doctrine WAVE B shipped** — the `--gui` tier on `things helpers setup` (Accessibility + Automation→System Events, `ui-enabled` set on full success, implied without the flag when it is already on, hinted at on base-tier success), the shared TTY wizard on BOTH ceremonies (an explainer per dialog that is actually coming, an Enter gate, the tier question; strict mode byte-for-byte unchanged off a TTY), Article IV enforcement (the ui vector gated on the deputy's own standing, keyed on a `drivesGui` DECLARATION never on the vector id, with `THINGS_API_UI_DIRECT=1` as the lab's documented non-consumer escape), MCP startup baking (one prompt-free survey, stated in the instructions, immediate refusals with kill→setup→restart, never re-surveyed per call), doctor's per-vector provenance table, and the staleness matrix in the doctrine's enforcement inventory · per doctrine.

- 2026-08-24 · **`things setup`'s read leg is a two-way CHOICE, and it never polls for Full Disk Access (Mike, live feedback).** Three rulings from one sitting at the real command. (a) **The FDA poll is deleted as structurally dead code.** MEASURED: macOS applies FDA to a process only across a relaunch — the Settings sheet says so itself ("… will not have full disk access until it is quit", Later / Quit & Reopen) — and the responsible process, plus every child it spawns including a rerun of `things` in the same window, keeps the old answer. Wave A's "setup waits and watches for the switch" could therefore never succeed. The `f` branch now deep-links the pane, prints the three steps, marks the leg `pending` with the relaunch as its reason, and CONTINUES the remaining legs so the post-relaunch rerun has only the read leg left. (b) **The default is the SESSION grant, chosen by Enter, never a timeout fallback.** FDA is "the nuclear option" (Mike's words) and is offered, not assumed: Enter provokes the app-data modal now, which is the only read grant obtainable within the sitting, and is what strict mode answers with silently. (c) **Ceremony copy is flat** — no nested parentheticals; the banner names its dialogs in one clause each ("about to raise 2 dialogs — data access for Ghostty, and app control of Things. Someone must be at the screen."), pinned by a regression cell. (`src/direct-setup.ts`, `src/wizard.ts`; doctrine enforcement inventory.)

- 2026-08-24 · **A setup ceremony runs with the process's DEFAULT signal disposition; Ctrl-C is never caught in raw mode (Mike, live feedback).** MEASURED under a pty: the CLI's startup `process.once("SIGINT", …)` (`src/cli/interrupt.ts`) replaces the kernel's default disposition with a libuv watcher whose handler can only run ON the event loop — which the ceremony's synchronous gate (`read(2)` on /dev/tty) and bounded waits (`Atomics.wait`) hold for their whole duration. The signal is queued, libuv restarts the EINTR'd read, ISIG keeps ^C from ever arriving as a byte, and Ctrl-C does nothing at all while the copy promises it stops the run. Ruling: both ceremonies wrap their synchronous span in `withDefaultInterrupts`, lifting the JS listeners so the kernel terminates at the conventional 130; the terminal is NEVER put in raw mode to read `\x03` (raw mode disables ISIG and is what causes this bug class, and it leaves state to restore on paths a signal never lets us reach). End of input and a `\x03`/`\x04` arriving as data raise `CeremonyStopped` — a clean, resumable stop, nonzero. (`src/wizard.ts`, `src/cli/commands/{setup,helpers}.ts`.)

- 2026-08-24 · **The reader's rendezvous is inside its sandbox container, so client-side reader probes are per-host TCC-gated — guarded now, relocated later.** Found live from a sandboxed host (ChatGPT.app): `things helpers status` raised the "access data from other apps" modal outside any ceremony (Article I violation, screenshot on record) and crashed with a raw EPERM after the deny, because the reader's socket and token live in `~/Library/Containers/com.pixelcog.things-reader/Data` and every client stat/read of them is a cross-app container access evaluated against the HOST's lineage. Ghostty's FDA had masked it for the whole arc. Rulings: (a) every reader-container touch OUTSIDE the two ceremonies is guarded by `readerContainerAccessible()` — true only on an explicit `THINGS_API_READER_DIR` (not a container), a passing FDA probe, or a live session app-data grant — and the guard touches nothing to decide; (b) an unguardable host gets the honest new reader state `unreachable` (never a claimed false "not running") plus one remediation line, and the read chain proceeds to the direct verdict; (c) **the no-fallback rule is SCOPED to a reachable-but-failing reader** — it does not govern a host that cannot see the rendezvous at all; (d) every rendezvous read is EPERM-safe and never retried, ceremonies included. The durable fix — launchd `Sockets` at a host-neutral path plus token-delivery relocation — is promoted out of δ polish into up-next's feature-design queue; until it lands, reader reads are FDA/session-grant-gated per host. (`src/host-access.ts`, `src/deputy/routing.ts`, `src/deputy/install.ts`; doctrine § DISCOVERED 2026-08-24.)

- 2026-08-24 · **Template mutations ride "Create Next Copy, then mutate the instance" — the chooser recipe is SUPERSEDED (Mike).** A repeating template refuses every schedule/status write (`H-REPEAT-SCHEDULE`), and the app's own Make Exception chooser is not on the automation path. CNC1 measured the alternative and it is exact: `Items ▸ Repeat ▸ Create Next Copy` followed by an ordinary instance write reproduces `Make Exception` **field for field** on the template (weekly against REPX3 §1.2, daily against §2.1; vacated slot silent against a live control), differing across the whole composite by ONE column on ONE row — the minted instance's `userModificationDate`, which SYNCX1 measured to be the winning side of the merge arbitration. So: (a) **`todo complete` / `cancel` on a template are UNFLAGGED** — act on an open materialized current instance if one exists, else CNC + the status write, disclosing which occurrence was resolved and when the next lands (CNC1 §6 certifies both, cancel measured for the first time). (b) **Scheduling writes still refuse bare**, but `H-REPEAT-SCHEDULE`'s message becomes a two-way steer — `--exception` for the next occurrence only, `reschedule-repeat` for the series — and `--exception` runs the composite. (c) **Three measured refusals**: a target that is a live slot of the same rule (double-books, CNC1 §2 / oddities §17), an after-completion template (CNC duplicates today, CNC1 §5 / new oddities §18), and repeating PROJECTS (no `Items ▸ Repeat` menu for a project selection, CNC1 §8). (d) **Half-reversible and disclosed**: `things undo` restores the instance write perfectly and can neither un-mint the row nor rewind either cursor column (CNC1 §7). (e) The **chooser recipe stays characterized-but-unbuilt** — CNC needs no natural-language search field, no variable button set, no HID click. Evidence: [lab/cnc1-template-mutations.md](../lab/cnc1-template-mutations.md).

- 2026-08-24 · **The reader's rendezvous is a launchd-owned socket plus an installer-minted env token at `<state>/reader`; the container rendezvous is retired and reader routing is host-universal.** The interim guard ruled hours earlier is deleted, not extended: the reader BINDS the socket itself at `<state>/reader/reader.sock` (launchd `Sockets` activation is sandbox-blocked — `launch_activate_socket` returns 159 "Sandbox restriction", measured; the working grant is the home-relative file exception + raw-SBPL `(allow network-bind (subpath "/Users"))`, since unix bind is the network-bind operation class file exceptions do not cover); the installer mints the access token, writes it 0600 beside the socket for clients, and injects the same value into the plist as `THINGS_READER_TOKEN` (the plist is therefore 0600 too). Rulings: (a) no fallback bind and no self-minted token — a reader started outside its LaunchAgent exits 78 naming what is missing, so the old placement cannot silently return; (b) the reader owns its socket again, so the SIGTERM drain unlinks it first (the deterministic accept-wake) then closes the fd; (c) the security-scoped bookmark STAYS in the container (reader-internal, never client-read) and the deputy is untouched (its rendezvous was never in a container); (d) every client-side gate on reader probes is removed along with the `unreachable` reader state, keeping only the EPERM-safe read belt; (e) migration is the wholesale install story, and the pre-1.3.0 in-container socket/token — undeletable from the client, which is the very access this escapes — are unlinked by the reader itself on first 1.3.0 boot. Helpers 1.3.0. (Mike's directive; design by the orchestrator. `deputy/reader/main.swift`, `src/deputy/{protocol,install,routing}.ts`, `src/host-access.ts`; doctrine § RESOLVED 2026-08-24.)

- 2026-08-24 · **The CNC composite's second refusal keys on the CURSOR, not on the rule kind — the after-completion refusal is LIFTED.** #573 shipped "an after-completion series has no upcoming occurrence to work on until the current one is done" on the strength of CNC1 §5, which measured `Create Next Copy` duplicating on a never-completed after-completion template. CNCAC1 measured the state the refusal was actually firing in and found it inverted: an after-completion series acquires a real `rt1_nextInstanceStartDate` the moment its current occurrence is RESOLVED — completed, canceled, **or trashed**, all three measured — so a series with a completed history renders a projection in Upcoming under a day header and the GUI checks it off, while the state the copy described (an unfinished current occurrence) never reaches the refusal at all, because the composite resolves the open row directly. Measured on golden-v4: the GUI check-off is a **JIT mint born `status=3` dated the projection day at the gesture wall-clock, with the series re-anchored to the completion day** and the cursor re-derived; `Create Next Copy` + the status write reproduces it with **no oddities §18 twin**, and — discriminated on a day that is NOT the anchor, removing the pinned-clock confound — both paths land byte-identical template state (`acRef=2026-07-08 next=2026-07-15 icCount=2`). Rulings: (a) the guard becomes `cursor === null`, the measured precondition, on every rule shape; (b) `complete`/`cancel` and `update --exception` all ride it for after-completion series, with the disclosure naming the restarted interval — **cancel restarts it too**, measured for the first time; (c) the live-slot collision check is skipped for after-completion by construction, not by assumption (no calendar means the cursor is the only future date the rule owns); (d) the only reachable cursor-less state is a PAUSED series, where CNC defeats the pause and strands the anchor (new [oddities §19](../things-app-oddities.md)), so that refusal names `resume-repeat`. Evidence: [lab/cncac1-after-completion-checkoff.md](../lab/cncac1-after-completion-checkoff.md).

- 2026-08-25 · **A template-target composite NAMES the occurrence it wrote and TAKES an `--op-id` — one verb, one summary record (Mike).** Two contract gaps in #573/#576's CNC composites, ruled together because they are the same fact seen twice: the composite is one logical op, and its caller has to be able to say so. (a) **The result names both uuids.** Every success from `complete`/`cancel`/`update --exception` aimed at a repeating to-do carries `occurrence: {templateUuid, occurrenceUuid, minted, date}` — CLI `--json`, MCP result, library `MutationResult`, human render, and the audit record — mirroring `add-repeating`'s template/instance pair. `minted` is stated, not implied: an occurrence created for the call cannot be un-minted by undo, one that was already open can. (b) **`--op-id` is honored, not dropped.** The key was ACCEPTED by both surfaces and recorded on the LEGS while no lookback ever ran on this path (the composites bypass the client's single-op `run`), so every retry after an ambiguous failure minted a second occurrence and advanced the series again — the add-repeating hazard class, on a verb an agent retries by reflex. The lookback moves to a shared `replayIfApplied` gate called at the consumer entries (`runCompleteWithDate`/`runCancelWithDate`/`runTemplateExceptionWrite` as well as client `run`), the key rides the SUMMARY record alone, and a match replays the recorded result — occurrence pair included — without dispatching. Phase 1 is unchanged: a `verify-failed:timeout` original is NOT replayed. (c) **The composites now leave a summary record at all**, which they did not before: both legs were `leg`-role and excluded from undo targeting, so the shipped "undo restores the occurrence's own change" promise had nothing to reach. The summary is the undo target (legs replayed in reverse — the mint is skipped as irreversible, exactly as disclosed) and the record the key is addressed to. A composite nested inside another (`complete --completed-at` on a series runs the flip-dance as its status leg) JOINS the outer transaction and writes no second summary — two summaries would leave the outer one looking leg-less, i.e. not undoable. (`src/write/template-mutation.ts`, `src/write/{opid,pipeline,resolution-timestamps}.ts`.)

- 2026-08-25 · **A promote composite establishes GUI STANDING before it seeds, and a drive that loses the window reports `verify-failed:ui-unreachable`, never `silent-noop` (#512).** Two halves of one honesty gap. (a) **Pre-seed.** SESSGATE (#480) already probed session REACHABILITY before the seed, but Article IV STANDING — has this machine granted GUI-driving to the helpers at all? — was only checked by the pipeline on the PROMOTE leg, i.e. after the seed existed: a machine with no helper tier created the row, refused, and trashed its own artifact for a verdict readable from a config file. The pre-seed gate now runs standing FIRST (keyed on a vector's `drivesGui` declaration, exactly as the pipeline's gate is, so a simulator is never gated on the developer's TCC state), then reachability — which also keeps the probe doctrine-clean, since no System Events call is attempted on a host that granted none. Refusal is `blocked:environment` with the pipeline's own copy plus "nothing was created". (b) **Mid-drive.** No preflight can catch a session that degrades UNDER a running drive (the Mac locks, a full-screen app takes the Space, a step's osascript is killed by its deadline). That case used to flatten into `verify-failed:silent-noop`, whose documented meaning — the app accepted the command and changed nothing — sends the caller hunting a bad parameter and reads as safe to retry. The ui vector now TAGS such a failure (`ExecuteResult.uiUnreachable`, cause `unreachable` when the cleanup's own blindness probe had to run blind, `unresponsive` when the step was killed by its deadline; an ordinary step failure on a reachable window stays untagged) and the pipeline shapes `verify-failed:ui-unreachable` — naming the step that stopped, stating that nothing landed, and carrying an environmental retry path — with the fail-closed seed cleanup unchanged. `silent-noop` keeps its narrower, now-true meaning. (`src/write/promote-clone.ts`, `src/write/vectors/{ui,types}.ts`, `src/write/pipeline.ts`; contract registry updated.)

- 2026-08-25 · **Every operation's PARAMETERS have a declared shape, checked at two choke points; a container reference is an object and a bare string is REFUSED, never normalized (#580).** The duck-test genus: `containerGiven(ref)` asked whether `params.project` LOOKED like a container and read a bare uuid STRING as "no destination given" — no `list-id` compiled, no placement assertion emitted, the to-do landed in the Inbox, verify passed against the empty delta, and the batch reported ok=1/exit 0. Rulings: (a) ONE zero-dep registry, `src/write/param-schema.ts`, EXHAUSTIVE on both axes (`{[K in OperationKind]: {[F in keyof Params]-?: FieldSpec}}`) per the #491 RRF1 exhaustive-map doctrine — a new op or a new field breaks compilation until its shape is named; hand-written in the repeat-rule.ts style, NO zod (guest-bundle constraint), and the calendar-anchor/ends validators are WRAPPED, not restated. (b) Two choke points: the batch's `staticLineError` (so a malformed field takes the existing whole-batch static refusal — every bad line enumerated, nothing dispatched, dry-run identical, `--continue-on-error` orthogonal because it governs RUNTIME failures) and `runMutation`/`runReorder` before any param is read (so the programmatic API, MCP `run_operation`, MCP `batch`, and the CLI all refuse identically). (c) The contract: required = present and well-shaped; optional = absent, `null`, or well-shaped — any other type is a structural error, NEVER a silent "treat as absent"; unknown keys refused by name, at the line level and inside closed nested bags. (d) A container ref is `{uuid}`/`{title}`; a bare string is refused with the object spelling (ALPHA-CONTRACT — a shorthand alias would reinstate the degradation one layer up), and `$name` temp-refs stay legal VALUES (`{"uuid": "$proj"}`), ref DECLARATION checking staying `staticRefError`'s job. (e) The refusal is a `ParamSchemaError extends RangeError` — an input-contract error (MCP `usage`, CLI exit 2), thrown before the lock, the drift gate, and any audit record, so a structurally invalid call writes nothing and records nothing. (f) The duck tests are downgraded to belt-and-braces THROWS (`containerGiven`, the simulator's `containerUuid`, pre-state's `ref.uuid ?? ref.title ?? ""`), `resolveRefs` never writes a bare uuid back into a container field, and `buildUpdatePatch`'s silent non-string drop becomes a refusal. (g) `capabilitiesTable` publishes a per-op `params` summary, so the MCP promise that `capabilities` carries "parameter shapes" is met by data.

- 2026-08-25 · **The app-data grant is RESPONSIBLE-APP-INSTANCE-pinned, so `things setup`'s read leg provokes the modal from a BOUNDED CHILD.** [APDP1](../lab/apdp1-grant-pinning.md) (golden-v4 / Things 3.23 / macOS 15.7.7, one clone): the `kTCCServiceSystemPolicyAppData` row is written against the responsible app's bundle id (`com.apple.Terminal`, `client_type=0`) and pinned to that instance's `pid` + `pid_version` — the requester was a python child four levels down, and the row named Terminal's pid. A child provokes and answers it; the parent shell, siblings, grandchildren, and shells in other windows of the same app then read in ~10 ms with no dialog; relaunching the app re-arms the prompt. The 2026-08-21 "pid-pinned" reading is CORRECTED (its three data points were three launchd-hosted deputy launches — three responsible processes, not three pids under one). Consequences, all measured: killing the stalled child neither dismisses the dialog nor loses the grant (a late Allow still lands it on the app), a Don't Allow makes every later open in that app instance fail instantly with EPERM and no re-prompt (a new instance is asked again), and SIGTERM does reap a child stalled in the TCC-held `open(2)`. Shipped with it: `openContainerDefault` runs the open in a 60 s-bounded child, and the leg distinguishes "still waiting" from "refused" in its copy (`src/direct-setup.ts`). Nothing about the doctrine's FDA floor changes.

- 2026-08-25 · **Responsibility is fixed at SPAWN and sticks; host identity comes from the inherited `__CFBundleIdentifier`, never from a PPID walk — and a per-host-app-instance session grant is a coherent (non-durable) fallback tier for helper-less MCP use.** Measured on the live host (no dialogs, kernel responsibility API, read only, not shipped): a `tmux` server re-parented to launchd is still attributed to the terminal app that first started it, and every pane shell inherits that attribution — which is exactly what the inherited `__CFBundleIdentifier` already reports, while a PPID walk inside tmux tops out at launchd and would name the wrong app or none. Rulings: (a) `hostApp()` stays environment-derived and no product code adopts `responsibility_get_pid_responsible_for_pid` (private API — a maintainer decision if it is ever needed); (b) the session marker's instance key is the RESPONSIBLE APP's pid + start time, resolved by bundle id through LaunchServices (`lsappinfo`), never the CLI's own pid and never a tree walk — audited unchanged; (c) an MCP host app is a responsible app under the same [APDP1](../lab/apdp1-grant-pinning.md) semantics, so one Allow inside it covers every MCP-server process it spawns until it quits, making the existing session tier a coherent helper-less fallback for MCP — recorded, not a build item, and no change to the FDA/helpers floor; (d) copy states the measured reach (every process under the host app, until it quits) and hedges the cause of a refused read, because "refused" and "never asked" are indistinguishable without the dialog. (`docs/design/permissions-doctrine.md` Article III.)

- 2026-08-25 · **A GUI picker is COMMITTED by addressing its row, never by pressing Return on whatever it highlighted — and a picker that cannot offer the destination is a pre-state refusal (HXPC1).** `project.move-heading-to-project` typed the destination title into the Move… picker's filter field and pressed Return blind. Measured on Things 3.23: the picker publishes **no `AXSelected`/`AXFocused`/`AXHighlighted` on any row**, so there was never a highlight to read back — and it lists **OPEN projects only**, so a completed or canceled destination (which resolves fine in `TMTask`, and by uuid skips the title resolver's `status = 0` filter) leaves the trailing `New Project "<typed text>"` row as the ONLY offer. The blind Return took it: a second project of the destination's name was created and the heading moved into it (projects 14 → 15). Rulings: (a) the commit CLICKS the row whose `AXDescription` equals the destination title — the New-Project row's description is the quoted form, so an exact match cannot reach it — after confirming the window's `AXIdentifier` begins `MovePopUpDialog-`, requiring exactly one matching row, and requiring that row's centre to lie inside the picker's own scroll area (the CNCAC1 off-screen-frame hazard); every miss fails closed NAMING every row the picker offered. (b) A **non-open destination** is refused up front (`dest-not-open`), so the ordinary case gets a sentence instead of a driven app. (c) The exact-TWIN destination refusal STAYS (two identical rows, nothing to choose between) but is deliberately **NOT widened to prefix collisions**: an exact-title row is a different row from one that merely starts with the same text, measured and certified end to end, and refusing it would decline an operation the app expresses perfectly well. (d) The filter text rides a plain keystroke at the already-focused field rather than the select-all + Tab `set-value` mechanic, which is wrong for a search field and is one of the recorded AX-beep candidates. Evidence: [lab/hxpc1-picker-assert.md](../lab/hxpc1-picker-assert.md) §B.

- 2026-08-25 · **A Repeat-dialog control is addressed by the ROW it sits on when its index depends on dialog state (HXPC1).** `DIALOG_INTERVAL` and `DIALOG_ENDS_COUNT` were both `text field 1 of group 1` — the same control at different moments. Measured on Things 3.23: selecting `Ends: after` INSERTS the count ahead of the interval (count `#1 @y372`, interval `#2 @y283`), and the create path survived only because it drives the interval while it is still the group's sole field. A RESCHEDULE opens the dialog PRE-POPULATED, so a rule that already ends after N wrote the requested interval into the COUNT field and the interval never changed — with the read-back reporting `OK`, since the field it wrote did hold the value (falsified live, cell a4). Ruling: each numeric field is resolved by the row it shares with the group's `Ends:` static text (count = on that row, interval = not on it), which is order-independent and covers the after-completion dialog, whose group offers no ends bound at all; anything other than exactly one match fails closed reporting the numeric-field inventory. The general form: **when a structural index is a function of dialog STATE rather than of the control's identity, address the control by its row, not by its index** — the same discriminator `probe-dialog-shape` already uses. Evidence: [lab/hxpc1-picker-assert.md](../lab/hxpc1-picker-assert.md) §A.

- 2026-08-25 · **An AX `whose` clause searches DIRECT children only — a nested control needs an explicit walk, and a recipe path is not certified until something resolves it live (HXPC1).** The heading `…` button, which carries its heading's title in `AXDescription` and is the whole enabler of the ellipsis recipes, was addressed as `first UI element of <content table> whose description is "More. <title>"`. It sits at `UI element N of cell 1 of row M`; the table's direct children are its rows, which carry no description, so the selector matched NOTHING and both ellipsis drives (`move-heading-to-project`, `dissolve-heading`) died at their own frame resolution before any click, on every host. It had shipped for a month behind a `pending HXPC1 certification` marker, and the failure was on record — read as a headless-rig limitation in the timestamp residual cells ([reference/timestamps.md](../reference/timestamps.md) §2c) rather than as a wrong path. Rulings: (a) the button is resolved by an explicit rows → cells → cell-children walk that fails closed by name; (b) a post-click assert must be a POSITIVE match on the element that should have appeared — the old one waited for "a detached `AXUnknown` window", which the popover already open satisfied, so a click that did nothing looked like success; (c) a provisional recipe path stays a KNOWN GAP until a live resolution certifies it, and a probe verdict that reads as a rig limitation is re-examined against the shipped selector before it is believed. Evidence: [lab/hxpc1-picker-assert.md](../lab/hxpc1-picker-assert.md) §B0/§B2.

- 2026-08-25 · **A macOS alert beep is a FAILURE STATE for lab automation, gated by default, and the opt-out is accounting-only (BEEPSEN1).** A beep means the app declined a gesture it was handed; the write can still land, so a beep is invisible to every DB-delta assertion the harness has. Every `lab:` suite and the write-layer e2e now assert zero beeps via `lab/guest/beep-sentinel.sh` — post-hoc `mark`/`assert` over one `log show`, never a detached `log stream` — with per-probe-phase (suites) and per-step (e2e) attribution, judged host-side in `run.ts`. Research/probe drivers may set `THINGS_LAB_BEEPS_OK=1`, which downgrades the gate to reporting but still prints the count and the offending log lines: probes are exempt from failing, never from counting. Fail closed — a missing report, an un-shipped sentinel, or a `log show` error is red. Evidence: [beepsen1-beep-sentinel.md](../lab/beepsen1-beep-sentinel.md).

- 2026-08-25 · **In a lab clone, unified-log timestamps are not identity — a guest-side log oracle MUST pin to `bootUUID` (BEEPSEN1).** The log store ships inside the disk image, so every clone inherits the golden's own log history, and because every clone pins its clock to the SAME date those entries fall inside the current run's displayed window. Measured: three beeps from a months-old build session were returned into an EMPTY window by both `--start/--end` and `--last 30m`. `log show`'s time bounds are a prefilter only; the window is applied against the harness's own marks, and every record is matched against `sysctl -n kern.bootsessionuuid`.
- 2026-08-25 · **AX surfaces are undocumented private APIs — scrutiny tier, over-caution fail direction (maintainer-ratified after HXPC1/BEEP1).** Every AX-driven workflow gets exhaustive regression tests AND unrelated-field assertions (an unattributable post-drive delta is a failure, never a warning); probing/recipe-creation re-audits the FULL dialog shape after EVERY input step (dialogs shape-shift: controls are minted/moved by checkboxes, pop-up selections, date pickers — probe law in [lab/harness.md](../lab/harness.md) §AX-drive scrutiny); production drives carry every deterministic fast-fail check that is cheap (discriminated addressing, label asserts, pre-commit full-dialog audit). Fail direction: better a refusal on an anodyne localization change than a permissive write into an unintended field. Evidence: [lab/hxpc1-picker-assert.md](../lab/hxpc1-picker-assert.md), [lab/beep1-numeric-field-beep.md](../lab/beep1-numeric-field-beep.md).

- 2026-08-25 · **The CLI's SIGINT/SIGTERM listeners are SCOPED to spans that can dispatch them; every other command keeps the kernel's default disposition.** A registered JS listener replaces the default disposition with a libuv watcher that only runs ON the event loop, so arming it at startup (#487's `installSignalHandlers`, called from `runCli`) made every synchronous command SWALLOW a caller's SIGTERM — measured on a clean host 2026-08-24, and reproduced here with a FIFO standing in for the TCC-held container `open(2)`: `timeout -k 5 5 things today` took 10 s and a `Killed: 9` (137) before, 5 s and a clean TERM (124) after. Rulings: (a) the startup install is DELETED — reads, `doctor`, `config`, `show`, and every other one-shot path die to a signal under the kernel's disposition, which is correct because the report builder returns null with no write in flight; (b) the guard is armed by `armInterrupt`/`disarmInterrupt` for the span of a write, whose osascript dispatch is async (`osaExec`) and can therefore honor it; (c) `things mcp` installs it for the process lifetime (`installServerSignalHandlers`) — event-loop-resident by construction, and a supervisor's SIGTERM mid-write still gets the honest stderr line (never stdout, which is the JSON-RPC channel); (d) the ceremonies' `withDefaultInterrupts` (#567) STAYS as their own guarantee, now holding a disposition they usually already have. KNOWN RESIDUAL, measured (137): a write invocation is armed from the top of its driver, so one stalled in `openThings`'s read gate still needs SIGKILL — narrowing to "armed only once the in-flight marker is set" would close it at the cost of the pre-write window's `signal` trace entry, and wants its own ruling. (`src/cli/interrupt.ts`, `src/cli/main.ts`, `src/cli/commands/mcp.ts`, `src/wizard.ts`.)

- 2026-08-25 · **That residual, RULED and CLOSED: the write guard arms PAST the client open, and the pre-write `signal` trace entry is EXPENDABLE.** The seam is the client open, not the in-flight marker: `armInterrupt` is called once `openThings` has RETURNED, which is the cheapest cut that takes the blocking `open(2)` out of the armed span and needs no library-side hook (an in-flight-marker trigger would have to reach back across the consumer boundary for the same result). It is a SINGLE seam — `beginWriteInvocation` now hands each driver an `openClient(dbPath)` alongside its teardown, so all six drivers open through one place and none can grow its own pre-open arm; a source guard pins exactly one `armInterrupt(`/`openThings(` in `commands/writes.ts` with the open first. Same FIFO repro as the parent ruling, now on the WRITE path: `timeout -k 5 5 things todo add … --db <fifo>` was 10 s and a `Killed: 9` (137), is 5 s and a clean TERM (124). The cost is accepted deliberately: the window between the driver's start and the client existing is the one window where the guard is armed but can have nothing to report (no client, so nothing in flight, so `interruptReport` returns null), and its `signal` trace entry is a dev-build diagnostic only — killability outranks it. (`src/cli/interrupt.ts`, `src/cli/commands/writes.ts`.)

- 2026-08-25 · **`--op-id` is settled by ONE RECORDED RESULT PER KEY, and a timed-out original is RECONCILED against its own recorded assertion rather than re-run or replayed blind.** Two open items, closed together because they are the same question — what does a key address? (a) **The promote asymmetry.** `todo make-repeating` ACCEPTED `--op-id` while `project make-repeating` and both `add-repeating` verbs refused it, and the accepting one silently DROPPED the key (the compound strips it from every leg and its summary never stored it), so a resubmission deduped nothing at all. The 2026-08-25 template-target precedent applies verbatim: the promote verbs already record ONE summary record standing for the whole clone → trash → promote sequence, so the key rides that summary and all four now accept the flag symmetrically, wrapped in `addWriteFlags` so help follows runtime. One summary = one undo unit for the whole promote, which is what it already was. (b) **Timeout reconciliation (phase 2).** A `verify-failed:timeout` record now stores `expected` — the state assertion the attempt was verifying — and a resubmission whose key matches one RE-EVALUATES that assertion against current state: satisfied → replay as already-applied (disclosed, and with NO `undoToken`, because the trail holds no completed-change record to reverse); not satisfied → run. The presence oracle is always the attempt's OWN oracle, never per-op logic invented at reconcile time. Assertions that cannot distinguish the two — nothing recorded, no field named, a one-item ordering, an untime-bounded create probe, or a whole-database singleton naming none of the items the call touched (`trash.empty`, `log-now`) — REFUSE with `blocked:reconcile` pointing at `things op-result`, never a guess. The promote verbs write an AMBIGUOUS summary (`verify-failed:timeout` + the "a repeating template with this title, created since this call started" probe) so their reconciliation keys off the summary like everything else. (`src/write/{opid,batch,pipeline,promote-clone}.ts`, `src/audit/schema.ts`.)
- 2026-08-25 · **The lab gets a WRITE-vector escape alongside the ui one, bounded to the bundle-id-less path (`THINGS_API_WRITE_DIRECT=1`).** Wave A's write gate returns `direct-unknown` whenever `host.bundleId === null`, which is every sshd-descended guest shell, so every AppleScript-vector verb — and every composite with an AppleScript leg — was unreachable in a clone; it bit five campaigns (CNC1, HXPC1, BEEP1 and two concurrent ones) and would have blocked the write-layer e2e on the next `lab:regress`. The ruling is the ui escape's shape, not a new mechanism: one env var, read by `writeCapability` alone, spelled as the literal `1`, not consumer surface (no `--help`/MCP copy, no banned-vocabulary change), with `direct-escape` as its own honest verdict mode so `doctor`'s `applescript` row names *the lab's direct escape* as the provenance rather than claiming a TCC grant. The bound is what keeps Article I intact: the escape is consulted ONLY where the bundle id is null — the case where macOS has no identity to have recorded anything against, so what is lifted is an UNKNOWABILITY, not a missing grant (a guest under the AXVM1 layer genuinely holds automation). A host WITH an identity is still answered from its TCC row, so a recorded refusal (`direct-denied`) can never be escaped and no real terminal can manufacture a grant. The rejected alternative — giving the guest bundle a bundle identity — would have made the lab's write path structurally different from the shipped one at exactly the seam the lab exists to test. Evidence (2026-08-25, golden-v4 / Things 3.23, one clone): in-guest `doctor` reads `applescript direct-unknown / none` without the escape and `applescript direct-escape / the lab's direct escape (not consumer surface)` with it, and the 132-step write-layer e2e — blocked at every AppleScript step since the gate landed — runs end to end with 0 alert beeps. The first pass measured ONE stale expectation in the smoke itself: `EXIT_TODAY_WITH_PROJECT` still demanded the pre-3.23 refusal that VMRES1 retired on 2026-08-23, one day after the smoke's last green run; with it corrected a second clone ran 132/132 clean. (`src/capability.ts`, `lab/scripts/env.sh`, `lab/guest/e2e-write-smoke.sh`; [lab/harness.md](../lab/harness.md) §The lab escapes.)

- 2026-08-25 · **The JIT composites write the ambiguous summary too, and each one's presence oracle is chosen by WHAT ITS TIMEOUT POINT KNEW — with `update --exception` deliberately recording an unusable one.** #600 shipped the reconcile machinery but not record CREATION for the template-target composites or the `--completed-at` flip-dance, so a keyed resubmission after a timeout found no record and ran the whole verb again (for a series, a second occurrence and a second consumed slot). Rulings: (a) all three record the summary in `recordAmbiguousPromote`'s exact shape (`appendCompositeSummary({ ambiguous })` → result `verify-failed:timeout`, `expected` set, `observed: null`, not an undo target), and only when a key was given; (b) the oracle is LIFTED from the failing leg's own recorded `expected` wherever the leg recorded one — never re-derived; (c) `complete`/`cancel`: a MINT timeout records the mint's own time-bounded create probe EXTENDED with the promised status (an existence-only oracle would report the series checked off when only the copy had been made), and NOT-satisfied is safe there because a re-run resolves a bare occurrence through the open-instance branch instead of minting; once the occurrence is named, its own uuid-keyed assertion is recorded instead; (d) `update --exception`'s MINT timeout is the one point with NO safe usable oracle — the verb always mints, so "not satisfied → run" takes a second slot, and a mint that never confirmed is exactly the point at which the patch is KNOWN not to have landed, so any satisfiable oracle would lie — it therefore records the vacuous series-keyed spec (the verb leaves the template byte-unchanged BY DESIGN, so it genuinely names no field) and the resubmission REFUSES; its update-leg timeout, where the slot is already spent and the row is named, takes the leg's own assertion; (e) the flip-dance records its own `finalDelta`, and a NESTED run records nothing (the outer composite owns the record). KNOWN RESIDUAL: `runUpdateDates` (`update --completed-at`) writes the ambiguous summary but has no `replayIfApplied` gate of its own, so nothing consults it on that path. (`src/write/{template-mutation,resolution-timestamps,pipeline}.ts`.)

- 2026-08-25 · **Heading order ships on the app's own arrow chords, as a database-verified closed loop, with an ARCHIVED-heading fence and NO `activate` step (`project.move-heading`, CHORDMH1).** Maintainer's endorsement, on HEADORD1's evidence: *"I fully endorse using this to move headings. This seems far less dangerous than changing containers, and I really like that this can be done without foregrounding the app."* Four rulings follow from building it. (1) **The op un-refuses in place** — same name, same params, same surfaces — and leaves the `privateReorderIsNoOp` range entirely rather than being gated inside it; its AppleScript matrix entry is DELETED, not disabled, because no reorder spelling on any vector addresses a heading at all (HEADORD-b), so there is no headless twin to fall back to. Every other reorder scope keeps its gate. (2) **The drive is a closed loop, never a keystroke macro:** each chord is computed from a fresh database read, posted once, and then proven — the intended heading is where the chord aimed, exactly one heading's `index` moved and it is one the gesture passed over, and every child is byte-identical behind its heading FK. A chord that moves nothing STOPS the drive naming the boundary and is never re-fired, which is also what keeps the beep count at zero on every normal path. (3) **Delivery is BACKGROUNDED and the recipe carries no `activate` step** — measured with Things never activated and Finder frontmost at every stage, the first shipped op whose whole gesture is tier 0/1. (4) **An archived heading anywhere in the project refuses the whole move.** Heading rows expose no title to the Accessibility tree, so they are addressed positionally; whether Things renders a completed heading in the project view is unmeasured, and an unmeasured ordinal is not one to chord against — the over-caution direction, and it retires the `#V11` reopen-disclosure policy with the wire it described. Evidence: [lab/chordmh1-move-heading-build.md](../lab/chordmh1-move-heading-build.md), [lab/headord1-heading-order.md](../lab/headord1-heading-order.md).

- 2026-08-25 · **An index-rewrite law measured in ONE direction is not a law — assert the resulting ORDER and that exactly one row moved, never WHICH side of the swap got the new number (CHORDMH1 §2).** HEADORD1 recorded the arrow chord as rewriting the moved heading's `index` and renumbering no sibling; it had only ever fired a ⌘↑. A ⌘↓ leaves the mover byte-identical and renumbers the SIBLING it passes instead — exactly one row either way, always one the gesture crossed, with Things picking whichever single write is cheapest. AXDRAG1 had recorded the same shape for the sidebar drag ("a drag may renumber a NEIGHBOUR rather than the dragged row") and the two now generalize: any verifier over a Things ordering write fences the rows OUTSIDE the moved span and counts the rows inside it, and never names the row it expects to be renumbered. Caught by the shipped verifier on its first live drive, at the cost of one refused (and zero mis-landed) move.

- 2026-08-25 · **The archived-heading fence on `project.move-heading` is LIFTED — an unrendered row is not an ambiguous ordinal, it is no ordinal at all (CHORDMH2; SUPERSEDES point (4) of the CHORDMH1 ruling above).** The fence refused any project holding a completed/canceled heading, on the grounds that heading rows are addressed positionally and it was unmeasured whether Things renders an archived one. CHORD2 cell 7a′ measured it: an archived heading does NOT render as a content row, takes NO ordinal in the `select-heading-row` walk, and a live heading's ±1 SKIPS its slot in one chord with no beep — the rendered order is the DB order filtered to `status = 0`. So the real hazard was never the rendering; it was an ORDINAL MISMATCH between a plan that counted archived rows and a walk that does not, and the fix is a filter, not a refusal: the pre-state plan, the driver's per-chord ground truth and the bare-placement anchor all read the live order now (the child digest stays unfiltered — no heading chord may disturb any child of any heading). Two refusals survive because an unrendered row cannot be selected: an archived heading as a MOVEE and as an ANCHOR. One assertion is deliberately withheld: the archived row's own rank is excluded from the untouched-siblings set rather than frozen, because the live rows are renumbered around it over time and its drift is invisible. Certified through the production CLI — one chord past an archived slot, that heading the only row rewritten, the archived row byte-untouched on status/index/stopDate/umd, zero beeps. Evidence: [lab/chordmh2-archived-fence-lift.md](../lab/chordmh2-archived-fence-lift.md), [lab/chord2-reorder-laws.md](../lab/chord2-reorder-laws.md) §7.

- 2026-08-26 · **A resolution timestamp accepts `T` OR a single space between date and time — both are first-class spellings of the same instant, and neither is rewritten into the other (#612).** `--completed-at "2026-08-19 09:30"` used to be refused for want of the ISO `T`, which is the separator a serializer emits and not the one a human types. The grammar widens at the TWO layers that parse it — the registry's `timestamp()` (static preflight, every untyped surface) and `resolveResolutionInstant` (the instant resolver) — so the accepted sets stay identical and there is no third place to fall out of step; the raw string is never normalized on the way through, because the value is only ever consumed as a resolved instant. Exactly one separator character: a doubled space, a tab, and any other joiner are still refused, and the refusal names both accepted spellings from the shared `RESOLUTION_TIMESTAMP_EXPECTED` vocabulary. This is NOT the container-reference precedent (#580, one canonical shape): a bare-string container was refused because normalizing it would silently re-admit a DIFFERENT mutation, whereas the two datetime spellings denote the same instant and can degrade into nothing. (`src/surface-copy.ts`, `src/write/param-schema.ts`, `src/write/commands.ts`.)

- 2026-08-26 · **LIVENESS IS RESOLVED BEFORE AUTHORIZATION IS JUDGED, and the wake is a LAUNCH — never an Apple event (`target-unreachable`, #610).** The deputy's `automation.systemEvents` carried two different kinds of fact in one field: `granted`/`denied`/`unknown` are TCC standings, `not-running` is a statement about the process (`AEDeterminePermissionToAutomateTarget` answers procNotFound for a target that is down, and macOS reaps the on-demand agent whenever it idles). The GUI preflight read every non-`granted` value as a missing grant, so a fully onboarded machine was refused `blocked:environment` and steered back through `things helpers setup --gui` — which "repaired" it only because the ceremony's own probe event wakes the target, until macOS reaped it again. Rulings: (a) on `not-running` the target is STARTED with a background LaunchServices dispatch (`open -g -b com.apple.systemevents`) and the ask-false determination re-read; the ORDER is doctrine, because sending the target an event to wake it auto-launches it AND raises the consent dialog on an ungranted machine, which is the Article I violation the whole preflight exists to prevent, while an app launch is not a TCC-gated act at all. (b) The wait is a bounded closed loop over the determination itself — the probe is the liveness test — with no fixed sleep standing in for the answer. (c) A wake that does not take is its own verdict mode, `target-unreachable`, whose copy names System Events as not reachable and offers `open -g -a "System Events"`; it must never mention permissions or onboarding, because nothing is known to be missing. (d) The wake lives at the ONE choke point every caller shares (`uiCapability`), so the pipeline gate, `promote-clone`, `doctor` and the MCP survey all see the resolved truth, and in the ceremony it runs BEFORE the upfront banner counts dialogs, which closes the matching over-announce. (e) It is deliberately CLIENT-side: the launch needs no privilege, so putting it behind the privileged proxy would have forced a helpers-version bump and a reinstall ceremony before an already-installed 1.3.0 pair could be fixed. Evidence: [SEWAKE1](../lab/sewake1-system-events-wake.md) — golden-v4 / macOS 15.7.7, one clone: -600 in ~1 ms for a down target and the same -600 for any other down app (so it tracks liveness, not the target), System Events already down on a freshly booted clone that had done nothing, and the launch resolving it to `granted` on the FIRST ask with both consent-dialog agents holding zero windows and zero beeps. (`src/deputy/wake.ts`, `src/capability.ts`, `src/deputy/install.ts`, `src/cli/commands/doctor.ts`.)

- 2026-08-26 · **The URL vector gets a pre-dispatch gate of its own, refusing only what we can POSITIVELY read as not-enabled — and `unreadable` is deliberately permissive, the opposite of the app-control gate (URLEN1, #611).** The doctrine's vector table used to end this row with *"nothing further to gate"*, on the reasoning that Things' "Enable Things URLs" prompt is raised by the app rather than by TCC and so falls outside the doctrine. URLEN1 measured what actually happens and that reasoning does not survive it: a not-authorized app does not refuse a URL mutation, it puts up its own alert **sheet** and PARKS the command behind it — invisible to the sender, indefinitely, stacking one sheet per command, and **applying LATE** if anyone ever clicks Enable, so a write already reported `verify-failed:silent-noop` can still land. That is both a modal on an unattended screen and an unbounded queue of pending mutations, so it is gated like the others (`urlSchemeCapability()`, pipeline gate 5c, keyed on the vector's `dispatchesUrls` declaration for the same reason its two siblings are keyed on theirs). Five rulings. (a) **`disabled` and `never-asked` both refuse**, because they are behaviorally identical — same sheet, same park, same late-apply — and differ only in the key's reading (`0` vs absent). (b) **`unreadable` DISPATCHES.** The asymmetry with the app-control gate is deliberate and is the whole shape of the decision: there, resolving an unknown standing means sending the Apple Event that IS the dialog, so unknown must refuse; here, dispatching costs nothing on the machines this verdict occurs on, and refusing on a state we never read would break callers over a guess. The verify plus its likely-cause hint carry that case instead — which is what the issue's second leg asked for, and it is needed only because of this ruling. (c) **The read is consent-disciplined by construction.** `uriSchemeEnabled` lives ONLY in the group-container prefs plist — the un-TCC'd user domain has no copy under any spelling — so the read is the same app-data class as the database, and it happens only on a standing that already covers the container (`helpers`, `direct-fda`, `session-grant`); `explicit-db` is excluded because a caller-supplied database path says nothing about the container. This also closes a latent hole: the previous `readUrlSchemeEnabled()` opened the plist unconditionally, so `doctor` on a bare machine could itself have raised the modal it was diagnosing. (d) **The failure-attribution HINT is keyed on the same declaration as the gate, not on the vector id.** Caught by CI, not locally: an id-keyed lookup let the shipped default read the DEVELOPER's own Things preferences whenever any fake substituted under the `url-scheme` id produced a silent no-op, so `write-pipeline`'s launch-window cell passed on a workstation whose setting is on and failed on Linux CI, where nothing is readable and every silent no-op became `feature-disabled`. A vector that opens no URL now passes `null` and the theory is not evaluated at all — the Wave A lesson, re-learned on the attribution side. The `explicit-db` exclusion in (c) buys the same immunity for `doctor`: every `diagnose(fixturePath)` test reads `explicit-db`, so no unit test can touch the real container. (e) **The `things setup` leg raises nothing and is NOT counted in the dialog banner** — an app setting is not a macOS grant and the ceremony cannot flip it; the leg detects, instructs, and reports `pending` so the closing line carries it into a rerun. Evidence: [../lab/urlen1-url-enable.md](../lab/urlen1-url-enable.md); oddities §23 (which also corrects §5h's "no DB row appears either way").

- 2026-08-26 · **A window census is not a modal census — an `AXSheet` is invisible to `AXChildren` of the app element, and two probe phases shipped a wrong verdict on it (URLEN1 §7).** URLEN1's phases 1–2 answered "did a dialog appear?" by listing the application's windows, saw only the ordinary Today windows, and recorded the disabled URL vector as a *total silent drop with no dialog*. The alert had been on screen the entire time, hanging off the main window as a sheet; the campaign's own AX dumps show it accumulating one instance per dispatched URL through the very cells that reported none. The blind spot then manufactured derived nonsense — a re-enable cell that "failed", 17 unexplained beeps, a `⌘,` that would not open Settings — all because stacked modal sheets swallow keystrokes and block a graceful `quit`. Three standing rules, now in [../lab/harness.md](../lab/harness.md): census sheets explicitly by walking the tree for `AXRole == AXSheet` (`ax sheets`); reach a clean app state with `pkill -x Things3`, never `tell application "Things3" to quit`, because a modal survives the polite ask; and apply the CNCAC1 positive-control law in its DETECTION form — a negative result from an oracle that has never been shown a positive is not evidence. The harness's own disruption tiering inherits this bound: `window-new` counts windows, so a probe whose expected failure mode is a sheet needs its own assertion.

- 2026-08-27 · **`not-running` is LIVENESS wherever it appears, and the caller's PURPOSE decides whether a dormant target may be started — the write path wakes Things, a survey never does (#617, the Things-target twin of #610).** The deputy's `automation.things` carries two different kinds of fact: `granted`/`denied`/`unknown` are TCC standings, `not-running` is a statement about the app's process (procNotFound from the ask-false determination). Three layers were reading the second as the first, and because Things is closed most of the day the blast radius was the whole write path on a DEFAULT machine: `deputyRouting`'s `auto` onboarding gate deactivated the deputy outright — printing a notice claiming a missing app-control permission — so every AppleScript-vector call ran direct; `writeCapability` then fell through to the host branch, silently engaging the terminal's own historical grant (against the routing doctrine's no-fallback rule) or refusing a fully onboarded machine with "run `things setup`"; and `things helpers setup` announced an app-control dialog that never appeared. **The ruling is deliberately asymmetric with #614.** System Events is a headless macOS component, so `uiCapability` wakes it for every caller; Things is the user's own app, so only a caller with DISPATCH intent may start it — the write gate (a change is imminent, and the background pre-launch is the A40/A41 tier-0 recipe anyway: an Apple Event to a closed Things auto-launches it WITH focus steal) and the two setup ceremonies (which are about to send that very event). `doctor`, the MCP startup bake and `things setup --dry-run` are SURVEYS: they report the new `deputy-target-dormant` verdict — "app control is unreadable while Things is not running", zero permission or onboarding vocabulary, rendered by doctor as its own liveness row — and start nothing. `auto-launch: false` is honored at the gate too, so the one setting that says "do not start Things for a write" is not circumvented by the verdict that precedes the write. Mechanically: `wake.ts` is generalized to a `WakeTarget` descriptor (bundle id + hello field + bound; Things gets 10s to System Events' 5s) with `wakeSystemEvents`/`wakeThings` over one primitive; the routing gate DEFERS on `not-running` (active-but-unproven) and the write gate settles it via `settleDeputyAutomation`, the only place allowed to launch — activation itself runs on every invocation, reads and `doctor` included, and must never start the user's app. Evidence THWAKE1 (golden-v4 / Things 3.23, one clone): [../lab/thwake1-things-wake.md](../lab/thwake1-things-wake.md). (`src/deputy/wake.ts`, `src/deputy/routing.ts`, `src/capability.ts`, `src/write/pipeline.ts`, `src/direct-setup.ts`, `src/deputy/install.ts`.)
