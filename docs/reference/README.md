# Reference compendium — everything probed, in one navigable place

The index over ALL evidence this project has produced (roadmap §F). The two **living rollups** remain the primary lookups — this compendium is the map that gets you to raw evidence fast:

- **[capability-matrix.md](../capability-matrix.md)** — the CRUD × vector wish-list/checklist (what works, where, with evidence ids)
- **[things-app-oddities.md](../things-app-oddities.md)** — every app bug/quirk/hazard, report-ready for Cultured Code (incl. §7, the consolidated crash catalog)
- **[things-app-craft.md](../things-app-craft.md)** — the appreciation log (oddities' flattering twin): the genuinely clever Things engineering we've found, each entry evidence-linked

Companions in this directory: **[glossary.md](glossary.md)** (lifecycle verbs and states — the canonical vocabulary, preferring Cultured Code's GUI language, with caveats on undiscovered canon), **[assumption-register.md](assumption-register.md)** (the law-level register — one row per behavioral assumption a shipped mutation depends on, each with its recurring lock + runtime canary; the drift-runbook re-walk target and the behavioral-drift gap list), **[novel-paths.md](novel-paths.md)** (the surprising capabilities that work), **[suite-audit.md](suite-audit.md)** (op catalog × recurring-coverage, with the open gaps), **[task-api-landscape.md](task-api-landscape.md)** (2026-07-25 comparative study of the Todoist / MS Graph To Do / TickTick APIs vs ours — the ratified primitive concordance table, borrow verdicts, compat-layer feasibility), and **[timezones.md](timezones.md)** (the consolidated timezone-sensitive-behavior reference — the log boundary, Logbook day-grouping, `when=today`/`evening` device-clock stamping, the dated-evening impossibility + remote-zone caller table, cross-zone rendering asymmetries, the date-only → noon convention + effective-zone chain, the pinned-zone workaround, and the two-synced-devices model — each entry marked certified / model-derived / unprobed), and **[timestamps.md](timestamps.md)** (the timestamp side-effect inventory — a synthesis of what changes each of the three task stamps `creationDate` / `userModificationDate` / `stopDate` per operation class: the creation-date identity-replacement class, the `umd` bump/silent tables with the `things watch` hazard, the `stopDate` resolution semantics, and the explicit overwrite levers — every cell cited to a probe id, UNPROBED cells flagged).

## How evidence ids work

Every claim in the living docs carries a probe id. Families, by prefix:

| Prefix | Campaign | Results doc |
|---|---|---|
| T01–T20 | pre-lab URL-scheme validation (Codex sessions, March 2026) | [research/validation-notes-step3.md](../research/validation-notes-step3.md) (provenance: [research/PROVENANCE.md](../research/PROVENANCE.md)) |
| U01–U20 | URL re-validation of the T-matrix, instrumented | [lab/u-suite-results.md](../lab/u-suite-results.md) |
| A00–A54 | AppleScript campaign (+ private sdef discoveries) | [lab/a-suite-results.md](../lab/a-suite-results.md) |
| X01–X05 | cross-vector identity/interleave | in a-suite-results.md |
| E01–E19 | editing-completeness (renames, notes modes, duplicates, restores) | [lab/e-suite-results.md](../lab/e-suite-results.md) |
| O01–O14 | ordering (private reorder + bounce) | [lab/o-suite-results.md](../lab/o-suite-results.md) |
| P01–P30 | gap-closure Phase 18/19 (cascades, container clears, checklists, tag un-nest) | [lab/p-suite-results.md](../lab/p-suite-results.md) |
| P6–P14 (2026-07-09 series) | aggregate-list ordering, crash sweep, backdating — distinct from P01–P30 | [lab/o-suite-results.md](../lab/o-suite-results.md) §P7–P9/P13, [lab/s-campaign-results.md](../lab/s-campaign-results.md) (scf/scf2), oddities §7 (P14) |
| R01–R21 | reminders (codec + time-parser classes + dated stickiness) | [lab/r-suite-results.md](../lab/r-suite-results.md) |
| S01–S05, S-detail, S-delperm, scf/scf2/scf3 | Shortcuts campaign (proxies, consent model, backdating; scf3 = round-3 reconfirm + Someday convention lock, logInterval enum, deadline-less repeat, repeating-reminder clear, oddity-6½ screenshots) | [lab/s-campaign-results.md](../lab/s-campaign-results.md) |
| P10–P12, HX0–HX4b | heading verb matrix + escape-hatch sweep | [lab/heading-research.md](../lab/heading-research.md) |
| HEADARC-1a/1b/2/3/4 | archived-heading MOVE semantics: every headless surface (URL update/add, our `todo move --to-heading` by name+uuid) reopens a completed heading on landing an open child (`status 3→0`+`stopDate` NULL, heading-only, byte-identical to the AS un-archive), so `resolveHeadingRef`'s missing status filter does NOT strand (no PLOG1 cousin); AppleScript has no heading-placement verb (−1700/301); `heading=` name-match includes archived headings (oddity 2c/5o); the odd archived-heading+open-child state is not headlessly reachable (app self-heals). Closes the heading-research standing item | [lab/headarc-archived-heading-moves.md](../lab/headarc-archived-heading-moves.md); `lab/scripts/research-headarc.sh` |
| HEADARC2-A/B/C | archived-heading residual captures (rendering + trash-restore): (A) an archived heading is invisible in the default project view and renders as a GROUPED section header under "Show N logged items" with its swept children nested; (B) a swept child of a still-OPEN heading carries a muted HEADING sublabel in the flat logged toggle (in-project toggle labels heading vs global Logbook labels project); (C) GUI Put Back of a trashed open child restores it in place under an archived heading WITHOUT reopening — the §6¾ heading analog (odd state IS reachable, but only via Put Back, never a shipped headless surface). Wires WG-6 → u-suite U21 (certified GREEN). macOS matches maintainer iOS recollection (no delta) | [lab/headarc2-residual-captures.md](../lab/headarc2-residual-captures.md) |
| HEADARC3-1/2/3 | logged-children ORDERING law, multi-day discrimination (resolves the HEADARC2 same-day ambiguity). Fixture: heading Foo + children A,B,C,D,E in index order, completed B,C,A across three days then D,E same-day (index order) — so index order (A,B,C,D,E) ≠ stopDate order (B,C,A,D,E) and the same-day pair separates the candidate laws. Verdict (all three contexts render **E,D,A,C,B**): the flat project logged toggle (heading open), the archived-heading group, and the global Logbook all order swept children by **`stopDate DESC`** (most-recently-completed first), within-day tiebreak by **completion instant, NOT index** (D-then-E → E,D). Shape differs only in that the global Logbook adds day-section headers (Yesterday/month) while the in-project toggle/group is a continuous per-row-dated list. Maintainer's literal B,C,A-over-3-days answer = **A,C,B**. Shipped rendering (`project-view.ts` `stopDate DESC`) CONFIRMED — no divergence; law locked by two render.test.ts HEADARC3 regressions. RD-7 | [lab/headarc3-logged-ordering.md](../lab/headarc3-logged-ordering.md); `lab/scripts/research-headarc3.sh` |
| HEADSORT H-BASE/UNSWEPT/SWEPT/MIXED/RESTORE/SIDEFX/REFUSE | heading-`index` reorder across the full lifecycle (open / archived-unswept / archived-swept). The private `project id` reorder verb reaches heading rows of EVERY sweep state (reachability sweep-agnostic — swept headings invisible to GUI drag are reached identically) and re-ranks `index` to the exact sent order in one comma-joined wire (partial wire front-clusters the named rows). Index-only + umd-SILENT for OPEN headings; for ARCHIVED headings (either sweep state) the same re-rank ALSO **REOPENS** them (`status 3→0`+`stopDate→NULL`+umd-bump, heading-only, swept children resolved byte-identical) — no index-only reorder of an archived heading exists; the reopen trigger is the re-rank itself, not gaining an open child (a fifth §5o reopen surface). Unarchive is index-silent (restore re-enters at retained index); children never touched. Feeds the reorder-planner open-only guard decision + read-shape re-audit. ORD-12; oddities §5o addendum; novel-paths #3½ | [lab/headsort-heading-lifecycle-reorder.md](../lab/headsort-heading-lifecycle-reorder.md); `lab/scripts/research-headsort.sh` |
| BACKDT B-PROJ-AS / B-PROJ-AS-OPEN / B-PROJ-JSON / B-DATEONLY / TODO-OPEN / B-FLIP / B-FLIP2 | **project backdating + resolution-flip stopDate semantics** (Things 3.22.12, golden-v2). PROJECT backdating mirrors to-do P4b/P4d byte-for-byte: AS `set completion date`/`set creation date of project id X` rewrites exact (single-row — children byte-identical, no cascade); `things:///json` `completed`+dates imports a completed project to the Logbook exact (caveat: a plain OPEN child trips the §5b reopen at creation, dropping completion-date — creation-date survives). `set completion date` FORCES status=completed (silently completes an OPEN item / re-completes a CANCELED one 2→3) — the H-BACKDATE-OPEN rationale, project-confirmed + a latent canceled-item guard gap (WG-7). **Resolution FLIPs PRESERVE stopDate on every surface** (URL `update`/`update-project` completed/canceled, AS `set status`, both kinds/directions — status flips, stopDate byte-identical, umd bumped; re-complete = true no-op) — headless matches GUI ground truth, sweep-invariant. **Date-only:** json rejects a bare date (no time); AS `date "M/D/YYYY"` = midnight → engine normalizes to local noon. oddities §5p/§5q + 2h; WG-7; matrix backdate rows + complete/cancel row | [lab/backdt-project-backdating-and-flips.md](../lab/backdt-project-backdating-and-flips.md); `lab/scripts/research-backdt.sh` |
| LOGSORT L-BASE/CANCELED/UNSWEPT/SWEPT/MIXED/RESTORE/HEADED/SIDEFX/REFUSE | direct-child TO-DO `index` reorder across the lifecycle (open / completed-unswept / canceled-unswept / completed-swept) — the direct-children mirror of HEADSORT. Same `project id` verb re-ranks a project's DIRECT children (`heading=NULL`) of every state to the exact sent order in one comma-joined wire; reachability covers swept (Logbook) to-dos the GUI never exposes to drag. **Divergence from HEADSORT: the reopen is SWEEP-DEPENDENT** — OPEN and UNSWEPT-resolved children (completed OR canceled) reorder `index`-only + umd-SILENT with NO reopen; only a SWEPT to-do is REOPENED by the re-rank (`status 3→0`+`stopDate→NULL`+umd-bump, a §5o move-in reopen). `umd`-bump count = #SWEPT-in-wire (vs #archived for headings). Canceled ≡ completed. Complete/sweep/unarchive index-silent (swept restore re-enters at retained index; swept re-ranked re-enters at new index). L-HEADED: project-scope reorder of a heading's children reparents them (O06/§9k, re-confirmed under golden-v2) + reopens the swept member. ORD-13; oddities §5o addendum + §9v; novel-paths #3¾ | [lab/logsort-logged-child-reorder.md](../lab/logsort-logged-child-reorder.md); `lab/scripts/research-logsort.sh` |
| F-DL-* | Today membership/ordering UI-oracle | [lab/today-order-research.md](../lab/today-order-research.md) |
| A1–A6, B0–B4 (21b series) | environment/TCC/uriSchemeEnabled + wish-list piggybacks | [lab/phase21b-research.md](../lab/phase21b-research.md) |
| SX0–SX4 | shortcut extraction/signing distribution pipeline | lab/scripts/research-sx*.sh + [roadmap §A](../roadmap.md) |
| SX5 | find-items filter repair (consent-preserving DB surgery) + malformed-predicate crash family (oddities §7 C4) | [lab/s-campaign-results.md](../lab/s-campaign-results.md) "VM repair campaign results" |
| SX6 | repaired-asset import validation in a fresh clone via VNC synthetic click (first §E½ demonstration) | [lab/s-campaign-results.md](../lab/s-campaign-results.md) "VM repair campaign results" |
| UI1 | §E½ UI-vector feasibility (VNC-drive `File → New Repeating To-Do`) + the deadline-less-repeat discriminator (template `deadline` column) | [lab/s-campaign-results.md](../lab/s-campaign-results.md) "UI-vector campaign (UI1)"; `lab/scripts/research-ui1.sh` |
| UI2-a…h | Hybrid ui-vector: make-repeat / edit-rule / pause / convert-to-project (to-do AND heading) are all VNC-drivable and all IDENTITY REPLACEMENT except edit (preserves) + pause (`rt1_instanceCreationPaused`); the frontmost/focus-steal disruption profile (dedicated-Mac zero-cost vs own-Mac most-disruptive); folds: closed rows always carry `stopDate` (UI2-g), heading-targeted placement into a completed project auto-reopens it (UI2-h). (UI2-c's "no Stop command exists" was later falsified — see UI2-i.) | [lab/ui-vector-research.md](../lab/ui-vector-research.md); `lab/scripts/research-ui2.sh` |
| UI2-i | The hidden **Stop** action (falsifies UI2-c's "no Stop"): Stop exists but ONLY in the open-card repeat-bar popover (absent from the Items menu + row context menu — menu-parity oversight); it is a confirmation-guarded **identity-replacement un-repeat** (template uuid hard-deleted → NEW plain to-do, `rt1_recurrenceRule` cleared; spawned instance survives; terminal/no-Resume). Recipe: double-click row → card → click "↻ Repeat every …" bar → Stop | [lab/ui-vector-research.md](../lab/ui-vector-research.md) §UI2-i; `lab/scripts/research-ui2i.sh` |
| LOCK1, SYNC1 | headless "closet Mac" research: locked-session vector verdicts (a–e work under lock, f hits the lock screen; headless-lock mechanism) + last-sync/freshness signal archaeology without a cloud account | [lab/headless-research.md](../lab/headless-research.md); `lab/scripts/research-lock1.sh` |
| AXVM1-a…e | **FALSIFIED "Accessibility is SIP-blocked in VMs"**: AX UI-scripting works in a stock Tart guest **with SIP enabled** after a one-time **user-path TCC toggle** (System Settings → `tccd`, not a direct DB write; SX4's "readonly database" was only the direct-write path). The denied attempt auto-creates a toggleable `sshd-keygen-wrapper` row; flipping it (auth 0→2) makes System Events press Items ▸ Repeat ▸ Pause **by element name over SSH** → `rt1_instanceCreationPaused` 0→1. AX press needs **no frontmost app, steals NO focus** (contra VNC/UI2-e) and **works under a locked session** (resolves LOCK1's AX-under-lock unknown; contra VNC arm f). Grant **persists across reboot** → golden-v2 bakeable. Discriminator note: `get name of first process` is NOT AX-gated (use a UI-element read). ui-vector is now lab-certifiable | [lab/axvm1-accessibility.md](../lab/axvm1-accessibility.md); `lab/scripts/research-axvm1.sh` |
| UIC1-a…d | **In-VM certification of the shipped ui vector** (Accessibility granted via AXVM1, ops run through the REAL pipeline in one clone). **5/7 LAB-CERTIFIED** (Things 3.22.11): pause/resume (`rt1_instanceCreationPaused` toggle + next-date clear), make-repeating (identity replacement, new template `fu`=256 + instance), reschedule (`fu` 16→8, identity preserved), `todo.convert-to-project` (new `type=1`, notes kept). **2/7 FAILED**: `stop-repeat` (Stop card opens only via mouse double-click) + `heading.convert-to-project` (`things:///show` doesn't select a heading) — both blocked by list rows exposing no AX/URL selection handle. Addressing catalog: menu items share `performCommand:` (title-pinned); only confirm-sheet buttons carry semantic ids (`action-button-1/2`, upgraded); `window 1` is a hidden 40×40 `AXUnknown` (sheets attach to the standard window); frequency pop-up `set value` is a no-op (→ select-popup). Locale probe: fail-closed under German, recovers under English. Recipe/driver fixes shipped. | [lab/uic1-certification.md](../lab/uic1-certification.md); `lab/scripts/research-uic1.sh` |
| UIC2-a…d | **Project `…`-menu + repeating-PROJECT ops + the stop-then-select crash** (VM, AX over SSH + VNC synthetic clicks). Four project repeat ops (make/reschedule/pause/resume) **DB-verified feasible** (same repeat dialog + codec as to-dos: make → new template `fu`=256 + instance, reschedule `fu` 256→8, pause/resume toggle `rt1_instanceCreationPaused`) but **HYBRID-only**: the `…` **button is not an AX node**, and the `…`-menu / repeat-bar popover items are **AX-readable but inert to `AXPress`** → opening/navigating needs a synthetic MOUSE click (item positions ARE AX-resolved); only the Repeat **dialog sheet** is pure-AX (backgroundable, no focus steal). Repeating-project reschedule/pause/resume/stop live in an **always-visible repeat bar** (no card double-click — unlike to-dos). `open -g things:///show?id=<project>` reveals+selects WITHOUT activating. **CRASH1 (UIC2-c) REPRODUCED 2/2**: stop-repeat a project → select the demoted project → `EXC_BREAKPOINT`/SIGTRAP (Swift trap, `ThingsModel`); **transient** (gone after relaunch), **no data loss** — oddities §7 C5. **UIC2-d: to-do Stop still card-only → DROP.** New oddities §8j (project menu asymmetry, "to-do" confirm copy on projects, inert-`AXPress` custom menus, `AXEnhancedUserInterface=true` collapses the tree). | [lab/uic2-project-menu.md](../lab/uic2-project-menu.md); `lab/scripts/research-uic2.sh` |
| SYNC2 | Things Cloud ("Syncrony") conflict-resolution semantics, live-probed (two clones, one throwaway account): per-attribute merge; same-field TEXT conflicts (notes, title) = timestamp-ordered **3-way merge** with `--` separator — NOT last-writer-wins, nothing dropped, both devices converge; checklists merge as union; delete-vs-edit loses nothing; `BSSyncronyMetadata` populates once an account attaches (last-sync-ATTEMPT NSDate at stable key `GryCJ44xPcJG6go5KeTZp1`) | [lab/headless-research.md](../lab/headless-research.md) §SYNC2; `lab/scripts/research-sync2.sh` |
| SYNCLAT | **Do programmatic writes trigger a Things Cloud push? — HYPOTHESIS FALSIFIED** (two concurrent clones A=writer/B=observer, one throwaway account, pinned-clock networked). EVERY write vector (GUI create, URL `add` fg+bg, URL into project+someday+1.2KB note, AppleScript `make`) advances A's `BSSyncronyMetadata` last-attempt signal within **~2–3 s** and the item reaches the server (B pulls it); Things syncs ON CHANGE (idle baseline flat 16 min+, no periodic heartbeat). Destination/note-size/foreground irrelevant. The minutes-of-latency is **receiver-side** (idle B never naturally pulls; APNs push unavailable in VM per SYNC2 → VM can't measure real-phone pull). Settings-panel "Last Updated" **==** the last-attempt signal. No "Sync Now" menu (UIC1). Mitigation MOOT (writes already push). Account-specific signal key `KgDieoLhfTENjtYqU1sCxX` (reconfirms SYNC2 one-account caveat → nearest-to-now reader). | [lab/synclat-results.md](../lab/synclat-results.md); `lab/scripts/research-synclat.sh` |
| SYNC2B (SY-1/2/2M/3) | **Durable-account sync probes** (golden-v2 / Things 3.22.12; two networked clones, ONE durable account minted-once-never-burned; A=UTC, B=America/Chicago). **SY-1** baseline convergence both directions + `BSSyncronyMetadata` 0→11; a THIRD account confirms the last-sync key is account-specific (here `XZfVzfh8ZXTkw8p1GCGfoo`) → the value-based nearest-to-now reader is the robust path. **SY-2 (THE GATE)** `--preserve-modified` / AS `set modification date` is **SYNC-SAFE** — a hand-written past `umd` (even a 6-yr backdate) propagates to the peer AND survives the round-trip both directions, no dup/ghost; Things Cloud treats `umd` as ordinary per-attribute synced data, NOT a monotonic clock; **SY-2M** confirms the accompanying content edit (notes / tag apply) propagates independent of `umd`. Residual: a concurrent same-row edit re-bumps `umd` via SYNC2's per-attribute max-merge (fails safe). The "UNSYNCED-only" caveat is retired from `--help`/MCP/skill/docs. **SY-3** two disconnected devices materializing the same repeating occurrence reconverge to ONE row (no duplicate); a spawned occurrence's `creationDate` = occurrence-day midnight in the **spawning device's local zone** (resolves SERDEL S5) — winner tiebreak deferred to SY-3b. | [lab/sync2b-durable-account.md](../lab/sync2b-durable-account.md); `lab/scripts/research-sync2b.sh` |
| SYNC3 (SY-3b) | **Cross-device spawn-dedupe tiebreak — RESOLVED** (golden-v2 / Things 3.22.12; durable account #2, LOGIN path; three forced-order runs, one with swapped zones). The spawned instance **uuid is DETERMINISTIC** (both devices mint the identical uuid — the same one SYNC2B recorded as its "winner"), so reconvergence is a per-attribute **add/add 3-way merge on ONE shared row**, not a two-row dedupe: `creationDate` → **MAX**, `userModificationDate` → **MAX**, `notes` → **union** (both markers, `\n\n--\n\n` separator — proof it is per-attribute, not whole-row LWW). Winner is **value-based, independent of reconnect order AND device**: R1 (A-first) & R2 (B-first, opposite) both keep the later `creationDate`; R3 (zones swapped) flips the winner to the now-later device. Refines SYNC2B SY-3's "different uuids / dedupe" framing. Residual: `creationDate`-max vs `umd`-max not independently isolated (co-varied by construction). | [lab/sync3-dedupe-tiebreak.md](../lab/sync3-dedupe-tiebreak.md); `lab/scripts/research-sync3.sh` |
| NOTESMD | **What markdown the notes field renders** (offline clone; VNC card screenshots of a to-do + a project, identical render). Things applies markdown **formatting but keeps the syntax markers VISIBLE** (source-preserving). RENDERS: `#`/`##`, `_`/`*` italic, `**`/`__` bold, `` `code` ``, fenced blocks, `-` bullets (nesting), `1.` ordered, bare URL + `<url>` auto-linkify. INERT: `[label](url)` (label literal, only URL links → don't wrap URLs), `*`-bullets, `> quote`, `---`, `- [ ]`/`- [x]` checkboxes (`- [x]` renders dimmed — oddities §9d). Feeds the §6 doc sweep (queued). | [lab/notesmd-results.md](../lab/notesmd-results.md); `lab/scripts/research-notesmd.sh` |
| ANYORD | **Anytime aggregate-reorder convention, SETTLED = none** (offline clone; 7 loose anytime to-dos across 2 areas + area-less). `reorder to dos in list "Anytime"` is **DESTRUCTIVE** (strips `area`→NULL, isolated on a pristine pair — oddities §9c) **and non-deterministic** (5 identical calls → 5 different orders, never converging); writes `index`. Resolves P7b-vs-P8d (neither model; P13 stands) → do NOT ship an `anytime` scope. **Semantics map:** Anytime is a GROUPED view (ungrouped area-less + per-area + per-project groups); within-group order = `index` ascending (global sequence, GUI == DB verified); cross-group = grouping not index. Clean path = CONTAINER (area) specifier (deterministic, area-preserving); area-less loose anytime = un-reorderable gap. | [lab/anyord-results.md](../lab/anyord-results.md); `lab/scripts/research-anyord.sh` |
| UPC1-A/B/C/D | Upcoming membership & deadline forecasting (UI-oracle + VNC): future-deadline anytime/someday to-dos AND someday projects forecast into Upcoming under the DEADLINE date (Inbox excluded); when+deadline groups by the WHEN-date (one row); the **blue Someday circle = a PROJECT with an OVERDUE deadline** (not "in Today", not "suppressed" — the equally-suppressed to-do stays gray); `deadlineSuppressionDate` is stamped by rescheduling an overdue item to Someday/Anytime (no dismiss button); the "new to-dos" banner OK **materializes** deadline overlays (start→1, startDate:=deadline) | [lab/upcoming-research.md](../lab/upcoming-research.md); `lab/scripts/research-upc1.sh` |
| PLOG1-a/b/c/d | Completed project + restored (open) trashed child (VNC GUI + DB): the completion **modal ignores trashed children** (project completes silently; a non-trashed open child DOES raise the sheet — control-proven), so restoring the trashed child (GUI **Put Back**) yields a COMPLETED project holding an OPEN to-do; **Put Back does NOT reopen the project** (contra §5b); the Logbook sweep is a **derived time-boundary** (`log completed now` mutates no rows) so the open child rides into the Logbook still `status=0`, absent from Anytime/Today/Inbox (reachable only by drilling into the logged project); restore before vs after the sweep converge; URL project-complete cascade shares the trashed-child blind spot. Report-ready = oddities §6¾ | [lab/plog1-research.md](../lab/plog1-research.md); `lab/scripts/research-plog1.sh` |
| NATIVE1-a…h | **The row-selection / mouse-synthesis primitive — feasibility SPIKE** (Things 3.22.11). Tier 1 wins: AX frames + synthetic mouse from **JXA (`osascript -l JavaScript`) via the ObjC bridge**, no Swift helper. Bridging incantations: `ObjC.import('AppKit'/'ApplicationServices'/'CoreGraphics')`; attribute names must be NSStrings (`$('AXPosition')`, plain string → −25201); CF out-params via `ObjC.castRefToObject(out[0])`; frames via `CFCopyDescription`-regex; AX points map 1:1 to CGEvent points. **Double-click → the to-do card opens** (title/notes `AXTextArea` + TaskDetails buttons; repeat bar reachable → unblocks `todo.stop-repeat`). **Drag → an area reorders** (DB column confirmed **`TMArea."index"`**: moved area 0 → −448, sparse/negative, lower sorts higher). BUT the working post is the **HID event tap** (`CGEventPost(kCGHIDEventTap)`) — **`CGEventPostToPid` is INERT for Things' hit-testing** (brief's prescribed mechanism, falsified). Consequently **foreground-bound**: NO backgrounded (Finder frontmost → doesn't land), NO under-lock (hits lock screen) — opposite of the AX menu path (AXVM1-d). **TCC: Accessibility grant alone authorizes CGEvent posting** — no PostEvent/ListenEvent/Input-Monitoring row needed or created. `AXScrollToVisible` UNAVAILABLE on rows (empty action list, −25206) → AXDRAG1 scroll edge case needs synthesized scroll-wheel events. Golden does NOT carry the AX grant (applied AXVM1 rung-b). | [lab/native1-spike.md](../lab/native1-spike.md) |
| AXDRAG1-a…f | **Hardened sidebar-AREA drag-reorder + scoped TAG reorder** (probe campaign; builds on NATIVE1; 17 seeded areas so the sidebar scrolls). **(a) Arbitrary rank placement WORKS** both directions incl. to-first/to-last, DB-verified via `TMArea."index"` sort. **Drop geometry:** the sidebar interleaves h=24 rows with h=16 spacers (40px/slot); **upward drags aim at static coords, downward drags subtract one slot-height** (lifting the source collapses its slot, shifting lower rows up 40px). **Index rewrite:** first drag renumbers ALL areas (sparse, irregular); each later drag reassigns ONE index (intermediate value, not exact midpoint) — and may renumber a **NEIGHBOUR, not the dragged row** (undo must capture the full order, not one index). Row identity is via descendant `AXStaticText`, **not** the row's `AXDescription` (empty here — diverges from NATIVE1). **(b) Scroll-then-drag:** scroll-wheel synth (`CGEventCreateScrollWheelEvent`→HID tap, ~30px/3-line click) brings off-viewport rows in; the sidebar **AXScrollBar exposes `AXValue` 0.0–1.0** so scroll distance is computable. **(c) Mid-drag auto-scroll WORKS bidirectionally** (hover an edge → scrolls to range end) but is time-based/imprecise → prefer pre-scroll for deterministic landing; full list exceeds one viewport so far moves need it. **(d) NO structural hazard for areas** — dropping an area on an area/project row centre just reorders (areas can't nest; `TMArea` count + every `TMTask.area` invariant); **Escape (Key 53) mid-drag aborts cleanly** (index vector byte-identical). **(e) TAG reorder FEASIBLE** (Window▸Tags AX menu press; `AXDialog`; `TMTag."index"`/`parent`): **drop-on-centre NESTS (re-parent), drop-on-boundary REORDERS** — a mis-aimed drop silently nests. Tag names NOT AX-exposed (placeholder text only; chevron marks parents) → address by position. **(f) Reversible** via pre-rank capture + drag-back: relative order restores exactly, index values don't. | [lab/axdrag1-reorder.md](../lab/axdrag1-reorder.md) |
| AXDRAG2-a…d | **`area.reorder-sidebar` build probes + certification** (Things 3.22.11; 25 areas). **(a) Scroll-while-held GO, both halves + both directions**: wheel `CGEvent`s posted mid-drag scroll the list under the held item (scrollbar `AXValue` moved monotonically) and AX frames re-resolve FRESH per tick (watch-row y tracked ~30px/tick; post-abort resolve matched; ~8px post-wheel settle drift → the gesture waits for two stable boundary reads); Escape abort left the index vector byte-identical. **(b) chevron (record-only)**: a persistent 18×18 `AXImage d="Source Toggle Template"` node exists on every area row (hover changes nothing in the tree) but rows expose ZERO actions — frame-resolvable, not AX-actuatable. **(c) certification through the production CLI**: rung-1 down/up, rung-3 multi-hop (3 hops + final, per-hop DB asserts), to-first/to-last, undo round-trip (exact prior slot), gating + duplicate-name negatives (no mutation), invariants (`TMArea` count + assignments byte-identical across ~20 gestures). Discovered the **sidebar AX-mirror ghost bug** (oddities §9) → rung 2 ships disabled (travel-capped, lab knob `THINGS_UI_DRAG_LADDER=held-scroll`); `NSTableViewDefaultSizeMode` is IGNORED by Things (no in-app text-size variation reachable). **(d) tag.reorder BLOCKED**: Tags-window rows are nameless placeholders and `TMTag."index"` ties (the norm) make the DB→row mapping unverifiable — mis-aims silently re-parent, so it does not ship. | [lab/axdrag2-reorder-certification.md](../lab/axdrag2-reorder-certification.md) |
| BJ-0…d (BOUNCEJSON), HEADXPROJ | **`json`-array bounce collapse + cross-project heading move** (`bjhx-lab`, Things 3.22.11, 2026-07-27). **BOUNCEJSON:** a `things:///json` update-array collapses a whole BACK-insert bounce (heading children; project unheaded-anytime children) to **ONE dispatch** — interleave `[someday,anytime]` per item in target order, applied in EXACT array order (scrambled-target 4-item confirmations exact, heading FK 4/4), ~7× faster (30-op array 242 ms vs 30 sequential 1763 ms). FRONT-insert / someday-placement classes (area-someday etc.) are a DEAD END — `json when=someday` + area-direct members are index-inert (oddities §9i). Elements land as DISTINCT sub-transactions (per-element `userModificationDate`); a single bad ref FULL-ABORTS the array (validate-first). **HEADXPROJ:** cross-project heading move is FEASIBLE via the heading row's `…` → `Move…` menu (`AXUnknown desc="More. <title>"` — AX node carrying the title, `AXPress`-inert → HID-click; picker is keyboard-driveable); DB delta = single project-FK rewrite on the heading, children follow via intact heading FK, indices preserved. No drag needed. | [lab/reordgaps-results.md](../lab/reordgaps-results.md) BOUNCEJSON / HEADXPROJ; `lab/scripts/research-bouncejson-headxproj.sh` |
| UIC3-a…b | **Mouse-hybrid build certification: repeating-PROJECT ops** (Things 3.22.11). **(a) micro-probe → NO**: selecting a project *as a row* (its only row-handle is the sidebar entry, whose click navigates/shows it) does **not** surface **Repeat…** in the Items menu (matches UIC2's shown-project finding) — so `project.make-repeating` + the two-step create composite are **BLOCKED-ON-RULING** (the only opener left is the AX-nodeless `…` button → guessed offset, doctrine-forbidden). **(b) certification → 8/9 LAB-CERTIFIED**: `project.reschedule-repeat` (`fu` 256→8, identity preserved), `project.pause-repeat` (`rt1_instanceCreationPaused` 0→1, next cleared), `project.resume-repeat` (1→0) run END-TO-END through the production CLI (`--dangerously-drive-gui`), DB-verified, plus a fail-closed negative test on a non-repeating project. Driver: the new **`click-element`** primitive (AX-resolved frame → HID click, post-click assertion + Escape-abort). **Discovery correction:** the repeat-bar popover is a **separate `AXUnknown` top-level window** (≈215×220), NOT `pop over 1` — addressed by subrole + not-40×40, items are `UI element`s of its scroll area (Change…/Pause/Resume/Stop/Show Latest). **`todo.stop-repeat` DROPPED**; no `project.stop-repeat` (CRASH1). **(a is CORRECTED by UIC4-a/b, below.)** | [lab/uic3-build-certification.md](../lab/uic3-build-certification.md); `lab/scripts/research-uic3.sh` |
| UIC4-a…f | **Project row-selection → `project.make-repeating` is PURE-AX (UIC3-a corrected)** (Things 3.22.11, probe-only). **(a) `AXSelectedRows` is SETTABLE via pure AX** (`AXUIElementIsAttributeSettable`→YES; set→`kAXErrorSuccess`, selection lands) — NATIVE1/AXVM1 only proved *reads*. **(b) projects DO render as selectable content rows** in the area + Someday views (falsifies UIC3-a); title text is NOT AX-exposed on the row (only images). Pure-AX `select-row → Items ▸ Repeat… → sheet` make-repeating DB-verified **foreground AND backgrounded (no focus steal)**; new template `fu`=256 + instance, **area preserved**. FG HID click also selects (no-nav, Repeat… appears); backgrounded the HID click only *raises* Things (focus steal, foreground-bound per NATIVE1). Backgrounded, the Repeat editor **detaches to a top-level `AXUnknown` window** (not an attached `AXSheet`). **(c) someday is MOOT**: make-repeating normalizes **every** project to `start`=2 (template + scheduled instance) regardless of prior anytime/someday; area/area-less preserved, `start` is not. **(d) area-less ANYTIME = a HEADER in Anytime (no row)** → coerce-to-someday (`update-project?when=someday&auth-token`, quiet URL) is **cleanup-free** (make-repeating normalizes `start` anyway); repeating **templates resist quiet-vector schedule edits** (URL `when=` no-op; AppleScript `move`→err 301). **(e) right-click = a real `NSMenu` with Repeat…** on a project row (area) AND an area-less project header (Anytime) — but **NOT AX-exposed** (no `AXMenu` node; `entire contents` blocks in the tracking loop) → keyboard-typeahead only, foreground-bound. **(f) recipes:** area/someday → pure-AX (backgroundable, #1); area-less-anytime → right-click header (FG) or Someday-coercion+pure-AX (background). `project.make-repeating` moves **blocked-on-ruling → path-identified (pure-AX)**; not built. **(BUILT by UIC5, below.)** | [lab/uic4-project-selection.md](../lab/uic4-project-selection.md); `lab/scripts/research-uic4.sh` |
| UIC6-a…m | **The FULL repeat-rule vocabulary + reschedule reversibility** (Things 3.22.11). **CODE + DESIGN + TESTS COMPLETE; in-VM sitting NOT YET RUN.** `RepeatRuleParams` extended backward-compatibly to the whole UIC1 field map (after-completion, weekly weekday set, discriminated monthly/yearly anchor, ends bound, reminder, deadline+start-offset); full combination validation; a unified dual-form dialog-entry recipe builder driving every control (base three LAB-CERTIFIED, new controls PROVISIONAL-pending-this-sitting); CLI flags on all four commands. **Reschedule reversibility RE-EVALUATED → `conditional`** (was irreversible): the undo re-drives reschedule with the CAPTURED prior rule (`ruleToInverseParams`); faithful for every rule the dialog can produce, irreversible only for dialog-inexpressible shapes (two end bounds, multi-anchor month/year, after-completion with a calendar day) or uncaptured/undecodable rules. Unit-tested end to end through the injectable seams (validation matrix, inverse mapping, per-control recipe shape, CLI mapper, do/undo DB round-trip) — no GUI fires. **REMAINING: the UIC6 sitting** (discover/correct the provisional control paths; DB-verify the rule JSON for the a–m coverage matrix; certify the reschedule round-trip undo; two negatives). | [lab/uic6-rule-vocabulary.md](../lab/uic6-rule-vocabulary.md) (plan + verdict) |
| UIC5-a…g | **The pure-AX build: `project.make-repeating` + the `project.add-repeating` composite** (Things 3.22.11). BUILT on the UIC4 pure-AX path through the production pipeline: a new **`select-row`** driver primitive (walk the content table, set `AXSelectedRows`, read back `name of selected to dos` — coordinate-free, readback-verified) + **dual-form dialog addressing** (`pathCandidates`: attached `AXSheet` OR detached `AXUnknown` window). Orchestrator (`runMakeRepeatingProject`) does taxonomy dispatch (area/someday direct; area-less-anytime coerced to Someday first, cleanup-free) and refuses fail-closed via `H-PROJECT-REPEAT` (already-repeating / trashed / logged / duplicate-title row ambiguity). Composite = `project.add`→promote (non-atomic). **LAB-CERTIFIED — the in-VM pass RAN (all 7 cases a–g + gating DB-verified through the production CLI, `--dangerously-drive-gui`).** a=area (template `fu`=256 + instance, area preserved, both `start`=2), b=area-less someday, c=area-less anytime (coercion cleanup-free, post-op == b), d=composite (monthly `fu`=8), e=backgrounded/detached (Finder frontmost throughout, no focus steal), f=already-repeating refusal (H-PROJECT-REPEAT, exit 4, no mutation), g=duplicate-title refusal (ambiguous-row, exit 4, no mutation); gating no-ack→H-UI-DRIVE exit 4, ui-disabled→unsupported exit 6. **Two provisional paths CORRECTED at the sitting:** (1) the select-row primitive set the table's `AXSelectedRows` attribute to a list — a **silent no-op** via System Events; the working route is the row **`select` action** (UIC4-a's settability was via the ObjC-bridge NSArray set, a different API); (2) the detached editor's interval field nests in **group 1** (`text field 1 of group 1 of <detached>`), like the sheet, not as a direct child. Manifest flipped to `lab-certified`. | [lab/uic5-build-certification.md](../lab/uic5-build-certification.md); `lab/scripts/research-uic5.sh` |
| TAGORD1-a…d, TAGINH1-a…c | **Tag-ordering + tag-inheritance knowledge probes** (Things 3.22.11; knowledge-only, does NOT reopen the abandoned `tag.reorder`). **Tie-break oracle = UUID, not title** (TAGORD1-a): never-dragged tags ubiquitously tie at `TMTag."index"=0`; the app orders ties by tag **UUID ascending**, proven identical across the Tags window, a to-do's pill row (input-order independent), and the filter-bar chips — `ORDER BY "index", uuid` reproduced the whole Tags-window order byte-for-byte where `, title` diverged (TMTag has no creation-date column; names are NOT AX-exposed — reconfirms AXDRAG1-e). → **shipped the CLI fix** (`fetchTagsForTasks`/`areaTags`/`tagsView` title→uuid + unit test). **`TMTag."index"` is GLOBAL, not per-parent** (TAGINH1-b/TAGORD1-b): a seeded child landed at -378 among roots. **A HEADING CANNOT be tagged** (TAGINH1-a) on ANY surface — URL `update?tags=` no-op, AppleScript `set tag names` no row, GUI has no tag affordance / Items ▸ Tags… never enables — so heading→child inheritance is moot and `tagScopeSql` (no heading-direct clause) is correct by omission. **Inheritance axes vs our SQL** (TAGINH1-b): direct + project→child + area→child match T18's GUI oracle; the heading-chain clauses (5/6) and tag-hierarchy expansion model the same principle by analogy (tag-hierarchy stays documented-not-oracled). **Surface audit** (TAGINH1-c): `things show`/project card render a separate `inherited:` line; list rows carry direct tags only; MCP/JSON carry additive `inheritedTags` + filters honor full inheritance. | [lab/taglab-probes.md](../lab/taglab-probes.md); `lab/scripts/research-taglab.sh` |
| TAGINH2-a, TAGINH2-b | **The two never-oracled inheritance axes, now GUI-oracled** (Things 3.22.11; VNC-screenshot oracle, NO AX grant). **Tag-hierarchy descendant expansion** (TAGINH2-a): filtering by a PARENT tag matches a child-tag-only item (✅ in an area scope AND built-in Anytime); the inverse (child filter, parent-only item) does NOT match (✅) — `tagWithDescendants` confirmed, one-directional. **Heading-chain** (TAGINH2-b): in a flat list, a heading-nested child with NO direct tag filters IN under its project's tag (clause 5 ✅) and its project's area tag (clause 6 ✅), and is EXCLUDED by "No Tag" (`untaggedScopeSql` ✅). **Zero divergence in the modeled (flat-list) context — no SQL change.** Decisive finding: filter inheritance is CONTEXT-DEPENDENT — a project's OWN in-place filter bar IGNORES inheritance (opposite result), a newly documented Things quirk (oddities §9a) the `--tag`/`untagged` filters do not model. Screenshots hand-verified. | [lab/taglab-probes.md](../lab/taglab-probes.md) TAGINH2; `lab/scripts/research-taginh2.sh` |
| TAGW1-a…f | **Tag-WRITE semantics** (Things 3.22.11; disposable clone `tagw1-lab`, DB oracle, no AX grant). **Unknown-tag handling DIVERGES by vector** (oddities §9b): URL `add/update?tags=` silently DROPS an unknown tag and applies the known ones (partial write, TAGW1-a); AppleScript `set tag names` silently CREATES the unknown tag as a root and applies both (TAGW1-b). **Duplicate names are uncreatable through any app surface** (TAGW1-c): `make new tag` COALESCES to the existing same-named tag (same uuid returned, one row), same-name-under-two-parents collapses, and the URL scheme cannot create tags at all → a real duplicate-name pair is a Cloud-sync-only pathological state → the resolver stays fail-closed-with-candidates. **`/` is a legal literal in a tag title** (TAGW1-d): `sl/ash` stored + matched literally via both `set tag names` and URL `tags=sl%2Fash` → path-qualified `parent/child` input needs literal-over-path precedence. TAGW1-e (GUI filter bar) deferred — no VNC; TAGW1-f (duplicate determinism) MOOT — no duplicate stageable without a forbidden direct write. Drove the tag-write-ergonomics build (uuid/path tag refs, `--create-tags`, duplicate refusal, `area.add` verify gap). | [lab/tagw1-tag-writes.md](../lab/tagw1-tag-writes.md); oddities §9b |

| UIC6-a…m | **Full repeat-rule vocabulary certification** (Things 3.22.11). All four repeat ops (to-do + project make/reschedule) driven END-TO-END through the production CLI (`--dangerously-drive-gui`), DB-verified against the decoded `rt1_recurrenceRule`. **12/13 rule dimensions LAB-CERTIFIED**: weekly multi-day (`of`=[{wd:1},{wd:3},{wd:5}]), monthly last-weekday (`{wd:5,wdo:−1}`) + last-day (`{dy:−1}`), yearly (`{mo:9,dy:7}`), after-completion (`tp`=1 + a NOMINAL `of`=[{wd:0}]), ends-after (`rc`), deadline+start-earlier (`ts`=−3), the two PROJECT cases, ends-on-date (`ed` committed), and the **reschedule round-trip undo** (rule A→B→undo→A, identity preserved; incl. an after-completion round-trip). **The provisional control paths were structurally WRONG** and corrected (the "Ends" pop-up is always `pop up button 1 of group 1`; the per-frequency pop-ups follow it; the dialog opens in after-completion mode; three driver bugs fixed — candidate-poll for revealed controls, self-healing pop-up open-click, keystroke-commit for numeric fields which `set value` never committed). **Two findings, LOUD:** (1) the reminder-time `AXDateTimeArea` IGNORES AX `AXValue` writes on commit (an app accessibility defect — the "ends on date" picker, same role, honors them) → `--reminder` refused fail-closed, not driven; (2) after-completion rules carry a nominal offset, so the `ruleToInverseParams` after-completion null-branch was too strict (would have made after-completion reschedule-undo irreversible) — **fixed + certified**. | [lab/uic6-rule-vocabulary.md](../lab/uic6-rule-vocabulary.md) |
| UIC7-a…h | **The `reschedule-repeat` GUI-drive cluster fix** (Things 3.22.11; run `uic7-lab`) — certifies the CONVERSION shapes UIC5/UIC6 never exercised (fixed↔after-completion, interval 1 vs >1). **The field-report incident is FIXED:** a fixed→after-completion biweekly reschedule now lands `tp=1 fu=256 fa=2` and returns `ok` (was reported as `VERIFY FAILED` while the change silently landed). **(a)** reschedule delta now asserts the rule **type** (both rules were weekly/2 — only the type flipped, so verify couldn't previously see the conversion); a nonzero-transport drive **re-verifies with bounded backoff** and returns `ok` if the rule landed (loud "DID land" warning); a **pre-drive idempotency** no-op skips the GUI drive when the target already holds (validated live: elapsedMs 33 vs 4676). **(b)** every drive's `--json` stdout is one clean envelope, incl. two GENUINE live failures — no banner leak. **(c)** the after-completion unit pop-up is driven by a singular+plural candidate list (`week`/`weeks`…), fail-closed if neither. **(d)/(e)** verified-abort (assert the sheet gone before claiming dismissal) + open-sheet preflight diagnosis — host-unit-tested (the live (e) repro was confounded: the reveal preamble dismissed the hand-opened sheet). **(f)** a fixed→after-completion reschedule PRESERVES `ts` (start-offset) + deadline but RESETS the calendar `of` to the unit nominal (oddities §8p). **NEW defect discovered → RESOLVED in UIC7b:** the interval field, when it is the FIRST field after a frequency/type switch, races the re-layout and lands `1`. UIC7b closes it — the `set-value` primitive is now closed-loop (type → read-back → bounded re-type, fail-closed), which lands interval>1 live (fixed weekly/2→`fa=2`, monthly/3→`fa=3`), and the make-repeating create-probe verifies the decoded rule so a mis-commit is `verify-failed:mismatch`, never silent `ok` (todo + project make + add-repeating promote leg). Zero silent-wrong across 5 in-VM cases; the plural after-completion unit label was exercised live (MR4b). (oddities §8l addendum.) | [lab/uic7-reschedule-cluster.md](../lab/uic7-reschedule-cluster.md); `lab/scripts/research-uic7.sh` + `research-uic7b.sh` |
| REM1-a/b/c | **Repeat-reminder gate workaround hunt — NO workaround, the gate stands** (Things 3.22.11). Probed both candidate workarounds for the UIC6-g / §8l reminder-picker defect and BOTH FAIL. **(a) inheritance seeding FAIL:** seeding a plain to-do with a DB-verified 09:00 reminder (`reminderTime=603979776`) then driving `make-repeating` does NOT inherit it — the new template's rule has no time key and both template + spawned instance are `reminderTime=NULL` (make-repeating drops the reminder). **(b) HID sub-field entry FAIL (3/3):** HID-clicking the reminder `AXDateTimeArea`'s hour segment (AX-frame-derived, no guessed pixel) then typing absolute digits (07:00) never registers — the control's `AXValue` stays at midnight through the click and both digit phases, and OK commits the DEFAULT 12:00 (`805306368`) every run, never the typed `469762048`. **(c) quiet path closed:** keyword `update?when=someday` on a template is a silent no-op (§8k), and the only timed URL reminder spelling is the §1 crash. `--reminder` stays refused fail-closed (`assertRepeatRule`); no code changed. | [lab/rem1-reminder-workaround.md](../lab/rem1-reminder-workaround.md); `lab/scripts/research-rem1.sh` + `research-rem1b.sh` |
| RSIM1–RSIM6 | **Recurrence-creation mutation shapes** (Things 3.22.11; prereq for the AGENTBENCH recurrence-simulator appliers). Full row-level DB deltas around every recurrence-creation op, driven through the production CLI in a clone. **Creation spawns exactly ONE instance immediately** in every create path: a FIXED rule (make-repeating to-do/project, add-repeating) DELETES the source (identity replacement) and mints a fresh instance at the current-occurrence `startDate`; an AFTER-COMPLETION rule KEEPS the source and relinks it as that single instance (**refines §8g — identity replacement is fixed-only**). Template = `start=2`/`startDate=NULL`/`deadline=NULL` + `rt1_recurrenceRule`; instance = `rt1_repeatingTemplate`→template/rule NULL/`startDate`=occurrence. **Completing** an after-completion instance stamps the template's `rt1_afterCompletionReferenceDate` (=completion date) + `rt1_nextInstanceStartDate` (=completion+interval) but does NOT materialize the next instance (future-dated, pending). **Reschedule** is in-place (identity preserved), rewrites `fu`/`of`/anchors — one caveat: the interval-field edit didn't apply and the op fail-closed (recipe-vs-app follow-up). Area preserved on a project make-repeating; `start` normalizes to Someday. | [lab/rsim-results.md](../lab/rsim-results.md); `lab/scripts/research-rsim.sh` |
| RSIM-P (P1–P5) | **Repeating-PROJECT children semantics** (Things 3.22.11; follows RSIM, prereq/target for SIMFID). Full TMTask+TMTaskTag+TMChecklistItem deltas around `project make-repeating` on a project WITH a heading + tagged/checklisted children, driven through the production CLI in a clone. **Making a project repeat (fixed OR after-completion) hard-DELETES the source project + its whole descendant subtree and mints TWO deep copies** — one under the hidden template project, one under the instance — with tags + checklist items duplicated per copy. **Template-side children are completely plain rows** (`rt1_recurrenceRule`/`rt1_repeatingTemplate` NULL, `start=1`); the only tie to templatehood is the `project`/`heading` pointer. FIXED leaves instance-side children plain; **AFTER-COMPLETION links each instance-side child's `rt1_repeatingTemplate` to its template-side sibling AND deletes the source** (breaks RSIM2's "after-completion preserves the source" — that law is to-do-only). Completing an instance cascades `status→3` through the subtree incl. the heading row (no re-spawn on a pinned-past clock); `todo move` treats a template project as an ordinary container (in/out clean, no guard); `things show` marks the PROJECT's template/instance status but leaves every CHILD's `repeating` block empty. **Flags 3 SIMFID divergences** (make-repeating appliers orphan children; after-completion-project applier wrongly preserves the source; `project.complete` doesn't cascade the heading row). New oddities §8g addendum. | [lab/rsim-results.md](../lab/rsim-results.md) §RSIM-P; `lab/scripts/research-rsim-p.sh` |
| RSIM-P2 (A1–A5, B1–B4) | **Nested repeaters + uuid-discovery traps** (Things 3.22.11; follows RSIM-P). **Converting a project that contains a repeating to-do FLATTENS the nested repeater** — the nested template row is hard-deleted, its instance demoted to plain (`rt1_repeatingTemplate` cleared), and both survive only as PLAIN duplicated copies (no template-of-template, no `rt1_*` survives); identical for fixed + after-completion nested rules (A1/A2). Edge-state children (A3): completed→copied-but-REOPENED, trashed→skipped, external-repeating-instance→copied-plain-with-FK-cleared (external template never polluted). **A5** (after-completion project WITH a heading): instance-side heading AND to-do children are ALL plain — only the project-level instance→template link exists (**confirms the simulator's headings-left-plain; contradicts RSIM-P P4's per-child to-do links**). Discovery traps: same-title "Ditto" gauntlet binds the new template CORRECTLY (B1); a mid-drive same-title add fails the ui-drive CLOSED rather than misbinding (B2); the minted template inherits notes/tags/checklist but NOT deadline (B3); the `rt1_repeatingTemplate` FK is exact per template, filter by type to disambiguate (B4). **Two systematic contradictions of RSIM/RSIM-P flagged for SIMFID: source PRESERVED-as-instance (not deleted) for fixed conversions; and no after-completion per-child FK links.** A4 clock-advance was impractical (a +15-day jump wedged the VM). | [lab/rsim-results.md](../lab/rsim-results.md) §RSIM-P2; `lab/scripts/research-rsim-p2.sh` |
| RSIM-R (R1–R8, S1–S3) | **Reconciling fixed-mode source fate + after-completion child links** (Things 3.22.11; resolves the two RSIM-P2 contradictions). Both are REAL app-behavior differences (re-derived uuid-by-uuid from raw snapshots — not differ bugs; same build + byte-identical ui-recipe — not a regression), each now a single reproducible law. **C1 source fate:** RSIM-P2's area/scheduling axis is **FALSIFIED** — every bare item DELETES regardless of area (R4) or today-schedule (R1/R5). The real axis for a **project** is a **nested repeater in the subtree**: a fixed `project make-repeating` PRESERVES the source (relinks as instance, flattens the nested repeater, mints only the template) IFF the subtree holds a nested repeating to-do (A1/A2/A3/S2/S2b = 5/5); a plain-children/empty/area'd/scheduled project DELETES (S1/R1/R3/R4/P1/RSIM6 = 6/6). A **to-do** deletes when bare or checklist-only (RSIM1/R5/R6/S3); the lone content-rich preserve (B3) is a deadline-led, unisolated trigger (parked). **C2:** RSIM-P P4's per-child instance→template links are a **non-reproducible anomaly** — reproducible law is PLAIN instance-side children (A5/R7/R8 = 3/3; R7 replicated P4's exact config). **Simulator:** `applyMakeRepeatingFixed` must delete-by-default and preserve only for project-with-nested-repeater; `applyMakeRepeatingAfterCompletionProject` must drop the per-child links. Discovery invariant "source absent OR relinked-as-instance" holds 16/16; per-template `creationDate` time-bound survives. | [lab/rsim-results.md](../lab/rsim-results.md) §RSIM-R; `lab/scripts/research-rsim-r.sh` + `research-rsim-r2.sh` |
| RSIM-S (S1–S4, Q2, S-R) | **Next-occurrence SPAWN semantics + Quick Find visibility of template children** (Things 3.22.11; follows RSIM-R; closes RSIM-P's parked re-duplication boundary). **Clock problem solved:** SMALL +1-day advances on a DAILY repeater (next occurrence = tomorrow) beat the +15-day wedge — 3 consecutive spawns (07-06/07/08) ran clean, no reboot (guest helpers moved to `~/things-lab/helpers` to survive one). **Each next occurrence materializes a FULL fresh subtree copy** (INSERT 10 / DELETE 0 / CHANGE 3 — the new instance + its child copies + the golden repeater; only the template PROJECT row's `icCount`/next-date change; prior occurrences accumulate). Occurrence children are **PRISTINE**: completed/canceled → reopened, scheduled/someday → unscheduled, deadline → dropped, **trashed template child → SKIPPED**, plain → verbatim; titles/notes copy from the template's LIVE state. **Per-child instance→template FK is NONDETERMINISTIC across consecutive spawns of ONE series (6/7, 5/7, 6/6)** — direct proof of RSIM-R C2; tolerate, never assert. **S4:** template-side children are STATUS/SCHEDULE-immutable (complete/cancel silent-no-op all surfaces; schedule/move→301/302; deadline no-op; only trash + title/notes + project-move land → oddities §8n). **Q2:** Quick Find surfaces the hidden template project (`start=2`), template-side children, instance project, and instance children — all findable, none marked. Design input: keep template children **findable-but-MARKED** in our search (GUI parity + the container-marker the GUI lacks). | [lab/rsim-results.md](../lab/rsim-results.md) §RSIM-S; `lab/scripts/research-rsim-s.sh` + `rsim-s-clock.sh` + `rsim-s-states.sh` + `rsim-s-quickfind.sh` + `research-rsim-sr.sh` |
| SL1 (A, B, C3, D1–D2, proj) | **The GUI "Show Latest" criterion** (Things 3.22.11; feeds the planned `latestInstance` read-layer derivation). "Show Latest" (Repeat submenu on the selected template / repeat-bar popover) selects the instance with **`max(creationDate)`** among the template's instances — the most recently SPAWNED occurrence — and NOTHING else. Disambiguated in a clone by accumulating 4 daily occurrences (+1-day advances, RSIM-S technique) then diverging keys via URL-scheme reschedule/complete: rescheduling an instance to the max `startDate` did NOT move the pick (rules out `startDate`), nor did being the max `userModificationDate` (edited) or the newest `stopDate` (completed), nor did completion status (a completed max-`creationDate` instance is still chosen — no open-vs-completed bias), nor "the current/today occurrence" (winner picked while dated in the future with none at today). Oracle = **`id of selected to dos`** (Things' own selection model; VERIFIED against a known `things:///show?id=` selection; also resolves a shown PROJECT to its uuid) — the menu-bar Copy Link clipboard oracle was **unreachable** (`Items ▸ Share…` has no submenu, `-1719`). **Replicated on a repeating PROJECT** via the repeat-bar popover (UIC3 recipe, ≈215×220 `AXUnknown` window): pick = max-`creationDate` project instance, not max-`startDate` → not to-do-specific. Rule: `SELECT uuid … WHERE rt1_repeatingTemplate=:t ORDER BY creationDate DESC LIMIT 1` (no status filter). `rowid` is collinear with `creationDate` (unbreakable via official surfaces); `TMTask."index"` uniform → not the key. Odd: URL-scheme `update(-project) when=` is a silent no-op for a past/today date on a scheduled repeating instance (future-only). | [lab/sl1-show-latest.md](../lab/sl1-show-latest.md); `lab/scripts/research-sl1.sh` + `sl1-clock.sh` + `sl1-round.sh` + `sl1-proj.sh` |
| SL2 (Q1–Q5) | **Trash / empty / restore dynamics of repeating instances** (Things 3.22.11; extends SL1, which never tested a trashed instance). **Q1 (L1):** Show Latest's candidate set **EXCLUDES trashed** — a trashed `max(creationDate)` instance is SKIPPED to the newest UNTRASHED one; the association is computed live (empty-trash re-resolves, no dangling); a template with ZERO instances **drops the "Show Latest" menu item** (Repeat submenu = `Reschedule…, Pause` only — not disabled). This **CONTRADICTED the shipped `latestInstanceUuid`** (SL1 shipped no `trashed` filter) → query + R11 pin + `contract.md` fixed in the SL2 PR to add `AND trashed = 0` (status filter still absent — completed newest still wins, SL1 D1). **Q3 (L2):** a FIXED repeater is date-driven — trashing the live instance mutates NO template `rt1_*` column and the next occurrence still spawns on schedule (trashed date not re-materialized). **Q2 (L3):** trashing the live open AFTER-COMPLETION instance is treated as a **completion** — the template stamps `rt1_afterCompletionReferenceDate` = the trashed instance's date + `rt1_nextInstanceStartDate` = +interval and the replacement spawns on schedule; NOT dormant, `rt1_instanceCreationPaused` stays 0 (filed to oddities). **Q4 (L4):** restoring a trashed instance beside its replacement yields TWO concurrent live instances (both FK-intact, both rendered in Today; no merge/mutate/error); Show Latest still picks `max(creationDate)` over the untrashed set. GUI **Put Back** ("Put Back in Today", right-click, PLOG1 recipe) restores in-place (keeps `startDate`); scriptable `move…to Inbox` (E15) de-schedules — both un-trash + keep the FK. **Q5 (L5):** hard-delete (trash+empty) of a live AC instance advances the series like L3 AND writes a `TMTombstone` (instance `leavesTombstone=1`, TOMB1); the series survives (replacement spawns). Oracle = `id of selected to dos` (SL1). | [lab/sl2-trash-dynamics.md](../lab/sl2-trash-dynamics.md); `lab/scripts/research-sl2.sh` + `sl2.sh` |
| HEADCERT1-a…c | **`heading.convert-to-project` certified — the LAST uncertified ui op** (Things 3.22.11). The UIC1 blocker (`things:///show?id=<heading>` selects to-dos only → Convert to Project… disabled) is cracked by revealing the heading's **parent project** and issuing the UIC5 row **`select` action**: a heading row DOES take selection (`selected`→true, `name of selected to dos`→empty — the heading discriminator), which enables Convert to Project…. **(a) AX tree:** heading rows expose no AXStaticText/AXDescription/actions (the title lives only in a hover-dependent "More" affordance, unreliable headless) → identity is **POSITIONAL** (Nth selectable-empty-readback row = Nth heading by DB `"index"`; `classifyHeadingConvert` computes the ordinal). **(b) paths:** the row `select` action works (background-capable); menu-item `enabled` reads without opening the menu are STALE (revalidate on menu-open). **(c) production-CLI cert:** ordinal 1 (Hx2) converted leaving Hx1 UNTOUCHED (positional identity), ordinal 0 (Hx1) converted; children reparent `heading→NULL`, new project into the parent's area (= UI2-d); gating no-ack→H-UI-DRIVE exit 4, ui-disabled→exit 6. Manifest flipped to `lab-certified` — every ui op now lab-certified. | [lab/headcert1-certification.md](../lab/headcert1-certification.md); `lab/scripts/research-headcert1.sh` |

| BANNER1 (L1–L5) | **The Today "You have N new to-dos" banner + yellow-pip law** (Things 3.22.11; extends UPC1 §8d). **The pip set is DB-derivable, with NO separate reviewed marker.** A Today member is **new/pip'd ⟺ `start != 1 OR startDate IS NULL`** (not yet materialized); banner **N = pip count**, verified row-for-row (6 predicted = 6 rendered = "6"). **Classes:** autonomous entrants are new — scheduled arrival (`start=2`), repeat-instance spawn (`start=2`), deadline-pull (`startDate NULL`); user-placed entrants are NOT — `add`/`update when=today` land already materialized (`start=1,startDate=today`), no pip/banner. **What OK mutates:** ONLY the new `TMTask` rows — `start:=1` for `start=2` members, `startDate:=todayIndexReferenceDate` for deadline-pulls (§8d) — **no marker row in any table** (12 changed dump lines = 6 rows; `TMMetaItem`/`Meta`/`TMSettings`/`BSSyncronyMetadata` byte-identical across OK → **H1 falsified, H2 confirmed**; cross-device sync-parity follows, propagation itself unverified on the airgapped rig). **Persistence:** pips survive relaunch-without-OK and stay cleared after OK (no reappear — durable, not a per-day recompute). **Stamp timing (L5):** `todayIndexReferenceDate` = the **deadline date**, written at creation, invariant to a late first-launch (deadline 07-07, closed across 07-07, launched 07-08 → stays 07-07) — closes UPC1's + today-order's open edge. **Side effect (§8s):** OK silently DE-INBOXES / DE-SOMEDAYs an untriaged pulled item (`start 0/2→1`). Oracle blind to banner+pips (custom NSViews) → screenshots. | [lab/banner1-research.md](../lab/banner1-research.md); `lab/scripts/research-banner1.sh` + `banner1.sh` |
| BANNER1b | **Provisional Today members vs the Anytime list** (Things 3.22.11; extends BANNER1 Q1b — closes the one open question). **Pre-OK provisional members ARE GUI-Anytime members, in every class** — deadline-pull from Someday (`start=2`), Inbox (`start=0`) & Anytime (`start=1`, control), scheduled arrival (`start=2`) and repeat-instance spawn (`start=2`) all appeared in the `Anytime` oracle pre-OK. **Anytime = a superset of Today's to-dos** (all actionable to-dos minus Someday-deferred & future-scheduled), so a deadline-pull that removes a row from Someday/Inbox simultaneously **re-files it into Anytime even while raw `start` still reads 2/0** — confirming R10.2's arrived-law GUI-side. **OK changes NO list membership** (pre/post-OK `Anytime`+`Today` oracles byte-identical; diff = 7 `TMTask` rows = the "7 new", re-confirming BANNER1 L4). **Wrinkle (§8s):** in the Anytime list a provisional member shows the ordinary yellow **Today ★** and **no pip** — indistinguishable from a materialized one; the banner+pips are a **Today-list-only** rendering. **Verdict: a provisional row models as stage `anytime` + provisional marker (NOT bucket-less limbo)** — someday/inbox views exclude deadline-pulled rows, the anytime view includes them. Repeating **template** in neither list (UUID-verified). | [lab/banner1-research.md](../lab/banner1-research.md) (BANNER1b §); `lab/scripts/research-banner1.sh` + `banner1.sh` |
| UPCORD1 (Arm A/B/C) | **The last ordering gap: LOOSE items within a future Upcoming day-group** (`upcord1-lab`, Things 3.22.11, 2026-07-30; headless, pinned 2026-07-05, test day 07-10). Loose future-dated to-dos live on the `todayIndex` axis (`index`=0), same as container children. **Arm A (re-when-same-date) = DEAD END:** URL `update?when=<the SAME date>` is a COMPLETE NO-OP — `userModificationDate` byte-identical across the call (zero writes; a hard date-equality short-circuit), so no in-day reindex primitive exists. **Arm B (park-sort-unpark) = the WIREABLE protocol, via a scratch PROJECT only:** PARK loose→project (`update?list-id=<project>`) → REORDER (`reorder to dos in project id <p> with ids <target>`, `todayIndex` re-ranked to sent order, date-preserving) → UNPARK (`update?list-id=` empty). All three legs preserve `startDate`/`todayIndex`/`start=2`/`reminderTime`/`deadline`; final loose order == scrambled target. Cost 2N+1 dispatches + scratch-project lifecycle; non-atomic, recoverable. The **AREA** variant is DESTRUCTIVE — the area reorder de-schedules dated members (`start 2→1`, `startDate→NULL`; §9f extension). Loose scheduled PROJECT ROWS carry `todayIndex` but have no clean day-reorder surface (can't nest in a project; area destructive; re-when inert) → stay app-default. **Arm C (templates):** re-parentable headlessly (URL `list-id=`) with `rt1_recurrenceRule` byte-identical (series stable, no spawn); the container-day reorder SKIPS the template row (§9e data-layer confirm); NO re-when path (URL `when=` = §1 CRASH, AppleScript `schedule` = err 302). GUI intermix law UNPROBED (no VNC). Candidate matrix flip: "loose future day" APP-DEFAULT → GUARANTEED via the park-sort-unpark protocol. | [lab/upcord1-loose-day-order.md](../lab/upcord1-loose-day-order.md); `lab/scripts/research-upcord1.sh` |
| HEADSUB1 (Arm A/B/C/C2/D) | **Ordering the buckets UNDER a heading + the child-evening bounce law** (`headsub1-lab`, Things 3.22.11, 2026-07-31; headless, pinned 2026-07-05, scheduled day 07-10). **Arm A — O06 is AXIS-AGNOSTIC:** the native container-day reorder (`project id`) over a mix of headed + unheaded same-day children re-ranks `todayIndex` date-preservingly BUT RIPS every headed child's `heading` FK → NULL and reparents it to the project root (same destruction as HEADORD-a's `index`-axis rip) → headed scheduled day-order NOT directly achievable; oddity §9k. **Arm B — move-to-heading (`update?list-id=<p>&heading=<title>`) append law per class:** anytime + someday BACK-INSERT deterministically on `index` (someday siblings renumbered down §9h; `start=2` preserved), so re-head-in-forward-target-order IS a sort protocol; SCHEDULED is a `todayIndex` NO-OP (move only sets the heading FK, order inherited — move order does NOT sort); reminder+deadline preserved. **EVENING IS REPRESENTABLE UNDER A HEADING** (novel) — the move preserves `startBucket=1` + today `startDate`, appends on `index`. **Arm C — unhead (`update?list-id=<p>`, no heading param) is clean** (heading→NULL, project set, `index`/`todayIndex`/date/`start` KEPT); the FULL round-trip unhead→SOMEORD-b→re-head lands target for someday; the SHORT direct-re-head FAILS for scheduled (rehead is a todayIndex no-op). **Arm C2 — scheduled full round-trip** unhead→DAYORD-b→re-head lands target (date-preserving; the mandatory path since Arm A rips). **Arm D — the child-evening bounce EXTENDS to container children:** a project child bounced `when=today→when=evening` FRONT-inserts deterministically (below the evening group `todayIndex` min), project FK + `startBucket=1` + today `startDate` preserved, SAME direction as the loose evening control → the shipped `evening` BounceSpec applies unchanged. Candidate matrix promotions (lab-doc only): within-heading someday/scheduled + container-child evening → GUARANTEED via the above protocols. | [lab/headsub1-heading-subbuckets.md](../lab/headsub1-heading-subbuckets.md); `lab/scripts/research-headsub1.sh` |
| HEADSUB2 (Q1/Q1fix/Q2) | **Confirming — and CORRECTING — two inferences #327 shipped on** (`headsub2-lab`, Things 3.22.11, 2026-07-31; headless, pinned 2026-07-05). **Q1 = (b): the shipped `heading-someday` compile was BROKEN.** Re-heading a row ALREADY under the target heading (`update?list-id=<p>&heading=<same-title>`) is a **same-heading NO-OP on `index`** — the block never reorders (4 someday children stayed byte-identical through all 4 scrambled-target re-head legs; anytime identical). HEADSUB1 measured the back-insert only for LOOSE→heading (Arm B) and unhead→re-head (Arm C) and OVER-GENERALIZED it; #327 shipped the inert direct-re-head. New oddity **§9l**. **Q1fix = the round-trip lands target:** unhead the block (clean, `index`/`start=2` preserved) → re-head each in forward order → each now-loose row genuinely back-inserts → final order == scrambled target. **`runHeadingSomeday` fixed to the unhead→re-head round-trip in the same change.** **Q2 = CONFIRMED:** an area-DIRECT this-evening to-do bounced `when=today→when=evening` FRONT-inserts (below the evening `todayIndex` min), **area FK byte-identical**, `startBucket=1` + today `startDate` preserved — SAME direction/state-law as HEADSUB1 Arm D's project child and the loose control, so #327's routing of area-direct evening to the `evening` scope is right. | [lab/headsub1-heading-subbuckets.md](../lab/headsub1-heading-subbuckets.md) (§HEADSUB2); `lab/scripts/research-headsub2.sh` |
| ORDFIN1 (Arm 1/2/3/4) | **The ordering ENDGAME: the last four open ordering questions** (`ordfin1-lab`, Things 3.22.11, 2026-07-31; pinned 2026-07-05; Arms 2/3/4 headless, Arm 1 via the AXVM1 Accessibility grant + VNC). **Arm 1 (TMPLIDX) — a repeating template's `todayIndex` is UNREACHABLE on every surface:** it is inert (`tIdx=0 idx=-940` byte-identical) to every headless repeat-series lever — PAUSE (`rt1_instanceCreationPaused` 0→1, next→NULL), RESUME, COMPLETE-the-current-instance (series advance), RESCHEDULE dialog cycles; and the GUI drag is INFEASIBLE — the Upcoming projected occurrence renders its title + a repeat glyph but its AX row exposes only generic cell-template chrome (`Task NewForToday Template`/`Task Alarm Template`), no title/uuid/repeat-marker, AX-indistinguishable from an ordinary to-do → fail closed, no drag (§8h addendum). **Arm 2 (EVEHEAD) — the evening bounce works on a HEADED evening child:** the today↔evening round-trip preserves the `heading` FK byte-identical (`startBucket` 1→0→1, today `startDate` kept) → headed-evening flips to GUARANTEED via the shipped `evening` scope, and the "GUI-ambiguous display axis" justification was wrong (maintainer law). Corollary 2c: the PROJECT-specifier reorder is evening-SAFE (keeps `startBucket=1`) — de-evening is Today-scope-specific (O03). **Arm 3 (AREADAY) — `area-day` is wireable:** park an area's dated children into a scratch PROJECT → `container-day` reorder → restore to the area; date/state/reminder/deadline preserved, area FK round-trips, canaries untouched. **Arm 4 (UPCORD2) — `upcoming-day` is wireable:** ONE scratch project interleaves a whole cross-container day-group (loose + project + headed + area children) on the global `todayIndex` axis, every FK restored, restore-leg order irrelevant. Matrix flips (evidence only, wiring deferred): headed-evening → GUARANTEED; `area-day`/`upcoming-day` → protocol-proven. | [lab/ordfin1-ordering-endgame.md](../lab/ordfin1-ordering-endgame.md); `lab/scripts/research-ordfin1.sh` |
| SIT3 (EVEPROJ · BANNERACK · REMSTALE · LATERLINK · SPECLIST · TOMORROWHEAD) | **Arrival / evening / lists — six arms** (`sit3-lab`, Things 3.22.11, 2026-07-31; pinned 2026-07-05, rolled +1→07-06→07-07; headless URL/AppleScript + VNC HID for the banner OK). **EVEPROJ:** `update-project?id=<u>&when=evening` sets a PROJECT to This-Evening in ONE leg (`startBucket=1, startDate=today, type=1`), area-less AND area'd (area FK preserved); the two-leg bounce preserves all state; but a project's `when=today` does NOT global-front-insert (a to-do's does) and a native `list "Today"` reorder DE-EVENS the project (O03 generalizes). **BANNERACK:** the banner OK (start `2→1`, startDate:=deadline where NULL) is NOT URL-reproducible — `when=today` no-ops on a provisional row, `when=anytime` flips start but NULLs startDate (leaves Today), and OK does NOT bump `userModificationDate` where every URL `when=` does → not byte-faithful (confirms BANNER1 L4 + adds the umd-invariance). **REMSTALE:** the This-Evening flag AND `reminderTime` render only while `startDate == current day`; once stale the GUI collapses the row to plain "Today" (no evening section, no reminder badge/popover) while the bytes are NEVER cleared (not by arrival, not by OK) — to-do AND project; maintainer popover oracle cited (oddities §9n). **LATERLINK:** `show?id=tomorrow` VALID (opens Tomorrow view); `show?id=later-projects` INVALID (error modal) though `list id "later-projects"` is valid AppleScript — show-id vocab ≠ list-id vocab. **SPECLIST:** `every list` unchanged (P9a); all speculative doors (This Evening/Next Week/Deadlines/date-shaped/`list id "evening"`/`"deadlines"`) error −1728 for BOTH get and reorder specifiers → P9a CLOSED. **TOMORROWHEAD** (de-risks PR #335): the one-call `reorder to dos in list "Tomorrow"` is **heading-PRESERVING** — a headed child in the wire (repositioned OR tail-pinned) keeps its `heading` FK byte-identical (project stays NULL), so §9k (the container-day heading rip) does NOT extend to the Tomorrow list; the cross-container tomorrow fast path is sound for headed children. | [lab/sit3-arrival-evening-lists.md](../lab/sit3-arrival-evening-lists.md); `lab/scripts/research-sit3.sh` |
| SIT5 (AREAPROJDAY · CONVINST · LOGSWEEP · FUTPROJ) | **Area-direct day residual / repeating-instance convert / logbook×trash / future-project read — four arms** (`sit5-lab`, Things 3.22.11, DB v26, 2026-07-31; pinned 2026-07-05; arms 1/3/4 headless URL+AppleScript, CONVINST via the AXVM1 Accessibility grant + VNC confirm; host CLI reads with `THINGS_NOW`). **AREAPROJDAY** (closes the SIT4 DAYBNC #342 residual): AREA-DIRECT project rows DO join the `day` membership — `update-project?when=<D>` preserves the area FK, the dated bounce front-inserts an area'd project at the global day `todayIndex` min (below to-dos; PA −2082 < seed min −1725) area FK intact through both legs, and a scrambled 5-row bounce (PA mid) landed T2,PB,PA,T1,T3 ×2 → the conservative exclusion is LIFTED (one membership-predicate relaxation). **CONVINST:** GUI Items ▸ Convert to Project… on a repeating instance is a new-uuid identity REPLACEMENT that DELETES the old instance, mints a project row (new uuid + new creationDate, type 0→1) and CLEARS `rt1_repeatingTemplate` — template+siblings untouched, no crash; our reader stays clean (converted row = plain project no `instanceOf`/`repeating` lie; Show Latest → newest REMAINING true instance; upcoming/today exit 0). No headless convert surface (oddities §8m). **LOGSWEEP:** completed-not-trashed → Logbook only; completed-then-trashed AND trashed-then-completed (identical terminal `status=3/trashed=1`) → Trash only; `things logbook`=[LG1], `things trash`=[LG2,LG3] — exact SL2-L1 parity, no fix. New quirk: AS `delete to do id` fails −1728 on a COMPLETED to-do, `move … to list "Trash"` works (oddities §5n). **FUTPROJ:** future-scheduled project rows (area-less + area-direct) render in the Upcoming day-group interleaved with to-dos by `todayIndex` — GUI and `things upcoming` both order FPAL,T2,PB,PA,T1,T3 byte-for-byte; `projects --show-later` lists them. | [lab/sit5-areaproj-convinst-logsweep.md](../lab/sit5-areaproj-convinst-logsweep.md); `lab/scripts/research-sit5.sh` |
| UPCDL-1a/2/3/4/5/6/7/7b | **Deadline-forecast ordering axis + the hidden-list blind-writer question** (`upcdl-lab`, Things 3.22.11, 2026-08-03; pinned 2026-07-05; all arms headless). A same-deadline-day SOMEDAY row (`deadline` set, `startDate=NULL`) is a full ordering-axis member of its Upcoming block: it rests with a distinct non-zero `todayIndex` (front-inserted at creation, `tiRef=deadline`) AND a distinct `index`, opposite directions (UPCDL-1a, oddity §9o). **The maintainer's question — can `list "Today"` coerce their `todayIndex`? — answered: it IS a blind writer (accepts non-members, re-ranks `todayIndex`) but a HAZARD, not a lever** — it MATERIALIZES the rows into Today (`start 2→1`, `startDate→today`); `list "Tomorrow"`/`list "Upcoming"` likewise re-rank `todayIndex` but RE-DATE (to tomorrow / first-upcoming-day §9g); all three stamp a `startDate` that ejects the row from its forecast block, all heading-SAFE (oddity §9p). The only non-destructive reorders — `project id` (UPCDL-3) and the someday↔anytime bounce (UPCDL-7b) — re-rank **`index` only, leaving `todayIndex` byte-identical**; a plain `when=` update preserves both axes (UPCDL-7). The two-step rescue FAILS (UPCDL-7a: the `when=someday` restore re-front-inserts both axes). Net: no `startDate`-preserving `todayIndex` writer exists; the whole question collapses onto ONE GUI residual — whether the block renders forecast rows by `index` (clean lever exists) or `todayIndex` (none). NO wiring (probes only). **[Render-axis residual RESOLVED + a clean lever FOUND by DLBNC — see next row.]** | [lab/upcdl-deadline-axis.md](../lab/upcdl-deadline-axis.md); `lab/scripts/research-upcdl.sh` |
| DLBNC-1d/1abc/2/3/4/5/5b/6 | **Deadline-forecast day-block: GUI drag ground truth + the deadline-cycle reorder protocol** (`dlbnc-lab`, Things 3.22.11, 2026-08-03; pinned 2026-07-05; headless + `screencapture` GUI observe). **The render axis is RESOLVED = `todayIndex`** (maintainer-established; DLBNC-1d GUI-confirmed — a day block rendered top→bottom in ascending `todayIndex`, forecast + scheduled rows interleaved on one axis, opposite to `index`). **The `todayIndex` IS cleanly writable — the DEADLINE-CYCLE**: URL `deadline=` clear + `deadline=<same>` re-set front-inserts a forecast row at the block's global `todayIndex` min, so a reverse-target cycle lands the exact block order with `index`/`start=2`/`startDate=NULL`/`deadline`/`tiRef`/heading+project FK **byte-identical** (DLBNC-2/3; 2N public-URL legs, reminder-safe, heading-rip-safe — the `startDate`-preserving lever UPCDL found absent). A maintainer-proposed **compound** (when=-cycle to front-insert `todayIndex` + one `project id` reorder to repair `index`, DLBNC-6) also works but is strictly inferior (2N+1 legs, private reorder surface, index order-only-restored, when= legs reminder-lossy §9n/R07). **Supersedes** UPCDL-7 Q2 (single-row artifact — a multi-row `when=<date>`→`someday` round-trip front-inserts BOTH axes, DLBNC-5). **GUI drag** = maintainer-observed within-block re-rank (like recurring templates, on `todayIndex`); direct byte-capture is a synthetic-events-gated VNC residual (`osascript` −25211 assistive-access denied, no vncdotool). New oddity §9q (deadline-set is write-vector-dependent; AS can't clear a deadline); §9o hedge resolved. NO wiring (probes only — deadline-cycle is the wiring candidate pending ratification). | [lab/dlbnc-deadline-cycle.md](../lab/dlbnc-deadline-cycle.md); `lab/scripts/research-dlbnc.sh` |
| TMPLDL-1a…f, PROJDL-2a/2b/2c | **Can the deadline-cycle extend to repeating TEMPLATES and forecast PROJECT rows?** (`tmpldl-lab`, Things 3.22.11, 2026-08-03; pinned 2026-07-05; headless, ARM 1 PID-watched). **ARM 1 (TMPLDL) — DEAD via SILENT NO-OP:** `update?deadline=`/`update-project?deadline=` on a repeating to-do OR project template is a complete no-op — no crash, byte-identical row (deadline stays NULL, no `userModificationDate` bump, `rt1` untouched), and the deadline CLEAR is inert too; the instance-advance spawns a deadline-less instance as normal (no contamination vector, since nothing lands). So a template's deadline is unwritable on URL and the deadline-cycle cannot reach it — TMPLIDX extends to the deadline surface. Notably DISTINCT from the §1 `when=` CRASH: the deadline path is silently guarded on templates where the schedule path is not (new oddity §2i + a §1 contrast note). The maintainer-directed **1f reschedule-bounce mechanism** is BLOCKED-BY-VM-INPUT-GATING (projection is display-only → no auditable `todayIndex`; GUI reschedule needs Accessibility + vncdotool, both absent; AS `schedule`→302, `when=`→§1 crash) — flagged as a VNC/AX-grant residual, not guessed. (WITH-offset control fixtures need the GUI repeat editor / vncdotool, absent — compared against the UI1 sentinel reference instead; the no-op makes the control moot.) **ARM 2 (PROJDL) — WIREABLE:** forecast PROJECT rows (type=1, `start=2`, `startDate=NULL`, future deadline) ARE distinct members of the block `todayIndex` axis, interleaved with forecast to-dos (PROJDL-2a), and `update-project?deadline=` clear+re-set front-inserts them identically + cleanly (`index`/`start`/`startDate`/area-FK/tags byte-preserved, PROJSTAR-safe, 2 `umd`/leg; PROJDL-2b/2b'); a 3-row MIXED to-do+project protocol landed the exact block order with someday `index` byte-identical (PROJDL-2c). ARM 2 doubles as the ARM-1 control (proves `deadline=` works on ordinary rows in the same clone). Closes #384's forecast-PROJECT exclusion → the `move.ts` `type===0` gate can widen to `type IN (0,1)`. NO wiring (probes only — a maintainer-ratification follow-up PR). | [lab/tmpldl-projdl-deadline-cycle.md](../lab/tmpldl-projdl-deadline-cycle.md); `lab/scripts/research-tmpldl-projdl.sh` |
| TDRAG-1a…c, TDRAG-2 (2a/2b), TDRAG-3(b), TDRAG-4, TDRAG-5(b), TDRAG-6 | **The AX-dependent ordering residuals on golden-v2 — GUI drags + headless template `todayIndex` + ORD-18 + the §6 `.ips`** (`tdrag-lab`, **golden-v2 / Things 3.22.12**, 2026-08-03; pinned 2026-07-05; GUI input via **vncdotool** on the baked AXVM1 layer). **TDRAG-1 — a template projection drag writes the TEMPLATE row's `todayIndex` and NOTHING else** (full-DB diff = one row, one byte): front-insert at the block min when dropped at top, INTERPOLATED value mid-block; `rt1`/`tiRef`/`index`/`start`/`startDate`/**`umd`** byte-identical; PERSISTS across quit+relaunch (formalises + reconciles the maintainer's prod +5.8M observation — sign tracks the block's coordinate space). **TDRAG-3 — TMPLIDX OVERTURNED for one surface:** `_private_experimental_ reorder to dos in list "Upcoming" with ids "<template>"` cleanly FRONT-INSERT-writes the template `todayIndex` (clean, `umd`-silent, no reparent, no crash, no instance contamination); the `project id` specifier is a REPARENT HAZARD (`project` NULL→P + `umd` bump). Template-cell PROTOCOL CANDIDATE, NOT wired. **[The "front-insert-ONLY / mixed wire no-op" clause was a `with ids` LIST-literal `-1700` artifact — CORRECTED by TMPLSORT-2; see the TMPLSORT row below.]** **TDRAG-4 — the reschedule-bounce mechanism (TMPLDL-1f) DOES NOT EXIST:** `Items ▸ When…` on a template opens the repeat popover, not a date picker — no per-occurrence reschedule surface (resolves TMPLDL-1f from "VM-input-gated" to "mechanism absent"). **TDRAG-2 — the forecast within-block drag is `todayIndex`-only, no `startDate` stamp** (byte-faithful to the wired deadline-cycle, closes DLBNC-1b); the BETWEEN-block drag stamps `startDate`=destination day + updates `tiRef`, keeps the deadline, bumps `umd` (a reschedule, DLBNC-1c). **TDRAG-5 — ORD-18 RESOLVED byte-level:** a scheduled row CAN carry a distinct `index` (scheduling preserves it) and the dated `day` bounce leaves it byte-identical. **TDRAG-6 — the §6 `.ips` CAPTURED** (`EXC_BREAKPOINT`/`SIGTRAP`, heading byte-identical). Cross-cutting oddity §9r (native/GUI re-rank is `umd`-silent) + §9s (TMPLIDX surface-specific + reparent hazard). NO behavior code (any wiring is a ratification follow-up). | [lab/tdrag-ax-residuals.md](../lab/tdrag-ax-residuals.md); `lab/scripts/research-tdrag.sh` |
| TMPLSORT-1/2/3, rp-coerce/mixed/tt/ord18 | **The template-sort protocol + the `with ids` wire-syntax audit recovery** (`tmplsort-lab`, **golden-v2 / Things 3.22.12**; 2026-08-03 arms 1/2 + **2026-08-04** re-probe; pinned 2026-07-05; templates VNC-created, reorder wires PID-watched). **TMPLSORT-1 — templates-only sort PROVES OUT for repeating TO-DO templates** (reverse-target single-id `list "Upcoming"` front-insert → exact block order, byte-clean, contamination-free); **repeating PROJECT templates are INERT** (single-id reorder = genuine zero-delta no-op; no reorderable projection row). **TMPLSORT-2 — mixed scheduled+forecast+template interleave lands the exact target** (single-id template legs + URL bounce/cycle legs; the three families share one block `todayIndex` min-space). **THE WIRE-SYNTAX ARTIFACT (2026-08-04):** the multi-id reorder probes in `research-tdrag.sh`/`research-tmplsort.sh` used AppleScript LIST literals `with ids {"a","b","c"}` where the shipped op sends comma-joined TEXT `with ids "a,b,c"`; a MULTI-item list literal throws `-1700 Can't make {…} into type text` at the AppleEvent parameter boundary (the app never runs) and the harness swallowed the error → it masqueraded as a no-op (single-item `{"x"}` coerces cleanly = accidentally valid). **TMPLSORT-3 CORRECTED via re-probe (comma-text):** a VALID multi-id day-block wire treats a to-do template as a FIRST-CLASS member — `list "Tomorrow" with ids "TM2,TEMPLATE,TM1"` lands the EXACT sent order with the template at an arbitrary mid-slot; `list "Upcoming"` writes its `todayIndex` alongside the co-wired rows; a non-member template in a `list "Today"` wire is skipped while the valid members re-rank (`umd`-silent, no reparent, no crash) — overturning TDRAG-3-2's "front-insert-ONLY / mixed no-op / template poisons the wire". The `project id` reparent hazard STANDS (single-id, valid). **ORD-18/TDRAG-5 re-verified** — conclusion holds byte-identically; provenance corrected (distinct `index` from anytime-creation + a valid reorder, not the `-1700`-erroring list-literal). Corrects novel-paths #52, oddities §9s, register WG-2/ORD-18, capability-matrix. NO wiring (probes + docs only). | [lab/tmplsort-template-protocol.md](../lab/tmplsort-template-protocol.md); `lab/scripts/research-tmplsort.sh` |
| GRNDINT (to-do arm fwd/rev, native-tomorrow, proj-suffix accept/refuse, exp-off, rider) | **The grand-interleave o-suite certification (register ORD-19, PR 2) + the mixed-kind+template BUG + the reschedule-bounce two-fact rider** (`grndint-lab`, **golden-v2 / Things 3.22.12**, 2026-08-04; pinned 2026-07-05; all day-scope reorders driven through the SHIPPED CLI; rider via `todo reschedule-repeat --dangerously-drive-gui`; PID-watched). **CERTIFIES #393's template day-block wiring:** the three-mechanism GRAND INTERLEAVE (scheduled `when=` + forecast deadline-cycle + to-do-template `list "Upcoming"` front-insert) sorts in ONE `things reorder … --in <day>` op landing the exact target order AND its reverse (template leg `umd`-silent, `rt1`/`index` byte-preserved); the native `list "Tomorrow"` one-call wire places the template at its exact sent slot (`umd`-silent, all members); the project-template SUFFIX ACCEPT (byte-untouched) + REFUSE (`H-REORDER-SCOPE`) + experimental-off refusal (names the template, never a crash-leg). Locked as o-suite **O34–O37** (two green passes `o-20260804-191106`/`191443`, `lab:compare` identical/36) + e2e-write-smoke template CLI locks (130/0). **BUG (unpatched, real in #393):** a MIXED to-do+project day set that ALSO contains a template is refused UPSTREAM ("one kind at a time") — `globalAxisIntermix` ([move.ts](../../src/write/move.ts)) gates on scheduleBucket/forecastDeadlineDay and was not taught templates, so a template row fails the day-axis `.every()` and the mixed-kind relaxation never fires; single-kind arms unaffected. **RIDER (reschedule-bounce, two byte-verdicts):** (1) DOOR CLOSED — daily→weekly→daily does NOT restore `rt1_recurrenceRule` byte-identically, the next-occurrence anchor drifts 07-06→07-12 and does not return; (2) a previously-written `todayIndex` SURVIVES both reschedule legs byte-identically. | [lab/grndint-grand-interleave.md](../lab/grndint-grand-interleave.md); `lab/scripts/research-grndint.sh` |
| PTMPL-A1…A3, B1…B5, C (up-noop/subset/perm, tm-fwd/subset, rpmixed) | **Repeating PROJECT-template projections in Upcoming day blocks + the `list "Upcoming"` multi-id placement law** (`ptmpl-lab`, **golden-v2 / Things 3.22.12**, 2026-08-04; pinned 2026-07-05, re-pinned 2026-07-11 for the Tomorrow arm; VNC eyes-on + PID-watched comma-text reorder wires). Opened by the maintainer's correction that repeating PROJECT templates DO render inside Upcoming day blocks. **ARM A — CONFIRMED eyes-on + SUPERSEDES #391:** the project-template projection RENDERS as a first-class, selectable, drag-sortable `todayIndex` row inside its 07-12 day block AND the Later Projects view (screenshots); the GUI drag writes its `todayIndex` ALONE (0→−1585, `umd`-silent, `rt1`/`start`/`startDate`/`tiRef`/`index`/`area`/`project` byte-identical, no reparent), persists across relaunch; the sidebar shows only the active series ROOT, never the projection. **ARM B — surface-specific reach (overturns #391's blanket INERT):** single-id `list "Upcoming"`/`list "Today"`, `area id`, and `list "Later Projects"` are no-ops/skip on the project template, BUT `list "Tomorrow"` (projection day == tomorrow) WRITES it — single-id front-insert AND multi-id EXACT-slot member (`umd`-silent, no reparent, no crash, no instance contamination). `list "Upcoming"` multi-id SKIPS the template and §9g-re-dates off-day co-wired rows; `list "Later Projects"` (≡ `list id "later-projects"`, AS-only — URL `show` rejects it) writes `todayIndex` for ordinary later projects, §9m-re-dates date-less someday projects, and a template id POISONS the whole call. **ARM C — the `list "Upcoming"` PLACEMENT LAW (resolves RP-MIXED):** the wired ids front-insert as a group at the block top in wire order above the pre-existing-min ANCHOR (left byte-unchanged) → landed reverse-of-wire; non-wired untouched; no on-day re-date; `umd`-silent — this EXACTLY reproduces RP-MIXED (SB was the wired block-min → anchored; template<SA<SB). `list "Tomorrow"` implements the OPPOSITE law (EXACT sent order, min-writes, no re-date) — a one-call day-sort is possible only via `list "Tomorrow"`. NO wiring (probes + docs only). | [lab/ptmpl-project-templates.md](../lab/ptmpl-project-templates.md); `lab/scripts/research-ptmpl.sh` |
| TOMB1-a…e, ARM5, ARM7 | **`TMTombstone` lifecycle** (Things 3.22.11; airgapped control + throwaway-account single-clone + two-clone remote + the gate test; account burned). **A `TMTombstone` row is written iff the deleted row carried `leavesTombstone=1`** — a flag set ONLY on repeating-template lineage (template + spawned instances), NEVER on ordinary to-dos/projects/checklist items, and unchanged by attaching a Cloud account. The delete *path* is irrelevant (trash/empty-trash/area-delete/applied-remote-delete all behave identically for a given row). Proven: every ordinary (`leavesTombstone=0`) delete leaves NO tombstone with sync off (phase 0, extends A25/A27), sync on + local (phase 1), AND applied as a remote delete on a second synced clone (ARM5 — the row vanished from the observer tombstone-lessly; deletions propagate via the Syncrony change-log, not `TMTombstone`); while deleting a `leavesTombstone=1` repeating instance DID write one tombstone with a control `leavesTombstone=0` to-do in the same empty-trash writing none (ARM7). `deletionDate` = deleter's wall-clock epoch (trustworthy), but only for the narrow tombstoned set. **S-delperm confirmed sync-agnostic** (not a Shortcuts defect). **Verdict: `TMTombstone` is NOT a general deletion log → DON'T build `changes --since` deletion tracking on it.** | [lab/tomb1-results.md](../lab/tomb1-results.md); `lab/scripts/research-tomb1.sh` |
| TIMEZ (Z-SUB · Z-XDATE a–e · Z-TODAY · Z-ROLL a–c · Z-LOGVIEW) | **The evening substrate, cross-zone evening writes, and zone-shifted derivations** (`timez-lab`, **golden-v2 / Things 3.22.12**, 2026-08-05; base clock 2026-07-05 12:00 in TZ America/New_York = the clean UTC-noon anchor; `settimezone` for zone shifts + `sudo date` for day-rolls; headless + VNC framebuffer; no cloud account — cross-device MODELED). **THE CORE ANSWER — there is NO dated-evening vector (TIMEZ-NODATE):** `startBucket=1` (This-Evening) is inseparable from `startDate == the device's current local day` on EVERY surface. URL `when=evening` re-stamps to today; `when=<ISO-date>@evening` DISCARDS the date, evening-stamps today, and injects a spurious reminder = the date's YEAR misread as HH:MM (2026→20:26, 2027→20:27, 1945→19:45 — oddity §9w); `when=tomorrow@evening` lets the date win + DROPS the bucket (`start=2, sb=0`); AppleScript `schedule for <date>` sets `startDate` but `sb=0`, `activation date` is read-only, no bucket setter; no Evening list exists (`move`/`reorder … "This Evening"`/`"Evening"` → −1728); Shortcuts `set-detail Start` text→Anytime; every two-step is mutually exclusive (evening re-stamps to today / a date resets `sb:=0`). **Z-TODAY (TIMEZ-CLOCK):** `when=today` stamps the GUEST device's local calendar day (Kiritimati UTC+14 → `startDate=07-06`) — the cross-device bridge. **Z-ROLL:** an overdue evening (viewer date > startDate) rolls into flat Today with no This-Evening section (a); a future evening (viewer date < startDate) stays in Today/Anytime a day early, NOT Upcoming, no moon (b); pure `settimezone` = zero DB delta and a rollover relaunch mutates only one opaque `TMMetaItem` day-cursor BLOB, ZERO `TMTask` rows — the derivation is PURE (c). **Z-LOGVIEW:** Logbook day-grouping is VIEWER-LOCAL render-time (same `stopDate 07-05 16:00Z` groups under Today in NY=Jul5 and Kiritimati=Jul6; bytes byte-identical). Feeds a planned `docs/reference/timezones.md`. NO wiring (probes + docs only). | [lab/timez-evening-and-zones.md](../lab/timez-evening-and-zones.md); `lab/scripts/research-timez.sh` |
| TIMEZ2 (T2-ENV · T2-EVE · T2-SIDEFX · T2-DEDUPE · T2-REVERSE) | **The pinned-zone relaunch workaround for the dated-evening gap + its side-effect bill** (`timez2-lab`, **golden-v2 / Things 3.22.12**, 2026-08-05; base clock 2026-07-05 at the 12:00Z instant in TZ America/New_York; per-process `TZ` env for zone pins + `settimezone` control + `sudo date` day-rolls; headless byte + full `.dump` diffs; no cloud account — two-device MODELED, SYNC2). **The workaround WORKS and is operationalizable:** quit → `TZ=<ahead-zone> open -a Things3` → `when=evening` → quit → relaunch. **T2-ENV:** `TZ=<zone> open -a Things3` (and direct-exec of the binary) **pin the app's effective zone per-process WITHOUT changing the system zone**; `launchctl setenv TZ` does NOT (ssh-session launchd domain ≠ Aqua); precondition = quit first (already-running app ignores the env). **T2-EVE:** under the ahead-pinned app `when=evening` lands the otherwise-impossible **dated-evening row** (`sb=1, startDate=shifted-day`), byte-identical after the reset (pure), rendering in the un-shifted viewer's flat Today/Anytime a day early (Z-ROLL-b), NOT Upcoming. **T2-SIDEFX:** the shifted-forward launch's ENTIRE bill = the 1 `TMMetaItem` day-cursor blob + the repeat engine materializing any template due on the shifted day (new instance + `next`-cursor advance + counter, all `umd`-silent); NO `start 2→1` promotions, NO reminder writes, NO user-row re-ranks; the reset steps only the cursor blob back — **the early materialization persists** (oddity §9x). **T2-DEDUPE:** cursor-keyed DEDUPE — the real clock later reaching that day is a byte-identical no-op; the backward cursor step is tolerated with zero data effect. **T2-REVERSE:** `when=today` under a behind zone stamps the behind local day; the backward launch is inert (only the cursor blob steps back, no (de)materialization). NO wiring (probes + docs only). | [lab/timez2-pinned-zone-workaround.md](../lab/timez2-pinned-zone-workaround.md); `lab/scripts/research-timez2.sh` |
| RESID1 (R-AXRETRY · R-JSONPAR · R-DAILYMAN · R-RESTAGE) | **Four batched queued residuals** (`resid1-lab`, **golden-v2 / Things 3.22.12**, 2026-08-06; pinned 2026-07-05, leg 2 advances to 2026-07-09; guest CLI + AppleScript oracles; no crash). **R-AXRETRY:** the BACKDT Settings-window AX flake is **clone/state-local, NOT a golden-v2 residual** — the HEADSORT `⌘,` recipe opens `window "General"` fine; the failure is stale-window state, cleared by a **quit+relaunch** (opens on attempt 1). Log-interval popup = `AXPopUpButton` #3 (a 2nd unrelated `"Daily"` popup exists → target by index); `things:///preferences` has no URL route. **R-JSONPAR:** the timestamped-`add` json path has **full attribute parity EXCEPT the checklist** — dates byte-exact (bare date → local NOON), tags, project/area `list-id`, and heading placement all land live; but `--created-at`/`--completed-at` + `--checklist-item` **silently no-ops the whole import** (engine emits `checklist-items` as a bare STRING array, commands.ts:446, which `things:///json` rejects wholesale — even a plain open to-do; the OBJECT-array shape at commands.ts:838 works — OBJ3 landed everything). CLI verify catches it as `verify-failed:silent-noop`; **one-line fix flagged** (evidence-only). New oddity §9y. **R-DAILYMAN:** the Daily→Manually flip does **NOT** forward-sweep — the [UNPROBED] TIMEZ prediction is **FALSIFIED**; the `manualLogDate` stamp fires only on *leaving Immediately*, and 1→4 carries it forward byte-unchanged (pending window preserved). timezones.md §1 corner upgraded [UNPROBED]→[CERTIFIED]. **R-RESTAGE:** a swept DATED item whose date passes while swept **re-derives `when` to the current clock on reactivation, landing in TODAY** — (a) future-scheduled → arrived-Today, `start 2→1`, past `startDate` retained; (b) someday+overdue-deadline → Today, app **stamps `startDate := the deadline**`, `start 2→1`; (c) L-RESTORE (index-silent/heading-retained) still holds. Register RD-13; new oddity §9z. NO wiring (probes + docs only). | [lab/resid1-batched-residuals.md](../lab/resid1-batched-residuals.md); `lab/scripts/research-resid1.sh` |
| MOVPLC (steps 1–7) | **The `todo move` placement-leg Today rewrite** (`movplc-lab`, **golden-v2 / Things 3.22.12**, 2026-08-11; pinned 2026-07-05, rolled 07-06→07-07 to manufacture 5 `tiRef` cohorts; guest URL/AppleScript + AX log-interval flip; no crash). The disposable-VM reproduction the field audit ([research/today-canceled-grouping-audit.md](../research/today-canceled-grouping-audit.md)) required. A container-only `todo move` with NO explicit position auto-fires a native `_private_experimental_ reorder to dos in list "Today"` over ALL open Today members (`computeReorderPre` `today` census: movee first, then open bucket-0 by raw `todayIndex ASC`). **The write set (the audit's open question): the native `list "Today"` reorder writes `todayIndex` (to the wire order) AND re-stamps `todayIndexReferenceDate → today` on EVERY named row, leaving `startDate`/`start`/`startBucket`/`status`/`stopDate`/`userModificationDate` byte-identical** — the `tiRef` collapse fuses every entry-date cohort into one "today" cohort, so the visible order becomes pure wire order (the whole-view rewrite from a one-item move). This is the **O03/UPCDL-2a materialize law confirmed on EXISTING multi-cohort members** (§9p addendum). Controls: cancellation alone reorders nothing (step 2); a raw membership move — the exact CLI `update?list-id=` leg — does NOT reorder Today, the movee keeps its slot (step 3), so the placement leg is the sole cause. Excluded classes byte-untouched: unswept **canceled** (status≠0) and stale **evening** (`startBucket=1`, O03) rows keep old `tiRef` and sink below the collapsed block (the "regrouping" — a by-product, not a rule). The `today` census **LEAKS derived-trashed children** (own `trashed=0`, project `trashed=1`) into the wire and the app blindly writes them (§9p blind-writer law), though the reader still hides them (OUR census bug — `computeReorderPre` filters own-row `trashed` only). Fix (evidence-gated): no-position container move skips the today/evening placement reorder; `computeReorderPre` gains the reader's `CONTAINER_UNTRASHED` exclusion. | [lab/movplc-move-placement-today.md](../lab/movplc-move-placement-today.md); `lab/scripts/research-movplc.sh` |
| TODWIRE (EXP1–4 + cross-cohort drag) | **Partial-wire laws on the native `list "Today"` reorder — the MOVPLC sequel** (`todwire-lab`, **golden-v2 / Things 3.22.12**, 2026-08-11; pinned 2026-07-05, rolled 07-06→07-07 for 5 `tiRef` cohorts; guest URL/AppleScript + vncdotool drag; no crash). Characterizes PARTIAL wires so the engine can compile the SMALLEST id list realizing a placement while preserving observable order. **The today axis obeys the HEADSORT partial-wire law**: a subset wire re-ranks to `[named ids in wire order] ++ [unnamed rows in prior VISIBLE order]`, touching ONLY named rows (all other cohorts + unswept-canceled + stale-evening bystanders byte-identical). **EXP1 single-ID** = clean front-insert to the VISIBLE TOP (movee alone: `tiRef→today`, `todayIndex→` new min, `umd`-silent). **EXP2 prefix** = named block clusters at the top in SENT order, unnamed interleave fully preserved. **EXP3 mid-list anchor** (place C7c after C6c) requires naming the whole visible PREFIX through the anchor + movee (9 ids) — the tightest wire the primitive allows (it can only RAISE `tiRef` to today), fusing only that prefix; unnamed tail untouched. **EXP4 re-stamp scope** = UNCONDITIONAL: re-firing an already-placed wire rewrites every named row's `todayIndex` into a fresh min-space + re-stamps `tiRef→today` (not a byte no-op), `umd`-silent. So the fix = `minimalReorderWire(visibleTodayOrder, targetOrder)` + a non-movee re-stamp-count disclosure (register ORD-22). **GUI cross-cohort drag (addendum, read-side):** an ordinary Today drag across a cohort boundary ADOPTS the drop neighbour's cohort — rewrites the dragged row's `tiRef` := the destination band's date (older OR newer), a SINGLE-ROW write, interpolates `todayIndex` between the drop neighbours, `startDate`/`umd`/all-other-rows untouched — so `tiRef` is NOT a faithful "entered Today" date (bounds any "time in Today" field; corroborates scrapping `todaySince`). New oddities §9p TODWIRE addendum + §9y. | [lab/todwire-partial-wires-today.md](../lab/todwire-partial-wires-today.md); `lab/scripts/research-todwire.sh` |

| CLONE (A1–A8 · B1–B4 · C1–C5) | **Clone-fidelity matrix + minted-clone promote-fate + template-trash semantics** (`clone-lab`, **golden-v2 / Things 3.22.12**, pinned 2026-07-05; guest CLI + json/URL/AS + ui-vector make-repeating; no crash). Gates the ratified promote-via-clone direction. **A (fidelity):** every content field reproduces via official write surfaces (title/notes/tags/all `when` stages incl. evening `sb=1`/reminder/deadline/checklist-titles/`creationDate` via `--created-at` at minute res/logged+canceled terminal state via `--completed-at` exact/area/headings/children); CAVEAT set needs a follow-up leg (pre-checked checklist item → post-`--check`; logged children in a MIXED project → post-`complete/cancel` per §5b; trashed clone → post-`delete`); the ONE **UNCLONABLE** field is a nested repeating template inside a project (`rt1_recurrenceRule` settable only via the make-repeating GUI, which mints a new series identity) — `project clone` must refuse-or-flag. **B (fate on minted rows):** RSIM-R/T/U + RSIM1 source-fate hold BYTE-FOR-BYTE on backdated clone rows (bare/open-children-project → DELETE; deadline-todo/all-terminal-project → PRESERVE-as-instance) — backdating does NOT perturb the fate axis; shipped `templateUuid` binds after each drive. **C (template-trash undo story):** shipped `todo.delete` REFUSES a template (`H-REPEAT-SCHEDULE`, fail-closed) but `project.delete` does NOT (kind-asymmetry GAP, register WG-8) — trashing a project template RETAINS the rule, CLEARS the fixed `nextInstanceStartDate` cursor, ORPHANS the live instance (not cascaded), and is NON-restorable via AS move (error 301, extends oddity §8k); trashing only a fixed INSTANCE is clean + template-preserving (`start 2→1`+`trashed=1`, series survives). Confirms RSIM-R/T/U + §8k under golden-v2/3.22.12. New oddity §8k extension; register WG-8. | [lab/clone-fidelity-and-template-trash.md](../lab/clone-fidelity-and-template-trash.md); `lab/scripts/research-clone.sh` |
| TRSHREP (R1–R9) | **Repeating-template spawn/pause/catch-up while trashed + restore/relocation/empty-trash/mixed-promote/checklist-carry** (`trshrep-lab`, **golden-v2 / Things 3.22.12**, pinned 2026-07-05 advanced +1/+2-day to 07-09; guest CLI + json/URL/AS + ui-vector make-repeating; no crash/reboot). **R1 (headline):** container-trash is NOT a repeat pause — a fixed repeater whose PROJECT is trashed keeps its cursor (contrast CLONE C3 direct template-trash which clears it) and keeps SPAWNING new occurrences INTO the trashed project on schedule, catching up one per elapsed period on a multi-day advance (4 instances 07-05→08). **R2:** `project restore` materializes NOTHING — the back-occurrences already accumulated in the Trash, so restore floods all N at once and the cursor does not re-anchor. **R3:** shipped `todo move` REFUSES to relocate a template (H-REPEAT-SCHEDULE covers move) — no CLI restore-by-relocation. **R4:** after-completion is clock-dormant while trashed, but completing its instance (url-scheme, unguarded) spawns the successor INTO the trash on the next tick. **R5 (closes CLONE residual):** raw AS `delete to do id <template>` trashes a TO-DO template with the SAME broken shape as the project template (cursor cleared, instance orphaned); `todo restore` also guard-refused. **R6:** `CONTAINER_UNTRASHED` reader is honest (hides in-trash template+occurrences, reveals on restore, no leak). **R7 (oddity §6⅘):** `empty trash` CASCADES through the trashed container and hard-deletes its derived-trashed COMPLETED/CANCELED children (own `trashed=0`) with NO tombstone — silently destroying logged history. **R8 (resolves the RSIM-U unisolated cell):** a fixed `project make-repeating` on a MIXED open+completed project PRESERVES the source (2/2) — so the completed history rides the preserved instance and is NOT destroyed by the promote. **R9:** a spawned occurrence's checklist items are born all-UNCHECKED regardless of the template's checked-state (extends RSIM-S pristine-occurrence to `TMChecklistItem`). Flag: R8/R9a/R9b preserve-cases contradict golden-v1 RSIM-T/RSIM-U boundaries (a checked checklist item / a terminal child appears to trigger source-preserve) — reconciliation re-run recommended, not folded. New RD-14; WG-8 extended (move/restore guarded, to-do-template raw-trash residual closed); oddities §6⅘ + §8t. | [lab/trshrep-trashed-repeater-spawn.md](../lab/trshrep-trashed-repeater-spawn.md); `lab/scripts/research-trshrep.sh` |
| SRCFATE (SF · CVT · PHC · UMD · RSTG · MISC) | **Consolidated single-VM sweep: source-fate reconciliation + Convert/promote creationDate + umd micro-cells + RESTAGE + future-project rep** (`srcfate{,2,3}-lab`, **golden-v2 / Things 3.22.12**, pinned 2026-07-05; three clones; native make-repeating driven via `things batch` since the standalone CLI now wraps promote-via-clone). **SF (resolves the TRSHREP R8/R9 flag):** the terminal-element preserve hypothesis is CONFIRMED deterministically (14 cells, 2/2) → **a fixed make-repeating PRESERVES its source iff the subtree holds a TERMINAL element — a completed/canceled child OR a checked (completed) checklist item anywhere — OR (to-do) a deadline OR (project) a nested repeater; else DELETES** (checked-checklist to-do PRESERVE vs unchecked/bare DELETE; completed/canceled-child + open-child-with-checked-checklist project PRESERVE vs bare-open-child DELETE). Generalizes RSIM-U's terminal-*child*; **no v1→v2 drift** (RSIM-T's checklist cell was *unchecked*). `replacedUuid` (null=preserve / uuid=delete) is the authoritative signal. **CVT:** plain Convert-to-Project = identity replacement (old to-do deleted, new project uuid), `creationDate`=conversion wall-clock, notes preserved, **checklist items promoted to child to-dos**. **PHC:** `promote-heading` new-project `creationDate`=conversion wall-clock (in-window); child reparent BUMPs umd. **UMD:** granular checklist add/check/uncheck/rename/delete are umd-SILENT on the owning to-do (a `things watch` hazard); reminder set/clear + title + notes BUMP. **RSTG:** reopening a logged PAST-dated to-do restores its ORIGINAL date (when/index-silent; surfaces in Today as overdue) — NOT re-derived. **MISC:** future-scheduled PROJECT = `start=2`+future `startDate`. Correction banner in rsim-results §RSIM-R; register RD-24; promote-via-clone note refined; craft §2d/§2e. **Two findings → up-next:** `project dissolve-heading`/`move-heading-to-project` are CLI-uninvokable (missing `addDriveGuiFlag`) and their ellipsis-menu drive doesn't resolve the More-button headlessly (those two umd cells stay UNMEASURED). | [lab/srcfate-reconciliation-sweep.md](../lab/srcfate-reconciliation-sweep.md); `lab/scripts/research-srcfate{,2,3}.sh` |
| ANCH1 (P0 · A1–A5) + ANCH1-B (FIX1–4) | **Fixed-recurrence ANCHOR law + phase-correction viability + the issue #476 fix re-cert** (`anch1{,b}-lab`, **golden-v2 / Things 3.22.12**, pinned 2026-07-05 Sunday, +1-day steps; guest CLI, DB oracle). **A2 (5/5):** a fixed rule anchors its first occurrence to the next calendar match ≥ TODAY and IGNORES the item's scheduled `when` (`when` ∈ {today, aligned Wed, misaligned Thu, someday} → all `ia`=cursor=07-08); INTERVAL-INDEPENDENT (interval 1 == interval 2). **A3 (3/3 incl. a Monday probe):** weekday-less weekly defaults to SUNDAY (`of=[{wd:0}]`), constant — not today's/source's weekday. **A5:** no phase-correction vector (AS `schedule` template → 302; instance-move leaves the cursor; reschedule re-anchors to the cursor) → fix is fail-closed refusal, not a correction leg. **P0:** the issue repro reproduces; `--ends-on <date>` additionally collapses ia/sr/cursor to the ends date on 3.22.12 (a version-specific anomaly, NOT on 3.22.13 — oddity §8v). **ANCH1-B re-cert:** the wrong-phase repro now REFUSES `H-REPEAT-ANCHOR` (zero mutation, make+add); an on-phase `--when` SUCCEEDS (first occ = requested, +14 cadence); weekday DERIVED (of=[{wd:3}], not Sunday); after-completion + BOTH `--ends-after` (drive-fails, exit 3) and `--ends-on` (refused pre-mutation) can't set an end bound → the fix refuses any non-never ends under afterCompletion. Fix landed `mg/476-repeat-anchor`. **NOTE: ANCH1's "no first-occurrence control / fail-closed refusal" conclusion was CORRECTED by ANCH2 (row below) — its census failed; the control exists.** | [lab/anch1-repeat-anchor.md](../lab/anch1-repeat-anchor.md); `lab/scripts/research-anch1{,b}.sh` |
| ANCH2 (census · write-test · A2 cells a–e · A4 §8v · A5 reminder + RC1–RC5) | **The Repeat dialog's "Next:" first-occurrence field — census, drive, semantics, §8v + reminder disambiguation, fix re-cert** (`anch2-lab`, **golden-v2 / Things 3.22.12**, pinned 2026-07-05 Sunday; live AX census + guest CLI, DB oracle — corrects ANCH1 #476). **Census:** the fixed-weekly Repeat sheet HAS a "Next:" `AXDateTimeArea` (ANCH1's census failed → UIC6-field-map fallback missed it); Ends=never→1 date area, Ends=on-date→2 (Ends `@y360`, Next `@y338`), +reminder→3 (reminder `@y408`, the only time-bearing). **Write test:** Next honors AX writes (typed 07-22 → `ia=sr=next=icStart=07-22`). **Semantics (a–e):** aligned Next → first occ verbatim + cadence; off-rule Monday Next weekly/Sunday → Monday verbatim then Sundays; monthly/2 + yearly/2 Next honored (residual CLOSED); Next + ends-on distinct (no collapse); reschedule driving Next corrects phase post-creation. **§8v:** the `--ends-on` collapse is OUR first-by-role targeting defect (deterministic targeting is clean), NOT an app anomaly — retracted. **Reminder:** honored with deterministic targeting (09:00→`9<<26`, 14:30→`14<<26\|30<<20`) — UIC6-g retracted. **Re-cert (RC1–RC5, fixed dist):** the #476 repro (when=08-26 weekly/2/wed) yields first occ=08-26 (make+add); off-rule, monthly/2, Next+ends-on, and `--reminder 18:00` all pass. Fix landed `mg/476-anchor-next`. | [lab/anch2-next-field.md](../lab/anch2-next-field.md); `lab/scripts/anch2-{setup,helpers,ax,recert}.sh` |
| RRX1 (EA/EB/EO/EP · RW/RC/RD · clear-surface table) | **Repeat-rule count exhaustion + reminder storage** (`rrx1-lab`, **golden-v2 / Things 3.22.12**, pinned 2026-07-05 Sunday, advanced +1 day/step to 07-09; shipped CLI, read-only DB oracle). **Q1:** `--ends-after N` writes `rc=N` + omits `ed`; `rc` is the IMMUTABLE configured total (never decrements — held at 3 through all 3 spawns + 2 days past, and completing every occurrence moved it identically); the app counts spawns in `rt1_instanceCreationCount` and ENDS by clearing the cursor `rt1_nextInstanceStartDate → NULL` at `icCount==rc`. `--ends-on D` spawns through+including D then clears the cursor; a PAST ends-on is born already ended (cursor NULL, icCount=0, zero instances). Exhaustion = a fixed template with a NULL cursor → the read model's `templateStatus` now reads exhaustion from the cursor (the dead `remainingCount===0` branch removed; field renamed `occurrenceCount`). **Q2:** a rule-level reminder (`make-repeating --reminder`) IS stored in the `reminderTime` COLUMN on the template AND every spawned instance (current + later spawns), NO blob time key — falsifies RCLEAR's 3.22.11 NULL claim (oddities §8b corrected). No surface clears a TEMPLATE's rule reminder in place (Shortcuts no-op / AS 301 / URL crash) → `todo.clear-dated-reminder` refuses a repeating template (WG-10; was masked by H-NO-REMINDER while the column was NULL). `add-repeating --reminder` = base item reminder, DROPPED on promote (REM1). Fix + re-cert on `mg/rrx1-rc-reminder`. | [lab/rrx1-rc-and-reminder.md](../lab/rrx1-rc-and-reminder.md); `lab/scripts/rrx1-{setup,create,create-rc,advance,clear,clearop,recert}.sh` |

| APPRUN1 (measure · cells a–d) | **Closed-app launch-readiness law + auto-launch certification** (`apprun1-lab`, **golden-v3 / Things 3.22.14**, pinned 2026-07-05; guest Python one-clock measurement + production CLI; issue #486). **The startup URL-drop window did NOT reproduce in a clean airgapped clone** — the first raw complete-URL, fired ~0.031 s after `open -g -a Things3`, LANDED; 0/15 targets dropped (same "does not reproduce on clean 3.22.14" pattern as #479/#480; the reported failure survived the pre-fix 2 s settle, so the real-host window is >2 s or not purely time-based — a loaded prod DB / active sync the lab does not model). **Signal correlation:** process (`pgrep`), `lsappinfo` (StatusLabel `[ NULL ]` → `{label=8}`, never "Not Finished Launching"), and AS `count of areas` all trip at ~0.031 s (instant/murky); the **WAL mtime advanced at ~0.907 s** — the one signal that reflects the app actually writing post-launch. Shipped readiness = the consent-free WAL-advance floor (a `stat`, no Apple Event → no Automation-consent drag onto the URL path). **Cell A (auto-launch default):** `todo complete` on a quit app → `ok`, auto-launched, landed, `warnings:["Things was not running, so it was launched in the background for this write"]`. **Cell B (auto-launch off):** → `blocked:environment` exit 4, `likelyCause:app-not-running`, zero dispatch, app stayed quit. **Cell D (foreground):** frontmost read `Things3` but confounded by the headless single-app session — `open -g` is the shipped least-disruptive launch; on-hardware backgroundedness pending. | [lab/apprun1-launch-readiness.md](../lab/apprun1-launch-readiness.md); `lab/scripts/research-apprun1.sh` + `lab/scripts/apprun1-measure.py` |
| RRD1 (cells a–d) | **Repeat-dialog checkbox convergence — reschedule on a PRE-POPULATED dialog** (`rrd1-lab`, **golden-v3 / Things 3.22.14**, pinned 2026-07-05; production CLI `--dangerously-drive-gui`, DB oracle). Fixes the live bug where a `reschedule-repeat` on an already-deadlined series BLIND-pressed "Add deadlines" — the dialog opens PRE-POPULATED with the box ticked, so the press UNCHECKED it, hid the "start N days earlier" field, and collapsed the drive. The checkboxes now converge via a deterministic closed-loop `ensure-checkbox` step (read → press-only-on-mismatch → confirm), deadline converged BEFORE "Next:" (deadline mode makes "Next:" the deadline date). **Cell A:** the exact repro (`--on-weekday tuesday --on-ordinal 4 --when 2026-09-01 --deadline --start-days-earlier 21`) now DRIVES — `Add deadlines` (no-op converge of the already-checked box) → `start 21 days earlier` → `Next=2026-09-22` → OK — landing `of=[{wd:2,wdo:4}]`/`ts=-21`/`deadline=4001-01-01`/`next=2026-09-01`. **Cell B:** reschedule NO `--deadline` (interval→2) emits NO deadline step; `deadline`+`ts=-21` PRESERVED. **Cell C:** reschedule NO `--reminder` (interval→2) emits NO reminder step; template `reminderTime=603979776` (09:00) PRESERVED. **Cell D:** re-run of A's command → zero-drive `ok` idempotent no-op (37 ms). **Selectability (YANCH1 residual closed):** the template drove fine with `instanceUuid=null`/`icCount=0` — the reschedule reveal binds the template row directly, no materialized instance needed. **Audit:** the weekly multi-weekday "+" add-row logic is not idempotent on a pre-populated multi-weekday rule (out of scope; folds into the RRF1 breadth cell). | [lab/rrd1-checkbox-converge.md](../lab/rrd1-checkbox-converge.md); `lab/scripts/rrd1-checkbox.sh` |
| RSPA1 (setup+materialize · cells a/b/c · spawn) | **Reschedule a deadlined yearly template that HAS a pending materialized instance** (`rspa1-lab`/`rspa1-spawn`, **golden-v3 / Things 3.22.14**, pinned 2026-07-05 → +1 day materialize → 2028-10-16 spawn; production CLI `--dangerously-drive-gui`, DB oracle — the residual YANCH1/DACON1/DBLSPAWN1 all deferred). Create a deadlined yearly series first-occ-tomorrow (fixed `add-repeating`, deadline→rule), advance +1 to materialize the current-occurrence instance, then reschedule. **(a)** on-rule `--when`-only + deadline COMMITS in ~7s with the d1 anchor-derivation fix — derived DUE anchor `of=[{dy=29,mo=9}]` (Oct 30), start back-shifts to `--when` (`sr=2028-10-16`), `ts=-14`, cursor `next=2028-10-16`, pending instance untouched (the pre-fix anchorless drive was the live 120s zero-movement discard). **(b)** off-rule Next (explicit anchor ≠ `--when`) HONORED (`ok` ~6s, anchor `m10d16`), cursor `next=2029-10-02` = rule-correct next aligned start (the DACON1 live "2029 skip", normal-by-design) — NOT discarded/snapped. **(c)** rule-only reschedule preserves anchor/deadline/cursor, instance untouched (RRD1). **Spawn:** re-anchored cursor never double-books the materialized occurrence (`icCount=1`, distinct future cursor — opposite of the DBLSPAWN `icCount=0` collision). **Verdict:** no validation refusal warranted; the pending-instance zero-movement-discard hypothesis FALSIFIED. Residual RSPA1-D: off-rule disclosure blind to a PRESERVED deadline (copy-only). | [lab/rspa1-reschedule-pending.md](../lab/rspa1-reschedule-pending.md); `lab/scripts/rspa1-pending.sh`, `lab/scripts/rspa1-spawn.sh` |
| VMQ1 (§§1–6, VM-batch closeout) | **VM-batchable probe-queue closeout** (`vmq1-*`, **golden-v3 / Things 3.22.14**, pinned 2026-07-05; production CLI `--dangerously-drive-gui`, DB oracle). **§1 lone-Next inertness — FALSIFIED:** a DAILY lone-Next reschedule (no anchor pop-ups, Next the SOLE change) COMMITS, as do yearly same-anchor and deadlined-yearly no-op cells — the live 2026-08-19 zero-movement failure predated the #503 anchor-derivation fix and its drive shape is now unreachable. **§2 RRF1 breadth GREEN** (weekday-SET `{mon}→{tue,thu}`=`w2,w4`; ends `after 5→on 2027-12-31`), **RRD1 blind-"+" CONFIRMED BROKEN** (2Tb: pre `{mon,wed}`→`{tue,thu,sat}` landed `{mon,tue,thu,sat}` — stale row, caught fail-closed `verify-failed:mismatch`; app stores weekdays as a dedup SET). **§3 after-completion has NO Ends control at any index** (census: only cadence-unit + frequency pop-ups) → the #476 refusal is PERMANENT. **§4 RSPA1-D disclosure fix** (preserved-deadline appear/due) unit-tested; live confirm blocked by a §1 Next-snap. **§5 SESSGATE (b):** modal-block CONFIRMED live in-lab (open sheet blocks AS delete, `trashed=0`, lock-independent, via the shell-`open` reveal fix); the AX-blind-sheet close+reopen recovery NOT reproducible headlessly. **§6 template-clone (a)/(b)/(f) CERTIFIED** (exact rule bytes, source untouched, new-series warning, UIC8 future-dated 0-instance spawn, undo trash-both); (c) not live-reachable, (d)/(e) residual. | [lab/vmq1-probe-closeout.md](../lab/vmq1-probe-closeout.md); `lab/scripts/vmq1-{diag,validate,sessgate,clone}.sh` |
| DBV27 (migration diff) | **The Things 3.23 database migration, measured on the live host** (2026-08-22; pre = Things' own same-day daily backup, dbv 26; post = live, dbv 27; read-only, counts only). DDL delta is INDEX-ONLY (new partial template index; repeatingTemplate index gains `creationDate`) — fingerprint correctly unchanged, v27 baseline shipped (#518). Data rewrites as first read (nextInstanceStartDate nulled, counter shift, forward cursors) — SEE THE 2026-08-22 CORRECTION postscript (and GV4 below): the column is NOT retired (non-template rows only; templates keep it), the counter move is a -1-sentinel cleanup, the cursor moves are a catch-up-to-now; the #520/#522 projection-day work fixes a PRE-EXISTING gap. Full 3.23 re-cert campaign queued in up-next. | [lab/dbv27-migration-diff.md](../lab/dbv27-migration-diff.md) |

| GV4 (golden-v4 mint + 3.23 sweep) | **Minting `things-lab-golden-v4` (Things 3.23 / DB 27) and the first regression sweep against it** (2026-08-22, unattended; DRIFT-1 in-place swap from a golden-v3 clone — a from-L0 rebuild was measured impossible, 23.7 GB of uncached base layers against 31 GB free, and would have carried none of the human-seeded layers). Structural gates PASS: sdef byte-identical, fingerprint unchanged, dbv 26→27 migrated live, trial clock not reset, every inherited TCC/Shortcuts/LaunchAgent layer re-verified. **Re-measures DBV27 in-lab and CORRECTS it twice:** `rt1_nextInstanceStartDate` is **not retired** — the migration nulled only NON-template rows and left every template's cached value byte-identical (confirmed read-only on the live host: 0/21,962 non-templates vs **73/114 templates** still carrying one; the 41 template NULLs predate the migration), so **the migration did not break the projection day** and the queued engine-break framing is wrong; and the counter move is **`-1` sentinel → computed `0`**, not a leaf self-count. **Sweep deltas vs golden-v3 (which was green):** `_private_experimental_ reorder` is now a **SILENT NO-OP** (exit 0, zero row delta, 14 o-suite failures — and the sdef canary cannot see it, the command is still declared); AppleScript `make new to do at beginning of list "Today"` no longer applies the list's scheduling (A01B); a first-touch `window-new "Today"` with no launch/activate flips A10 + R01 to tier 3. u/x/e green; U12 + R09 crashes still reproduce. NOT certified — nothing reconciled. | [lab/gv4-323-campaign.md](../lab/gv4-323-campaign.md), [lab/golden-v4-metadata.json](../lab/golden-v4-metadata.json); `lab/scripts/golden-inplace-swap.sh`, `lab/scripts/research-ord323.sh` |
| RDLG1 / RDLG1b (3.23 repeat-surface census) | **AX census of the REDESIGNED Things 3.23 repeat surface** (golden-v4 / Things 3.23, pinned 2026-07-05; the sdef is byte-identical, so every 3.23 repeat feature is AX-only). Dialog shell UNCHANGED (attached `AXSheet` / detached `AXUnknown`), and `DIALOG_FREQUENCY`/`INTERVAL`/`ENDS`/`AC_UNIT`/`ADD_WEEKDAY`/`OK`/both checkboxes still resolve — the plain `make-repeating --frequency daily --interval 1` GUI drive lands end-to-end under 3.23. **BROKEN:** a NEW `Next:` **pop-up** (`Today` + the next 14 on-rule occurrences + `More…`) sits between Ends and every per-frequency control, shifting `DIALOG_WEEKDAY`/`MONTH_*`/`YEAR_*` by **+1**, and replacing the first-occurrence `AXDateTimeArea` that `setDateTime(target:"next")` drives; `Items ▸ Repeat ▸ Reschedule…` is **renamed `Edit Rule…`** (`-1728`, `rescheduleRepeatRecipe` dead). New surface: `Create Next Copy`, `Show Previous Copy`, `File ▸ New Repeating To-Do`, `View ▸ Go To ▸ Repeating`; instance rows now carry an ordinary `Checkbox Regular`; the dialog renders an occurrence-preview static text usable as a read-back oracle. OPEN: `Next: ▸ More…`, and the Make Exception / Update Rule chooser (NOT reproduced — neither the When-picker commit nor `Shortcuts ▸ Someday` surfaced it). | [lab/rdlg1-323-repeat-dialog-census.md](../lab/rdlg1-323-repeat-dialog-census.md); `lab/scripts/research-rdlg1.sh`, `lab/scripts/research-rdlg1b.sh` |

| RDLG2 (a–e: 3.23 recipe adaptation + certification) | **The Things 3.23 repeat-dialog recipes, adapted and CERTIFIED — on 3.23 AND still on 3.22.14** (golden-v4 + golden-v3, pinned 2026-07-05; production CLI `--dangerously-drive-gui`, DB oracle). One binary drives both dialogs: a `probe-dialog-shape` step MEASURES which is open — reading the CONTROL CLASS on the `Next:` row, never the app version — and the recipe carries both index sets (`shaped`) plus both first-occurrence drives (`onlyShape`); a third, unknown dialog refuses fail-closed. **Corrects RDLG1:** the `Next:` LABEL and the occurrence-preview line exist on 3.22.14 too — only the control changed — and a label-presence probe misread 3.22 as 3.23 (caught by the regression arm, and by verify-per-write, which refused the wrong rule rather than reporting it landed). **`Next: ▸ More…` is NOT a date picker** but a cascade of further ON-RULE occurrences (~10 y, `AXIdentifier=nextDateOptionAction:`), so an OFF-RULE first occurrence is now **inexpressible** in the app ([oddities §11](../things-app-oddities.md)) — the new `select-next-occurrence` primitive parses item titles to dates, descends the cascade, reads back, and fails closed naming why. **RRD1 CLOSED:** a `converge-weekdays` closed loop (read rows → grow via the geometry-resolved add button → assign every row from the target set, CYCLING → read back) fixes the stale-weekday trap and adds a shrink path with no remove gesture (the app dedupes the set) — certified GROW and SHRINK on both versions. `Reschedule…`/`Edit Rule…` ship as ordered candidates. Certification 21/21 (3.23) + 11/11 (3.22.14). Also censused: `File ▸ New Repeating To-Do` (dialog-first create: +2 rows, title only by keystroke — NOT a replacement for the `add-repeating` composite), `Create Next Copy` (materializes the pending occurrence AND advances the cursor), early-complete (instance-only, series unmoved), the Make Exception chooser (**still not reproduced** — every headless re-dating vector moves the instance with no prompt), and §9ff (precondition no longer forms on 3.23 — a different source fate, NOT a proven fix). Register walk: r-suite + a-suite GREEN, e2e 126/132 (all 6 failures the known reorder no-op), derived projection day reproduces the app's cache 8/8 — which surfaced a decoder bug of ours (`<array/>` on every after-completion rule was undecodable; fixed). | [lab/rdlg2-323-recipe-cert.md](../lab/rdlg2-323-recipe-cert.md); `lab/scripts/research-rdlg2{a,b,c,d,e}.sh` |

| GV4-CERT (3.23 certification) | **Certifying golden-v4: suite reconciliation, the row-by-row register walk, and the simulator re-model** (2026-08-22, unattended; drift-runbook steps 2–5). `lab:regress` GREEN end to end on Things 3.23 — the o-suite's 14 reds reconciled with a NEW version-conditional expectation mechanism (`expectFrom`) that makes the suite the behavioral canary the sdef declaration check cannot be, and the guest e2e 132/132 with three no-fallback reaches branching to their refusals. Two findings: `reorder --in today` has no working path when Today holds a PROJECT row on 3.23, and the intermittent A10/R01 tier 0→3 is a harness RACE (a fixed post-launch settle shorter than 3.23's ~3.5s launch→window), not an app behavior change — correcting GV4 §3.3. The register is walked per row (13 stamped / 10 SUSPENDED / the rest named as un-checkable residue) rather than blanket-amended. SIMFID re-certified host-side AND against a fresh golden-v4 clone. | [lab/gv4-323-certification.md](../lab/gv4-323-certification.md) |

Recurring, autonomously-runnable encodings of the locked verdicts live in `lab/suites/*.json` (a/e/o/p/r/s/u/x) and run via `npm run lab:regress`; the guest e2e (`lab/guest/e2e-write-smoke.sh`) exercises the shipped CLI surface end-to-end (106 steps). Coverage gaps: [suite-audit.md](suite-audit.md).

## Campaign map (what to read for what)

| Question | Doc |
|---|---|
| Does op X work on vector Y? | [capability-matrix.md](../capability-matrix.md), then the campaign doc its evidence id names |
| Why is this write guarded/blocked? | [things-app-oddities.md](../things-app-oddities.md) (hazards §1/§2/§6/§7) |
| Anything about headings | [lab/heading-research.md](../lab/heading-research.md) — the consolidated verb matrix |
| Ordering/reorder semantics (any scope) | [lab/o-suite-results.md](../lab/o-suite-results.md) (incl. someday anchor-stack models, Anytime non-determinism) |
| Reminder time parsing/encoding | [lab/r-suite-results.md](../lab/r-suite-results.md) (codec `hour<<26 | minute<<20`; three lexical classes) |
| Today membership & sort | [lab/today-order-research.md](../lab/today-order-research.md) |
| What forecasts into Upcoming? (deadline-driven membership by bucket, both-dates grouping, the someday blue-circle indicator) | [lab/upcoming-research.md](../lab/upcoming-research.md) — UPC1 verdicts |
| Completing a project with a trashed child / Logbook-sweep semantics / open child stranded in a logged project | [lab/plog1-research.md](../lab/plog1-research.md) — PLOG1 verdicts; oddities §6¾ |
| Sync & conflict behavior (Things Cloud / "Syncrony": what merges, is it LWW (no), last-sync signal, locked-screen/headless operation) | [lab/headless-research.md](../lab/headless-research.md) — LOCK1/SYNC1/SYNC2 verdicts; [lab/sync2b-durable-account.md](../lab/sync2b-durable-account.md) — SYNC2B (durable-account: `umd` sync-safety, cross-device repeat-instance dedupe, creationDate zone); [lab/sync3-dedupe-tiebreak.md](../lab/sync3-dedupe-tiebreak.md) — SYNC3 SY-3b (deterministic instance uuid → per-attribute add/add merge: `creationDate`/`umd` max, `notes` union; tiebreak value-based, order/device-independent) |
| Consent/TCC/"Enable Things URLs" | [lab/phase21b-research.md](../lab/phase21b-research.md) |
| Shortcuts vector & proxies | [lab/s-campaign-results.md](../lab/s-campaign-results.md); build cards [lab/l5-build-cards.md](../lab/l5-build-cards.md); signed files `shortcuts/` (extraction pipeline: roadmap §A) |
| Deletes (heterogeneous!) | [lab/a-suite-results.md](../lab/a-suite-results.md) A24–A27, s-campaign P12, oddities 5i |
| Deletion records / tombstones (`TMTombstone`) — when they appear, `deletionDate`, `changes --since` viability | [lab/tomb1-results.md](../lab/tomb1-results.md) — TOMB1 (`leavesTombstone`-gated; not a general deletion log) |
| `userModificationDate` (umd) footprint of tag + area lifecycle ops; the writable `modification date` capture-and-restore lever; status-dependent area-delete cascade | [lab/tagmod-tag-area-umd.md](../lab/tagmod-tag-area-umd.md) — TAGMOD (tag apply BUMPS members, rename/delete SILENT; tags/areas have no umd column; `set modification date` restore recipe — **now proven sync-safe**, SYNC2B) |
| DB schema ↔ UI meaning | [atlas/schema-v26.md](../atlas/schema-v26.md) |
| Harness mechanics / how probes run | [lab/harness.md](../lab/harness.md) |
| The bundled `thingscli` binary (what it is — an app-settings utility, not a task API; the `firstAppLaunchDate` trial-clock + `THCDataFolderCurrentDataFolderPath` reads the lab runner relies on) | [lab/thingscli-probe.md](../lab/thingscli-probe.md) |
| Golden image / VM lab conventions | [lab/golden-runbook.md](../lab/golden-runbook.md), [lab/drift-runbook.md](../lab/drift-runbook.md), [lab/things-update-runbook.md](../lab/things-update-runbook.md) |
| What's parked/queued | [roadmap.md](../roadmap.md), [lab/probe-backlog.md](../lab/probe-backlog.md) |
| Pre-lab history (March 2026 Codex research) | [research/](../research/README.md) — historical; matrix v1 superseded by capability-matrix |

## Certified environment

Things **3.22.11** · macOS **15.7.7** · DB schema **v26** · golden `things-lab-golden-v1` (clock-pinned 2026-07-05). Recertification for any new Things version: [lab/things-update-runbook.md](../lab/things-update-runbook.md).
