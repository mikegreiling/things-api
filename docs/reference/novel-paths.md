# Novel working paths — the surprising capabilities the lab uncovered

The capabilities that exist NOWHERE in Cultured Code's documentation, discovered by probing. Each is lab-validated with the evidence id shown; the living [capability-matrix](../capability-matrix.md) tracks which are wired into shipped operations. Ordered roughly by how much product surface each unlocked.

## AppleScript

1. **`_private_experimental_ reorder to dos in <specifier> with ids "…"`** — the undocumented sdef command behind every native reorder scope. Accepts to-dos in `list "Today"` (A50) and `list "Inbox"` (21b A6, reversed wire list), un-headed children in project/area specifiers (O04/O05/O09/O10), **projects** in an area specifier (O14), **heading rows** in a project specifier (scf P1 — children follow their FK), and `list "Someday"` members (P6h) with a distinct anchor-stack model: the call's original top item never moves, to-dos stack ASCENDING (P8b two-call protocol), someday PROJECTS stack DESCENDING (P9e inverted protocol). Gated behind `allow-experimental` + the sdef canary.
2. **By-id addressing reaches rows list reads can't see.** Repeating templates (A12) AND heading rows (P10 — `to do id "<heading-uuid>"` resolves type=2 despite "no heading class") are fully addressable: `get properties`, `set name` (rename, P10d), `set status` (archive/unarchive, P10d/P10b — cascade semantics P11c/P11d). The entire shipped heading lifecycle rides this.
3. **`_private_experimental_ json of to do id …`** (A51) — full JSON document exposing checklist items and recurrence config invisible to every public read surface.
3½. **The heading reorder reaches ARCHIVED headings of EVERY sweep state — including SWEPT ones the GUI never exposes to drag** (HEADSORT). `_private_experimental_ reorder to dos in project id X with ids "…"` re-ranks the `index` of open, archived-unswept, AND archived-swept headings alike (reachability is sweep-agnostic — the sweep is a pure view boundary that writes no rows), honoring the exact sent order in one comma-joined wire (partial wire = named rows front-clustered). CAVEAT — not a clean index write for archived rows: reordering an archived heading also **REOPENS** it (`status 3→0` + `stopDate→NULL`, heading-only, children resolved; umd-silent for open members, umd-bumping for the reopened archived ones). So this is a working path to *reach and re-rank a swept heading headlessly*, but it un-archives it in the act — there is no index-only reorder of an archived heading. See [headsort](../lab/headsort-heading-lifecycle-reorder.md); the reopen quirk is [oddities §5o](../things-app-oddities.md).
3¾. **The direct-child to-do reorder also reaches SWEPT (Logbook) to-dos the GUI never exposes to drag** (LOGSORT). The same `_private_experimental_ reorder to dos in project id X with ids "…"` re-ranks a project's DIRECT children (`heading=NULL`) of every lifecycle state — open, completed-unswept, canceled-unswept, AND completed-swept. For a SWEPT to-do this is a working path to *reach and reposition a Logbook to-do headlessly*, but — like the swept heading (#3½) — it **REOPENS** it in the act (`status 3→0` + `stopDate→NULL` + `umd` bump; the reposition re-crosses the body↔Logbook boundary). CONTRAST with #3½: for to-dos the reopen is **sweep-DEPENDENT** — an UNSWEPT resolved child (completed OR canceled, still struck-through in the live body) reorders `index`-only + umd-SILENT with NO reopen, so there IS a clean index-only reorder of an unswept resolved to-do (there is none for any archived heading). See [logsort](../lab/logsort-logged-child-reorder.md); the reopen quirk is [oddities §5o](../things-app-oddities.md).
4. **Backdating existing items**: `set completion date` / `set creation date of to do id X` rewrite history (scf2 P4b) — dead on URL (oddity 2g) and Shortcuts (P4a). Ships as `todo.set-dates` / `project.set-dates` (exposed as `--created-at`/`--completed-at` on `update`/`complete`/`cancel`). **Kind-agnostic — `set completion date`/`set creation date of PROJECT id X` works identically** (BACKDT B-PROJ-AS, exact values, single-row: children byte-identical, no cascade). CAVEAT: `set completion date` is really "set-completed-at" — it FORCES `status=completed` (silently completes an OPEN item, silently re-completes a CANCELED one 2→3), so the AS completion-date leg fires ONLY on an already-COMPLETED row (generalized WG-7 / H-BACKDATE-OPEN); a canceled item is backdated via the certified flip-dance (→completed · AS backdate · →canceled). `set creation date` is status-safe + umd-silent. [backdt](../lab/backdt-project-backdating-and-flips.md); the forced-complete quirks are [oddities §5p/§5q](../things-app-oddities.md).
4½. **`log completed now` moves resolved-but-unlogged items into the Logbook** (A28/LOGNOW/plog1) — the ONLY headless surface for the log-move (URL/Shortcuts have none). Ships as `log-now` (`things log-now` / MCP `log_now` / `client.write.logNow`). It advances `TMSettings.manualLogDate` to ~the completion instant and **mutates ZERO task rows** (the sweep is a pure derived boundary — the whole complete→sweep→restore `index`-retention family rests on this). It advances **only when there are pending completions** (a clean no-op otherwise), so the op pre-reads the boundary-predicate census for the disclosed count and verifies as a `TMSettings` singleton delta (the stamp advances iff `pending>0`), not a task-row delta. IRREVERSIBLE (no official surface rewinds the boundary — TIMEZ/settings-stamp). [plog1](../lab/plog1-research.md); [s-campaign-results.md](../lab/s-campaign-results.md) round 3 (LOGNOW).
5. **`delete <property>` as the un-setter**: `delete parent tag of tag X` un-nests to root (P29); `delete keyboard shortcut of tag` clears the shortcut (21b A4) — where `set … to missing value`/`""` error out.
6. **`schedule to do id <PROJECT>` works** (P14-A3 — projects inherit the `to do` class); an unwired alternative to `update-project?when=` (decision: document-only, roadmap §D). Also `schedule … for (current date)+N*days` fills the Upcoming gap (A21B).
7. **`move <trashed to-do> to list "Inbox"` restores from Trash** (E15 — the UI's "Put Back", scriptable); `move <trashed project> to list "Anytime"` restores a project IN PLACE, restoration-faithful (P06/P07). The inverse is the trash path for a **COMPLETED** row: **`move to do id <X> to list "Trash"` trashes a logged to-do that `delete` REFUSES** (`delete to do id <X>` → −1728 "Can't get to do id" on a `status=3` row, though `get`/`set` on it work; `delete` is silently OPEN-only — oddities §5n, SIT5). `move … to list "Trash"` covers both open and completed rows.

## URL scheme / TJSON

8. **The empty-parameter clear pattern**: `update?list-id=` detaches a to-do from its project/area (P21/P22); `update-project?area-id=` detaches a project (P24) — the ONLY container-clear on any surface (four-behaviors-for-one-intent, oddity 5g). Empty `tags=` / `checklist-items=` clear likewise (P14/P15).
9. **TJSON `{"type":"heading"}` items inside a NEW project's `items` create real type=2 rows** (HX0; used by the golden seeder and the e2e heading fixtures). The ONLY headless heading-create — but only at project creation; every relocation/append shape is dead (HX1–HX4b).
10. **At-creation backdating via TJSON attributes**: `{"completed":true,"creation-date":…,"completion-date":…}` honored exactly (scf2 P4d) — the logbook-import path. Ships folded into `todo.add` / `project.add` via `--completed-at [--created-at]` (routes to `things:///json`). **Works for PROJECTS too** (BACKDT B-PROJ-JSON.1 — a completed project imports exact, lands in the Logbook), with the §5b caveat that any plain OPEN child in the same payload reopens the project at creation (completion-date dropped, creation-date kept) — so the engine REFUSES a project `add --completed-at` that would seed open children. Timestamps must be full second-precision: a bare date (no time) is REJECTED, same as the milliseconds rejection (oddity 2h). [backdt](../lab/backdt-project-backdating-and-flips.md).
11. **`update?duplicate=true` copies a to-do or a project INCLUDING children** (E07/E17) — AppleScript `duplicate` is refused (-1717).
12. **The `when=` bounce family**: front-insert on attach/round-trip powers the evening reorder (O08) and top-level sidebar-project ordering via a someday↔anytime round-trip (P7c/P8e) — the only write path to sidebar project order.
13. **A dated `when=` on a repeating TEMPLATE is a full series RE-ANCHOR — Things 3.23 only** (REANCH1). `things:///update?id=<to-do template>&when=<a strictly FUTURE ISO date>` (and `update-project?…` for a project template) rewrites the template's two cursor columns, `todayIndexReferenceDate`, `umd` AND the `rt1_recurrenceRule` blob, recomputing the rule's start anchor and its calendar anchor from the target date — producing a blob byte-identical to the GUI `Update Rule` chooser's (REPX2 §1.4). The next spawn lands on the new date, the skipped slots are never backfilled, existing instances and `rt1_instanceCreationCount` are untouched, and a `@<time>` component sets a rule-level reminder every later spawn inherits. The whole branch is fatal outside its envelope: a target on or before today, an after-completion template, and every build before 3.23 all kill the process ([oddities §1](../things-app-oddities.md), [§15](../things-app-oddities.md)). **WIRED 2026-08-28** as `reschedule-repeat <ref> --when <date>` with no frequency/interval (url vector, tier 0, no GUI-drive gate; `H-REPEAT-REANCHOR` enforces the whole envelope pre-dispatch and the pipeline version-gates it at ≥ 3.23). It must be dispatched **BARE**: a `deadline=` parameter in the same url voids the entire write on a template — the re-anchor included, whatever the deadline value, in either order (REANCH2 §4, [oddities §27](../things-app-oddities.md)). `H-REPEAT-SCHEDULE` still covers every other `when=` spelling on a template. [reanch1-url-reanchor.md](../lab/reanch1-url-reanchor.md), [reanch2-url-deadline-reminder.md](../lab/reanch2-url-deadline-reminder.md)

## Shortcuts (App Intents)

13. **`Create Heading` makes a heading in an EXISTING project** (S02) — the marquee capability, dead on both other vectors; exhaustively confirmed unique by the HX sweep.
14. **Single-item permanent delete** (S-delperm — `Delete` + Delete Immediately: row gone, no tombstone) — interactive-only (delete-class consent has no Always-Allow, oddity 5j).
15. **`Edit Items → Reminder Time ""` clears a DATED reminder IN PLACE** (scf P3b; works on projects too — RC avenue D) — the only *schedule-preserving* dated-reminder clear (`startDate` untouched, headless); it cannot SET (P3a). **NOT the only clear (corrected 2026-07-11, RC campaign):** a pure-URL bounce `when=today`→re-date also clears (RC01/RC02, non-atomic + transient Today re-date + crashes on repeating items), and AppleScript `move … to list "Inbox"` drops it by de-scheduling (RC03, loses the date). AppleScript has no writable reminder property — sdef exposes only read-only `activation date` + read-only `_private_experimental_ json`, `set` → −10006 (RC avenue C). See [rc-suite-results.md](../lab/rc-suite-results.md).
16. **Output-class consent is inherited by VM clones** (scf) — grant Always-Allow once in the golden, every clone runs proxies headless.
17. **The proxies themselves are extractable and re-signable**: workflow blobs live verbatim in `Shortcuts.sqlite` (`ZSHORTCUTACTIONS.ZDATA`), wrap into old-format plists, and `shortcuts sign --mode anyone` mints importable signed files (SX2–SX4) — the distribution path for `shortcuts/*.shortcut` (no iCloud, no manual rebuild).
18. **Programmatic shortcut AUTHORING — no golden GUI sitting needed** (SX5, 2026-07-10). A hand-composed (never-GUI-touched) `WFActionParameterFilterTemplates` predicate runs CORRECTLY: the SX5 repair of `find-items` was written in Python (plistlib), injected, and returned real case-folded matches. Combined with #17, new proxies can be minted entirely in code: compose the `WFWorkflowActions` bplist → wrap in the old-format envelope → `shortcuts sign --mode anyone`. The action vocabulary comes free from the app bundle: `Things3.app/Contents/Frameworks/ThingsCommon.framework/…/Metadata.appintents/extract.actionsdata` lists every entity property identifier + per-property query comparators (that's how `title` was confirmed and `name` ruled out, host-side, before any VM run). Two supporting facts: **DB surgery on a consented shortcut preserves its consent** (swap `ZSHORTCUTACTIONS.ZDATA` after killing `siriactionsd`; identity, not content, keys the grant — the SX5 iteration loop), and predicate `Property`/`Operator` values must come from the metadata (unknown ones crash Things — oddity C4).

## UI (VNC synthetic input — dedicated-Mac "ui" vector)

22. **`File → New Repeating To-Do` is VNC-drivable end-to-end** (UI1, §E½ feasibility) — the ONLY working repeat-create path on any surface (URL/AS/Shortcuts all dead). `tart run --vnc-experimental` + vncdotool posts hardware-level HID (no TCC), so menu clicks, dialog interaction, checkbox toggles, and **keyboard entry (titles AND numeric offset fields)** all work despite Accessibility being ungranted (osascript System Events → −1719, so there is NO AppleScript UI-scripting fallback). Layout is deterministic at the golden's 2048×1536 framebuffer. Green-lights a dedicated-Mac "ui" vector for the conclusively-UI-only surface (repeat create/edit, sidebar order, to-do↔project convert). VNC-keyboard gotchas: `vncdo type` sends letters lowercase; `super-a` types a literal 'a' (no Cmd) — clear a field with N×bsp + N×del, never select-all.
23. **The deadline-ness of a repeat lives in the TEMPLATE's `deadline` column, not the rule** (UI1) — NULL = deadline-less (the GUI default), `4001-01-01` sentinel = deadlined. The rule blob is byte-identical for deadlined-vs-deadline-less ts=0; a non-zero start offset also surfaces as `ts=−N`. Universal across fixed AND after-completion. Fixed the phantom-deadline projection (`RepeatingInfo.deadlined`; oddities §8a).
24. **System Events Accessibility scripting drives Things transforms by element NAME — in a VM, SIP enabled** (AXVM1, 2026-07-14). A one-time **user-path TCC toggle** (System Settings → `tccd`; the denied AX attempt auto-creates a toggleable `sshd-keygen-wrapper` row, flip 0→2 — NOT a direct DB write, which SX4 proved read-only under SIP) makes `osascript … System Events` UI-scripting work over SSH. Recipe: select the target by stable handle `things:///show?id=<uuid>` (Things list rows are NOT AX-addressable — sparse rendering), then `click menu item "Pause" of menu 1 of menu item "Repeat" of menu "Items" of menu bar item "Items" of menu bar 1` → `rt1_instanceCreationPaused` 0→1. This is a SECOND ui-vector driving path alongside VNC, and strictly better for a headless host: **no frontmost app required, NO focus steal** (paused a template with Finder frontmost), and **works under a locked session** (VNC coordinate clicks do neither). Grant persists across reboot. Discriminator gotcha: `get name of first process` is NOT AX-gated — use a UI-element read to detect the −1719 gate. See [axvm1-accessibility.md](../lab/axvm1-accessibility.md).
25. **Synthetic mouse input from pure JXA — no compiled helper — opens the card and drags the sidebar** (NATIVE1, 2026-07-15). `osascript -l JavaScript` + the ObjC bridge resolves any AX element's frame (`AXUIElementCopyAttributeValue` for `AXPosition`/`AXSize`, read via `ObjC.castRefToObject` + `CFCopyDescription`-regex; attribute names must be NSStrings — `$('AXPosition')`) and synthesizes clicks/drags (`CGEventCreateMouseEvent`). **A double-click at an AX-resolved list-row center opens the to-do card** (the card-only surface UI2-i/UIC1 needed — the repeat bar becomes reachable, unblocking `todo.stop-repeat`); **a mouse-down → interpolated `kCGEventLeftMouseDragged` → mouse-up between two AX-resolved sidebar rows reorders an AREA** (unblocking the LAST unautomatable ordering, matrix line 107) and writes **`TMArea."index"`** (0 → −448; sparse/negative, lower sorts higher — the column O13 predicted the GUI writes only on drag). **Critical: the events must go through `CGEventPost(kCGHIDEventTap, …)`; `CGEventPostToPid` is inert** for Things' hit-testing. So this path is **foreground-bound** (no background, no under-lock — unlike the AX menu path #24) and needs **only** the Accessibility grant (CGEvent posting is Accessibility-authorized; no Input-Monitoring/PostEvent class). See [native1-spike.md](../lab/native1-spike.md).
26. **Sidebar entities ARE AX-addressable by description** (NATIVE1) — a refinement of "Things list rows aren't AX-addressable" (#24, UIC1): that holds for the *main list* (rows expose only images, no title/actions), but **sidebar area/project rows carry an `AXDescription`** (`d="LAB-AREA-A."`) so they're locatable by name + frame, and **`AXSelectedRows` READS correctly** on both tables (resolves the `things:///show`-selected row for a double-click target). Rows still expose **zero AX actions** (empty action list; `AXScrollToVisible` → −25206), so *setting* selection / scrolling stays mouse-synthesis territory.
27. **Scroll-then-drag recipe for off-viewport sidebar targets — with a computable scroll distance** (AXDRAG1, 2026-07-15). Because `AXScrollToVisible` is dead on rows (#26), off-viewport drop targets are reached with **synthesized scroll-wheel events**: move the pointer over the sidebar, then `CGEventCreateScrollWheelEvent($(), kCGScrollEventUnitLine, 1, ±delta)` → `CGEventPost(kCGHIDEventTap)` (~30px content travel per 3-line click; negative reveals lower rows). Crucially the sidebar `AXScrollArea`'s child `AXScrollBar` (`AXVerticalOrientation`) **exposes `AXValue` as a Float64 fraction 0.0→1.0** (0 top, +1.0 bottom) — so scroll distance is *computable* from the target row's virtual frame + the viewport rect, not just scroll-and-check. **Things also auto-scrolls mid-drag** when the drag hovers near the top/bottom edge (bidirectional, runs to the range end) — but it is time-based/imprecise, so a deterministic op should pre-scroll the target into the visible band, re-resolve frames, then drag within one viewport. See [axdrag1-reorder.md](../lab/axdrag1-reorder.md).
28. **Drag-drop geometry: lifting the source collapses its slot (the "downward −1 slot" rule) + drop-centre-vs-boundary semantics** (AXDRAG1, 2026-07-15). Sidebar area rows interleave with h=16 spacer rows → **40px per slot**. When a row is picked up, everything below its original position shifts **up by one slot (40px)**, so **downward** drops must target `neighborStaticY − 40` while **upward** drops use static coordinates (targets above the source don't move). Insert-before/after follows the pointer's row-half against the live layout. In the **Tags window** the same drag doubles as re-parent: **dropping on a row CENTRE nests (`TMTag.parent`), dropping on a row BOUNDARY reorders (`TMTag."index"`)** — a mis-aimed reorder silently nests. Both `TMArea` and `TMTag` reorders use a sparse/negative index (lower sorts higher) and can renumber a *neighbour* rather than the moved row (so undo captures the full order, not one index). See [axdrag1-reorder.md](../lab/axdrag1-reorder.md).
29. **Repeating-PROJECT ops via a HYBRID synthetic-click + AX path** (UIC2, 2026-07-15). A project's title-adjacent **`…` menu** carries **Repeat…** (the Items menu bar does NOT, for a shown project), and it opens the **same Repeat dialog sheet as a to-do** (byte-identical controls: freq pop-up `_NS:116`, OK `_NS:164`). So all four project repeat ops work: `make-repeating` (new template `fu`=256 + spawned instance, identity replacement), `reschedule` (repeat-bar ▸ Change…, `fu` 256→8, identity preserved), `pause`/`resume` (repeat-bar ▸ Pause↔Resume, `rt1_instanceCreationPaused` toggle). A repeating project exposes an **always-visible repeat bar** in its view (no card double-click — the to-do repeat bar is card-only). BUT this is **not pure-AX**: the `…` button is not an AX node, and the `…`/repeat-bar popover items are AX-readable-but-**inert-to-`AXPress`** — opening/navigating them needs a synthetic MOUSE click (their `AXPosition` IS resolvable, so synthesis can avoid guessed pixels); only the dialog sheet is AX-actuatable (and backgroundable, no focus steal). `open -g things:///show?id=<project>` reveals+selects a project WITHOUT activating Things. See [uic2-project-menu.md](../lab/uic2-project-menu.md).
30. **The project repeat-bar POPOVER is a separate `AXUnknown` top-level window** (UIC3, 2026-07-15) — a single HID click at the AX-resolved repeat-bar frame center (`text area 2` of the header cell) opens the [Change… · Pause↔Resume · Stop · Show Latest] popover **not** as `pop over 1` of the standard window but as a **new ≈215×220 `AXUnknown` window** (same custom-window shape as the `…` menu). Two `AXUnknown` windows coexist while it's open (the popover + the hidden 40×40 utility window), so it's addressed by subrole **and** `size is not {40, 40}`; its items are `UI element`s of the window's `scroll area 1`, matched by `AXDescription`, clicked at their AX-resolved frames (`AXPress` is inert). This is the addressing that made **`project.reschedule-repeat`/`pause-repeat`/`resume-repeat` lab-certifiable through the production CLI** (UIC3-b), on the new `click-element` mouse-synthesis driver primitive. See [uic3-build-certification.md](../lab/uic3-build-certification.md).
31. **`AXSelectedRows` is SETTABLE by pure AX → a project selects with ZERO mouse input, and `project.make-repeating` is 100% pure-AX** (UIC4, 2026-07-15 — **corrects UIC3-a**). A project **does** render as a **selectable content row** at the top of its area's (and the Someday view's) content `AXTable` — the row exposes only images (no title text), but `AXUIElementSetAttributeValue(contentTable, "AXSelectedRows", NSArray[projectRow])` returns `kAXErrorSuccess` and the selection **lands** (`AXUIElementIsAttributeSettable`→YES; NATIVE1/AXVM1 only proved *reads*). With the project so selected, **`Items ▸ Repeat…` is present and enabled**, so `select-row (AX) → Items ▸ Repeat… (AX) → Repeat sheet (AX)` drives make-repeating **100% pure-AX and background-capable with no focus steal** (DB-verified: identity replacement, new template `fu`=256 + spawned instance, **area preserved**). This is a *better* profile than the reschedule/pause/resume ops (which still need mouse-synthesis for the `…`/repeat-bar popover). Wrinkle: **backgrounded, the Repeat editor detaches to a top-level `AXUnknown` window** (address by `subrole AXUnknown, size ≠ {40,40}`; controls are direct children — `pop up button 1 of window 1` = frequency) rather than an attached `AXSheet`. See [uic4-project-selection.md](../lab/uic4-project-selection.md).
32. **A project's right-click context menu is a real `NSMenu` carrying Repeat… — but AX-opaque** (UIC4, 2026-07-15). A synthetic HID right-click on a project **row** (area view) or an area-less project **header** (Anytime view, the "Image 22" pattern) opens a genuine native `NSMenu` (system styling, `Services ▸` submenu, `Repeat…` present). Unlike the custom `…` menu (#29, AX-readable-but-inert), this one is **not published to the AX tree at all**: while open, a JXA walk of the app's children shows no `AXMenu` node, `menu 1 of window 1` → `-1728`, and `entire contents` **hangs** (the modal tracking loop blocks synchronous AX). So it's **not `AXPress`-drivable** — the only drive is **keyboard typeahead** (`keystroke "repeat"` + Return, which the tracking loop consumes), English-title-pinned and foreground-bound. It's the in-place opener for the area-less-anytime case (which has no selectable row). See [uic4-project-selection.md](../lab/uic4-project-selection.md).
33. **`make-repeating` normalizes a project's `start` to the repeating bucket + templates resist quiet-vector reschedule** (UIC4, 2026-07-15). Making any project repeat writes `start`=2 to the template AND its spawned instance **regardless of the project's prior anytime/someday state** (even the app-made seed is `start`=2; the instance carries a `startDate` → shows in Upcoming). Consequence: **area is preserved through the conversion, `start` is NOT** — so a temporary Someday-coercion of an area-less anytime project (via the `update-project?when=someday&auth-token` quiet vector, to make it row-selectable) is **cleanup-free**, since the conversion discards the prior bucket anyway. Separately, a repeating **template refuses quiet-vector schedule edits**: `things:///update-project?when=…` is a silent no-op on its `start`, and AppleScript `move … to list "Anytime"` → **error 301** (re-homing a template's schedule needs the in-app UI). See [uic4-project-selection.md](../lab/uic4-project-selection.md).
34. **Select a project row purely via the System Events `select` action** (UIC5, 2026-07-15). To select a project as a content-table row with zero mouse input, `select (row i of <content table>)` via System Events works, replaces the prior selection (single-select), and is background-capable with no focus steal — after it, `Items ▸ Repeat…` enables (~1 s to materialize). This is the correction to UIC4-a's "settable `AXSelectedRows`": that settability was proven with the **ObjC-bridge** `AXUIElementSetAttributeValue(table, "AXSelectedRows", NSArray[row])`; the *System Events attribute-set* form (`set value of attribute "AXSelectedRows" … to {row i}`) is a **silent no-op** (oddities §8). The production `project.make-repeating` select-row primitive uses the `select` action. See [uic5-build-certification.md](../lab/uic5-build-certification.md).
35. **Select a HEADING row purely via the System Events `select` action — the LAST uncertified ui op, certified** (HEADCERT1, 2026-07-17). UIC1 failed `heading.convert-to-project` because `things:///show?id=<heading>` selects to-dos only (selection went empty, Convert to Project… stayed disabled). The fix: reveal the heading's **parent project** (`things:///show?id=<project>`) — whose content table renders each heading as a row — and issue the row `select` action on it. **A heading row DOES take selection** (`selected of (row i)` → true) and reads back an **empty** `name of selected to dos` (a heading is not a to-do); with it selected `Items ▸ Convert to Project…` **enables**. Heading rows expose **no stable AX title** (only a hover-dependent "More" affordance: `AXUnknown` desc `"More. <title>"`/`"‎<title>"` + `AXImage` desc `"Heading More Template"`, unreliable headless), so the target is addressed **POSITIONALLY** — the Nth selectable-empty-readback row = the Nth heading in DB `"index"` order. Gotcha: a menu item's `enabled` read *without opening the menu* is **stale** (macOS revalidates on menu-open only); the drive presses via System Events (which opens the menu), so it never bites. DB effect = UI2-d (heading uuid dies, new type=1 project into the parent's area, children reparent `heading→NULL`). See [headcert1-certification.md](../lab/headcert1-certification.md).

36. **Within-heading child ORDER via a FORWARD-order `someday→anytime` bounce** (BOUNCE2-h, 2026-07-27). No native surface reorders a heading's children (project scope rips them out — O06; heading-as-container is rejected on every spelling — HEADORD-b), but the `when=` bounce IS the surface: bouncing a headed anytime child `someday→anytime` **BACK-inserts** it (appends to the END of the heading bucket — the app keeps the bounced row's `"index"` and renumbers the non-bounced siblings down) while PRESERVING the heading FK every leg. So to place headed children `T1..Tn` in exact order, bounce them in **FORWARD** order `T1,T2,…,Tn` (each appends after the last). Deterministic (5-item map + 4-item forward confirmation, FK intact); the inverse of the loose reverse-order front-insert. Resolves the HEADORD-c anomaly. See [reordgaps-results.md](../lab/reordgaps-results.md) BOUNCE2-h.
41. **Loose FUTURE-day order via a scratch-PROJECT park-sort-unpark** (UPCORD1, 2026-07-30) — STANDALONE loose (no project/area/heading) to-dos sharing one future Upcoming day have no direct reorder surface (`list "Upcoming"` re-dates destructively §9g; date-shaped `list` specifiers don't exist −1728; only `list "Tomorrow"` reaches next-day). The wireable protocol: create a scratch PROJECT, PARK each day-group member via URL `update?list-id=<project>` (preserves `startDate`/`start`/`todayIndex`/reminder/deadline), run the shipped `container-day` reorder against the scratch project (native `todayIndex` re-rank, DATE-PRESERVING), UNPARK via URL `update?list-id=` (empty → loose again, order preserved), then trash the scratch project. Every leg is date/state-preserving; cost 2N+1 dispatches + the scratch lifecycle. The AREA scratch variant is DESTRUCTIVE (the area reorder de-schedules dated members, §9f addendum) — the protocol uses a PROJECT scratch exclusively. Loose scheduled PROJECT rows (nest only in areas) and repeating TEMPLATES (the reorder skips them, §9e) stay app-default. ~~Wired as reorder scope `loose-day`~~ **SUPERSEDED 2026-07-31 by the dated `day` bounce (entry 48) — the `loose-day` scratch-park scope is DELETED (the bounce needs no scratch project).** See [upcord1-loose-day-order.md](../lab/upcord1-loose-day-order.md) Arm B.
37. **Within-container SOMEDAY order via a `someday→anytime→someday` bounce (no destructive area command)** (SOMEBNC, 2026-07-27). Toggling a container's someday member out to anytime and back re-inserts it state-preservingly (`start` returns to 2, container FK + `reminderTime` NULL + `deadline` NULL intact) — the re-entry DIRECTION splits on containment: an **AREA's** direct someday member **FRONT-inserts** (below the someday group min) → **reverse-order** legs place them in order, closing the §9f within-area gap WITHOUT the de-somedaying area reorder command; a **PROJECT's** someday child **BACK-inserts** (append to end) → **forward-order** legs, a 2nd path beside the native SOMEORD-b project reorder. Both deterministic + clean 3/3. See [reordgaps-results.md](../lab/reordgaps-results.md) SOMEBNC.

42. **Within-heading SOMEDAY order via an UNHEAD → RE-HEAD round-trip** (HEADSUB1 Arm B-someday / Arm C + HEADSUB2 Q1, 2026-07-31) — the move-to-heading URL (`update?id=<u>&list-id=<project>&heading=<title>`, the shipped `todo.move` leg) BACK-INSERTS a movee at the heading someday-bucket end (`start=2` preserved, §9h renumber) **only when the row is arriving from ELSEWHERE**; re-heading a row ALREADY under that heading is a same-heading **NO-OP on `index`** (HEADSUB2 Q1(b), §9l), so a direct re-head-in-order can NOT sort an already-headed block. The wireable sort is the round-trip: UNHEAD the block (`update?list-id=<p>`, clean — `heading`→NULL, `index`/`start=2` preserved, Arm C), THEN re-head each in FORWARD target order so each now-loose row genuinely back-inserts (HEADSUB2 q1fix). Two URL move legs per item, still NO experimental surface and NO bounce. Wired as reorder scope `heading-someday` (`src/write/reorder.ts` `runHeadingSomeday`); a mid-protocol failure leaves items unheaded in the project root and fails loudly. (HEADSUB1 originally shipped the inert direct-re-head — corrected in the HEADSUB2 change.) See [headsub1-heading-subbuckets.md](../lab/headsub1-heading-subbuckets.md) §HEADSUB2.
43. **Within-heading same-day SCHEDULED order via unhead → container-day reorder → re-head** (HEADSUB1 Arm C2, 2026-07-31) — the native container-day reorder RIPS a headed child's heading FK on the `todayIndex` axis too (§9k / Arm A — O06 is axis-agnostic, so a headed row must NEVER be fed to it), and the SHORT direct re-head is a `todayIndex` no-op (Arm C), so the ONLY path is the round-trip: UNHEAD the whole same-day headed sub-bucket (`update?list-id=<project>`, clean — `heading`→NULL, `startDate`/`todayIndex`/`start` preserved), re-rank the now-unheaded rows via the shipped `container-day` native reorder (DATE-PRESERVING), then RE-HEAD each (`update?…&heading=<title>`, `todayIndex` preserved). ~~Wired as reorder scope `heading-day`~~ **SUPERSEDED 2026-07-31 by the dated `day` bounce (entry 48) — DAYBNC proved the cross-date round-trip PRESERVES the heading FK, so a headed same-day child just bounces; the `heading-day` unhead→re-head scope is DELETED.** See [headsub1-heading-subbuckets.md](../lab/headsub1-heading-subbuckets.md) Arm C2.
44. **A CONTAINER child's evening order rides the shipped loose evening bounce unchanged** (HEADSUB1 Arm D, 2026-07-31) — a project/area child flagged this-evening, bounced `when=today → when=evening`, FRONT-inserts below the evening group's `todayIndex` min exactly like a loose evening item, with the project/area FK + `startBucket=1` + today `startDate` all preserved (FK hex byte-identical pre/post). So the `evening` reorder scope extends to container children with no BounceSpec change — the planner drops the child-refusal and routes a container-child evening movee to `evening`. Inherits the R07 reminder-loss caveat (a bare `when=today/evening` clears an existing reminder). See [headsub1-heading-subbuckets.md](../lab/headsub1-heading-subbuckets.md) Arm D.
50. **The flag-safe index MOVE re-entry law — the de-Today-free twin of the index bounces** (SIT6, 2026-07-31; protocol-proven, UNWIRED) — the `index`-axis reorder bounces (`heading` BOUNCE2-h, `anytime` ANYBNC, `projects` P8e) are `when=`-only and their `when=someday`/`anytime` legs OVERWRITE the Today/Evening flag (PROJSTAR confirmed the `projects` bounce silently DE-STARS a Today-flagged project — `when=someday` NULLs `startDate`, `when=anytime` never restores it). The MOVE family (`list-id`/`area-id` re-parenting) is the flag-safe alternative, same insertion geometry: **re-head INTO a heading BACK-inserts** past the heading-bucket max (unhead `update?list-id=<p>` → re-head `update?list-id=<p>&heading=<title>` in FORWARD target order; HEADMOVE); **unpark/detach to a LOOSE bucket FRONT-inserts** at the bucket `index` min in dispatch order, net order = REVERSE of dispatch (loose to-dos: park into a scratch PROJECT → unpark `list-id=` empty in REVERSE target order, LOOSEPARK; area-less projects: park into a scratch AREA `area-id=<scratch>` → detach `area-id=` empty in REVERSE target order, PROJPARK). Every move leg preserves `start`/`startDate`/`startBucket`/`todayIndex`/`reminderTime`/`deadline` + untouched container FKs — scrambled targets landed byte-exactly with the Today star intact. Scratch container is REQUIRED (a bare `list-id=`/`area-id=` on an already-loose/area-less row is a no-op) and its teardown MUST verify-empty (deleting a scratch container trashes a still-parked movee — AREADEL). An AREA scratch is also flag-safe for anytime+Today to-dos (§9f de-schedule fires on `start=2` only; a Today star is `start=1`) but a PROJECT scratch is the robust default. Route a Today/Evening-FLAGGED movee through the matching move protocol instead of the de-Today bounce (a host de-Today guard on the `projects` scope is the immediate prerequisite). See [sit6-flagsafe-index-protocols.md](../lab/sit6-flagsafe-index-protocols.md).
45. **A HEADED evening child's order rides the shipped evening bounce, heading FK preserved** (ORDFIN1 Arm 2b, 2026-07-31) — the today↔evening bounce (`update?when=today` then `update?when=evening`) on a headed this-evening child preserves the `heading` FK **byte-identical**, round-trips `startBucket` 1→0→1, and keeps today's `startDate`; the re-entry front-inserts on `todayIndex`. So the `evening` scope handles a headed evening child unchanged (extending HEADSUB1 Arm D / HEADSUB2 Q2 to the headed case) — the "headed evening = GUI-ambiguous, needs a VNC oracle" blocker was false (the maintainer law: today/evening membership is a status pip on an `index`-sorted anytime-bucket row outside Today, the Today/Evening bucket on `todayIndex` inside Today). Not yet wired (routing a headed movee to `evening` is a follow-up, no new machinery). Corollary (Arm 2c): the native PROJECT-specifier reorder over unheaded evening children is evening-SAFE — keeps `startBucket=1`, re-ranks `index` — unlike the `list "Today"` de-evening (O03), so de-evening is Today-scope-specific. See [ordfin1-ordering-endgame.md](../lab/ordfin1-ordering-endgame.md) Arm 2.
46. **`area-day` and `upcoming-day` — the scratch-PROJECT park-sort-restore generalized to areas and to a whole cross-container day-group** (ORDFIN1 Arm 3/4, 2026-07-31; protocol-proven, NOT yet wired) — an area's direct dated children have no clean native day-reorder surface (the AREA specifier de-schedules dated members, §9f). The wireable path reuses the UPCORD1 idiom: PARK each dated child into a scratch PROJECT (`update?list-id=<scratchProject>`) → `container-day` reorder → RESTORE via `update?list-id=<areaUuid>` — date/`todayIndex`/`start=2`/reminder/deadline all preserved, the area FK round-trips, anytime/someday canaries untouched (`area-day`, Arm 3). And a WHOLE Upcoming day-group spanning containers (loose + project children + headed children + area children) interleaves via ONE scratch project: PARK all members → ONE global `container-day` reorder to the target interleave → RESTORE each to its origin FK (loose←empty `list-id`, project child←`list-id=<P>`, headed child←`list-id=<P>&heading=<H>`, area child←`list-id=<A>`). Every FK round-trips, the global `todayIndex` order lands the target, schedule+reminder+deadline preserved, and **restore-leg order is irrelevant** (the reorder alone sets `todayIndex`; membership moves are `todayIndex` no-ops) — `upcoming-day`, Arm 4, generalizing `loose-day`+`container-day`+`area-day`. **SUPERSEDED 2026-07-31 by the dated `day` bounce (entry 48) — the `area-day`/`upcoming-day` scratch-park scopes are DELETED (the bounce serves the same population plus area-less project rows, no scratch project).** See [ordfin1-ordering-endgame.md](../lab/ordfin1-ordering-endgame.md) Arm 3/4.
47. **`list "Tomorrow"` is a clean ONE-CALL cross-container day-sort for exactly one day out** (ORDFIN2 Arm 2, 2026-07-31; protocol-proven, NOT wired) — `_private_experimental_ reorder to dos in list "Tomorrow" with ids "…"` (≡ `list id "tomorrow"`) re-ranks the whole next-day group on `todayIndex` in the exact sent order and **preserves `startDate`** (no §9g re-date, unlike `list "Upcoming"`). It ACCEPTS a scheduled area-less PROJECT uuid inline and re-ranks it in position (O12 analog — `project` inherits from `to do` in the sdef), and preserves `start`/`startBucket=0`/area+project FKs on every row. So for the single tomorrow case there is a DIRECT surface (no scratch-park needed), the counterpart to the `upcoming-day` protocol; for any other future day the scratch-park stands (only `list "Tomorrow"` reaches next-day; date-shaped `list` specifiers don't exist, −1728). See [ordfin2-followups.md](../lab/ordfin2-followups.md) Arm 2.
48. **The DATED BOUNCE — cross-container day order for an ARBITRARY future day, PROJECT rows and headed children included** (DAYBNC, 2026-07-31; **WIRED as reorder scope `day`**, `src/write/reorder.ts` — supersedes + replaces the scratch-park `loose-day`/`area-day`/`upcoming-day` compounds and the `heading-day` round-trip, entries 41/43/46, all deleted) — for a future Upcoming day beyond tomorrow (no `list` specifier reaches it, −1728), re-whening a scheduled row OUT to D+1 and BACK (`update?id=<u>&when=<D+1>` then `when=<D>`; projects via `update-project?when=`) FRONT-inserts it at the day's GLOBAL `todayIndex` minimum — the axis is SHARED across loose to-dos, headed same-day children, AND area-less scheduled PROJECT rows (a project row landed below the loose min, confirming one cross-container axis). So a REVERSE-target-order bounce places the whole cross-container day group in exact order (a scrambled 6-row target — 4 to-dos + 2 project rows — landed byte-exactly). NON-DESTRUCTIVE: bare `when=<date>` PRESERVES `reminderTime`+`deadline` (unlike `when=evening`, §9n; confirms §2e/R21) and re-when PRESERVES the `heading` FK (unlike the container-day reorder which rips it, §9k). Cost 2N dispatches; stage via D+1 (not `when=today` — pollutes Today; never a repeating template — §1 crash). This is the days-beyond-tomorrow counterpart to the `tomorrow` one-call surface, and the ONLY day scope besides `tomorrow` that can place a REQUESTED project-row movee. Closes the last app-default ordering cell. See [sit4-daybounce-eveord-axdrag4.md](../lab/sit4-daybounce-eveord-axdrag4.md) DAYBNC.
49. **A project's evening order rides a reverse-order `when=evening` bounce — the project when=evening leg FRONT-inserts** (EVEORD, 2026-07-31; protocol-proven, NOT wired) — `update-project?when=today`→`when=evening` re-enters the evening group at its `todayIndex` MINIMUM (below to-dos AND projects on the one shared evening axis), ×3 repeatable — NOT the mid-pack landing of the daytime `when=today` leg (sit3 EVEPROJ). So a reverse-target-order evening bounce accepting PROJECT movees (interleave to-do `update?when=today→evening` and project `update-project?when=today→evening`) is deterministic — the project counterpart to the loose/child evening bounce. Inherits the §9n/R07 reminder-loss caveat (`when=evening` clears `reminderTime`, re-confirmed: a `today@09:00` reminder → NULL after `update?when=evening`). See [sit4-daybounce-eveord-axdrag4.md](../lab/sit4-daybounce-eveord-axdrag4.md) EVEORD.
50. **The experimental-reorder CONTINGENCY backups — a proven non-experimental twin for every private-surface-only reorder scope** (SIT7, 2026-07-31; protocol-proven, NOT wired) — re-entry geometry follows destination CONTAINMENT (LOOSE-like buckets FRONT-insert → dispatch in reverse target order; CONTAINERS BACK-insert → dispatch in forward target order), every move leg flag-preserving: **`inbox`** = park each row to a scratch project (`update?list-id=<scratch>`) then re-enter `move to do id X to list "Inbox"` in reverse target order — front-inserts at the inbox `index` min AND RESTORES `start=0` (a same-list `move … to "Inbox"` is a NO-OP, the inbox analog of §9l); **`someday`** loose to-dos AND area-less someday projects = the URL `anytime↔someday` `when=` bounce (`update`/`update-project`) in reverse target order — front-inserts state-preservingly (`start=2` kept), the project front-insert notably differing from the native anchor-stack's project-DESCEND (ORD-3); **`project`** unheaded children = park to a scratch project + re-home `list-id=<P>` in forward target order — BACK-inserts (a project root is a container like a heading, the SIT6 HEADMOVE law extended), FLAG-SAFE, with the `when=` bounce a de-Today-hazardous plain alternative that also back-inserts; **`area`** members + within-area **`project`** order (O14) = park to a scratch project/area + re-home `list-id=`/`area-id=<container>` in reverse target order — FRONT-inserts (an area is loose-like), FLAG-SAFE + area FK preserved. Surprise: the URL `when=` bounce is NOT index-inert for an area-direct ANYTIME member — the `when=anytime` leg front-inserts it on the area axis (area FK kept), unlike the `things:///json` surface which freezes it (§9i(c)). Wire these only if the runtime canary `sdefDeclaresPrivateReorder()` ever fails. See [sit7-backup-laws.md](../lab/sit7-backup-laws.md).
51. **The DEADLINE-CYCLE — a clean, `startDate`-preserving `todayIndex` reorder for a same-deadline forecast day-block** (DLBNC, 2026-08-03; **WIRED 2026-08-04 as the `day` reorder scope's forecast leg**, `src/write/reorder.ts` + `src/write/pre-state.ts` + `src/write/move.ts` planner — deadline-forecast to-dos are first-class `day`-scope members, INTERLEAVED with scheduled rows on the one `todayIndex` axis via a unified reverse-target dispatch with per-row-class legs; mixed interleave CERTIFIED on the golden, o-suite O31/O32, run `dlbnc-cert-20260804-004619`, #383) — the Upcoming day block orders on `todayIndex` (maintainer-established; DLBNC-1d GUI-confirmed), and a deadline-forecast row (`start=2`, `startDate=NULL`, future `deadline`) is a full `todayIndex`-axis member of its ROOT Upcoming block despite `startDate=NULL` (§9o). Its block `todayIndex` is cleanly writable via a **URL `deadline=` clear + `deadline=<same>` re-set**: each re-set FRONT-inserts the row at the block's global `todayIndex` minimum (`min − delta`, deterministic — §9q), so cycling the rows in **REVERSE target order** lands the exact block `todayIndex` order while `index`/`start=2`/`startDate=NULL`/`deadline`/`todayIndexReferenceDate`/heading+project FK stay **byte-identical** (a scrambled 3-row block landed target with the someday-bucket `index` integers unchanged; DLBNC-2/3). This is the `startDate`-preserving `todayIndex` writer UPCDL found absent among the hidden-list specifiers (each of which stamps a `startDate`, §9p) — cost 2N public-URL legs, NO experimental/private surface, reminder-safe (`deadline=` is orthogonal to `reminderTime`), heading-rip-safe (`deadline=` is not a containment write). A maintainer-proposed **compound** twin (when=-cycle to front-insert `todayIndex` — DLBNC-5 confirms a `when=<date>`→`someday` round-trip front-inserts both axes on a multi-row fixture, superseding UPCDL-7 Q2 — then ONE `project id` reorder to REPAIR the clobbered `index` to the captured order; DLBNC-6) also lands the target but is strictly inferior (2N+1 legs, private reorder surface for the repair, `index` order-only-restored with integers changed, `when=` legs reminder-lossy §9n/R07). The GUI drag (maintainer-observed within-block re-rank, like recurring templates) writes the same `todayIndex`-only shape the deadline-cycle reproduces (direct byte-capture is a synthetic-events-gated VNC residual). WIRED as the deadline-cycle (the compound is a documented fallback only). The one wiring claim NOT directly in DLBNC — that scheduled `when=` bounce legs and forecast `deadline=` cycle legs, both front-inserting at the block's global min, INTERLEAVE exactly under one reverse-target pass — was certified separately (o-suite O32 mixed interleave, run `dlbnc-cert-20260804-004619`). **Extends to forecast PROJECT rows (PROJDL, 2026-08-03, pending wiring):** a someday PROJECT (type=1, `start=2`, `startDate=NULL`, future deadline) is a distinct member of the SAME block `todayIndex` axis, and `update-project?deadline=` clear+re-set front-inserts it identically + cleanly (`index`/`start`/`startDate`/area-FK/tags byte-preserved, PROJSTAR-safe), mixable with to-dos in one block (a mixed 3-row protocol landed the exact target, someday `index` byte-identical) — so `move.ts` `forecastDeadlineDay`'s `type===0` gate can widen to `type IN (0,1)` in a follow-up (the day leg op is already per-type). See [dlbnc-deadline-cycle.md](../lab/dlbnc-deadline-cycle.md) and [tmpldl-projdl-deadline-cycle.md](../lab/tmpldl-projdl-deadline-cycle.md).
52. **A repeating TEMPLATE's block `todayIndex` IS headlessly writable — `_private_experimental_ reorder to dos in list "Upcoming" with ids {<template>}` FRONT-INSERTS it cleanly** (TDRAG-3, 2026-08-03, golden-v2/3.22.12; protocol candidate, NOT wired) — overturning TMPLIDX's "a template's `todayIndex` is unreachable by every surface" (ORDFIN1 Arm 1, which tested only the repeat-series levers + a fail-closed GUI drag, and TMPLDL, which added the `deadline=` no-op). A single-element `list "Upcoming"` reorder wire carrying only the template uuid writes the template row's `todayIndex` (0 → block-min front-insert) with `rt1_recurrenceRule`, `start`, `startDate`, `todayIndexReferenceDate`, `index`, and **`userModificationDate` all byte-identical**, no reparent, no crash, and no instance contamination (a subsequent series advance spawns a clean `tIdx=0` instance). A reverse-target dispatch of one single-id front-insert per template lands an exact block order (TMPLSORT-1). **CORRECTION (TMPLSORT-2, 2026-08-04):** the earlier "FRONT-INSERT-ONLY: a MIXED wire `{scheduledA, template, scheduledB}` is a full no-op" was a `with ids` LIST-literal `-1700` artifact (a multi-item AppleScript list literal is rejected at the AppleEvent boundary; the app never ran — the shipped op sends comma-joined TEXT). A VALID comma-text multi-id wire treats the template as a FIRST-CLASS member: `list "Tomorrow" with ids "TM2,TEMPLATE,TM1"` lands the exact sent order with the template at an arbitrary mid-slot, `list "Upcoming"` writes the template's `todayIndex` alongside the co-wired rows, and a wire whose template is not a member of the specifier's day skips only that id — so a template group is sortable via a single multi-id call too, not only a per-row single-id dispatch. **HAZARD:** the `project id` specifier with a template id REPARENTS the template into the project (`project` NULL→P + `umd` bump), NOT a placement — never carry a template id on the project-id reorder. Gated behind `allow-experimental` + the sdef canary like every private reorder. Characterise-only; wiring is a maintainer-ratification follow-up. See [tdrag-ax-residuals.md](../lab/tdrag-ax-residuals.md) TDRAG-3.
53. **The GUI projection DRAG writes a template's (and a forecast row's) `todayIndex` — ONE row, ONE byte, `umd`-SILENT, and it PERSISTS** (TDRAG-1/2, 2026-08-03, golden-v2/3.22.12; app-native behavior, the byte-capture of the maintainer's prod observation) — dragging a repeating template's Upcoming projection within its day block writes the TEMPLATE row's `todayIndex` and nothing else (full-DB before/after diff = one row, one column): dropped at the top it front-inserts at the block min, dropped mid-block it lands an INTERPOLATED value between the two neighbours; `rt1`/`tiRef`/`index`/`start`/`startDate`/`userModificationDate` are all byte-identical, and it SURVIVES quit+relaunch (the app re-reads the byte and re-renders the dragged order). The forecast within-block drag is identically `todayIndex`-only with no `startDate` stamp (byte-faithful to the wired deadline-cycle); the forecast BETWEEN-block drag is instead a reschedule (stamps `startDate`=destination day + updates `tiRef`, keeps the deadline, bumps `umd`). Cross-cutting: **a native/GUI `todayIndex`/`index` re-rank is `userModificationDate`-SILENT** (the drag AND the private reorder placement writes don't bump `umd`; only URL field-writes and the containment reparent do) — a watcher diffing on `umd` misses a pure placement re-rank. VNC-drag recipe caveat: use explicit `move` waypoints (NOT vncdo's `drag` command, which drops the trailing mouseup on py3.14/Twisted) and end the session with a `capture` to flush the release. See [tdrag-ax-residuals.md](../lab/tdrag-ax-residuals.md) TDRAG-1/2.
54. **`list "Tomorrow"` reaches a repeating PROJECT TEMPLATE's projection — the headless writer the Upcoming/Later-Projects specifiers lack** (PTMPL-B5, 2026-08-04, golden-v2/3.22.12; protocol candidate, NOT wired) — a repeating-project-template projection is a first-class, drag-sortable `todayIndex` row of its Upcoming day block (eyes-on; §9t), and when its projection day == tomorrow the private `_private_experimental_ reorder to dos in list "Tomorrow" with ids "…"` writes it: single-id front-inserts the template's `todayIndex` (`umd`-silent, `rt1`/`start`/`startDate`/`tiRef`/`index` byte-identical, no reparent, no crash, no instance contamination), and a multi-id member wire (`"TM2,PROJTMPL,TM1"`) lands the EXACT sent order with the template at its arbitrary mid-slot — extending entry 47 (`list "Tomorrow"` accepts project rows) and entry 52 (to-do-template first-class membership) to repeating PROJECT templates. It is inert to single-id `list "Upcoming"`/`list "Today"`, the `area id` specifier, and `list "Later Projects"` (a template id poisons that call) — those re-date wired rows and a template's date is immutable. So #391's blanket "project templates are INERT" is superseded → surface-specific, mirroring the to-do template. The GUI drag reaches the same byte on ANY projection day (entry 53). See [ptmpl-project-templates.md](../lab/ptmpl-project-templates.md) PTMPL-A/B.
55. **`list "Later Projects"` (≡ `list id "later-projects"`) is a native `todayIndex` sorter for the later-projects population** (PTMPL-B4, 2026-08-04, golden-v2/3.22.12; characterized, NOT wired) — the AppleScript reorder specifier (URL-`show`-invisible, entry 41) re-ranks area-less someday/future-scheduled projects on `todayIndex` in one call: it writes `todayIndex` cleanly + `umd`-silent on an already-future-scheduled area-less project (a clean sorter for THAT subset) but DESTRUCTIVELY re-dates a date-less SOMEDAY project into the projection-day block (stamps `startDate` + bumps `umd`, §9m), so it is NOT the state-preserving surface for someday projects (that stays `list "Someday"` on the `index` axis). A repeating-project-template id POISONS the whole call to a full no-op (single-id or as a wire member). The `list "Upcoming"` and `list "Tomorrow"` day-scope specifiers implement OPPOSITE placement laws — `list "Upcoming"` multi-id front-inserts each wired id in wire order above the pre-existing-min anchor (landing reverse-of-wire) and §9g-re-dates off-day rows, while `list "Tomorrow"` lands the exact sent order with no re-date (PTMPL-C, the law that explains RP-MIXED). See [ptmpl-project-templates.md](../lab/ptmpl-project-templates.md) PTMPL-B4/C.

56. **A PROJECT row's daytime Today order rides the same `when=` bounce a to-do's does — the `when=today` placement leg FRONT-INSERTS** (VMRES1, 2026-08-23, golden-v4 / Things 3.23; **WIRED** — `bounceSpecOf("today")` `legOp: "per-type"`, `src/write/reorder.ts`) — `update-project?when=today` re-enters the day cohort at its GLOBAL `todayIndex` MINIMUM, measured over five separate re-entries across both spellings (`when=today` and the dated `when=<ISO today>`) and both away-legs (`anytime` and the shipped `evening`); the gap the app leaves varies but the ORDER is deterministic, so a reverse-target-order bounce places a mixed to-do + project Today group exactly (interleaved 5-row target landed 2/2, and the shipped `things reorder --in today` landed it end to end). This **falsifies the ORD-12 / SIT3 EVEPROJ "a project's daytime `when=today` lands mid-pack" caveat** on 3.23 — the caveat the project refusal rested on ([gv4-323-certification](../lab/gv4-323-certification.md) §1.5) — and makes the daytime Today axis the fourth mixed-kind bounce alongside `evening` (EVEORD), `day` (DAYBNC) and `tomorrow`. Three negatives banked with it, so nobody re-derives them: a `when=today` re-issue on a row ALREADY in Today is a **NO-OP** (the row must leave the day first — which is what the away leg is for); an area park+re-home MOVE round-trip (the AREABACK/PROJROOT shape) leaves `todayIndex` **byte-unchanged** (it re-ranks the area `index` axis only); and the DLBNC deadline-cycle is **inert** for a scheduled project's Today rank. See [vmres1-residuals.md](../lab/vmres1-residuals.md) §3.

65. **A dialog can be dismissed WITHOUT owning the screen — press its own Cancel button** (FGRD1, 2026-08-27, golden-v4/Things 3.23). The cleanup path every GUI driver reaches for is Escape, and Escape is a keystroke: it goes to whatever application is frontmost, so an abort while another app owns the screen sends it into that app's window and leaves the Things dialog standing (measured — the field incident's sheet was still open hours later). `click button "Cancel" of <the sheet, or the detached AXUnknown editor window>` is addressed at an ELEMENT, so System Events delivers it to Things whether or not Things is in front: with **Finder frontmost throughout**, a failed drive's cleanup closed the Repeat dialog and a fresh census confirmed `sheetKind: none` — the user's focus never moved. This is the first rung of the shipped cleanup ladder; Escape (guarded on a frontmost assertion) and the app-level `close window 1` + `reopen` (SESSGATE, works AX-blind) are the rungs below it.

66. **The whole window/focus state is readable in one prompt-free System Events shape — including WHICH dialog is open, semantically** (FGRD1, 2026-08-27, golden-v4/Things 3.23). `first application process whose frontmost is true` + its `AXFocusedUIElement`'s role, plus a role-count census of the attached sheet (or the detached `AXUnknown` editor), answers "is a dialog open, which one, who owns the keyboard" with **zero clicks, zero keystrokes and zero state change**, identically in all four quadrants of (dialog none|repeat × frontmost Things|other). The Repeat sheet is identified STRUCTURALLY — exactly two checkboxes, one direct pop-up, two buttons, one group carrying its own field, no direct text field (`cb:2 pu:1 bt:2 gp:1 tf:0`, the RDLG1 §2.1 census) — so the identification survives a localized app, and the Move… picker by its own `MovePopUpDialog-` identifier. A frontmost surface that will not answer at all is the signature of a SECURE SYSTEM MODAL (macOS exposes no tree for one), which is reportable as exactly that rather than guessable. Shipped as `things rescue status` / `things doctor --ui-state`, and as the pre-step and pre-cleanup oracle inside the ui vector.

## Reads / environment

19. **`uriSchemeEnabled` on disk** (21b) — the group-container preferences plist carries the true "Enable Things URLs" state (the auth token does NOT track it); powers the availability layer + failure attribution.
20. **The Today comparator** (`startBucket, todayIndexReferenceDate DESC, todayIndex, uuid` + the deadline-pull membership rule with `deadlineSuppressionDate` re-arming) — UI-oracle-derived, exact 393/393 live reconciliation (today-order-research).
21. **Hidden lists** `list id "tomorrow"` and `list id "later-projects"` exist in the sdef enumeration (P9a). **PROBED as reorder specifiers (HEADCERT residuals, 2026-07-17):** the private `_private_experimental_ reorder to dos in` command **ACCEPTS both**, and **both map to `todayIndex`** (NOT `"index"`) — `list "Tomorrow"` (next-day-scheduled to-dos, startBucket 0) reorders exactly like `list "Today"`, and `list "Later Projects"` (**area-LESS** someday projects) rewrites `todayIndex` too. Separately, area'd someday projects live in `list "Someday"` (not Later Projects) and reorder by `"index"`. Evidence-only, nothing wired; see [o-suite-results.md](../lab/o-suite-results.md) "Hidden-list & area'd-someday residuals". **REFINED (ORDFIN2 Arm 3, 2026-07-31):** the `list id "later-projects"` reorder is DESTRUCTIVE for the someday case — it re-ranks `todayIndex` forward in one call BUT also stamps a `startDate` (first upcoming day) onto date-less someday projects, converting them to scheduled (§9m — the §9g mechanism). An already-future-scheduled member on a different day is left inert. So it is NOT a clean someday-order surface; the membership (area-less someday OR future-scheduled) is confirmed and also surfaces in the Later Projects SIDEBAR VIEW (maintainer-observed GUI). See [ordfin2-followups.md](../lab/ordfin2-followups.md) Arm 3.
24. **The sidebar area sort law: `TMArea."index" ASC, uuid ASC`** (ORDFIN2 Arm 3ax, 2026-07-31) — the sidebar orders areas by the `index` column ascending (sparse/negative, lower sorts higher), and **breaks `index` ties by uuid lexicographic ASC** (proven by forcing an `index=0` batch tie and watching a renumber materialize it in uuid order Q<U<V, not creation/rowid order; a brand-new low-index area also sorts BETWEEN older areas by `index`, not by recency). Separate `make new area` calls assign distinct sparse indexes; a single AppleScript batch leaves the new areas tied at `index=0`. Consequence for the ui-vector: duplicate-titled areas ARE positionally disambiguable — sort the duplicate-titled uuids by `(index, uuid)` ASC, map the Nth same-title AX sidebar row → the Nth uuid, then DB-assert the intended uuid's `index` moved after a drag (self-invert on mismatch). A genuine one-area drag confirmed Nth-AX-row ↔ Nth-DB-row-by-`index`. This refines AXDRAG2-d ("ties break the DB→row mapping" — they do not). Law + feasibility only; the `area.reorder` driver's duplicate-title refusal is unchanged. See [ordfin2-followups.md](../lab/ordfin2-followups.md) Arm 3ax.
22. **The Upcoming deadline-forecast rule** (UPC1) — a future deadline forecasts a `startDate IS NULL AND start IN (1,2)` row (anytime/someday to-dos AND someday projects) into GUI Upcoming under the **deadline** date, unless nag-suppressed; **Inbox (start=0) is excluded**. Scheduled rows still group by when-date; a when+deadline row is one row under its when-date. Group/sort key `COALESCE(startDate, deadline)`. Mirrors the Today deadline-pull one step earlier — the CLI `upcomingView` fix (upcoming-research.md).
23. **`deadlineSuppressionDate` is set by rescheduling an overdue item to Someday/Anytime** (UPC1, a WRITE path via URL `update`/`update-project when=someday|anytime`) — the only way to stamp the suppression the todayView predicate reads; no dismiss command exists (oddities §8e). Rescheduling to a future DATE relies on `startDate` instead (no stamp).

28. **Scroll-wheel events DELIVER while a synthetic drag is held — and mid-drag AX reads stay fresh** (AXDRAG2-a, 2026-07-15). With a HID mouse-down held on a sidebar row (plus periodic `kCGEventLeftMouseDragged` keep-alives), line-unit `CGEventCreateScrollWheelEvent` posts scroll the list underneath the held item in either direction, and per-tick `AXPosition` reads of any row return the LIVE post-scroll frame (~30px/3-line tick; the scrollbar `AXValue` fraction moves monotonically). There is an ~8px settle drift after the last tick — wait for two stable reads before aiming a drop. This makes a single-gesture "grab, scroll the destination into view live, drop at the live-resolved boundary" possible (built as the `area.reorder-sidebar` ladder's rung 2) — but long held travel (≳1.5 viewports) can corrupt the sidebar's AX mirror for the rest of the session (oddities §9), so the production ladder keeps it disabled and uses pre-scroll + multi-hop instead. See [axdrag2-reorder-certification.md](../lab/axdrag2-reorder-certification.md).

38. **Cross-project heading move via the heading row's `…` → `Move…` MENU** (HEADXPROJ, 2026-07-27) — the ONLY working spelling for relocating a heading (with its children) to another project (URL/AppleScript/Shortcuts all dead). ui-vector, AX-granted: HID-click the heading `…` button (`AXUnknown desc="More. <title>"`, `AXPress`-inert → click the AX-resolved frame center; the golden is 2× Retina so VNC pixels = AX points ×2) → popover `Archive/Move…/Convert to Project…/Delete` → HID-click `Move…` → a searchable project picker (`Move` filter field + list) → type the destination name → Return. DB delta = a single project-FK rewrite on the heading row (index/area unchanged, index preserved on an empty dest); children's `heading` FK + `project`(NULL) + `index` unchanged (they follow via the heading FK). Deterministic, keyboard-driveable, no drag, no §9 AX-mirror fragility. Load-bearing enabler: the heading `…` button's `AXDescription` CARRIES the heading title (headings are AX-identifiable where to-do content rows are not). Green-lights a future `project.move-heading-to-project`. See [reordgaps-results.md](../lab/reordgaps-results.md) HEADXPROJ.

39. **A whole back-insert BOUNCE collapses to ONE `things:///json` array** (BOUNCEJSON, 2026-07-27) — for the back-insert reorder classes (heading children; project unheaded-anytime children) a single `things:///json` update-array carrying `[{when:someday},{when:anytime}]` per item IN TARGET ORDER applies in exact array order and reproduces the sequential bounce's `index` order (scrambled-target confirmations landed exactly, heading FK 4/4), collapsing 2N URL dispatches → 1 (~7× faster: 30-op array 242 ms vs 30 sequential 1763 ms). Constraints: pre-validate every id (a single unresolvable ref FULL-ABORTS the batch — validate-first, + error modal); elements land as distinct sub-transactions (per-element `userModificationDate`) so they are independently verifiable. Does NOT work for FRONT-insert / someday-placement classes (`json when=someday` and area-direct members are index-inert — oddities §9i); those stay on the sequential URL bounce. Feeds `src/write/reorder.ts`. See [reordgaps-results.md](../lab/reordgaps-results.md) BOUNCEJSON.
40. **`id of selected to dos` resolves a SHOWN PROJECT to its uuid** (SL1, 2026-07-29) — Things' AppleScript `selected to dos` accessor returns the uuid (count 1) not only for a selected to-do but for a **project** navigated to via `things:///show?id=<project>` (the app's selection model treats a shown project as the selected "to do" for this property). This is a clean, coordinate-free **uuid oracle for project navigation** — it read back which project instance the GUI's "Show Latest" selected in the SL1 project replication, where list rows aren't AX-addressable and the menu-bar Copy Link path is absent (`Items ▸ Share…` has no submenu, `-1719`). Verified against a known `things:///show?id=` selection before use. See [sl1-show-latest.md](../lab/sl1-show-latest.md).
41. **`things:///show?id=tomorrow` opens the Tomorrow view** (SIT3 LATERLINK, 2026-07-31) — the built-in **Tomorrow** virtual list (which has no sidebar row) IS navigable by URL: `show?id=tomorrow` opens it in the main pane (calendar icon, tomorrow's scheduled items). Caveat and boundary: the `show`-URL id vocabulary is **narrower** than the AppleScript `list id` vocabulary — `show?id=later-projects` **errors** ("Cannot show the list with ID 'later-projects' because it does not exist") even though `list id "later-projects"` is a valid P9a specifier. So a `list id` is NOT guaranteed to be `show`-navigable; `tomorrow` is valid on both surfaces, `later-projects` only in AppleScript. See [sit3-arrival-evening-lists.md](../lab/sit3-arrival-evening-lists.md) LATERLINK.
42. **`things:///update-project?id=<u>&when=evening` sets a PROJECT to This-Evening in a single leg** (SIT3 EVEPROJ, 2026-07-31) — one dispatch writes the project's This-Evening shape (`startBucket=1`, `startDate=today`, `start=1`, `type=1` preserved), and it renders in Today's "This Evening" section. Works for area-less AND area'd projects — the **area FK is preserved** (an area'd project can be This-Evening). Unlike the to-do evening BOUNCE (two legs `when=today`→`when=evening`), the project needs no bounce to reach evening; the bounce still works and preserves every FK/schedule byte if used. Two caveats shared with to-dos: a project's `when=today` leg does not front-insert to the global Today minimum (a to-do's does), and a native `list "Today"` reorder DE-EVENS the project (O03 generalizes). See [sit3-arrival-evening-lists.md](../lab/sit3-arrival-evening-lists.md) EVEPROJ.

43. **The app's effective timezone is pinnable PER-PROCESS via the `TZ` env, with no system-zone change** (TIMEZ2 T2-ENV, 2026-08-05, golden-v2/3.22.12; probe-only, NOT wired) — launching Things with `TZ=<zone> open -a Things3` (or direct-exec `TZ=<zone> /Applications/Things3.app/Contents/MacOS/Things3`) makes the app adopt `<zone>` as its effective "today" (Foundation's default-zone honors `TZ` on Darwin) while the SYSTEM zone stays put — byte-proven: under `TZ=Pacific/Kiritimati` (UTC+14) a fresh launch stamped `when=today` → `startDate` = the pinned-zone local day (07-06), with `systemsetup -gettimezone` still `America/New_York` throughout. This is the enabler for the **dated-evening workaround** (TIMEZ2): quit → pin-ahead-launch → `when=evening` (lands `startBucket=1` on the shifted day, the only known dated-evening write) → quit → relaunch. Two constraints: (1) **quit first** — `TZ=… open` on an already-running app is a no-op activation (the env only applies to a fresh spawn); (2) **`launchctl setenv TZ` does NOT work** from an ssh session (its launchd domain ≠ the Aqua session LaunchServices spawns the GUI app into). Side-effect bill of the shifted launch is bounded (1 day-cursor blob + early materialization of any repeat template due that day, umd-silent, dedupe-safe — §9x / RD-11). See [timez2-pinned-zone-workaround.md](../lab/timez2-pinned-zone-workaround.md) T2-ENV/T2-SIDEFX.
44. **`set modification date of <to do|project> id X` is a writable, surgical, durable, reversible `userModificationDate` setter** (TAGMOD-T5, 2026-08-12, golden-v2/3.22.12; probe-only, NOT wired) — the AppleScript `modification date` property (declared writable in the sdef, RC Avenue C) genuinely writes the `userModificationDate` column and NOTHING else: a full-row byte-diff on to-dos, projects, and logged rows shows every other column byte-identical. The written value **sticks across an app relaunch** (never re-stamped to now) and survives unrelated activity; it accepts arbitrary past AND future instants. The only limit is second-granularity (the AppleScript `date` type has no sub-second, so a restore lands on the floored second — always ≤ the original, the safe direction for a `changes --since` query). This is the enabler for a **capture-and-restore** of the `changes`/sync tripwire: read umd → make a genuine edit (which bumps umd, e.g. a tag apply — TAGMOD-T1) → `set modification date` back to the captured second, leaving only the intended change with umd neutralized. The URL scheme has NO modification-date field (URL `completion-date`/`creation-date` are no-ops, P4c/oddity 2g — the umd analogue is dead too), so AppleScript is the sole writer. **SYNC-UNSAFE until SYNC2 unblocks** (umd is a merge discriminator; see the TAGMOD sync caveat). Green-lights a `--preserve-modified` bulk-apply option (up-next). See [tagmod-tag-area-umd.md](../lab/tagmod-tag-area-umd.md) T5.
45. **Raw AppleScript `delete to do id <template>` trashes a repeating TO-DO template** (TRSHREP R5, 2026-08-12, golden-v2/3.22.12) — the shipped CLI `todo.delete` (and `todo.move`/`todo.restore`) REFUSE a repeating template with `H-REPEAT-SCHEDULE` (WG-8), but the raw AppleScript `delete to do id "<template>"` surface is UNGUARDED and DOES trash it (`trashed 0→1`). The resulting shape matches the project-template row-trash (CLONE C3) exactly: the fixed `rt1_nextInstanceStartDate` cursor is CLEARED to None (generation stops) and the live instance is left ORPHANED (`trashed=0`, `rt1_repeatingTemplate` FK dangling to the now-trashed template — not cascade-trashed). So the two template kinds share one raw-trash fate — closing CLONE's "to-do-template raw-app trash fate unprobed" residual. NOT a working RESTORE path: the trashed template is neither `todo restore`-able (guard) nor moveable, and a subsequent `Empty Trash` hard-deletes it and repairs the orphaned instance's dangling FK to None (TRSHREP R7). This is app-behavior/feasibility evidence, not a wired op — the shipped guard deliberately blocks the CLI from reaching it. See [trshrep-trashed-repeater-spawn.md](../lab/trshrep-trashed-repeater-spawn.md) R5.
46. **A real HID click into Things' non-AX-exposed list is postable headlessly via Python `ctypes` → CoreGraphics `CGEventPost`** (SERDEL, 2026-08-13, golden-v2/3.22.12; lab-tooling, not a shipped path) — Things' main window CONTENT is not AX-exposed (only the menu bar is, AXVM1): list rows enumerate as `AXRow`s of `table 1 of scroll area 1 of window "<view>"` but their cells are `missing value`, and `set selected of row N to true` is visually IGNORED by the custom list. System Events `click at {x,y}` posts a click but Things' rows don't honour it for selection; `cliclick`/pyobjc/AppleScript-ObjC-`CGEventPost` are absent or fail to coerce the opaque CGEvent pointer. The working primitive is **Python 3.9 `ctypes`** loading `CoreGraphics` and calling `CGEventCreateMouseEvent(NULL, type, CGPoint, button)` + `CGEventPost(kCGHIDEventTap=0, ev)` (left down/up, right down/up) — a genuine HID event the custom view accepts (verified: a click selected the target project, `selected to dos` read it back). Coordinates come from `position/size of (first row … whose selected is true)` after a `things:///show?id=<uuid>` selection (point-space, retina-agnostic); a second spaced single-click + Return opens/expands the inline card; `screencapture` captures it (Screen-Recording granted). This is the reusable eyes-on driver for any campaign needing to select/open Things list rows that AX cannot address. See [serdel-series-removal.md](../lab/serdel-series-removal.md) (AX-driving toolkit).
47. **The NATIVE (destructive) make-repeating is reachable via `things batch` — the only way to observe raw app source-fate now that the standalone CLI wraps it** (SRCFATE, 2026-08-14, golden-v2/3.22.12) — since promote-via-clone landed (2026-08-13), the standalone `todo/project make-repeating` CLI clones the source + trashes the original + promotes the clone, so the native source-fate lands on a disposable clone the caller never sees. A **`things batch`** line — `{"op":"todo.make-repeating"|"project.make-repeating","params":{"uuid":…,"frequency":"daily","interval":1},"options":{"dangerouslyDriveGui":true}}` — dispatches the **native destructive GUI promote through the pipeline** (batch.ts: "a batch make-repeating is the destructive native promote, not promote-via-clone"), letting a probe observe the app's real source-fate on a source it owns. The `repeating.replacedUuid` field is the authoritative source-fate signal: `null` = source PRESERVED (relinked as instance), a uuid = source DELETED. Constraint: a `project.make-repeating` native drive REFUSES an **area-less Anytime project** (`H-PROJECT-REPEAT` — no selectable row in Anytime, it renders as a header, UIC4-d); place the project in an **area** (or Someday) to give the pure-AX drive a selectable row (area is irrelevant to source-fate per RSIM-R). Same pattern reaches other batch-registered ui ops missing a CLI drive-gui flag (e.g. `project.dissolve-heading` / `project.move-heading-to-project`, which are otherwise CLI-uninvokable — up-next). See [srcfate-reconciliation-sweep.md](../lab/srcfate-reconciliation-sweep.md).
48. **The Repeat dialog's "Next:" first-occurrence field is drivable + honored — the fixed-repeat first occurrence CAN be set headlessly** (ANCH2, 2026-08-15, golden-v2/3.22.12 — issue #476, corrects ANCH1) — the Repeat dialog's cadence group contains a **"Next:" `AXDateTimeArea`** (id `_NS:140`, drifts) whose default is the today-anchored next match but which accepts an `AXValue` NSDate write: setting it lands the series' first occurrence on the typed date VERBATIM (`rt1_instanceCreationStartDate` = `sr` = the typed date; on-rule dates also set `ia`/cursor, off-rule dates leave `ia`/cursor at the first rule-aligned occurrence). Frequency-agnostic (weekly/monthly/yearly all honored). The dialog can show up to **three** `AXDateTimeArea`s at once (Ends-on-date `@y≈360`, Next `@y≈338`, reminder `@y≈408`, the last the only time-bearing one), so the control must be picked DETERMINISTICALLY — reminder = the time-bearing area; among midnight pickers Next = top row, Ends = bottom — NOT "the first area by role" (that ambiguity was the §8v collapse + the UIC6-g reminder mis-verdict, both retracted). The same field on an EXISTING template via `Items ▸ Repeat ▸ Reschedule…` corrects a series' phase post-creation (identity preserved). Wired: the promote verbs drive Next with the item's `when`/`--when`; `set-datetime` gains `next`/`ends`/`reminder` targeting. See [anch2-next-field.md](../lab/anch2-next-field.md).
49. **`reschedule-repeat` on a deadlined yearly template that HAS a pending materialized instance is drivable + HONORED for all three shapes** (RSPA1, 2026-08-19, golden-v3/3.22.14 — the residual YANCH1/DACON1/DBLSPAWN1 all deferred) — create a deadlined yearly series with the first occurrence tomorrow (fixed `add-repeating`, deadline mapped to the rule), advance +1 day to MATERIALIZE the current-occurrence instance, then reschedule the template (reveal binds the template row; the materialized instance makes it selectable). **(a)** an on-rule `--when`-only + deadline reschedule COMMITS in ~7s with the deliverable-1 anchor-derivation fix — the derived DUE anchor lands (`of=[{dy=29,mo=9}]`=Oct 30), start back-shifts to `--when` (`sr=2028-10-16`), `ts=-14` holds, cursor sane, pending instance untouched (the pre-fix drive drove Next but NOT the anchor pop-ups and the app discarded the whole commit — a 120s zero-movement verify; driving the anchor fixes it). **(b)** an off-rule Next (explicit anchor ≠ `--when`) is HONORED, not discarded/snapped — `ok` in ~6s, anchor `m10d16` lands, cursor `next=2029-10-02` = the rule-correct next aligned start (this IS the DACON1 live-host "2029 skip", normal-by-design). **(c)** a rule-only reschedule preserves anchor/deadline/cursor with the instance untouched (RRD1). No double-book on the re-anchored cursor (`icCount=1`, cursor at a distinct future date — the opposite of the DBLSPAWN `icCount=0` collision). Residual RSPA1-D: the off-rule disclosure is blind to a PRESERVED deadline (mis-states the appear date; data correct). See [rspa1-reschedule-pending.md](../lab/rspa1-reschedule-pending.md).

49. **The Things 3.23 "Next:" occurrence pop-up is drivable by parsing its own item titles — and it is the ONLY first-occurrence surface there** (RDLG2, 2026-08-22, golden-v4/Things 3.23) — 3.23 replaces #48's `AXDateTimeArea` with an `AXPopUpButton` listing `Today`, the rule's next occurrences, and a `More…` item whose submenu carries the next hundred, cascading ~10 years out (every occurrence item tagged `AXIdentifier = nextDateOptionAction:`, the separator `_popUpItemAction:`). Working path: open the pop-up (the self-healing open-click), PARSE each item title to a date (`date "Sun, Jul 12, 2026"`, retried after stripping the leading weekday token — the titles are localized, so they are read, never rebuilt), click the match, then read the pop-up's value back and require it to equal the clicked title; descend the trailing `More…` submenu when the level misses. Consequence, and the reason this is a narrowing rather than a win: the menu offers ONLY on-rule dates, so #48's off-rule first occurrence is **inexpressible on 3.23** ([things-app-oddities.md](../things-app-oddities.md) §11) and the drive refuses fail-closed. See [rdlg2-323-recipe-cert.md](../lab/rdlg2-323-recipe-cert.md) §1.1/§2.3.

50. **A weekday SET is driven by a closed-loop row converge with NO remove gesture — surplus rows are duplicated and the app dedupes** (RDLG2, 2026-08-22, golden-v3 + golden-v4) — the Repeat dialog's weekly mode carries one pop-up per weekday row, and pressing a row's add button INSERTS the new row at the FRONT of the enumeration (pushing existing rows one index later), which is what made the old "press + then re-drive the same index" drive rewrite its own new row. Working path: read the live row count (group pop-ups minus the base index — 2 on ≤3.22, 3 on 3.23), press the add button (resolved as the SMALLEST-x button in the group; the row buttons enumerate in an unstable order) until there are at least as many rows as target weekdays, assign EVERY row from the target set CYCLING, then read all rows back and require set equality. Cycling is what makes a SHRINK work without touching the remove button: the app stores the weekdays deduplicated, so `{tue,thu,sat}` → `{friday}` assigns Friday to all three rows and commits `{friday}`. Certified grow AND shrink on both versions. See [rdlg2-323-recipe-cert.md](../lab/rdlg2-323-recipe-cert.md) §2.4.

51. **`Items ▸ Repeat ▸ Create Next Copy` materializes the pending occurrence AND advances the cursor** (RDLG2, 2026-08-22, golden-v4/Things 3.23) — one menu press on a selected template mints the instance the spawn cursor points at (`rt1_repeatingTemplate` FK set, the cursor's own `startDate`) and steps `rt1_instanceCreationStartDate` to the next occurrence while incrementing `rt1_instanceCreationCount` — the same bookkeeping the clock-arrival spawn performs. No dialog, no confirmation. Not wired to any op; recorded as the headless-adjacent way to force a spawn. See [rdlg2-323-recipe-cert.md](../lab/rdlg2-323-recipe-cert.md) §5.2.

52. **`Create Next Copy` + an ordinary instance write is the app's `Make Exception`, reachable with no chooser** (CNC1, 2026-08-24, golden-v4/Things 3.23) — the Make Exception branch of the 3.23 chooser has never been reachable from automation (REPX1 §3.4 five negatives; REPX2 reached it only by a synthesized click on the projection row). Two ordinary steps reproduce it exactly: press `Items ▸ Repeat ▸ Create Next Copy` on the uuid-verified template (materializing the cursor's own occurrence and advancing the series), then re-date the minted instance with a plain `things:///update?id=<instance>&when=<date>`. The TEMPLATE delta of step 1 is field-for-field, value-for-value what the chooser writes — `rt1_instanceCreationCount +1`, `rt1_instanceCreationStartDate` → consumed slot + 1, `rt1_nextInstanceStartDate` → the next RULE date, `todayIndexReferenceDate` → the cursor, `userModificationDate` silent, rule blob byte-untouched — measured on a fixed WEEKLY rule against REPX3 §1.2 and on a DAILY rule against REPX3 §2.1, and the vacated slot then stays silent when the clock reaches it, against a live control that spawns normally. Step 2 is REPX1 §3.1's plain instance re-date, which the rule has no opinion about. The composite's ONLY residual difference from the chooser is the minted row's `userModificationDate` — NULL under the chooser, stamped by the second write, and SYNCX1 measured `umd` to be the winning side of the merge arbitration, so the divergence is in the safe direction. Two hard boundaries: it inherits [oddities §17](../things-app-oddities.md) whole (a target that is a live slot of the same rule double-books), and on an **after-completion** rule the primitive is not "materialize the pending occurrence" but "duplicate today" ([oddities §18](../things-app-oddities.md)). Half-reversible: `things undo` restores the instance write perfectly and cannot un-mint the row or rewind the cursor. See [cnc1-template-mutations.md](../lab/cnc1-template-mutations.md) §1/§2/§5/§7.

53. **A control nested inside a Things content ROW is addressable through System Events — but only by an explicit rows → cells → children walk, never by a `whose` clause on the table** (HXPC1, 2026-08-25, golden-v4/Things 3.23) — the heading row's `…` button carries its heading's title in `AXDescription` (`"More. <title>"`, the HEADXPROJ enabler) and sits at `UI element 4 of cell 1 of row 3 of table 1 of scroll area 1 of <standard window>`. A `whose` filter searches DIRECT children only, and a table's direct children are its rows — which carry no description — so `first UI element of <table> whose description is "More. <title>"` resolves **false** while the raw Accessibility API finds the node at the same instant. Working path: `repeat with r in rows of t` → `repeat with c in UI elements of r` → `repeat with e in UI elements of c`, comparing `description of e` exactly, then reading `position`/`size` off the hit. Two corollaries worth carrying: the row's TITLE node (`desc="‎<title>"`) carries a U+200E LTR mark that must be stripped, while the `More. <title>` description does not; and `entire contents of <table>` is NOT a usable substitute (it returns references whose `description` reads empty, and they cannot be dereferenced with `contents of`). See [hxpc1-picker-assert.md](../lab/hxpc1-picker-assert.md) §B0.

54. **A GUI picker with no readable selection is committed SAFELY by clicking the row you can name** (HXPC1, 2026-08-25, golden-v4/Things 3.23) — the heading-ellipsis `Move…` picker is a detached `AXUnknown` window (`AXIdentifier = MovePopUpDialog-<uuid>`) whose project rows are `AXUnknown`s carrying the project title in `AXDescription`, and **no row publishes `AXSelected`, `AXFocused`, or `AXHighlighted`** — only the filter field is focused, so there is nothing to read back from a keyboard commit and no way to assert what Return would take. Working path: type the destination at the already-focused filter (one `keystroke`, not the select-all + Tab field mechanic — a popover filter has no next key view for Tab), then resolve `every UI element of scroll area 1 whose description is "<exact title>"`, require exactly one, require its centre inside the scroll area's own rect, and `CGEventPost` a click at it. This is the general recipe for any picker whose highlight is invisible to AX: address the row, don't guess the highlight. The safety it buys is concrete — the picker's trailing `New Project "<typed text>"` row (curly quotes, so an exact-title match can never reach it) CREATES a project when committed, and the picker omits completed/canceled projects entirely, so a blind Return on an absent destination minted a second project of that name. See [hxpc1-picker-assert.md](../lab/hxpc1-picker-assert.md) §B3/§B4.

55. **A Things dialog text field is retyped with NO select-all — focusing it already selects the whole value** (BEEP1, 2026-08-25, golden-v4/Things 3.23) — every numeric-field drive in the Repeat dialog opened with `keystroke "a" using command down`, and that keystroke does not reach the field at all: Things publishes an `Edit ▸ Select All` menu item and **DISABLES** it while the sheet is up, AppKit gives menu key equivalents first refusal on the key event, the disabled item swallows ⌘A, and the unhandled event is the macOS alert beep. It is also unnecessary. `set focused of tf to true` installs the field editor with the entire content **already selected** — measured directly, `AXSelectedTextRange` goes from `loc1/len0` (a bare caret) to `loc1/len1` on a one-character value and to `loc1/len2` on a two-character one — so `focus → keystroke "<value>" → key code 48` replaces the old value outright, including the shrinking case (`12` → `3`, which a stale caret would have made `123`). Working path for any AppKit text field behind a modal sheet: focus, type, Tab-commit, read back, retry — and never send a keystroke whose menu key equivalent the app has disabled. Two silent alternatives were also measured working but are not needed (`set value of tf to ""` first; an explicit `AXSelectedTextRange` write). See [beep1-numeric-field-beep.md](../lab/beep1-numeric-field-beep.md) §3.

56. **A beep is COUNTABLE from a headless, muted VM — the lab has an alert-sound oracle** (BEEP1, 2026-08-25, golden-v4/macOS 15.7.7) — there is no NSBeep hook under SIP, but two independent oracles track system alerts 1:1 and both survive `lab_mute_guest` (which every clone-boot path applies, so no probe needs an unmuted VM): (a) the unified log, where `systemsoundserverd` emits exactly one `SSServerImp.cpp:733  -> Incoming Request : actionID 4096` per play request — muting only adds `SSServerImp.cpp:774  Device is currently muted` beside it; and (b) `fs_usage -w -f filesys` counting `open` calls on a distinctively-named alert sound installed with `defaults write -g com.apple.sound.beep.sound /System/Library/Sounds/Submarine.aiff`. Both were validated POSITIVELY (three deliberate `osascript -e beep` calls → exactly 3, quiet control → 0, muted and unmuted) before any drive was judged, and they never disagreed across ~40 measured windows. **The predicate must be subsystem-scoped, not process-scoped:** the app never plays its own alert, so `process == "Things3"` matches nothing. See [beep1-numeric-field-beep.md](../lab/beep1-numeric-field-beep.md) §1.

57. **A state-dependent AX control is addressed by the LABEL ROW it sits on, and the whole dialog is re-read before it is committed** (CGRD1, 2026-08-25, golden-v4/Things 3.23) — a structural index into an Accessibility tree is a bet on a layout the app never promised, and a per-step read-back cannot call that bet: it re-reads the element it addressed, so it proves the keystrokes landed where they were aimed and nothing more (#589's interval drive typed into the ends-count field and read its own number back three times over). Two paths together make it safe. First, every value-bearing field is found by the pinned English static text sharing its row — the interval on `Every`@286 against its @283, the ends count on `Ends:`@375 against its @372, the start-days-earlier offset on `days earlier`@413 against its @409 — with a uniqueness fallback for the one measured state that offers no label at all (an after-completion cadence group: no `Every`, no `Ends:`, exactly one field) and a fail-closed refusal, naming the whole field inventory, for anything else. Second, the drive re-reads EVERY control it set immediately before the OK press, through those same addresses, and refuses to commit when any of them disagrees — the outside view a self-referential check structurally cannot provide. Certified end to end: a poisoned intended value stops the drive naming only the differing control with both values (`interval (intended "9", dialog shows "3")`) and leaves the rule byte-identical. Generalizes to any GUI whose controls appear and disappear with mode.

58. **A selected heading (or to-do) is REORDERED by a bare keyboard chord — ⌘↑/⌘↓ move it one slot, ⌘⌥↑/⌘⌥↓ move it to the end** (HEADORD1, 2026-08-25, golden-v4/Things 3.23; probe-only, NOT wired) — Things 3.23 left `_private_experimental_ reorder` inert, taking heading order offline with no fallback, and the recorded state of the art was that no surface addresses a heading's position at all (REORDGAPS HEADORD-b: every heading-as-container spelling rejected, -1708/-1728/-2740). It turns out the app still ships the capability, just with **no discoverable affordance**: with the row selected, `⌘↑`/`⌘↓` move it up/down one slot and `⌘⌥↑`/`⌘⌥↓` move it to the top/bottom of its bucket. Nothing represents them — all eight menu-bar menus were enumerated with enabled state and key equivalents in both the no-selection and heading-selected states (the whole diff is `Complete`, `Convert to Project…` and `Show in Project` enabling), the row's context menu carries `Move…`/`Duplicate Heading`/`Convert to Project…`/`Delete Heading` and no reorder item, the heading row publishes **zero AX actions**, and no menu item anywhere carries an arrow `CmdGlyph`. The write is the cleanest ordering primitive we have found: **the moved row's `index` is rewritten and nothing else is** — no sibling renumber (contrast the bounce, BOUNCE2 §9h), children keep byte-identical indices and follow through their intact heading FK. **A chord with nowhere to go is declined with zero delta and one alert beep**, so a driver must compute its move count from the database rather than firing until nothing changes (ten chords at a bottom-selected heading: four moved, six beeped, 6/6 on the sentinel). Two send-shapes: System Events `key code 126 using command down` works but **only while Things is frontmost**; `CGEventPostToPid(<Things pid>, …)` lands with Things **backgrounded**, which pairs with the already-background-capable `select-heading-row` primitive for a tier-0/1 reorder. The shipped `axKeyScript` has no modifier support, so either shape needs a new primitive. **The same chords reorder TO-DOS** with the same law — uncharacterised beyond loose project children. One hazard: a headed CHILD driven past its bucket edge does not decline, it **crosses into the adjacent heading** (heading-FK rewrite, index preserved on the up leg), both directions. See [headord1-heading-order.md](../lab/headord1-heading-order.md) §1/§2.

59. **A whole GUI gesture — reveal, row selection AND a modifier-bearing keystroke — is deliverable to a BACKGROUNDED app, with the user's focus never moving** (CHORDMH1, 2026-08-25, golden-v4/Things 3.23) — the three background-capable pieces were each known separately (`open -g things:///show?id=` reveals without activating, UIC2-a; the pure-AX `select-heading-row` walk needs no focus, HEADCERT1; `CGEventPostToPid` delivers a keystroke to a backgrounded Things, HEADORD1 1h2a) but 1h2a had ACTIVATED Things first and only then backgrounded it, so the composition was unproven. Measured end to end with Things launched by `open -g` and **never activated at any point**, reading `name of first process whose frontmost is true` back after every stage: reveal → `Finder`, select → `OK`/`Finder`, chord → the heading moved / `Finder`, a second chord on the same selection → moved again, `⌘⌥↓` → moved, `Finder`. Zero beeps. Two things this pins down for any future driver: **the selection FOLLOWS the row it moves**, so a run of hops costs ONE positional walk rather than one per hop (the walk is ~0.25 s per row probed and is essentially the whole wall time); and the keyboard tap to a pid is a genuinely different path from the mouse one — NATIVE1-e measured `CGEventPostToPid` INERT for Things' mouse hit-testing, which is why click synthesis is still foreground-bound, but keyboard events to the same pid land. This is the first shipped op whose entire gesture is tier 0/1. See [chordmh1-move-heading-build.md](../lab/chordmh1-move-heading-build.md) §1.

60. **Which row a Things reorder renumbers depends on the DIRECTION of the move — a ±1 chord going DOWN rewrites the sibling it passes, not the mover** (CHORDMH1, 2026-08-25, golden-v4/Things 3.23) — HEADORD1 measured the arrow-chord write as "the moved heading's `index` is rewritten and no sibling is renumbered" and it is half right, because it only ever fired a ⌘↑. Measured on identical three-heading fixtures: ⌘↑ renumbers the MOVER (`C 0 → -343`, between `A -532` and `B -246`), ⌘⌥↑ and ⌘⌥↓ renumber the MOVER, and ⌘↓ leaves the mover byte-identical and gives the PASSED sibling a value below it (`B -201 → -1152`, under `A -543`). Exactly ONE row is rewritten in every case and it is always a row the gesture passed over — Things picks whichever single write is cheapest. The consequence for any verifier: assert the resulting ORDER and assert that exactly one row moved, but never assert WHICH side of the swap got the new number. It is the same trap AXDRAG1 recorded for the sidebar drag ("a drag may renumber a NEIGHBOUR rather than the dragged row") arriving on a different surface, and the general lesson is that an index-rewrite law measured in one direction is not a law until the other direction is fired. Caught here by the shipped op's own per-chord check on its first live drive, not by a probe. See [chordmh1-move-heading-build.md](../lab/chordmh1-move-heading-build.md) §2.

61. **A repeating template's Upcoming day-block position IS reachable — the projection row is title-addressable, uuid-VERIFIABLE, and chord-movable on 3.23** (CHORD2, 2026-08-25, golden-v4/Things 3.23; probe-only, NOT wired) — ORDFIN1 §1c/1d closed this door on two independent grounds: the projected row exposes no identity (only generic cell-template chrome, so a drive cannot verify it grabbed the template rather than an ordinary to-do), and resting templates are drag-inert (§9e). Both grounds are gone on 3.23's AX tree. The projection carries its title as an `AXDescription` (`d:‎LAB-REPEAT-DAILY`), a `CGEvent` click at its frame selects it, and `Things3 → id of selected to dos` reads back **the template's own uuid** (`W3PZB9e7W6BEtKmEKP4deG`) — a closed identity loop, not a title guess. A `⌘↑` then writes `todayIndex` and nothing else (`2824 → 2032`), verified against a whole-table 192-row × 41-column diff: `rt1_recurrenceRule`, `rt1_nextInstanceStartDate`, `startDate`, `start`, `index` and `userModificationDate` are all untouched, so the series is re-ranked without being re-anchored. The `⌘↓` leg renumbers the passed sibling instead (path 60's direction law). Plain rows in the same day-group re-rank across the projection freely, never rewriting it. The second ground is bypassed rather than refuted: the chord is a keyboard gesture, so §9e's drag-inertness never applies. **The `index` axis stays closed** — in a project list the rule row (distinguishable from its same-titled visible occurrence by the repeat-day badge in its AX subtree) declines `⌘↑` and `⌘⌥↑` alike, zero delta, one beep each, identity verified both times. So the template's rank is reachable on `todayIndex` and unreachable on `index`, and ORD-19 reopens while §9e stands. See [chord2-reorder-laws.md](../lab/chord2-reorder-laws.md) §5.

62. **A multi-row keyboard reorder renumbers only the rows you did NOT select — and coalesces a scattered selection as a side effect** (CHORD2, 2026-08-25, golden-v4/Things 3.23) — the single-row law (path 58/60: exactly one row rewritten, which one depends on direction) generalizes in a way that is cheaper than any protocol we modelled. A contiguous block of three driven `⌘↑` moves as a unit with its internal order intact and rewrites **one** row, the unselected neighbour it passed (`V1.index −636 → −74`). A **non-contiguous** selection `{G2, G5}` coalesces into a contiguous block at the anchor — one slot beyond the topmost selected row — and does so by leaving both selected rows' indices **byte-identical** and lifting all four unselected rows above them (`G1 −390 → 625`, `G3 −79 → 1007`, `G4 −51 → 1665`, `G6 0 → 2093`). A `⌘⌥` endpoint chord is the same law with more displacement: every unselected row is renumbered, the block never is. Two riders for a driver: the write volume of an endpoint move is **O(container)**, not O(block), so a large container's `⌘⌥` is an unmeasured cost; and a selection **mixing a heading with a to-do** is refused wholesale — zero delta plus one beep, both directions, twice on two fixtures — so mixed-kind is a fast-fail, never a partial application. See [chord2-reorder-laws.md](../lab/chord2-reorder-laws.md) §2.

63. **`userModificationDate` is the oracle that separates a chord's reorder from a chord's silent REPARENT — the alert beep is not** (CHORD2, 2026-08-25, golden-v4/Things 3.23) — the arrow chords perform two categorically different operations through one gesture, and only one of them is announced. A pure rank move writes the rank column and nothing else: the entire delta of a `⌘↑` on a to-do is `index: −320 → −1133` across 4 rows and 164 compared fields, with **no `umd` stamp**, on every axis and every row kind measured (`index` in a project list / Inbox / Someday / Anytime; `todayIndex` in Today / This Evening / an Upcoming day-block, including a repeating template's projection). But the same chord at a container edge performs a **membership change**, silently and with no beep — a headed child at the FIRST heading's top is deported to the project root (`heading → None`, `project → set`), a loose row at the loose block's bottom is adopted by the first heading (`heading → set`, `project → None`), a child at its bucket's far end under `⌘⌥` crosses into the adjacent heading, and a row at This Evening's top edge crosses into the daytime section (`startBucket: 1 → 0`). **Every one of those stamps `umd`; none of them beeps.** The beep marks only a chord the app DECLINED — validated 1:1 at scale (a 10-chord burst on a 4-row list: 3 productive, 7 declined, exactly 7 beeps) and firing for boundaries, mixed selections, a template row in a project list, the Logbook and an empty selection alike. So a chord-driven op must fence its bucket and section edges from the pre-state and assert `umd` (plus the FK) afterwards; it cannot listen for a beep to learn it reparented something. See [chord2-reorder-laws.md](../lab/chord2-reorder-laws.md) §6/§3.

64. **A keyboard reorder is VIEW-relative — under a tag filter one ±1 crosses every hidden row between the mover and its displayed neighbour** (CHORD2, 2026-08-25, golden-v4/Things 3.23) — in a project view filtered to a tag (`things:///show?id=<project>&filter=<tag>`, AX census confirming three content rows of five), a `⌘↑` on the middle **displayed** row moved it above its displayed predecessor, not above its true sibling: `T1 < T2 < T3 < T4 < T5` became `T3 < T1 < T2 < T4 < T5` in one chord, a single-row write (`T3.index −209 → −1169`). Correct for a human — the row moves where the eye expects — and a trap for a driver, which computes ±1 counts from the container's sibling list and would land the row two true slots away from its plan. The rule for anything compiled onto this vector: own the view state, or derive the move count from the DISPLAYED set. The same relativity is what makes an ordinary unfiltered project view safe, and is measurable cheaply (the AX row census names the rendered rows). Note the related rendering law from the same campaign: an **archived** heading is not rendered at all, so it takes no ordinal in the shipped `select-heading-row` walk and a live heading's ±1 skips straight over its slot — the rendered order is the DB order filtered to `status = 0`. See [chord2-reorder-laws.md](../lab/chord2-reorder-laws.md) §4bf/§7.

65. **The detached Repeat editor resists every dismissal but NOT a scripting `quit` — while the attached sheet is the opposite** (RSCU1, 2026-08-28, golden-v4 / Things 3.23). [Oddities §26](../things-app-oddities.md)'s detached editor ignores its own Cancel, Escape, ⌘W and a real HID click at its Cancel frame — and `tell application "Things3" to quit` ends it cleanly, first try, no signal needed. The ATTACHED Repeat sheet inverts both halves: its own Cancel closes it instantly, and the same scripting `quit` does not answer at all (the ladder had to escalate to SIGTERM). So "can it be dismissed?" and "can the app be asked to quit?" are independent questions with opposite answers per dialog form, and a cleanup ladder that assumes either implies the other will strand one of the two. This is why `things rescue relaunch` keeps every rung. Evidence: [lab/rscu1-rescue-family.md](../lab/rscu1-rescue-family.md) §5.

66. **A standing sheet's `-1728 Can't get to do id` releases the instant the sheet goes — measured through the shipped CLI, in one cell, both directions** (RSCU1, 2026-08-28, golden-v4 / Things 3.23). With a Repeat sheet up: `exists to do id X` → `true`, `count to dos` → **0**, `delete (to do id X)` → `-1728`, row untouched. `things rescue dismiss` closes it (confirmed by re-census). Immediately after: `count to dos` → **30**, the IDENTICAL `delete` returns clean, the row trashes. [MODALX1 §2.1](../lab/modalx1-open-sheet-matrix.md)'s law reproduced as a closed loop with a shipped command as the release, which is what makes it a usable recovery rather than a diagnosis. Evidence: [lab/rscu1-rescue-family.md](../lab/rscu1-rescue-family.md) §2.

67. **A per-occurrence deadline is expressible on an AFTER-COMPLETION rule, and it rides the series through a CNC mint — driven end to end by the shipped verb** (CNCAC2, 2026-08-28, golden-v4 / Things 3.23). `things todo add-repeating "…" --when 2026-07-05 --deadline 2026-07-08 --after-completion --frequency weekly --interval 1` lands `tp=1 fu=256 fa=1 ts=-3 of=[]` with the template's `4001-01-01` deadline sentinel and a seed occurrence born `startDate 2026-07-05 / deadline 2026-07-08`. `--start-days-earlier 3` writes a byte-identical rule blob. Completing the seed anchors the series (`acRef=2026-07-05`) and derives the START-SHIFTED cursor `next=2026-07-09` (anchor + interval − `ts`); `Items ▸ Repeat ▸ Create Next Copy` then mints an occurrence dated 07-09 **due 07-12**. Confirms [CNCAC1 §9](../lab/cncac1-after-completion-checkoff.md)'s hand-built measurement on a series the production CLI built, and is what lifted the rule-kind diversion in the deadline geometry.

68. **The Repeat sheet's control census is `tf:1`, not `tf:0`, whenever "Add deadlines" is ticked — and a structural discriminator that spells only `tf:0` will disown the dialog it is driving** (CNCAC2, 2026-08-28, golden-v4 / Things 3.23). Ticking the checkbox reveals the `and start [n] days earlier` field as a DIRECT child of the shell, so the shipped census flips from `cb:2 pu:1 bt:2 gp:1 tf:0` to `cb:2 pu:1 bt:2 gp:1 tf:1` mid-drive and the shipped window-state census reports `sheetKind: other` — *an unrecognized dialog is open in Things* — about the Repeat sheet itself. Any per-step guard keyed on the narrower shape then refuses the next keystroke hop and abandons the sheet open. Both shapes are the Repeat dialog; a discriminator must admit 0 OR 1.

69. **A repeating template's CONTAINER is independently mutable — the rule and both spawn cursors survive a move byte-for-byte, and the spawner follows the template** (TMOV1, 2026-08-29, golden-v4 / Things 3.23). Everything measured about writing to a template row until now was a wall — `when=` crashes the app ([oddities §1](../things-app-oddities.md)), `deadline=` is a silent no-op, status and schedule are refused on every surface — so `todo.move`/`project.move` were fenced by analogy. They are not walls. An ordinary `things:///update?id=<template>&list-id=<project|area>` (and `update-project?…&area-id=` for a project template) moves a series between containers cleanly, as does the AppleScript alternate `set project of to do id …`: across nine arms — project↔project, project↔area, container↔loose, area↔area, area↔loose, and an after-completion rule shape — the whole-fixture 41-column delta is the moved row's container FK, its `userModificationDate`, and the two containers' cached leaf counts, with `rt1_recurrenceRule` byte-identical by sha256 and `rt1_nextInstanceStartDate` / `rt1_instanceCreationStartDate` / `rt1_instanceCreationCount` / `rt1_instanceCreationPaused` / `start` all unchanged. **The forward half is the surprise:** the spawner reads the template's container at MINT time, so occurrences created after the move land in the new container — confirmed on both minting paths, the app's own clock spawner and the shipped CNC composite — while occurrences that already existed stay put, splitting the series across two containers with nothing in the app or the API saying so (now disclosed). The one destination that genuinely is a wall is the built-in LIST: `move … to list "Inbox"` and `move … to list "Someday"` on the TEMPLATE both error **301** with zero row delta, extending [RSIM-S §S4/§S-R3](../lab/rsim-results.md)'s template-CHILD law up to the template itself. The general lesson is the one the fence's own annotation admitted: *unvalidated* is a reason to measure, not a class of hazard, and a fence inherited by analogy from a neighbouring cell can be wrong about both what it blocks and what it lets through. See [tmov1-template-container-move.md](../lab/tmov1-template-container-move.md); issue #655.

70. **An area's sidebar section can be FOLDED OUT OF THE WAY of a drag — and the fold is umd-silent, per-device, and exactly reversible** (SBCOL1, 2026-08-30, golden-v4 / Things 3.23). Every area row carries a persistent 18×18 `AXImage d="Source Toggle Template"` inside an `AXUnknown` wrapper. [AXDRAG2-b](../lab/axdrag2-reorder-certification.md) read the row's action names (empty) and the image's (empty) and filed the chevron as "frame-resolvable, not actuatable"; the wrapper between them advertises `AXPress`, and pressing it is a **decoy** — `AXError = 0`, zero effect, [REPX1 §1.2](../lab/repx1-instance-semantics.md)'s decorative-`AXPress` law reaching past content rows into the sidebar. What actuates is a **`CGEvent` click at the image's own resolved frame** (flags set explicitly to zero on every event, a `MOVED` settle first, the row scrolled into the viewport band because an off-screen row still exposes a valid virtualized frame). Measured: a 22-row section collapsed to 2 and back, **both directions, twice, 0 alert beeps, disruption tier 0** — no focus steal, no new window. Clicking the row BODY (single or double) does nothing to the census. The state is `collapsedAreaUUIDs`, an array of area uuids in the group-container prefs plist: **every database table is byte-identical across the toggle** (rows + content digest), so a collapse is not a data edit and Things Cloud never hears about it — but it **survives an app relaunch**, so a driver that borrows it must hand it back. This is what turns [AXDRAG5](../lab/axdrag5-field-stall.md)'s structurally-uncrossable tall section from a refusal into a working move: fold the wall, run the certified ladder across it, unfold. See [sbcol1-sidebar-collapse.md](../lab/sbcol1-sidebar-collapse.md).

71. **A STALE evening row reorders through the plain `today` bounce — and the one-leg `when=today` that looks like it should work is inert** (STEV1, 2026-08-31, golden-v4 / Things 3.23). A `startBucket=1` row whose day has passed is drawn by the app in Today proper (bottom of the daytime section, above the "This Evening" header) and is never rewritten by the app on any trigger. The obvious repair — dispatch `things:///update?id=<u>&when=today` to clear the bucket — DOES clear it (`startBucket` 1→0 + `userModificationDate`) but writes no rank at all, and on an already-arrived bucket-0 row it changes nothing whatsoever: an arrived Today member cannot re-enter Today. What works is the SHIPPED two-leg `today` bounce, unchanged: `when=evening` re-dates the row to today (This Evening is definitionally the current day) and `when=today` then front-inserts it at the global Today `todayIndex` minimum and stamps `todayIndexReferenceDate`. So the stale row needed no new machinery — only for the scope checks to stop refusing it (#657). Cost, now disclosed: that away leg re-dates ANY past-dated `today`-scope movee, overdue daytime rows included. [lab/stev1-stale-evening.md](../lab/stev1-stale-evening.md)

72. **A Things sidebar is found by the AREA TITLES it holds, not by how wide it is — and a hidden one is still in the tree, at its old frame, under the content list** (SBRES1, 2026-08-31, golden-v4 / Things 3.23 **and 3.23.1**). The shipped locator identified the sidebar as *"the narrowest `AXTable` under 400pt"*; the sidebar's resize handle (an `AXImage` described `MainWindowSidebarResizeHandle `, since the Things window has **no `AXSplitGroup` and no `AXSplitter` at all** — the two panes are sibling `AXScrollArea`s directly under the window) drags from a 190pt floor to **at least 790pt**, and at any width from 400 up the locator returned nothing, reproducing #665/#651's *"the sidebar did not resolve (is the window open and the sidebar visible?)"* on a plainly visible sidebar. The working address is **structural + semantic**: walk the window but STOP at every list container and never enter a row (125 AX calls, versus ~4,700 for the two full-subtree walks it replaces), then pick the candidate whose rows carry the caller's own area titles — which is what a sidebar *is*, and is immune to width, window size, full screen and split position alike. The window is picked by **`AXMain`** (`AXFocused` is false on every window, and with two main windows open only one is main); never by position, because the application element's `AXChildren` also holds the menu bar and an untitled 40×40 placeholder window that a "first window" fallback lands on — and that placeholder is System Events' `window 1`, with zero scroll areas, which is why an ordinally-addressed bulk read dies with `Invalid index`. **A hidden sidebar (`View ▸ Hide Sidebar`, ⌘/) keeps its `AXScrollArea`, its frame and all its rows** while the content pane slides on top of it, with no attribute distinguishing the states ([oddities §30](../things-app-oddities.md)); the only signal is that the two panes then **overlap horizontally by the sidebar's width**, and without that check a driver aims its drag into the content list. See [sbres1-sidebar-resolution.md](../lab/sbres1-sidebar-resolution.md).

73. **`AXUIElementCopyMultipleAttributeValues` collapses a per-row AX walk by 8×, and the attribute array must be built with `$([…])`** (SBRES1, 2026-08-31, golden-v4 / Things 3.23). Harvesting an 85-row sidebar with the classic one-call-per-attribute walk costs **3,376 AX round-trips / 1,675 ms**; fetching `AXValue`, `AXDescription`, `AXTitle`, `AXChildren`, `AXPosition`, `AXSize` and `AXRole` in ONE call per node costs **844 / 694 ms** for byte-identical output, and **235 / 197 ms** at the shallowest depth that still satisfies every consumer of the text. **The trap:** `$.NSArray.arrayWithArray([$('AXValue'), …])` compiles, returns `AXError 0`, and fills every slot with `kAXErrorNoValue (-25212)` — an empty harvest that reads exactly like an app exposing nothing; `$(['AXValue', …])` works. Two more numbers worth carrying: the bare floor for an `osascript -l JavaScript` hop that imports AppKit/ApplicationServices/CoreGraphics and resolves a pid through System Events is **70 ms**, and a scroll bar is a DIRECT child of its scroll area — reading it with `findAll(scrollArea,'AXScrollBar',4)` descends through the table and every row and cost 0.4 s of a 1.16 s snapshot. Net on the shipped snapshot: **3.68 s → 0.75 s, ~9,000 round-trips → ~430**. See [sbres1-sidebar-resolution.md](../lab/sbres1-sidebar-resolution.md) §6.
