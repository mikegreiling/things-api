# Probe backlog & in-flight validation plan

Durable plan (survives context compaction). Written 2026-07-09 after the L5 sitting; updated 2026-07-09 post-compaction after banking the first scf run. Tracks (A) the S-campaign follow-up probes, (B) the PR #42 ops validation, (C) parked work, and (D) release. Update the "STATUS" lines as each lands.

## ⟹ RESUME HERE (execution order)

1. ~~Bank §A round-1 results~~ — **DONE 2026-07-09** (PR #45): verdicts from `lab/artifacts/things-run-scf-20260709-041543/` folded into [s-campaign-results.md](s-campaign-results.md) (follow-ups table), capability-matrix, oddities (2e update + new 5k), gaps, o-suite-results.
2. **Validate PR #42** (§B): `npm run lab:regress` as a NO-REGRESSION check → merge on double-green. STATUS: pending.
3. ~~scf2 re-probe~~ — **RAN + BANKED 2026-07-09** (run `things-run-scf2-20260709-045454`; verdicts in [s-campaign-results.md](s-campaign-results.md) round 2): backdating = AppleScript-only for existing items + json at-creation import; reminder set via set-detail DEAD; set-detail Parent = DETACH footgun (oddity 5l); sidebar ordering exhaustively closed; Someday = new native reorder scope (P6h).
4. ~~P7/P8 lateral probes + operationalization~~ — **DONE 2026-07-09**: P7 (someday projects move; anytime moves loose to-dos; area detach/attach + when= round-trips FRONT-INSERT — Mike's bounce insight) and P8 (two-call someday protocol EXACT; inbox forward re-confirmed; 3-project when= bounce EXACT). Shipped: `todo.backdate`, `todo.add-logged`, reorder scopes `headings`/`someday`/`projects`. Evidence: [o-suite-results.md](o-suite-results.md) §P7/P8.
5. ~~npm publish~~ — **DONE 2026-07-09**: v0.6.0 tagged + published (`latest`), npx smoke green.
6. Parked §C work.

## A. S-campaign follow-up probes

### Round 1 — `lab/scripts/research-scampaign-followups.sh` — RAN + BANKED (run `things-run-scf-20260709-041543`)

- **P1 — heading reorder via the private command.** STATUS: **WORKS** 🎉 — heading uuids accepted in a project specifier; children follow. Banked.
- **P2 — `set-detail` Parent = heading move.** STATUS: **SILENT NO-OP** — heading move dead on all surfaces. Banked. (To-do re-parent variant NOT attempted → P2b, round 2.)
- **P3a — `set-detail` Reminder Time "14:30" on a scheduled to-do.** STATUS: **NO-OP** (text→Date coercion suspect) → format experiments, round 2.
- **P3b — clear a DATED reminder via empty value.** STATUS: **WORKS** 🎉 — oddity-2e gap closed (Shortcuts-only, headless). Banked.
- **P4 — Completion/Creation Date backdating.** STATUS: **INVALID RUN** (two script bugs: completion via `things:///update` WITHOUT the auth token → fixture never completed; stale `--output-path` file aliased P4 output to P3b's). → redo, round 2.
- **P5 (NEEDS A HUMAN CLICK)** — delete a NON-empty heading: do children re-parent, orphan, or cascade? Delete-class consent re-prompts every run (oddity 5j). STATUS: parked.

### Round 3 — `lab/scripts/research-scf3.sh` — RAN + BANKED 2026-07-12 (run `things-run-r3-20260712-171142`)

All Round-2 verdicts below RECONFIRMED exactly on a fresh clone (no drift), plus the genuinely-open residuals settled: **P6h Someday convention LOCKED** (reversed wire-list `[S3,S1,S2]`→`[S2,S1,S3]`, ready to wire); **P6 sdef inventory** = exactly one `_private_` command (read the bundle's `Things.sdef` directly — the `sdef` binary needs Xcode); **logInterval enum** = 0/1/4 only (NO weekly/monthly — §C closed); **`log completed now` updates manualLogDate** (§C closed); **deadline-less fixed repeat** exists + falsifies fixed⇒deadlined (§C closed, oddities §8a); **repeating-template dated-reminder clear** settled (RC residual closed — no in-place automation clear exists; oddities §8b); **oddity 6½ screenshots** captured. Full table: [s-campaign-results.md](s-campaign-results.md) round 3.

### Round 2 — `lab/scripts/research-scf2.sh` — RAN + BANKED (run `things-run-scf2-20260709-045454`)

One clone, autonomous. Harness fixes learned from round 1: `proxy()` must `rm -f /tmp/scf-out.txt` before each run; completion must go through AppleScript (`set status of to do id X to completed`) or URL WITH the auth token (read it from the guest defaults/plist as phase21b did).

- **P4 redo — backdating.** Complete a fixture properly (verify `status=3`/stopDate in DB before proceeding), then: (a) Shortcuts `set-detail` Completion Date with several value shapes (ISO `2025-01-15`, locale `1/15/2025`, `January 15, 2025`); (b) AppleScript `set completion date of to do id X to date "..."`; (c) URL `things:///update?...&completion-date=...` (+auth token). Same trio for Creation Date. Check `stopDate`/`creationDate` deltas after each.
- **P3a redo — Reminder Time set formats.** On a `when=today` fixture: `set-detail` Reminder Time with `"2:30 PM"`, `"14:30"` re-check, full datetime strings. Expect `reminderTime = hour<<26 | minute<<20`.
- **P2b — `set-detail` Parent on a TO-DO** (re-parent between projects) — the heading variant no-op'd; does it work for to-dos?
- **P6 — sidebar order, remaining spellings** (Mike's insight: Anytime view order mirrors sidebar order — consistent with both reading the same `index`; the two probed spellings are dead: O13 `move area to before area` errors, P17 private reorder in `list "Anytime"` with top-level project uuids no-ops). Untried spellings to sweep: (a) AppleScript `move project id X to before project id Y` (location specifier on projects, never probed — O13 only tried areas); (b) `set index of project/area` (property write); (c) private reorder in `list "Anytime"` with AREA uuids; (d) private reorder in `list "Someday"`; (e) grep the full sdef dump for any other `_private_` commands we haven't inventoried. Evidence target: `TMTask."index"` (top-level projects) / `TMArea."index"` (areas).

## B. PR #42 validation (project tags/reminders, tag-shortcut clear, inbox reorder)

1. `npm run lab:regress` (7 suites + guest e2e, ~40 min) — no-regression check (exercises the reorder pipeline/guards/pre-state PR #42 touches). STATUS: pending.
2. On double-green → merge PR #42 (`mg/phase21b-ops`, OPEN). STATUS: pending.
3. Recurring suite probes encoding the A1–A6 deltas (e-suite: project tags URL+AS, project reminder, tag shortcut clear; o-suite: inbox reorder) — deliberately deferred; author later with an iteration budget. STATUS: deferred.

### RC — dated-reminder CLEAR campaign — RAN + BANKED 2026-07-11 (runs `rc-20260711-172432` + re-validate, PR #73)

Suite [rc-suite.json](../../lab/suites/rc-suite.json) (RC01–RC03) + avenues C/D in a `--keep-vm` clone. Answered "is Shortcuts the ONLY dated-reminder clear?" → **no** (Shortcuts is the only *in-place / schedule-preserving* clear). Full writeup: [rc-suite-results.md](rc-suite-results.md).

- **RC01/RC02 — URL bounce (avenue B).** STATUS: **WORKS** — `when=today` (keyword) clears the reminder + re-dates to today; re-date back restores the date without re-attaching. Pure-URL dated-reminder clear; non-atomic, crashes on repeating items. This is the H-REMINDER-SCOPE remediation, now end-to-end verified.
- **RC03 — AppleScript move-to-Inbox (avenue A).** STATUS: **WORKS** — de-schedule drops `reminderTime` with `startDate` (both → NULL, start → 0). Most disruptive (loses the date).
- **Avenue C — AppleScript reminder property.** STATUS: **DEAD** — no reminder/alarm term in the sdef; read-only `activation date` + read-only `_private_experimental_ json`, `set` on either → −10006.
- **Avenue D — Shortcuts clear on a PROJECT.** STATUS: **WORKS, in-place, headless** — `set-detail` Reminder Time = `""` on a dated project: `reminderTime` → NULL, `startDate` untouched. Closes capability-matrix:43. Also banked: `add-project?when=<date>@time` attaches a reminder at project creation.
- ~~**Residual gap:** repeating-item dated-reminder clear~~ — **CLOSED 2026-07-12 (RCLEAR, oddities §8b)**: a repeating template's reminder is a repeat-RULE property, NOT a `reminderTime` column value (GUI "Add reminders" persists in the editor but writes no `reminderTime` and no rule time-key). So there is **no in-place automation clear** for it: Shortcuts `set-detail Reminder Time=""` on a template is a SAFE no-op (exit 0, no crash, nothing to clear); AppleScript `move … to list "Inbox"` on a template is a CLEAN refusal (error 301, no crash — the guard the URL `when=` path lacks). `todo.clear-dated-reminder`'s repeating refusal is CORRECT and stays. (The URL-bounce fallback for NON-repeating items shipped in PR #73; still DEFERRED to Mike whether to prefer it over the Shortcuts dependency — no write logic changed this round.)
- **Harness maintenance done in the same change:** the golden `schemaFingerprint` in [golden-v1-metadata.json](golden-v1-metadata.json) was stale (`5526…`) after commit c8eeb69 added `logInterval`/`manualLogDate` to the `TMSettings` depended-columns manifest (`src/db/schema.ts`); updated to `784bd2f6…`. Verified pure manifest drift, NOT golden-DB drift (old manifest still reproduces `5526…` against the clone; the two new columns exist in the golden). Any future `DEPENDED_TABLES` edit must re-record this fingerprint or `lab:run` aborts at bootstrap.

## C. Parked (bigger, not auto-runnable now)

- ~~**Parked-probe re-validation campaign**~~ — **DONE 2026-07-22 (PR mg/parked-probes; one clone `parked-probes-lab`).** Re-ran the P-series (P4 backdating, P3a reminder-SET, P2b to-do re-parent, P6 sidebar/area order) on a fresh golden clone — **zero verdict drift** from scf2/scf3 (full table: [s-campaign-results.md](s-campaign-results.md) "Parked re-validation") — and closed two genuinely-open residuals: **reminderTime bit-layout** = `hour<<26 | minute<<20`, re-derived 4/4 (schema atlas OQ1 CLOSED); and the **RSIM-T / RSIM-U preserve-trigger isolations** (see the struck RSIM-R / RSIM-S follow-ups below). New path banked: the AppleScript **date-literal** backdating spelling (`set completion date … to date "1/15/2025"`) works alongside the scf2 arith spelling. No capability-matrix feasibility change.
  - **Tooling note — tart 2.34.0 golden smoke: PASS (no bring-up break).** The golden image + lab bring-up were re-validated end-to-end under **tart 2.34.0** before the campaign: clone **1s** (APFS COW), boot→ssh **12s**, clean `tart-run.log` (no warn/softnet/denied lines), airgap OK, clock pinned 2026-07-05T12:00, DB readable (`PRAGMA user_version=0`, 37 TMTask rows / 2 areas / 2 repeatingTemplates — matches golden seed), guest CLI `doctor`+`today --json` exit 0, and a URL-scheme write + a CLI write both landed and DB-verified. The 2-VM ceiling + Softnet/`--net-host` constraints (harness.md) are unchanged on 2.34.0. Evidence (gitignored): `lab/artifacts/smoke-nohup.log`, script [`lab/scripts/research-parked-smoke.sh`](../../lab/scripts/research-parked-smoke.sh).

- ~~**RSIM — recurrence-creation mutation shapes**~~ — **ANSWERED 2026-07-18 (RSIM1–RSIM6, PR mg/rsim-probe; full writeup [rsim-results.md](rsim-results.md)):** creation spawns **exactly ONE instance immediately** in every create path — a FIXED rule DELETES the source (identity replacement) and mints a fresh instance at the current-occurrence `startDate` (= today); an AFTER-COMPLETION rule KEEPS the source and relinks it as that single instance (refines §8g — identity replacement is fixed-only). Template = `start=2`, `startDate=NULL`, `deadline=NULL` (deadline-less default), `rt1_recurrenceRule` tp/fu/fa/of/ts/ed…; instance = `rt1_repeatingTemplate`→template, rule NULL, `startDate`=occurrence. **Completion** of an after-completion instance stamps the template's `rt1_afterCompletionReferenceDate` (=completion date) + `rt1_nextInstanceStartDate` (=completion+interval) but does NOT materialize the next instance (future-dated, pending). **Reschedule** is in-place (identity preserved), rewrites `fu`/`of`/anchors (one caveat: the interval-field edit didn't apply and the op fail-closed — recipe-vs-app follow-up wanted). The bench simulator recurrence appliers + a repeat-heavy corpus family are now unblocked; the tier-3 catalog decision is evidence-capable. Script: [`lab/scripts/research-rsim.sh`](../../lab/scripts/research-rsim.sh).
  - ORIGINAL ASK (2026-07-17, Mike + orchestrator): characterize EXACTLY which rows the app writes when a repeating to-do/project is created, for BOTH fixed-schedule and after-completion rules — does creation spawn an immediate instance alongside the template (and with which fields/links: `rt1_repeatingTemplate`, startDate/deadline derivation, instance count)? Also: mutation shape when a template is rescheduled, and instance-spawn timing on completion for after-completion rules. Method: snapshot-diff around `make-repeating`/`create-repeating` in a clone.

- ~~**RSIM-P — repeating-PROJECT children semantics**~~ — **ANSWERED 2026-07-18 (P1–P5, PR mg/rsim-p; full writeup [rsim-results.md](rsim-results.md) §RSIM-P):** making a project repeat (fixed OR after-completion) **hard-deletes the source project AND its entire descendant subtree** (headings + to-dos + their tags + checklist items) and mints **two independent deep copies** — one under the hidden template project, one under the instance project — with TMTaskTag + TMChecklistItem duplicated per copy. Template-side children are **completely plain rows** (`rt1_recurrenceRule`/`rt1_repeatingTemplate` NULL, `start=1`); their only tie to templatehood is the `project`/`heading` pointer. **Fixed** leaves instance-side children plain; **after-completion** links each instance-side child's `rt1_repeatingTemplate` to its template-side sibling — and (surprise) the after-completion PROJECT **deletes its source** rather than preserving it like an after-completion to-do (RSIM2). Completing an instance project cascades `status→3` through the subtree **incl. the heading row**, no re-spawn on a pinned-past clock; `todo move` treats a template project as an ordinary container (in/out both clean, no guard); `things show` marks the PROJECT's template/instance status but leaves every CHILD's `repeating` block empty (design input for a "belongs-to-template" marker). **Flags 3 SIMFID divergences** in `src/write/vectors/simulator.ts` (the make-repeating appliers ignore children → orphans; the after-completion project applier wrongly preserves the source; `project.complete` doesn't cascade the heading row). Script: [`lab/scripts/research-rsim-p.sh`](../../lab/scripts/research-rsim-p.sh). Open boundary: the NEXT fixed occurrence's re-duplication (2026-07-12) is not observable on a pinned-past clock — needs a clock-advance follow-up.

- ~~**RSIM-P2 — nested repeaters + uuid-discovery traps**~~ — **ANSWERED 2026-07-19 (A1–A5, B1–B4, PR mg/rsim-p2; full writeup [rsim-results.md](rsim-results.md) §RSIM-P2):** converting a project that contains a repeating to-do **FLATTENS** the nested repeater — the nested template row is hard-deleted, its instance demoted to plain (`rt1_repeatingTemplate` cleared), and both survive only as PLAIN duplicated copies (no template-of-template, no `rt1_*` survives; identical for fixed + after-completion nested rules). Edge-state children: completed→copied-but-REOPENED, trashed→skipped, external-repeating-instance→copied-plain-with-FK-cleared. **A5** (after-completion project WITH a heading): instance-side heading AND to-do children are ALL plain — only the project-level instance→template link exists (**confirms simulator headings-left-plain; contradicts RSIM-P P4's per-child to-do links**). uuid-discovery: same-title gauntlet binds correctly (B1); mid-drive add fails the ui-drive CLOSED, no misbinding (B2); template inherits notes/tags/checklist but NOT deadline (B3); `rt1_repeatingTemplate` FK exact per template (B4). **Two systematic RSIM/RSIM-P contradictions flagged for SIMFID: (1) source PRESERVED-as-instance (not deleted) for fixed conversions — breaks the planned "source-gone" assertion; (2) no after-completion per-child FK links.** A4 clock-advance impractical (a +15-day jump wedged the VM — the RSIM-P re-duplication boundary stays open). Discovery-hardening constraints (derive the instance via the FK not a time/title heuristic; time-bound safe for the template only; don't assume source-gone; fingerprint on notes/tags/checklist not deadline) distilled in §RSIM-P2. Script: [`lab/scripts/research-rsim-p2.sh`](../../lab/scripts/research-rsim-p2.sh). ~~**Highest-value follow-up: one small clone re-running RSIM1 / RSIM-P P1 / RSIM-P P4 on main to isolate the source-preservation + child-link axis (build/recipe vs scheduling-state vs area) for SIMFID.**~~ **→ DONE: see RSIM-R below.**

- ~~**RSIM-R — reconcile fixed-mode source fate + after-completion child links**~~ — **ANSWERED 2026-07-19 (R1–R8 + S1–S3/S2b, PR mg/rsim-r; full writeup [rsim-results.md](rsim-results.md) §RSIM-R):** both contradictions are REAL app-behavior differences (not differ bugs — re-derived uuid-by-uuid from raw snapshots; not build/recipe — same Things 3.22.11 + byte-identical ui-recipe). **C1 source fate:** RSIM-P2's area/scheduling axis is **FALSIFIED** (every BARE item deletes — area'd R4, today-scheduled R1/R5, area-less R3/R6 all DELETE). The real axis for a **project** is a **nested repeater in the subtree**: a fixed `project make-repeating` PRESERVES the source (relinks it as the instance, flattens the nested repeater, mints only the template) IFF the subtree holds a nested repeating to-do (A1/A2/A3/S2/S2b = 5/5), else DELETES it (S1 plain-children, R1/R3/R4, P1/RSIM6 = 6/6). A **to-do** deletes when bare/checklist-only (RSIM1/R5/R6/S3); the lone content-rich preserve (B3) is unisolated (checklist EXCLUDED; **deadline** leading candidate) — parked. **C2:** RSIM-P P4's per-child instance→template links are a **non-reproducible anomaly** — reproducible law is PLAIN instance-side children (A5/R7/R8 = 3/3; R7 = P4's exact config). **Simulator deltas:** `applyMakeRepeatingFixed` must delete-by-default and preserve-source only for a project-with-nested-repeater (subsumes RSIM-P2 SIMFID #3 flatten); `applyMakeRepeatingAfterCompletionProject` must drop the per-child links (leave instance children plain). Discovery invariant "source absent OR relinked-as-instance" holds in 16/16 cases; per-template `creationDate` time-bound survives (every minted template = write wall-clock). Scripts: [`lab/scripts/research-rsim-r.sh`](../../lab/scripts/research-rsim-r.sh) + [`lab/scripts/research-rsim-r2.sh`](../../lab/scripts/research-rsim-r2.sh). ~~**Parked follow-up:** isolate the to-do content preserve-trigger (one 2-cell clone: deadline-only vs tag-only vs notes-only fixed to-do).~~ **→ ANSWERED 2026-07-22 (RSIM-T, PR mg/parked-probes; [rsim-results.md](rsim-results.md) §RSIM-T): the trigger is a DEADLINE** — a fixed `todo make-repeating` preserves the source IFF it has a deadline; bare/notes-only/tag-only/checklist-only all DELETE (1/1 preserve vs 4/4). Now a modellable applier branch (preserve iff pre-read `deadline` non-NULL), not a tolerance.

- ~~**RSIM-S — next-occurrence SPAWN semantics + Quick Find visibility (the RSIM-P re-duplication boundary)**~~ — **ANSWERED 2026-07-19 (S1–S4, Q2, S-R, PR mg/rsim-s; full writeup [rsim-results.md](rsim-results.md) §RSIM-S):** the parked "does the NEXT occurrence re-duplicate the subtree?" boundary (open since RSIM-P; A4 wedged on a +15-day jump) is **CLOSED with SMALL +1-day clock advances on a DAILY repeater** — 3 consecutive spawns (07-06/07/08) ran clean, no reboot (helpers moved to `~/things-lab/helpers` to survive one). **YES — each occurrence materializes a FULL fresh subtree copy** (INSERT 10 / DELETE 0 / CHANGE 3; prior occurrences accumulate; only the template PROJECT row's `icCount`+1 / next-date+interval change). Occurrence children are **PRISTINE** (completed/canceled→reopened, scheduled/someday→unscheduled, deadline→dropped, **trashed template child→SKIPPED**, plain→verbatim; titles/notes from the template's LIVE state). **Per-child instance→template FK is NONDETERMINISTIC across consecutive spawns (6/7, 5/7, 6/6)** — direct proof of RSIM-R C2; tolerate never assert. **S4:** template-side children are STATUS/SCHEDULE-immutable (complete/cancel silent-no-op all surfaces; schedule/move→301/302; deadline no-op; only trash + title/notes + project-move land → oddities §8n). **Q2:** Quick Find surfaces the hidden template project (`start=2`), template children, instance project, instance children — all findable, none marked → design input: keep our search findable-but-MARKED (GUI parity + the container-marker the GUI lacks). Scripts: [`research-rsim-s.sh`](../../lab/scripts/research-rsim-s.sh) + `rsim-s-clock.sh` + `rsim-s-states.sh` + `rsim-s-quickfind.sh` + [`research-rsim-sr.sh`](../../lab/scripts/research-rsim-sr.sh) (S-R restore). ~~**Parked residual:** RS2 (fixed daily, plain project, NO nested repeater) PRESERVED its source despite having a completed+canceled child — a counterexample to RSIM-R's "plain-children project always deletes"; possible second preserve-trigger = an edge-state child, or residual source-fate nondeterminism (isolate with N fixed conversions of a plain project holding exactly one completed child).~~ **→ ANSWERED 2026-07-22 (RSIM-U, PR mg/parked-probes; [rsim-results.md](rsim-results.md) §RSIM-U): a real, deterministic SECOND preserve-trigger** — a fixed `project make-repeating` on a plain project (no nested repeater) PRESERVES the source when its children are all terminal (completed/canceled); an OPEN child DELETES it (completed-only + canceled-only + both-terminal all preserve vs the open-child control). NOT nondeterminism. Fixed project conversion now has two preserve conditions: nested repeater OR no-open-child. (Untested: a mix of open + terminal children.)

- ~~**SIMFID — simulator-fidelity replay suite**~~ — **BUILT 2026-07-22 (branch `mg/simfid-suite`; full writeup [simfid-results.md](simfid-results.md); code `lab/simfid/**` + `lab/scripts/simfid.sh` + `npm run lab:simfid`).** Certifies `src/write/vectors/simulator.ts` against the real app op by op: (1) `lab/scripts/simfid.sh` drives each headless case's op through the guest CLI in a disposable clone and captures the row-level DB delta (existing `differ.ts`); (2) `lab/simfid/replay.ts` replays the same op through the simulator on the host from an identical synthetic pre-state; (3) `normalize.ts` + `compare.ts` compare NORMALIZED deltas — uuids → placeholders keyed by (kind, title, **container-aware** discovery order), wall-clock timestamps → date buckets, indexes → ranks — with declared TOLERANCES for the probe-proven app nondeterminism (per-child `rt1_repeatingTemplate` stamping — RSIM-R C2 / RSIM-S; junk instance next-sentinel). Verdict MATCH / TOLERATED(&lt;which&gt;) / DIVERGENT(&lt;detail&gt;); a divergence is REPORTED (sim bug OR new app behaviour), never silently folded. Normalizer/comparator unit-tested (`test/unit/simfid-normalize.test.ts`); drift-runbook step 5 re-certifies after every golden rebuild. First run: 33 cases / 21 covered ops → 28 MATCH · 5 TOLERATED · 0 DIVERGENT; **extended 2026-07-22 to 35 cases → 30 MATCH · 5 TOLERATED · 0 DIVERGENT** with the two newly-isolated fixed-conversion source-PRESERVE laws (`todo-make-repeating-deadline-preserve` §RSIM-T + `project-make-repeating-terminal-children-preserve` §RSIM-U, both MATCH). **The fresh-clone leg then RAN 2026-07-22 (disk freed to ~20 GiB; run `simfid-20260722-200229`)** over the six headless CRUD cases in `clone-manifest.json`, upgrading those rows from `suite-evidence` to fresh `clone-drive` captures → current standing **29 MATCH · 6 TOLERATED · 0 DIVERGENT**. Five corroborate the model exactly; the clone leg **surfaced one finding** — `todo-reopen` moved MATCH → TOLERATED(wallclock-bucket) because the sim-side completed fixture seeds `stopDate=null` (nothing for reopen to clear) while the real app stamps+clears it, a SIMFID fixture gap masked by an over-broad tolerance (NOT new app behaviour, NOT a proven applier bug; fixes deferred). It also forced a harness fix: `simfid.sh` now ships a SELF-CONTAINED guest node (the homebrew node's `/opt/homebrew/*` dylibs abort on the vanilla guest — SIGABRT). The recurrence/subtree family stays banked `rsim-evidence`. (Even once disk frees up, the recurrence/subtree family — incl. the two preserve laws — stays out of the headless `clone-manifest.json`: `make-repeating` is tier-3 ui-vector needing the AXVM1 Accessibility grant, so its real-app truth comes from the AX-granted `research-parked.sh`/RSIM clones, not the headless driver.) The inbox-promotion gap caught by hand on 2026-07-17 (todo.move filing semantics) is exactly the class of bug this automates away. **Follow-ups** (simfid-results.md): ~~run the clone leg for a fresh independent CRUD capture~~ **DONE for the starter manifest (2026-07-22, run `simfid-20260722-200229`)**; expand `clone-manifest.json` beyond the six pre-state-immune cases + fix the `todo-reopen` fixture (seed stopDate) and tighten `wallclock-bucket`; ~~add a content-rich to-do fixed-preserve case once the trigger is isolated~~ **DONE (RSIM-T deadline + RSIM-U terminal-children, both modelled + cased)**; optionally hand-author recurrence app-golden files.

- **Double-trash black hole — GUI screenshot evidence** (2026-07-12, oddities §6½): reproduce in a clone's GUI (VNC screenshot arm, §E½ machinery): trash a to-do, then separately trash its containing project → the to-do vanishes from BOTH the Trash view and the trashed project's view. Capture before/after screenshots of Trash + project view for the Cultured Code report. Data layer already characterized (row keeps trashed=1 + project link); only the GUI views lose it. things-api's divergent-on-purpose behavior (trash lists directly-flagged rows; trashed project's view shows a Trashed bucket) is regression-locked in test/cli/render.test.ts.

- ~~**Deadline-less FIXED repeat encoding**~~ — **CLOSED 2026-07-12 (DLREPEAT, oddities §8a)**: YES the GUI creates deadline-less fixed repeats — it's the DEFAULT ("Add deadlines" is opt-in). A deadline-less fixed daily rule spawns instances with a startDate but `deadline`=NULL, and its `rt1_recurrenceRule` is BYTE-IDENTICAL to a deadlined ts=0 rule (the "deadline on the day"/N=0 case collapses to the deadline-less encoding, `t2_deadlineOffset=0` in both). **The ts=0 plists DO collide** — deadline-ness is not derivable from the rule blob alone, falsifying the fixed⇒deadlined law for the deadline-less case.
  - **FOLLOW-UP CLOSED 2026-07-12 (UI1, PR mg/ui-vector-probe)**: the discriminator is the **template row's own `deadline` COLUMN** (NULL = deadline-less, `4001-01-01` sentinel = deadlined), captured by creating every variant via the GUI with a working VNC keyboard path (the DLREPEAT "days earlier" typing failure overcome). Universal across fixed AND after-completion; `t2_deadlineOffset` stays 0 (NOT the discriminator); a non-zero offset also shows as `ts=−N`. Projection fix shipped (`RepeatingInfo.deadlined` + views/occurrences gate on the column). Evidence `lab/artifacts/things-run-ui1-explore-20260712-180724/`.

- ~~**§E½ UI-vector feasibility probe**~~ — **CLOSED 2026-07-12 (UI1, PR mg/ui-vector-probe)**: VERDICT **FEASIBLE**. VNC-drove `File → New Repeating To-Do` end-to-end and verified the `rt1_recurrenceRule` row landed. Accessibility is NOT granted in the golden (osascript System Events → −1719) so all driving is VNC clicks + VNC keyboard (no AS UI-scripting fallback); menu/dialog layout deterministic at the golden's 2048×1536 framebuffer; VNC keyboard entry works (clear-by-delete, not select-all). Green-lights the dedicated-Mac "ui" vector. Script `lab/scripts/research-ui1.sh`.
- ~~**`TMSettings.logInterval` enum verification**~~ — **CLOSED 2026-07-12 (logInterval/LOGNOW, oddities §8c)**: the GUI dropdown offers ONLY `0=Immediately · 1=Daily · 4=Manually` — there is **NO weekly(2)/monthly(3)** in Things 3.22.11. The manual value is `4` (falls to the model's `default` branch, so behavior unaffected; only the enum doc was wrong — `src/read/log-boundary.ts` corrected). A mid-interval AppleScript `log completed now` DOES advance `manualLogDate` to the current time (confirms the boundary max()-in).

- ~~Lab runner/DSL **Shortcuts vector** support~~ — **DONE 2026-07-12** (PR: `mg/lab-shortcuts-arm`): the runner gained a `shortcut` DSL step (guest writes the JSON input to a file + `shortcuts run --input-path/--output-path`, output captured for `stdoutMatches`) and a `group:interactive` marker that skips the delete-class proxies (no Always-Allow; guest + host `activeProbes` both drop them). `s-suite.json` is now an autonomous recurring suite wired into `lab:regress` (S01 find-echo, S02 heading.create, S03 heading.rename, S-detail clear-dated-reminder run headless; S04/S-delperm are interactive, human-sitting only). Docs: [harness.md](harness.md) DSL section, [suite-audit.md](../reference/suite-audit.md).
- **Distribution/onboarding** — IN PROGRESS 2026-07-09, new plan: the six proxies were EXTRACTED from the golden's `Shortcuts.sqlite` (`ZSHORTCUTACTIONS.ZDATA` action blobs, SX2/SX3), reconstructed as old-format plists, and host-signed with `shortcuts sign --mode anyone` → repo-distributable signed `.shortcut` files in `lab/shortcuts/` (no iCloud links, no manual rebuild). Import+run validation: SX4. Still open: shipping location + `things setup shortcuts` flow. Gotcha: the signer can't write to `/Volumes/*` — sign to /tmp, then move.
- ~~Headings doctrine decision~~ — **DECIDED 2026-07-09** (roadmap §E / gaps §0): first-class always, NO flatten/dual mode. The HX sweep closed every non-Shortcuts heading-create/move escape hatch (heading-research.md).
- **Availability layer** (Phase 21b remainder): advisory `availability(env)` per vector; doctor reads `uriSchemeEnabled`; correct the `feature-disabled` classifier to key on `uriSchemeEnabled`, not a null token.
- ~~someday PROJECTS characterization~~ — **LOCKED + SHIPPED 2026-07-09** (P9e: descending anchor-stack; inverted two-call protocol exact ×2 + predicted-failure control; someday scope now takes area-less someday projects).
- ~~heading archive surfaces (P10/P10b)~~ — **DONE 2026-07-09**, consolidated in [heading-research.md](heading-research.md): AS by-id rename + archive/un-archive WORK (no Shortcuts); URL/Shortcuts status writes no-op; AS delete −1728; move dead on 4 surfaces; SUSPECTED schedule-on-heading crash (oddities §6) → todo ops hard-block non-to-do targets. Area tag clear via empty set WORKS (P10e).
- ~~heading.rename / heading.archive / heading.unarchive~~ — **SHIPPED 2026-07-09** with children policies (complete/cancel/reparent) + transactional undo; P11 locked the design inputs (someday survives round-trips; cancel-cascade quirk 6a; pre-resolved children untouched).
- ~~Crash verification~~ — **CONFIRMED** (P11e process death; oddities §6). A captured .ips remains nice-to-have for the Cultured Code report.
- **Short-uuid prefix resolution** (Mike, 2026-07-09): accept uuid prefixes (≥6 chars) on every uuid parameter — indexed range scan (`uuid >= p AND uuid < p+1`), ambiguity → error listing matches; prod check: 21–22-char ids over ~21k rows make 6-char prefixes ~99.7% unique. Phase 2: render shortened ids in list views (per-list minimal unique length). Design agreed; implementation queued.
- ~~ANYTIME list-scope convention~~ — **CLOSED 2026-07-09 (P13)**: the reorder write is non-deterministic (identical inputs → different orders; Anytime is a computed aggregate). Will not ship.
- ~~incoherent-mutation crash sweep~~ — **DONE (P14)**: crash family = schedule-class on unschedulable rows (repeating to-do/project via URL when=; heading via AS schedule); everything else is graceful. Two fresh `.ips` captured; catalog in oddities §7. Novel find: AS `schedule` works on projects.
- **Round-3 (remaining)**: ~~ANYTIME convention~~ (done) (P7b vs P8d disagree under every candidate model — needs a systematic series); ~~the hidden lists discovered in P9a (`list "Later Projects"`, `list "Tomorrow"`) as reorder specifiers~~ — **DONE 2026-07-17 (HEADCERT residuals): both accept reorders, both map to `todayIndex`; see [o-suite-results.md](o-suite-results.md) "Hidden-list & area'd-someday residuals"**; ~~area'd someday projects in the someday scope~~ — **DONE 2026-07-17: reorderable via `list "Someday"` two-call by `index`, semantics match the shipped protocol → proposed membership-widening follow-up (o-suite-results.md)**; set-detail Parent with an ENTITY-typed value — a `things-proxy-move-item` whose Parent field takes a second Find Items output (needs a golden sitting, human present); ~~P5 non-empty heading delete (human click, delete-class consent)~~ — **ANSWERED-BY-P12 (cascades: children reparented-to-root-then-trashed / subtree hard-deleted; see [s-campaign-results.md](s-campaign-results.md) "Container delete cascades")**.
- ~~P12 interactive delete-class batch~~ — **DONE 2026-07-09** (Mike present): heading delete cascades (Trash: row vanishes + children reparented-then-trashed; permanent: subtree hard-deleted), project delete is shallow, areas have no Shortcuts delete surface. Banked in heading-research + s-campaign-results + matrix.
- ~~**e2e reorder coverage**~~ — **DONE** (landed in commit `d38b05f`, shipped in v0.8.0): the guest e2e smoke (`lab/guest/e2e-write-smoke.sh`) now exercises every reorder scope — today (native + evening bounce), project, area (projects-in-area), inbox, someday (loose to-dos + area-less projects), a project's headings, and the top-level sidebar projects bounce (someday round-trip) — plus the mixed-member guards. Re-confirmed present 2026-07-12 during the Shortcuts-arm change.

## D. Release — publish `things-api` to npm (Mike, 2026-07-09)

Unscoped name `things-api` confirmed available; LICENSE (MIT) landed; repo prepped in PR #38. Steps: merge PR #42 first → decide version → move CHANGELOG `## Unreleased` under the version heading → `npm run check` + `npm pack --dry-run` sanity → git tag `vX.Y.Z` → `npm publish` (Mike's npm auth/OTP likely needed). STATUS: pending.
