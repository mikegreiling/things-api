# Up-next queue — the short-horizon pick list

The working queue for the next session(s): **open items only**. An item is DELETED from this file the moment it lands — git is the archive; there are no strike-throughs, no DONE annotations, no "shipped-but-kept-for-context" tails. If you want to know what was already built, read `CHANGELOG.md` and the living docs. Settled *decisions* (what will NOT be built, which shape won) live in [design/decisions.md](design/decisions.md); long-horizon strategy lives in [roadmap.md](roadmap.md).

**One-home rule:** every open item lives in exactly ONE of {this file, roadmap.md}. This file holds the short-horizon queue; roadmap holds durable strategy. No item is restated across the two.

Sections are blocker-classes: **needs-Mike** (a decision, near-zero effort) · **VM-batchable** (autonomous lab campaigns) · **needs-human** (a short in-person sitting) · **calendar-pinned** (date-driven) · **small code** (unblocked, low effort) · **feature design** (wants an ideation round before code).

## needs-Mike — decisions blocked on him, near-zero effort

- **Stateful display preferences.** The growing TTY toggle family (`--show-later`, `--show-logged`, `--until`, `--all`, the limit-knob family `--limit`/`--area-limit`/`--project-limit`) wants user-configurable defaults, plus three deferred knobs: `limitMode fixed|fit|none` (height-fit deliberately NOT default — determinism beats cleverness), `pager auto|never` (git-style `less -FRX`), and a global `--plain`/`--pretty` chrome override pair (`--plain` = disable the whole TTY chrome stack in one flag: ANSI + width-fitting + gutter + preamble, i.e. the flag form of `NO_COLOR=1 THINGS_WIDTH=0`; `--pretty` = force chrome into a pipe; `--json` ignores all of it so agents stay unconfused). Examples were delivered to Mike 2026-07-14; awaiting his ruling on the config surface + precedence. A config-file surface now EXISTS (`src/config.ts`, `things config set`, `~/.../config.json`) but carries policy keys only — the decision has a natural home waiting for it. Wary of plist-driven defaults confusing agents whose output changes without a flag changing. *(Consolidated home for what was triplicated across roadmap §H + up-next §1/§5.)*

## VM-batchable — autonomous lab campaigns

*(Remaining probes ride their parent feature items — the SYNCLAT on-hardware repro is needs-human, and the appliance-mode MASVM1 licensing probe lives in [roadmap.md](roadmap.md). The macOS 27 regression VM is calendar-pinned below. New probes get their own entry here when they're batchable and unblocked.)*

- **Things 3.23 certification — the last step, and the residue.** The campaign is otherwise DONE: golden-v4 minted and CERTIFIED, `lab:regress` GREEN end to end (all eight suites + the 132-step write-layer e2e), the assumption register walked row by row, the simulator/bench chain re-modelled to `databaseVersion` 27 with SIMFID re-certified host-side AND against a fresh golden-v4 clone (30 MATCH · 5 TOLERATED · 0 DIVERGENT). Evidence: [lab/gv4-323-certification.md](lab/gv4-323-certification.md) (+ [gv4-323-campaign.md](lab/gv4-323-campaign.md), [rdlg2-323-recipe-cert.md](lab/rdlg2-323-recipe-cert.md)).
  - **The one remaining step is Mike's, on the live host: `things config set certified-app-version 3.23`.** It is deliberately NOT run from a lab session — it stamps the operator's own config and silences doctor's passive behavioral-drift notice, so it waits on his review of the certification. The standing residue that makes the stamp honest-but-imperfect (the same shape 3.22.14 shipped with) is enumerated in the register's 3.23 audit block, item 3.
  - **Two ordering reaches have NO surface on 3.23 and now refuse** — heading order (`project move-heading`) and any day-group holding a repeating template (ORD-19). Both are app-side losses with no fallback protocol, not gaps in our code; they re-open the day Cultured Code fixes the private reorder command, which the o-suite will report (below).

- **The BEHAVIORAL canary for the private reorder command — the thing that LIFTS the version gate.** Mike's 2026-08-23 ruling took option (a) now: the ≥3.23 version gate shipped (`privateReorderIsNoOp`, `src/write/experimental.ts`), so a 3.23 host no longer burns a doomed attempt. What remains is the durable arm the DECLARATION canary lacks — a runtime probe that catches a silent no-op at ANY app version. Open design questions: what it reorders (it must touch NO user data — a scratch container it creates and destroys, or a two-row permutation that is provably order-preserving), when it runs (per-write is too expensive — memoize per app version + sdef hash?), and how it fails (a false negative must never disable a working surface, so the fail-closed direction has to be argued, not assumed). **Partial cover already exists and should be reused, not duplicated:** the o-suite's ≥3.23 `expectFrom` overrides assert the command's INERTNESS (`deltaEmpty`), so a release that restores it turns those 19 rows RED — that is the LAB-side canary, and lifting the gate is then a code change. The runtime canary is what protects a host whose app moves between lab runs.

- **`reorder --in today` has no working path when the Today list contains a PROJECT row (Things 3.23).** Found by the certification campaign ([lab/gv4-323-certification.md](lab/gv4-323-certification.md) §1.5). Only the native wire carried a project on the shared Today `todayIndex` axis (ORD-2/O12); the today BOUNCE fallback refuses a project member because a project's daytime `when=today` landing is MID-PACK, not the front-insert the protocol needs (ORD-12 EVEORD / SIT3 EVEPROJ) — so it fails closed rather than land a wrong order, and the refusal copy now names the real cause instead of advising the unavailable native strategy. Closing it needs a PROBE, not a patch: measure what `update-project?when=<today>` actually does to a project row's `todayIndex` relative to the day group (front-insert? mid-pack? re-stamp `todayIndexReferenceDate`?), and whether a park+re-enter MOVE protocol (the AREABACK/PROJROOT shape) reaches the Today axis for a project at all. If either lands deterministically, the today bounce gains a per-KIND leg exactly as ORD-17's deadline-cycle did, and the refusal goes away.

- **3.23 repeat-instance follow-ups opened by REPX1** ([lab/repx1-instance-semantics.md](lab/repx1-instance-semantics.md) §7). (1) **`Create Next Copy`'s `creationDate`** — RDLG2 §5.2 measured its cursor bookkeeping but not the minted row's birth stamp; REPX1 (c) showed the OTHER two gesture-materialization paths are born at gesture wall-clock rather than occurrence midnight, so this cell would make it three of three and settle the law's shape. One cheap clone. (2) **`Resume` after a MULTI-DAY pause** — REPX1 §5.3 could not discriminate "restore the pre-pause cursor" from RD-15's "re-anchor to today+1" because at the probe's clock both answers were the same date; a pause held across several days separates them, and RD-15's re-anchor claim rests on a golden-v2 measurement. (3) **`Stop`'s two unmeasured halves** — the SINGULAR confirmation copy (only the plural `Stop Them` sheet was driven) and `Stop` on a repeating PROJECT (UIC2-c / oddities §7 C5 records a 3.22.11 crash when the demoted project is then selected; unre-probed on 3.23). Both are preconditions for ever reviving `todo.stop-repeat`, whose DROP rationale REPX1 falsified — see the capability matrix. (4) **oddity §9ff via the OTHER source-preserve trigger** — a terminal checklist element (SRCFATE SF-Tck). REPX1 §3.3 already answers the question this cell was carrying (the double-spawn reconciliation is NOT fixed on 3.23 — it is reachable from a bare instance re-date, [oddities §13](things-app-oddities.md)), so this drops from load-bearing to completeness: it would only establish whether the *preserve* path specifically still forms.

- **SIMFID coverage gaps opened by the v27 re-model** ([lab/simfid-results.md](lab/simfid-results.md)). (1) The appliers never maintain `untrashedLeafActionsCount`/`openUntrashedLeafActionsCount` on PROJECT rows (a minted instance project with N open leaves carries 0) — pre-existing and generation-independent, invisible today only because those goldens derive from the sim delta; it needs an APP measurement of the number Things actually stamps before it can be modelled, because a guessed value in the golden is worse than an honest gap. (2) SIMFID's recurrence/subtree half still certifies against ≤3.22-captured evidence (`rsim-evidence`) — the family is tier-3 ui-vector and the headless guest driver deliberately does not attempt it; an AX-capable clone driver is what would upgrade it, and it is also what would settle whether a freshly minted 3.23 instance still carries the `69760` junk sentinel (the retirement condition for the `instance-next-sentinel` tolerance). (3) `Items ▸ Repeat ▸ Create Next Copy` (RDLG2 §5.2 — materializes the pending instance, advances the cursor, bumps `icCount`) is fully specified but is NOT a covered write op; if a spawn applier ever lands, add it alongside the RSIM-S spawn case, with RDLG2 §5.3's negative (early completion leaves the template byte-unchanged) as an assertion.

- **Template-direct clone — residual VM cells (d)/(e) (VMQ1 §6 closed (a)/(b)/(f)).** `todo clone`/`project clone` of a repeating template (SHIPPED 2026-08-14, `promote-clone.ts` `cloneTemplateViaRepromote`). VMQ1 CERTIFIED live on golden-v3: (a) a fixed weekly template clone mints a new template with EXACT rule bytes, source untouched, the new-series warning verbatim; (b) the UIC8 spawn-shape (a future-dated first occurrence mints template+cursor, 0 instances); (f) `things undo` trash-both removes the new series and leaves the source. Cell **(c)** (inexpressible-rule `H-CLONE-SOURCE` refusal) is NOT live-reachable — an inexpressible rule cannot be minted through any app surface — so it stays CI-only (`test/engine/write-promote-clone.test.ts`). REMAINING: (d) a PROJECT-template clone that keeps the source's title+area lands cleanly (the `sameTitleRowCount` template-exclusion row-select disambiguation, pre-state.ts); (e) a PAUSED source clones UNPAUSED (disclosed). Design: [design/promote-via-clone.md](design/promote-via-clone.md) §6(d); evidence: [lab/vmq1-probe-closeout.md](lab/vmq1-probe-closeout.md) §6.

- **SESSGATE wrong-Space relocation — END-TO-END VM certification (rides the next AX-vector run).** The session-reachability gate SHIPPED (2026-08-16, `session-reachability.ts`) and is lab-certified for the LOCKED-refusal branch (golden-v3: `1 0 0` → `blocked` exit 4, zero mutation) and the discriminator + recovery maneuver (`0 0 1` → `1 2 3`). Two residuals need a live app: (a) the **wrong-Space relocation branch** end-to-end — a genuine another-Space window (Things AX-0, others visible) driven through `make-repeating`, confirming the in-drive `close window 1`+`reopen`+`activate` relocates it and the drive then completes with the relocation warning; macOS exposes no scriptable Spaces API, so this needs a human sitting or a multi-display/Spaces rig (unit-level equivalence covers it for now). (b) the **AX-BLIND-sheet `close window 1`/`reopen` RECOVERY** — VMQ1 ([lab/vmq1-probe-closeout.md](lab/vmq1-probe-closeout.md) §5) CLOSED the AS-mutation-BLOCK half live in-lab (a Repeat sheet opened via the shell-`open` reveal blocks an AS `delete`, `trashed=0`, lock-independent), but the RECOVERY half did NOT reproduce: locking a session with a sheet already open keeps the sheet AX-REACHABLE (`1 1`, not the live-host `1 0 0`), so the AX-blind-sheet state can't be established headlessly, and `close window 1`+`reopen` left the delete still blocked (the close appears swallowed by the attached modal). Needs a real multi-Space/blind-window rig — same rig as (a). Also flag for the maintainer: does `close window 1` genuinely take down an ATTACHED modal sheet, or did the live-host recovery rely on the sheet sitting on a separate/blind window? Evidence: [lab/sessgate-session-reachability.md](lab/sessgate-session-reachability.md), [lab/vmq1-probe-closeout.md](lab/vmq1-probe-closeout.md) §5.

- **AX numeric-field drive triggers the macOS alert beep (Mike, live host 2026-08-20) — locate + silence + detect.** Every `set-field-value` drive of a repeat-dialog numeric field (interval / ends-count / start-days-earlier; `axSetValueScript`, `src/write/vectors/ui.ts`) audibly fires the system alert sound (NSBeep) on the live host — disruptive, though the value lands. The primitive is focus → `keystroke "a" using command down` → `keystroke "<n>"` → Tab (`key code 48`) → read-back, up to 3 attempts. Candidate beep sources, in likelihood order: (1) ⌘A arriving before the field editor is first responder (`set focused` + 0.15 s) so Select All falls through to the sheet/window unhandled → beep; (2) Tab with no next key view in the sheet's key-view loop; (3) the field's number formatter rejecting a keystroke (e.g. a retry re-typing into a field that already holds the value, or a stray character); (4) the SAME primitive is used to type the destination title into the Move… picker's search field (`ui-recipes.ts` ~L945) — Tab in a popover filter field is very likely unhandled; (5) `DIALOG_INTERVAL` and `DIALOG_ENDS_COUNT` both resolve to `text field 1 of group 1` (`ui-recipes.ts` ~L274/~L303) — if ends-after re-addresses the interval field the read-back never matches and the full 3× retype cycle runs (3 beeps, then an `error`); (6) `clearDialog`'s failure-path Escape (`axAbortScript`, app-wide `key code 53`, not process-targeted) beeps when nothing modal is open. Note the YANCH1 date-area REJECTION beep (an AX write the control refuses) is already known and caught (`ui.ts` ~L620/~L703, [lab/yanch1-derived-anchor.md](lab/yanch1-derived-anchor.md)) — this item is the keystroke-path beeps, which no doc covers yet. Probe: bisect by removing one keystroke at a time in a VM; candidate fixes that keep the closed loop — drop ⌘A in favour of `set value of tf to ""` followed by typing (the typed keystrokes still fire the binding), replace Tab-commit with a focus move to a known sibling, or verify first-responder (`focused of tf`) before sending ⌘A. **Detection for a regression arm:** no direct NSBeep hook is available (no DTrace under SIP). Try, in order: (a) `log stream --style compact --predicate 'process == "Things3" AND (eventMessage CONTAINS "beep" OR subsystem CONTAINS "com.apple.audio")'` during a drive; (b) set the VM's alert sound to a known file (`defaults write -g com.apple.sound.beep.sound <path>`) and watch for that file being opened (`fs_usage -w -f filesys coreaudiod,Things3` filtered to the path); (c) `com.apple.sound.beep.volume 0` is NOT a fix — only a mask. Whichever oracle fires reliably becomes a `lab:` assertion over the make-repeating drive so the beep can't regress silently. Also worth a line in oddities if the beep turns out to be the app's own (a formatter rejecting valid input).

- **GUI text-addressed selectors vs TRUNCATED/long titles — audit + long-title e2e cells (Mike 2026-08-20).** Question: does any GUI-driving path locate an item by its ON-SCREEN text (ellipsized for long titles) rather than the full stored title? Audit result (main @ 2026-08-20): the content-table `select-row` primitive (`axSelectRowScript`, `ui.ts`) does NOT read row text — it selects each row and reads back `name of selected to dos` via Things' AppleScript dictionary, i.e. the full model title, so long to-do/project titles are safe there; heading promote/demote/dissolve selection is POSITIONAL (`select-heading-row`), title-independent. Three surfaces DO match AX-exposed text against the DB title and are UNVERIFIED for long titles: (1) **area drag-reorder** (`ui-drag.ts` `rowMatchesTitle`/`findAreaRow` + the in-JXA `matches()`), which harvests each sidebar row's descendant `AXValue`/`AXDescription`/`AXTitle` joined by `|` and requires an EXACT segment equal to the area title (or `title + "."`); (2) the **heading `…` button** addressed as `first UI element … whose description is "More. <title>"` (`ui-recipes.ts` `headingMoreButton`, used by `move-heading-to-project` + dissolve); (3) the **Move… picker** types the destination project title and presses Return BLIND on whatever the filter highlighted — nothing asserts the highlighted row is the intended project, and the picker also offers `New Project "<typed text>"` ([lab/reordgaps-results.md](lab/reordgaps-results.md) §picker), so a mismatch (exact-prefix collision, a title the filter tokenizes differently) could CREATE a stray project rather than fail closed — this wants a read-the-highlighted-row assert before Return regardless of the truncation answer. Also note: the lab probe that certified the heading `…` recipe matched the description by SUBSTRING (`lab/scripts/research-bouncejson-headxproj.sh`) while shipped code uses exact `is`, and the heading title node carries a U+200E prefix that must be stripped — so the shipped exact-match has less field evidence than the lab run suggests. AppKit normally reports the FULL `stringValue` as `AXValue` even when the cell draws with a truncating tail, so (1)/(2) are PROBABLY safe — but Things' sidebar/heading rows are custom views, so it must be measured, not assumed. VM cells: an area title and a heading title long enough to ellipsize in a NARROW sidebar/window (≥120 chars; also one with emoji + a trailing `.`), then (a) dump the row's AX text and diff against the DB title, (b) run `area reorder` / `project move-heading-to-project` / dissolve end-to-end. If AX text IS truncated, the fix is to address by a non-rendered handle (e.g. select-and-readback as the content table does, or match on a stable prefix + DB disambiguation) and to add an explicit refusal for the ambiguous case. Either way land the long-title cells as permanent e2e regression coverage (`test/e2e/` / `lab:regress`) so a future Things release that changes AX exposure fails loudly.

- **Deadlined off-rule-first Next-SNAP on an `icCount=0` template — new probe (VMQ1 §1 finding; [lab/vmq1-probe-closeout.md](lab/vmq1-probe-closeout.md)).** A deadlined yearly template with NO materialized instance (`icCount=0`), driven off-rule (`--yearly-month 10 --on-day 16 --when 2028-11-05`, anchor ≠ --when), FAILED at the Next drive: the `AXDateTimeArea` committed `2029-10-16` (the next on-anchor occurrence), not the requested `2028-11-05`, and set-datetime's read-back rejected it (-2700) → the drive aborted (`verify-failed:silent-noop`). RSPA1 cell (b) drove the SAME command shape and COMMITTED, the difference being RSPA1's template had `icCount=1` (a materialized instance). So the deadlined off-rule Next-snap appears `icCount`-dependent. Probe: materialize vs not, and map whether the deadlined-yearly off-rule first is expressible only with a live current-occurrence instance — if the snap is a hard boundary for `icCount=0`, `assessOffRuleFirst`/validation may need to fail-closed on that shape (kin to the DACON1 monthly snap). Fails closed today (no wrong landing). Low priority.

- **Timestamp side-effect gaps — two residual `umd` cells only (from the SRCFATE sweep, [reference/timestamps.md](reference/timestamps.md) §2c).** SRCFATE ([lab/srcfate-reconciliation-sweep.md](lab/srcfate-reconciliation-sweep.md)) closed the rest: checklist writes are umd-SILENT, reminder set/clear + title + notes BUMP, plain Convert-to-Project + `project.promote-heading` `creationDate` = conversion wall-clock. STILL OPEN: **`project.dissolve-heading` surviving-children `umd`** and **`project.move-heading-to-project` heading `umd`** — the ellipsis-`…`-menu drive did not resolve the More-button position in the headless golden-v2 rig (clean `verify-failed:silent-noop`), so both need a **visible-framebuffer / on-hardware** sitting (predicted BUMP by the reparent-bump law, not measured). Low priority.

## needs-human — short in-person sittings

- **The framebuffer / HID rig — three 3.23 cells are blocked on it, and it is the same rig SESSGATE and the long-title audit want.** A headless clone provides no real framebuffer, so a synthetic HID drag and a genuinely-blind window are both unreachable. **Scope correction (REPX1, 2026-08-22): synthetic CLICKS are NOT blocked.** `CGEventPost` at an AX-resolved frame actuates Things' custom-drawn content rows inside an ordinary headless clone under the AXVM1 grant — REPX1 used it to check off an Upcoming projection row and to build a three-row shift-click multi-selection — and it also established that `AXPress` on those same elements is DECORATIVE (`AXError=0`, zero DB delta, proven against two controls), which is why several past "AX press did nothing" negatives should be re-read as vector artifacts rather than app behavior. Only DRAGS remain genuinely rig-blocked, which shrinks item (1) below to its drag arm alone. Blocked on it: (1) **the Make Exception / Update Rule chooser** — now FIVE headless re-dating vectors against a fixed-schedule instance of a daily series, all negative: the `Items ▸ When…` picker committed by `AXPress` (RDLG2) and again by RETURN on golden-v4 (REPX1), AppleScript `schedule`, and the URL `update?when=` — every one re-dates the instance cleanly with NO chooser and leaves the template byte-untouched, so the chooser is provably NOT on the automation path. REPX1 also measured what the chooser's absence COSTS (there is no exception semantics to reach: the moved occurrence is copied, not moved — [oddities §13](things-app-oddities.md)), so the remaining cell is narrow: the in-GUI calendar DRAG, the one vector still needing the rig ([lab/rdlg2-323-recipe-cert.md](lab/rdlg2-323-recipe-cert.md) §5.4, [lab/repx1-instance-semantics.md](lab/repx1-instance-semantics.md) §3.4); (2) **a repeating PROJECT under 3.23** — `projectMakeRepeatingRecipe` drives the same dialog through a different reveal (the repeat-bar popover, HID-clicked), so the 3.23 dialog findings carry but the REVEAL is uncertified (§7.3); (3) the SESSGATE wrong-Space + AX-blind-sheet residuals and the long-title AX-truncation cells listed below. Worth one sitting that clears all of them rather than four separate ones.

- **Cultured Code report — submission.** [docs/things-app-oddities.md](things-app-oddities.md) is effectively the draft (crashes §1/§6/§7, silent-failure families §2/§5, the double-trash black hole §6½, the completed-project black hole §6¾; the AX-era evidence — stop-then-select crash C5, sidebar AX-mirror blanking, context-dependent filter-bar tag inheritance, the Stop-confirm "to-do" copy for projects, the AX-invisibility gaps). (The reminder-picker "write-inertness" was retracted — ANCH2 proved it drivable, oddities §8l.) **Timing is ruled: held until v1.0** (Mike, keep-flagged-but-deferred). This is the single home for the CC-report item (the former §1/§3 duplicate pair is folded here). Sub-line, optional: capture the `.ips` for the heading-schedule crash (oddities §6) to round out the package.

## calendar-pinned — date-driven

- **macOS 27 public-beta regression VM — OVERDUE.** Target was ~late July 2026 (elapsed with no evidence run). Build a beta VM, run `lab:regress`, diff verdicts/tiers against the Things-update canary (rrv decode, fingerprint, doctor `repeats:`). The Things-version half of the re-pin question is now SETTLED — golden-v4 is Things 3.23 on macOS 15.7.7, so the remaining delta this campaign tests is macOS-only; Mike should confirm that framing before a fresh run. Note that 3.23 raised `LSMinimumSystemVersion` 10.15.0 → 13.3, which does not affect a macOS-27 guest but does bound how far BACK a fallback golden can go.
- **iOS 27 GA runbook — ~Sept 14 2026.** Execute [docs/lab/things-update-runbook.md](lab/things-update-runbook.md) + the Apple-Intelligence memo follow-ups ([docs/design/apple-intelligence-research.md](design/apple-intelligence-research.md)) when Things likely ships its App Intents 2.0 release. *(Apple-Intelligence / macOS-27 strategic notes home HERE, not in roadmap — one-home.)*

## small code — unblocked, low effort

- **`lab:run` preflight `gcRunVms()` reaps SIBLING campaigns' VMs (gv4-323-certification.md, reported 2026-08-22).** The runner's gc deletes every `things-run-*` VM including one a concurrent campaign legitimately holds, while `simfid.sh` deliberately refuses to reap a sibling's slot — the two policies disagree, and the aggressive one forces campaigns to serialize even inside the 2-VM ceiling. Make the gc as polite as simfid (or scope it to VMs older than the current run). Small, mechanical.
- **#509 — `todo delete` help (CLI + MCP) falsely says repeating to-dos are unsupported.** `things todo delete --help` (`src/cli/commands/writes.ts` ~L1428) and the MCP `delete_item` description (`src/mcp/server.ts` ~L2116) both say "Not available for repeating to-dos", but the op accepts a repeating TEMPLATE (dry-run plans `todo.delete`, the live run succeeds with the repeat-specific warnings: series stops generating, existing occurrences remain, restore from the app's Trash). Fix the copy to state that behaviour (per surface-copy.md), check `project delete` / the skill reference for the same stale claim, and add the help strings to the banned-vocabulary/help regression tests so the runtime contract and the help cannot diverge again. Trivial.
- **#508 — `todo add-repeating --after-completion` returns a FALSE `verify-failed:mismatch` (exit 3) after a correct creation.** Reproduced 6/6 on the live host (`0.17.0-dev`): the series + first occurrence land exactly as requested (read-back `startDate`/`when` = `--when`), yet the command reports the first occurrence "landed on <today+1>". Root cause (read from source, not yet fixed): `ruleParamsFor` deliberately does NOT drive the dialog's Next: field for after-completion rules (`src/write/promote-clone.ts` ~L226 — an after-completion series has no calendar anchor), but the post-drive verify in `runAddRepeatingTodo`/make-repeating (~L702, ~L922) still compares the template's `rt1_instanceCreationStartDate` cursor (`firstOccurrenceOf`) against `--when`. For after-completion templates that cursor is not the first-occurrence start (the real instance row carries the right `startDate`), so the oracle is wrong, not the drive. Fix: for `afterCompletion === true` verify against the materialized instance's own `startDate` (`instanceStartDate`) — or skip the Next verify and rely on the create delta — and mirror whatever `expectedStartIso` guard results in both the make- and add- entry points (~L549, ~L805). Add a unit cell (after-completion + future `--when` → ok) and a VM cell. Dangerous as-is: `add-repeating` refuses `--op-id`, so a caller retrying on exit 3 mints duplicate series — until fixed, callers must read back instead of retrying. Close the issue with the fix PR.
- **Audit other hand-enumerated vocabularies against the exhaustive-map doctrine (RRF1 follow-up, #491).** The #491 fix established the general law (decisions.md 2026-08-17): a multi-consumer vocabulary must never be hand-enumerated per consumer — only an EXHAUSTIVE `Record<keyof …, …>` (assert-producer or explicit skip-with-reason) or single-registry derivation is acceptable, so a new field breaks compilation until handled. The rule-writing verbs and the CLI repeat flag→param mapper were converted; the flagged NEXT candidate is the **`todo.update` field chain** (the update op's per-field opts→params→delta plumbing across `src/cli/commands/writes.ts`, `src/write/commands.ts` `todoUpdate`, and its `expectedDelta`) — verify no field is accepted-but-dropped or asserted-but-shallow, and if it is hand-enumerated, convert it to the exhaustive-map shape with a discrimination-style backstop. Sweep the other write ops'"'"' expectedDelta asserts for the same shallow-subset risk while there. Mechanical; the pattern is proven.
- **Extend dev-mode trace + `--json` interrupt envelope to the move/reorder/batch CLI drivers (TRACE1 residual, #487).** The dev-mode step trace and the SIGTERM/SIGINT `--json` `interrupted` envelope are wired into the single-mutation write driver (`runWrite`), which covers make-repeating and the other single UI-driving verbs. The other CLI write drivers (`runMove`/`runReorder`/`runBatch` and the area-drag reorder path) do NOT yet call `installCliTrace` / `armInterrupt`, so a UI-driving op invoked through them gets no trace file and, when interrupted, only the plain-stderr honest line rather than the `--json` envelope. The library-side pieces already work everywhere (the pipeline sets the in-flight marker for every write, so the watchdog + the stderr interrupt line fire regardless); this is just wiring the same three-line install/arm/close into those drivers' try/finally. Small, mechanical.
- **Hide `--op-id` from variadic move/reorder help (help/runtime contract bug).** Current `things todo move --help`, `things project move --help`, and `things reorder --help` all advertise the shared `--op-id <key>` option, but execution categorically refuses it before dispatch: `things todo move some-uuid --to-project x --op-id k --json` exits 2 with “`--op-id` is not available on a variadic move/reorder (a multi-leg compound) — express it as `things batch` with a per-line op_id for idempotent resubmission.” The runtime restriction is intentional in idempotency phase 1 and already has a rejection test (`test/cli/single-op-idempotency-cli.test.ts`); the public help is therefore lying. Register write flags per command capability (or otherwise suppress the unsupported option) so these compound commands omit `--op-id`, and add help-surface regression coverage for all move/reorder spellings while retaining the runtime refusal as defense in depth.
- **Composite-scoped mutation lock — hold ONE reentrant outer lock across a composite's legs (QUEUE-FOR-LATER, Mike 2026-08-18).** The pidfile lock (`src/write/lock.ts`) serializes writes PER-LEG only, so two concurrent composites (`make-repeating` / `add-repeating`, which run clone → trash → promote as separate mutations) can interleave at leg boundaries — composite A's clone, then composite B's clone, then A's promote… — with no single-writer guarantee across the whole composite. Give each composite an OUTER, reentrant mutation lock that its legs acquire once and hold end-to-end (the inner per-leg acquisitions become reentrant no-ops under the held outer lock), so two composites serialize as wholes rather than interleaving. Keep the per-leg lock as the base case for single ops. Low-frequency race (needs two concurrent GUI-driving writers), hence queued; the fix is a scoping change to the lock acquire/release, not new machinery.
- **`project add-repeating --deadline <date>` — map to the rule like the to-do verb did (DBLSPAWN1 residual).** The to-do `add-repeating` now maps a concrete `--deadline` onto the RULE (`promote-clone.ts` `runAddRepeatingTodo`, derive `start-days-earlier = deadline − when`); the PROJECT variant (`runAddRepeatingProject`) still passes the concrete deadline to the seed item-level, where it is DROPPED from the series (the seed is DELETE-fate — projects do not preserve on a deadline, so no double-book, just a lost deadline). Low value/priority (projects rarely carry per-occurrence deadlines and there is no duplicate-factory bug to fix), but the mapping should be aligned for consistency: apply the same `deadline − when` derivation in `runAddRepeatingProject` (or refuse a concrete project `--deadline` on a repeating series if per-occurrence project deadlines aren't wanted). `addRepeatingViaCreate` already carries the deadline-aware drive; only the project entry point needs the mapping. Mechanical.
- **`things repeaters` — a library-wide template listing.** Repeating templates are nearly invisible to reads: search matches a repeater's open *instances* but never the template; the template's full rule is reachable only via `show <template-uuid>`, whose uuid you can only learn from an area view. RS6 already closed most of the read-shape gaps; the residual is a dedicated listing (template uuid + full rule, library-wide). Small, high agent value. *(Note: "let search match templates" was floated but is unverified — treat as a separate probe, not part of this item.)*
- **Undo session-qualified actors (`mcp:<session>`).** The optional fourth layer of undo actor-scoping: session-qualified actors with prefix matching on `by:`. PARKED-CONDITIONAL — build only if parallel agents become real.
- **`--markdown` output mode.** A third render target alongside TTY and `--json`: YAML frontmatter for metadata, `- [ ]`/`- [x]` for to-dos/checklists, `##` for Things headings, project title as `#`. It is a RENDERING of the same data, never a third contract (derive from the JSON-shaping layer, drift-tested); NO markdown import/roundtrip in scope. Likely sweet spot: agent contexts + piping into notes. *(Mike: do NOT spec yet — a future design discussion.)*
- **Tag shorthand — `things @errand` (explicit-only).** Bare-noun routing of a tag NAME is rejected (collision surface; a rename could silently flip which view renders — see [design/decisions.md](design/decisions.md)). The open sliver is whether an explicit sigil (`things @errand` → `things anytime --tag errand`) is worth it at all, or whether tags stay explicit-only via `--tag`. Needs deliberate design; not scheduled.
- **Stale-DB stderr warning on ordinary reads.** `doctor` answers "data may be stale — Things is not running" on demand (sync-health section); whether every read should emit a one-line stderr nag is a separate UX call that overlaps the display-preferences / chrome decisions above.
- **Cross-terminal glyph audit.** The CLI glyph language (`src/cli/glyphs.ts` — checkbox marks, pie quarters, chips, `★/⏾/≡`) was chosen on one macOS font stack. Audit rendering across emulators/fonts (Terminal.app, iTerm2, Ghostty, kitty, VS Code, Warp × SF Mono/Menlo/JetBrains Mono/a Nerd Font, plus a CJK-wide `ambiguous=double` config and a `NO_COLOR`/piped pass). Watch for tofu on `⏾` (U+23FE) and the pie quarters, double-width drift breaking column alignment, dim-on-light legibility. Every glyph is a `glyphs.ts` constant, so retuning is one file.

## feature design — wants an ideation round before code

- **Change-watch (`things watch`) + the agent daemon — design of record now lives in [design/agent-daemon.md](design/agent-daemon.md) (v1.0 GATE via roadmap).** The full watch design (snapshot-diff engine, four layers, level-based semantics, umd-SILENT constraint, WAL-wake trigger) was transcribed there 2026-08-19 and reshaped: phase α builds the engine + `things changes` in-process; the long-running `watch` loop hosts in the launchd helper (phase γ). **β1 — the permission deputy — SHIPPED 2026-08-19** (design §3a): the TCC-identity half of the daemon exists as a dumb privileged broker. NEXT STEP: the maintainer ratifies the remaining phase plan (α → β → γ → δ), then phase α. Open maintainer calls carried in the plan §5: `modified` field granularity (recommend all-fields w/ per-event list), `changes` as a first-class command (recommend yes), project-completion as a terminal event, undo-reversal reading.
- **Helpers — enable routing + residuals (ceremony DONE 2026-08-22: bundle installed, reader granted on the live host).** **Mike:** rebuild + reinstall after the bundle PR merges (`bash scripts/build-helpers.sh && things helpers install`), then `things config set helpers-enabled true`. The first routed write fires the two one-time Automation prompts (Things3, System Events); Accessibility is a Settings toggle when the first GUI drive is wanted; then optionally revoke Ghostty's FDA. Residuals: sync-health's direct `statSync` on container files (metadata reads — verify whether Sequoia's app-data class gates them); MCP server has env/config routing only (decide if it needs a per-call flag); composite SIGKILL-immunity end-to-end stays a β deliverable (the deputy bounds the orphan window to one osascript step).
- **Helper UX batch (ratified 2026-08-21, design §3c).** (a) `helpers-enabled` → tri-state `auto|true|false`, default auto = installed-and-healthy ⇒ used, degradation always loud (notice + doctor), never silent; (b) `things doctor` helpers section (installed/loaded/running/granted/signing/skew/hung-socket) + passive install/upgrade steering notices (skill-check pattern; staleness remedy is `things helpers install`, a re-copy); (c) works under `npx things-api`; (d) **graceful upgrade drain (Mike, 2026-08-22):** install/restart bootout a helper mid-flight today — a request in progress dies with the old process; add a drain (stop accepting, finish in-flight within a bound, exit) so upgrade-while-busy is safe.
- **Helpers distribution batch (ratified 2026-08-21, design §3; single-bundle packaging SHIPPED 2026-08-22).** Release-time CI signing + notarization (p12 + App Store Connect API key as repo secrets, throwaway keychain, tag-triggered only); notarized `Things API Helper.app` shipped in the npm tarball with `things helpers install` as activation; probe cell: confirm a FREE Apple Development cert satisfies amfid for the sandboxed reader (self-builder path).
- **`--preserve-modified` follow-ups (the flag SHIPPED 2026-08-13 — universal write flag, best-effort restore, presence-keyed `preservedModified`/`preserveFailures`, captured pre-`umd` on the audit record).** The **SYNC-SAFETY GATE is RESOLVED** (SYNC2B, 2026-08-13, golden-v2/3.22.12): the flag is **sync-safe** — a hand-written past `umd` propagates to the peer device AND survives the sync round-trip in both directions (Things Cloud treats `umd` as ordinary per-attribute synced data, not a monotonic sync clock; the accompanying content edit rides the change-log independent of `umd`; a genuinely concurrent same-row edit elsewhere re-bumps `umd` via the per-attribute max-merge, which fails safe). The "UNSYNCED-only" caveat was **retired** from the `--help`/MCP/skill/`timestamps.md` copy. Evidence: [docs/lab/sync2b-durable-account.md](lab/sync2b-durable-account.md) (SY-2/SY-2M). Two open items remain from the TAGMOD arc: **(1) tag-cleanup RECIPE doc (now unblocked).** Write the documented mass-tag-cleanup recipe the flag enables (delete/rename freely — `umd`-silent; apply with `--preserve-modified`; deleting an `(archived)` area detaches its logged contents `umd`-silent, trashes only open ones — RD-5 refinement) into the skill/reference. **(2) symmetric undo (deferred, pending a maintainer ruling).** The `ok` audit record already carries `preModDates` (the captured pre-`umd`); a future undo of a preserve-modified write could restore the `umd` the inverse bumps. Undo interplay was explicitly out of scope for the flag's first cut.
- **Notes-are-Markdown documentation sweep (NOTESMD step 2).** The VM probe (step 1) is done — notes render markdown FORMATTING but keep the syntax markers VISIBLE (source-preserving live render): headings/emphasis/code/fenced/bullets/ordered/bare-URL-autolink RENDER; `[label](url)`, `*`-bullets, blockquote, `---`, `- [ ]`/`- [x]` checkboxes are INERT (with a `- [x]`-dims quirk, oddities §9d). **Step 2 (not started):** document the verdict everywhere agents form expectations — the skill references, `things help` copy, MCP tool descriptions — as "notes is a *markdown field with multi-line support*, not a text blob," plus "don't wrap bare URLs" (subject to surface-copy.md). Evidence: [docs/lab/notesmd-results.md](lab/notesmd-results.md).
- **Multi-line notes as first-class CLI input.** Already WORKS (`--notes $'line1\nline2'`), but nothing advertises it and there's no composable input path. Candidates: `--notes -` (read stdin; heredoc/pipe-friendly), maybe `--notes-file <path>`; document the `$'…\n…'` idiom regardless. Deliberately NOT: silently interpreting literal `\n` escapes inside the plain flag (ambiguity footgun). Cheap; could ride the NOTESMD doc pass.
- **Markdown checkboxes → checklist import (ideation only).** Let a `- [ ]`/`- [x]` block in supplied notes convert into REAL checklist items (import a run-list in one call). Must be an explicit opt-in flag (`--checklist-from-notes`), never a silent transform. Open questions: does the checkbox block get stripped from the stored note, does `[x]` map to checked, what's the roundtrip story. Depends on NOTESMD confirming checkbox syntax is inert in the rendered note (it is).
- **`things onboard` — per-app-context TCC onboarding (RESHAPED by the helpers, Mike 2026-08-22: fold into `things helpers grant` as a FULL onboarding ceremony).** Ratified direction: with the helpers installed, the onboarding target is the DEPUTY's stable bundle identity, and the grant ceremony should leave `things` fully headless-ready in ONE human sitting — fire every prompt while someone can click: the reader folder grant (shipped), deputy→Things3 + deputy→System Events Automation prompts (a benign osascript each), Accessibility registration (AXIsProcessTrustedWithOptions prompt → Settings deep-link; a toggle, not a modal), Shortcuts install (`things setup`) + a first `things-proxy-*` run, and URL-scheme readiness (no TCC class; verify the Things URL auth token where update ops need it) — then verify-report granted/denied per vector, so no on-demand modal ever lands mid-workflow later. All four mutation vectors durable in perpetuity under the helpers' identities. The original per-app-context framing below remains for the NON-helper (direct) path. A new host app (T3 Code nightly, a different terminal, an ad-hoc-signed rebuild) starts from zero macOS grants because TCC attaches to the responsible process, so consent modals land one at a time mid-workflow. Four deliverables: (1) a permission-class inventory (Apple Events per host→Things3 and host→System Events pair, the Sequoia "access data from other apps" group-container class, Accessibility, Shortcuts, URL dispatch; FDA as the blunt umbrella); (2) a one-swoop `things onboard` flow that deliberately fires every prompt NOW while the human can click, then verify-reports granted/denied per class; (3) prompt-free status detection (`AEDeterminePermissionToAutomateTarget` askUserIfNeeded=false, `AXIsProcessTrusted`, responsible-bundle resolution) surfaced as a warning in `things --help` and `doctor` ("this app context hasn't been onboarded — run `things onboard` now"); (4) a staleness matrix (host self-update, bundle-id channel differences, macOS majors, `tccutil reset`, Things-side updates, manual revocation). Cross-link the `scripts/prod-read.sh` one-stable-shape rule (same mechanism edge-on) and remember the MCP daemon inherits its launcher's attribution.
- **Heading-demotion Phase B.** Phase A shipped (project-scoped verbs, bulk create, move/reorder vocabulary, detach family). Phase B is `project set-layout` (unbuilt). **Re-verify what Phase B still means:** `reorder --scope` was already RETIRED in 0.14.0 (replaced by the kind-neutral `reorder … --in <target>`), so the old "retire reorder --scope" sub-goal is done; the `sort --by <key>` candidate is subsumed by the query-view ruling (see [design/decisions.md](design/decisions.md)). What remains: `project set-layout` + view-order polish. Two adjacent unlocks queued from the #298 campaign: **(a) BOUNCEJSON collapse** — rewrite the BACK-insert bounce classes (within-heading order, project-someday fallback) in `src/write/reorder.ts` to compile ONE pre-validated `things:///json` array (~7× faster, validate-first FULL-ABORT; FRONT-insert classes stay on the URL bounce, json is index-inert there per oddities §9i); **(b) `project.move-heading-to-project`** — a new ui-vector op on the lab-confirmed ellipsis-menu recipe (HID-click the heading `…` → Move… → keyboard picker; DB oracle = single heading-row project-FK rewrite, children follow via heading FK) — needs the standard certification arm + two-key gating; closes the last heading capability gap. Spec: [design/heading-demotion-and-move.md](design/heading-demotion-and-move.md).
- **CLI exposure of `project add-repeating` structured children (design note, maintainer to refine).** The LIBRARY `project.add-repeating` now threads the full project add vocabulary including the structured `items` field (#461 — headings + children + terminal-state), but the CLI `things project add-repeating` exposes only flat `--todo` (like `project add`'s flat `--todo`). Recommendation: a repeatable `--child <title>` for the flat case + `--items-json`/stdin for the full heading structure — mirroring whatever `project add` itself grows for structured children (they should share the flag surface). A design note, not a settled shape.
- **Checklist state-token conditional replace — ETag/If-Match.** Offer "produce the entire new checklist state in one call," guarded git-`--force-with-lease`-style by a token representing the state the caller last saw (HTTP ETag/If-Match is the spec to crib). Sketch: (a) token = deterministic content hash of `[{title, completed}…]`, version-prefixed, opaque, derivable from any read; (b) the token subsumes `acknowledgeChecklistReset` (presenting a fresh token proves the caller SAW the state being replaced); (c) a mismatch returns the CURRENT items + fresh token in the error payload so an agent rebases without an extra read; (d) granular intent ops stay (they have undo a replace can't reconstruct) and gain optional `--if-match`. Honest limit to document: no atomic CAS at the app layer — the check is re-read-and-compare immediately before dispatch (window shrinks to ms, not zero). Generalizes to demoted-heading structure later.
- **Idempotency phase 2 — timeout reconciliation.** Phase 1 (single-op `--op-id`) matches VERIFIED-OK records only. The open case is a resubmission whose ORIGINAL attempt is a `verify-failed:timeout` record (the write may or may not have landed). Phase 2: on a resubmission whose `opId` matches such a record, re-read current state to decide present (→ replay as already-applied) vs absent (→ execute), for both the batch line and single-op paths (they share the lookback). Contract confirms this is queued ([docs/design/contracts.md](design/contracts.md)). Sibling of the checklist ETag/If-Match round.
- **Disruptiveness deployment profiles — per-op capability gating + docs.** The `THINGS_API_PROFILE` knob EXISTS (`workstation` / `dedicated-server`, `src/config.ts`) — so the profile plumbing is not the open work. What remains: gate the disruptive mutation CLASSES per profile (app relaunch cycles, AX/ui-vector dialogs, anything that steals or kills the running app), have refusals under the stricter profile NAME the profile knob, and add a disruptiveness COLUMN to the capability matrix / timezones doc keyed to the profiles. Motivation: app-restart and AX-driven mutations are untenable on a daily-driver workstation but fine on a dedicated headless Mac-mini host. *(This item homes HERE only — roadmap points nowhere at it, per one-home.)*
