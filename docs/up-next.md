# Up-next queue (parked 2026-07-12, post-v0.8.0 · re-triaged 2026-07-22 at v0.11.0)

The working queue for the next session(s). Everything here was triaged with Mike on 2026-07-12 after the v0.8.0 release (PRs #73–#85: Shortcuts write vector + live validation, reversible/guarded undo, checklist intent undo, reversibility matrix, trash-cascade fix, the `things projects` sidebar mirror, upcoming window, hidden-item hints, `--when` validation, capabilities undo column). Read `docs/roadmap.md` for the long-lived doctrine; THIS file is the short-horizon pick list. Remove items as they land. **Re-triaged 2026-07-22 (v0.11.0):** landed items struck (agent primer/skill arc = fully shipped); everything else verified still-open against the codebase; §6 added (Mike's two new feature items).

## 0. Recurrence/reorder operationalization gaps (added 2026-07-22 — TOP PRIORITY per Mike)

Mike asked (2026-07-22) whether the AX-probed recurrence + reorder workflows are actually OPERATIONALIZED as agent-invocable commands or merely probed. Audit verdict: **almost everything is operationalized and lab-certified** — promote-to-repeating / reschedule / pause / resume for BOTH to-dos and projects (full rule vocabulary on make/reschedule: after-completion, weekly multi-day, monthly day/nth-weekday/last, yearly, ends after-N/on-date, deadline + start-N-days-earlier), `project.add-repeating`, every reorder scope (today/evening/project/headings/area incl. within-area projects, someday, top-level-projects bounce), and `area.reorder`. All ui-vector ops are two-key gated + `lab-certified` (none on-device `certified` yet — that axis is tracked in §2/§3). The audit surfaced these REAL gaps, queued here at top priority:

1. ~~**Capability-matrix doc drift (fix FIRST, cheap).**~~ — **DONE 2026-07-25 (PR #271, this change):** phantom MCP tool names fixed in the matrix AND in ui-vector.md / ax-initiative.md / two lab cert docs (the sweep found the same eight names there); stale SHIPPED-UNCERTIFIED labels corrected to lab-certified per the manifest; `convert_to_project` confirmed REAL (an action of the `heading` tool) and left intact. *Original item:* `docs/capability-matrix.md` (lines ~112/121/122) advertises eight discrete MCP tools — `make_repeating`, `make_project_repeating`, `reschedule_repeat`, `reschedule_project_repeat`, `set_repeat_state`, `set_project_repeat_state`, `create_repeating_project`, `reorder_area` — **none of which exist**: the code ships two multiplexer tools, `repeat` (`{scope: todo|project, action: start|reschedule|pause|resume|add}`, `src/mcp/server.ts:1858`) and `reorder` (`server.ts:2227`), plus generic `run_operation`/`batch`. Any agent wiring against the documented names fails. Same pass: the matrix's lingering "SHIPPED-UNCERTIFIED" labels on the to-do recurrence ops are stale — `ui-certification.ts` has every ui op `lab-certified` (UIC1/UIC3/AXDRAG2/UIC5/UIC6/HEADCERT1). Also check the skill/help copy quotes the real tool names. **PICKED UP 2026-07-25 — fix PR #271 opened (docs-only; the sweep found the same phantoms in ui-vector.md, ax-initiative.md, and two lab cert docs, all fixed; `convert_to_project` is REAL — an action of the `heading` tool — and was left intact); strike this item when it merges.**
2. **`todo.add-repeating` (create a repeating TO-DO from scratch).** The one asymmetry: projects have `project.add-repeating` (composite add→promote), to-dos have NOTHING — an agent must `todo add` then `todo make-repeating` (two calls, and the promote is an identity replacement, so the uuid from the add is dead). UI1 proved File ▸ New Repeating To-Do feasible, but the simpler build is the same composite the project op uses (add → make-repeating), no new AX surface needed. Decide: composite op (recommended, symmetric) vs File-menu path vs status quo documented.
3. **`project.add-repeating` full-vocabulary parity.** Deliberately frequency+interval-only today ("stays minimal"); the full vocabulary is reachable via add-repeating → reschedule-repeat, but that's TWO GUI drives. Decide: extend the composite to pass the full `RepeatRuleParams` through (the promote leg already accepts it), or keep minimal + document the two-step.
4. **Anytime aggregate reorder scope (loose to-dos).** — **SETTLED 2026-07-23 (ANYORD, `lab/scripts/research-anyord.sh`, verdict [docs/lab/anyord-results.md](lab/anyord-results.md)): NO operable convention — do NOT ship an `anytime` scope bound to `list "Anytime"`.** The aggregate command `reorder to dos in list "Anytime"` is (a) **DESTRUCTIVE** — a single call strips `area` → NULL on every loose to-do it touches (isolated on a pristine pair; new oddities §9c), and (b) **non-deterministic** — five identical `A1,A2,A3` calls gave five different orders, none matching, no convergence (a first call may coincidentally match; resolves P7b-vs-P8d = neither model holds; P13 stands). It writes `index`. **Semantics map:** Anytime is a GROUPED view (ungrouped area-less section + per-AREA + per-PROJECT groups); within each group order = `index` ascending (GUI verified to match DB `index`); `index` is a single GLOBAL sequence acting as a within-group key; cross-group placement = grouping, not index → there is no flat "Anytime order" to reorder. **Clean path (for the §6 round):** reorder loose anytime to-dos via their CONTAINER (area) specifier — confirmed deterministic exact forward re-rank on `index`, area-preserving (O04 generalises to within-area); area-LESS loose anytime to-dos have no clean container surface → un-reorderable via automation (documented gap). So "reorder within Anytime" = "reorder within the area group," fold that framing into the §6 sort-ergonomics round. **Follow-up probe queued 2026-07-23 (Mike's proposal, ANYBNC):** does the when= BOUNCE (`someday → anytime` round-trip — the P7d/P8e front-insert primitive that ships as `--scope projects`) also front-insert an AREA-LESS loose TO-DO into the ungrouped Anytime section? If yes, the residual area-less gap closes as a bounce-only path (reverse-order legs, ≤10/call, inherited abort safeguards) — with a LOUD warning / dry-run leg count on large reorders, since it costs 2 verified legs per item. Probe must verify no-residue state preservation (`start`/`startDate` restored, area stays NULL, `index` front-insert lands below the global min; reminders are a non-issue — anytime items carry none). Fallback avenue if bounce fails: HID row-drag in the Anytime view (the AXDRAG1 recipe generalized to CONTENT rows) — unprobed there, foreground-bound mouse synthesis (tier 3), a real drop-into-container nesting hazard the sidebar-area case doesn't have, plus the §9 ghost-bug fragility; only worth probing if area-less anytime ordering matters enough. **→ ANSWERED 2026-07-27 (REORDGAPS ANYBNC, [docs/lab/reordgaps-results.md](lab/reordgaps-results.md)): the bounce WORKS — a reverse-order `someday→anytime` round-trip front-inserts each area-less loose anytime to-do state-preservingly (`start=1`/`startDate NULL` restored, `area` stays NULL, `index` lands below the running global min, exact target order). The residual area-less gap closes as a bounce-only path (2 legs/item; the fallback HID drag is unneeded).**

Known non-gaps, for the record (deliberate or app-blocked, do NOT reopen without new evidence): un-repeat/stop for both kinds (dropped — card-only Stop popover, and stop-then-select CRASHES Things, §7 C5); repeat-rule `reminder` refused fail-closed (app bug §8l — the dialog's `AXDateTimeArea` ignores programmatic writes); heading-scoped child order (O06, app-unautomatable); tag reorder (ABANDONED by decision 2026-07-15); rung-2 held-scroll area drag (shelved, oddities §9 ghost bug).

## 0½. Field-report bug & friction triage (triaged 2026-07-25, from the 2026-07-23 agent-session reports)

A separate Claude Code session ran a real ~35-write audit of a work area in Mike's library on 2026-07-23 and left two reports on his Desktop: `~/Desktop/things-cli-friction-report-2026-07-23.md` (friction ranking) and `~/Desktop/things-reschedule-repeat-debug-2026-07-23.md` (verbatim debug dossier for one incident). **Both reports contain production task data (titles, area names, uuids) and MUST NOT be committed** (safety rails) — they stay Desktop-local; this section is the sanitized triage. For the record, the positives: ~35 writes with zero data drift and honest exit codes; the H-UI-DRIVE gate copy judged "genuinely helpful, not an obstacle"; the `{ok, data, meta}` envelope held on every read path.

1. ~~**BUG CLUSTER — `todo.reschedule-repeat` GUI drive (six defects from ONE real fixed→after-completion conversion).**~~ — **FIXED + IN-VM VALIDATED 2026-07-26 (branch `mg/reschedule-repeat-cluster`, UIC7 — [lab/uic7-reschedule-cluster.md](lab/uic7-reschedule-cluster.md); run `uic7-lab`).** Per-defect: **(a) ✅ FIXED+validated** — the incident now returns `ok` with the landed rule (`tp=1 fu=256 fa=2`); the reschedule delta now asserts the rule **type** (fixed vs after-completion — both rules were weekly/2, only the type flipped, so verify previously couldn't see the conversion at all); a nonzero-transport drive now **re-verifies with bounded backoff** and returns `ok` (loud "the change DID land" warning) if the rule landed; a **pre-drive idempotency** short-circuit returns an `ok` no-op with no GUI drive when the target rule already holds (validated live: elapsedMs 33 vs 4676, no drive). **(b) ✅ validated** — every drive's `--json` stdout is a single clean envelope, INCLUDING two genuine live failures (verify-failed:mismatch and a transport-fail→recovery-fail), no banner leak. **(c) ✅ FIXED** — the after-completion unit pop-up is driven by a candidate label list (singular AND plural for every unit), clicking whichever exists, fail-closed if neither; the conversions drove it live (singular in UIC7; the PLURAL label was then exercised live in UIC7b MR4b — rescheduling an interval-2 rule opens the dialog in plural state). **(d) ✅ host-unit-validated** — abort now asserts the sheet is gone (retry once) before claiming dismissal, else warns it may remain open. **(e) ✅ host-unit-validated** (live test inconclusive — the reveal preamble dismissed the hand-opened sheet before the canary; needs a detached-window repro) — a canary miss now checks for an open sheet and names it first. **(f) ✅ probed** — `ts` (start-offset) and the deadline SURVIVE a fixed→after-completion conversion; the weekday `of` resets to the unit nominal (oddities §8p, recurrence.ts caveat); the dynamic next-spawn observation was NOT captured (harness passed `--dangerously-drive-gui` to `todo complete`, which rejects it — follow-up). **NEW discovered defect — RESOLVED 2026-07-26 (branch `mg/make-repeating-interval-guard`, stacked on the above; UIC7b, run `uic7b-lab`):** the interval numeric field, when it's the FIRST field after a frequency/type switch, races the group re-layout and lands `1` (oddities §8l addendum). The silent-wrong case — `make-repeating --interval N>1` (fixed) landing `fa=1` while reporting `ok` (create-probe asserted only `isTemplate`) — is closed two ways: (1) the `set-value` primitive drives the interval CLOSED-LOOP (type → Tab → read-back → bounded re-type, fail-closed), which LANDS interval>1 live (VM: fixed weekly/2→`fa=2`, monthly/3→`fa=3`); (2) the make-repeating create-probe now verifies the decoded rule (type/unit/interval), so any residual mis-commit is `verify-failed:mismatch`, never silent `ok` (covers todo + project make + the `add-repeating` promote leg). **Zero silent-wrong across 5 in-VM cases**, and the PLURAL after-completion unit label finally exercised live (closes the UIC7 (c) gap). *Original triage retained below.* The desired rule change **did land**, yet the CLI reported failure on both drive attempts, and a retry with a *different* rule was only stopped by a preflight refusal — the failure mode actively invites clobbering. Defects: **(a) false-negative verify** — the drive reported `VERIFY FAILED (silent-noop)` although the template's rule matched verify's own assert afterward; design mitigations: re-verify with backoff before declaring silent-noop, and idempotency-aware retries (compare the LANDED rule to the target before re-driving). **(b) `--json` contract broken on the verify-failed path** — stdout began with the plain-text `VERIFY FAILED` banner (jq choked); the preflight-refused path correctly emits a JSON error envelope. **Triaged 2026-07-25 (PR #273): NOT REPRODUCIBLE on HEAD** — the banner is stderr-only and `--json` emits exactly one stdout envelope on both constructible failure paths (shared emit branch, in place since #161); regression tests now lock that contract. Plausible field explanations: the dossier's verbatim command piped `2>&1` into jq (a stderr banner would interleave into the stream), and/or the specific mid-drive AppleScript-abort shape (transport exit 1 after a partial drive) behaves differently from what is constructible in tests — re-check (b) empirically during the VM repro for (c) before declaring it closed. **(c) unit-popup pluralization** — the after-completion unit drive died on `menu item "week"` not found; with interval 2 the popup reads "weeks" (check the general days/weeks/months/years labels AND set-order: typing the interval before selecting the unit can flip the label singular→plural mid-drive). **(d) sheet-cleanup claim unreliable** — the abort said "sheet dismissed (Escape)" while the user watched the sheet stay open in an intermediate state. **(e) misleading preflight diagnosis when a sheet is open** — the still-open sheet disables the menu bar, and the preflight refusal then blames a Things update / Accessibility / non-English UI; add open-sheet detection (or at least list it as a cause). **(f) residual rule fields after conversion** — `startOffsetDays` and weekday `offsets` from the old fixed rule survive into the new after-completion rule (`nextOccurrence` correctly nulls); probe whether that residue skews the next spawn (RSIM follow-up candidate). *Analysis lead (unverified):* the Repeat dialog appears to PRESERVE unit/interval when frequency flips to after-completion, so the correct rule may have landed by inheritance BEFORE the explicit unit-set step aborted on (c) — that would explain applied-but-reported-failed, but NOT why verify's assert (which matches the after-state) still failed; verify timing (pre-flush read?) needs a hard look. **Repro recipe (VM):** throwaway fixed weekly repeater → `reschedule-repeat --after-completion --frequency weekly --interval 2` with the gates; watch (c), the verify verdict, whether Escape really dismisses the sheet, and raw `--json` stdout on failure. The certification suite must gain a regression probe for (c) — UIC5/UIC6 certified the vocabulary on CREATE paths and plainly never exercised this conversion shape.
2. **View `--area` filter (ergonomics).** `things anytime --area <ref>` doesn't exist; an area-scoped audit — a completely natural agent workflow — forces a fallback to the full `things areas <ref>` dump. **Implementation PR #272 opened 2026-07-25** (decisions filled by Fable: flag on `today`/`anytime`/`someday`/`upcoming`/`logbook`; transitive membership = loose items whose area matches PLUS items under the area's projects; existing ref resolution, fail-closed on no match; additive `meta` annotation; matching optional `area` param on the MCP view tools). Distinct from — and composes with — the §6 container-scoped sandbox (that is process-level policy; this is a per-call convenience).
3. **Inconsistent JSON shapes across kinds — ✅ IMPLEMENTED 2026-07-28 (branch `mg/contract-1-0-shapes`): the 1.0 envelope shape break (R1–R5 + the `error.detail`/`error.details` merge + the project-view heading sub-bucket fidelity fix) SHIPPED as one clean break.** `data` is object-rooted everywhere (`{items}`/`{sections}`/`{item}`/`{view}`); `show`/`todo-detail` unified into kind `detail`; a loose `show` of an area/project emits the identical `area-view`/`project-view` envelope; `later` flattened to arrays in both cards; heading groups carry their own `scheduled`/`someday`/`repeating`; `meta.grouped` folded into `meta.truncation` (`.truncated` is the universal completeness check); the redundant mutation/move success discriminator dropped from the wire entirely (envelope `ok` + `kind` are the success signal — single-op `data` carries no `result`/`kind`, batch JSONL lines flatten to a string `outcome` with variant fields hoisted to line level; amended 2026-07-29 per the envelope-school doctrine, superseding the earlier `kind`→`result` rename in R5); `error.detail`/`error.details` reconciled into one `error.detail`. `apiVersion` stays `1`. **MCP framing alignment (phase 2) — ✅ DONE 2026-07-31 (branch `mg/mcp-alignment`):** the MCP mutation/move content block no longer carries the internal success discriminator — a single-op success emits through `mutationWireData` (no `kind`/`result` in `data`), a dry-run emits the bare plan, and batch lines flatten to the string-`outcome` JSONL wire shape — matching the CLI. The same PR reshaped MCP `reorder` to the planner form (+ dedicated `reorder_areas`) and threaded the `loose` shadow-disclosure / area-view placement notices into MCP `meta.warnings` (read_view/search/get_area). Representative payloads regenerated for the maintainer's review. Remaining follow-up: the skill re-sweep (roadmap §N phase 4) then v1.0 (ALPHA-CONTRACT teardown). *Original ruling record retained below.* Mike's rulings on the shape-rework proposal (the five rules R1–R5: object-rooted `data` everywhere with `{items}`/`{sections}`/`{item}`/`{view}` wrappers; `show`/`todo-detail` kinds unify into `detail`; `later` flattens to arrays-only in BOTH area-view and project-view; one `meta.truncation` shape absorbs `meta.grouped`; the mutation payload's inner `kind` renames to `result`): **D1 YES** — this is the time for breaking changes; **D2–D5 per recommendation** (`{items}` wrapper; kill the show wrapper; flatten both views; MCP framing alignment = phase 2); **D6 clean break, NO compat shims** — "we have a single consumer right now: me; there is no barrier whatsoever to breaking changes at the moment. that may change in the future"; **D7 the heading-demotion breaking round happens FIRST** — these decisions are locked and parked here until the demotion round's shape lands, then the v2 build proceeds as one delegable PR. **MANDATORY rider (ruled): reconcile `error.detail` / `error.details` into ONE field** in the same break. **Shape addendum (2026-07-27):** the project-view payload additionally gains the heading sub-bucket fidelity fix — heading groups carry their own scheduled/someday/repeating arrays, mirroring the GUI ([design/heading-demotion-and-move.md](design/heading-demotion-and-move.md) §9) — superseding the flatten-`later` sketch for heading children. Standing directive attached to this arc (Mike, 2026-07-27): the API contract — programmatic TS, CLI, MCP — is the project's interaction surface and now gets DIRECT scrutiny, not vibe-trust; the v2 PR must present representative payloads for review, not just tests. **Doctrine landed 2026-07-27 (ALPHA-CONTRACT):** the D6 clean-break stance is now a standing rule in AGENTS.md (Conventions) and docs/design/architecture.md (Alpha contract) — the WHOLE contract is alpha until v1.0, no compat shims/alias maps/deprecation windows pre-1.0, and agents must not add or propose them; all three signposts are removed at v1.0 (`grep ALPHA-CONTRACT`). **Schema revisions accumulated for the parked break (2026-07-27, revisit when unparked):** the heading sub-bucket project-view shape (§9 of the demotion spec); batch summary additions already shipped (`tempIdMapping`, batch `undoToken`) to be presented in the payload review; heading uuids CONFIRMED staying in reads (demotion ruling); naming ruling — there is NO v2: the break ships as "the 1.0 contract", `apiVersion` keeps value 1 (pre-1.0 = alpha per ALPHA-CONTRACT); the v2-response-explorer artifact samples must be regenerated against all of the above before the payload review.
4. **Repeating templates nearly invisible to reads.** Search matches a repeater's open *instances* but never the *template*; the template surfaces only in an area view's `later.repeating[]` whose summary rows OMIT `rule`; the full rule is reachable only via `show <template-uuid>`, whose uuid you can only learn from the area view. Net: no clean before-snapshot path for rule mutations — exactly what the bug-cluster debugging needed. Minimal fix candidates (not started): include `rule` in `later.repeating[]` rows (additive), let search match templates (rows already carry `repeating.isTemplate`), and/or a dedicated `things repeaters` listing (template uuid + full rule, library-wide). Small, high agent value.
5. **Quick wins — PR #273 opened 2026-07-25:** `things config get <key>` (with `stored`/`env`/`default` provenance) + bare `get` listing; additive `title` echo on ok results — applied uniformly to the whole single-item mutation family (every op with a `pre.target`), not just cancel/complete. Surprise for Mike: **`things config show` already existed** (flat effective dump, no single-key form, no provenance) — the field reporter and the triage both missed it; `get` adds provenance + single-key rather than duplicating it, but the get/show overlap wants a ruling (keep both vs fold).

6. ~~**BUG — a MIXED-kind day-block reorder that ALSO contains a repeating template is refused UPSTREAM ("one kind at a time").**~~ **FIXED + CERTIFIED 2026-08-04 (PR 3, GRNDINT-2 — branch `mg/intermix-template-fix`; [lab/grndint2-mixed-interleave-fix.md](lab/grndint2-mixed-interleave-fix.md)).** `globalAxisIntermix` ([src/write/move.ts](../src/write/move.ts), `runInPlaceReorder`) gained a template-with-strictly-future-projection disjunct (mirrors `rowDayKey`), so a template-bearing mixed-kind set falls through to the day-axis resolver — day-axis-scoped (a forced INDEX token still refuses "one kind at a time"). The secondary surfacing wart is fixed too: the ratified `H-REORDER-SCOPE` template refusals (suffix non-conformant, experimental-off) now HOIST to the canonical top-level `move-refused` (`blocked` → exit 4, `blocked:H-REORDER-SCOPE` code + `BLOCKED (H-REORDER-SCOPE)` copy, via the new `MoveRefused.hazard` / `repositionFailed` helper) instead of being buried under a generic `move-leg-failed`/`verify-failed` (exit 3). Locks: o-suite **O38/O39** (full mixed inventory fwd+rev) GREEN on two clean passes (`o-20260804-201734`/`o-20260804-202132`, `lab:compare` identical across 38 probes); e2e-write-smoke GREEN **132/0**; engine tests in `test/engine/write-move.test.ts`. *Original triage retained below.* Root cause: a template row (startDate NULL, no deadline) satisfied neither `scheduleBucket`/`forecastDeadlineDay` branch, failed the `.every()` predicate, so the mixed-kind relaxation never fired and the upstream `indexKindRefusal` blocked the set before the #393 day-axis resolver ran.

7. **FIELD BUG — a container-only `todo move` can broadly rewrite Today order (added 2026-08-10; verify in VM before fixing). — REPRODUCED AT BYTE LEVEL 2026-08-11 (MOVPLC, golden-v2/3.22.12, [lab/movplc-move-placement-today.md](lab/movplc-move-placement-today.md)); FIX IN FLIGHT.** The VM settled the audit's open write-set question: the native `list "Today"` reorder writes each named row's `todayIndex` to the wire order AND re-stamps its `todayIndexReferenceDate → today`, leaving `startDate`/`start`/`startBucket`/`status`/`stopDate`/`umd` byte-identical — the `tiRef` collapse fuses every entry cohort into one, so the visible order becomes pure wire order (this is the O03/UPCDL-2a materialize law on EXISTING multi-cohort members). Controls confirmed: cancellation reorders nothing; the raw membership move (the CLI `update?list-id=` leg) does NOT reorder Today (the movee keeps its slot) — so the placement leg is the sole cause. Excluded classes (canceled `status≠0`, stale evening `startBucket=1`) stay byte-untouched and sink below the collapsed block (the "regrouping" is a by-product). The census leaks derived-trashed children (own `trashed=0`) into the wire. Fix (evidence-gated remedy 1): a no-position container move skips the today/evening placement reorder; `computeReorderPre` gains the reader's `CONTAINER_UNTRASHED` exclusion. This REVISES the ratified "destination + no position ⇒ `--first` implied per bucket" default ([design/heading-demotion-and-move.md](design/heading-demotion-and-move.md) §Bare invocations) for the today/evening view axis only — justified because the native Today reorder is not GUI-faithful (it rewrites the whole view via `tiRef` collapse, which a GUI drop does not). A one-item production move automatically emitted an open-only native Today reorder over 277 rows, excluding 127 unswept canceled rows. The canceled cohort changed from 73 interleaved runs to two near-contiguous blocks, but the effect was not status-only: among 267 common open rows, 45.6% of row pairs inverted relative to the pre-cleanup visible order, and several recurring lineages became adjacent. A later explicit reorder supplied an immediate before/after control: it moved only the requested row, while the resolved block and the same nine-row open tail already existed. The tail is fully explained by stale `startBucket=1` rows sorting after bucket 0 and being intentionally omitted from native Today wires (O03); the wire also leaked nine derived-trashed children from its planner census. The wire's ordering rule is requested-row-first + raw `todayIndex ASC`; no semantic grouping rule is established. Candidate read ergonomics to evaluate alongside the fix: (a) filter recurring instances by template lineage, independent of manually maintained tags; (b) sort/filter Today by original scheduled date, overdue age, or time in Today; and (c) expose original scheduled date plus Today/Evening bucket in read-only output. Overdue rows can retain their original stored schedule even while Things presents them through Today order, so these are diagnostics, not a proposed reorder policy. Full sanitized chronology, command ledger, metrics, private-evidence hashes, and VM plan: [today-canceled-grouping-audit.md](research/today-canceled-grouping-audit.md). Keep exact production artifacts gitignored.

8. ~~**FIELD BUG — `project update --when today` can falsely report `verify-failed:mismatch` after a successful update (added 2026-08-10).**~~ — **FIXED 2026-08-11 (PR #440, branch `mg/when-today-verify-fix`).** The symbolic `when: today` UPDATE assertion is now an arrived-date PREDICATE (`startDate != null && startDate <= today`) instead of exact `startDate == today`, added via a JSON-serializable `satisfies`/`FieldPredicate` form on the verify layer (`src/write/verify/delta.ts`) and gated by a `whenAssertions({ mode: "update" })` flag used by BOTH `todo.update` and `project.update`; adds and explicit ISO dates keep exact equality, `when: evening` unchanged (date-anchored to today exactly). Undo degrades cleanly (ALPHA — reconstructs from captured pre-values, not `expected.assert`). Tests: the report's 5-case matrix across both update ops + both add ops (`test/engine/write-when-today-verify.test.ts`) + the raw predicate (`test/unit/verify-delta.test.ts`). *Original item:* An already-Today to-do was converted to a project successfully, then updated with title, notes, `when=today`, and a deadline. Things saved all requested semantic state and retained Today membership, but preserved the already-arrived historical `startDate`; seven of eight assertions matched, while the shared `whenAssertions()` helper incorrectly required raw `startDate == today`. Detailed sanitized report, error shape, hashes, repro, and test matrix: [project-update-today-verify-mismatch.md](research/project-update-today-verify-mismatch.md).

## 1. Decisions Mike needs to make (blocked on him, near-zero effort)

- ~~**Hide `── Repeating To-Dos ──` behind a flag in `upcoming`?**~~ — **RULED NO 2026-07-14** (Mike): the section stays always-on; no hide flag. His "leftover budget" idea (dated rows first, remaining `--limit` budget fills resting templates in order, truncation hint if cut) turned out to be the ALREADY-SHIPPED behavior — `--limit` counts items in render order across both streams (same model as Today→Evening), live-verified (`upcoming --until 2w --limit 25` fills the tail with `‹waiting›` rows and hints). Decision closed with zero code change.
- ~~**Today/anytime showing checked-unswept items?**~~ — **RULED YES + SHIPPED 2026-07-14** (Mike: "this is the behavior of the Things GUI and I like it"). Closed items the log-move sweep hasn't passed now stay checked IN PLACE in the flat `today`/`anytime` lists too — same `OPEN_OR_UNSWEPT` membership + boundary the project/area cards already use (completion≠logged), keeping the comparator/index slot; they leave when the sweep boundary advances. The sidebar badge still counts OPEN members only (a checked-unswept row is listed but never moves it — the live 405−12=393 reconciliation is preserved). A closed-but-unswept PROJECT row shows in place with its children still cascade-excluded (the checked project row represents them). Mixed-context dim/dim+strike styling applies automatically in `formatItem`. See CHANGELOG (Unreleased) + `src/read/predicates.ts` (`OPEN_OR_UNSWEPT`). This partially covers the PLOG1 read-hint overlap flagged in §5.3 below: a closed project's OPEN child is still buried (it is neither open-in-an-active-container nor unswept-in-Today), so the "hint on `show` of a logged project with `status=0` children" follow-up remains open. **UI2-g addendum (2026-07-14):** a closed row (status IN (2,3)) **always carries a `stopDate`** — the golden has 0 closed-with-NULL-stopDate rows, and a freshly completed AND a freshly canceled to-do both get one stamped immediately by the app. So the `OPEN_OR_UNSWEPT` membership's "closed-with-NULL-stopDate = permanently unswept" branch is an internal `markLogged` state the app itself never emits (nothing contradicts markLogged; nothing confirms the app produces such a row) — provably app-unreachable, safe as written.
- **Stateful display preferences** (roadmap §H): config-file/env defaults for the growing toggle family (`--show-later`, `--show-logged`, `--until`, `--all`, and the #89/#93 limit knobs). Decide the config surface + precedence; wary of plist-driven defaults confusing agents. Scope grew 2026-07-12 (see §H for full context): `limitMode fixed|fit|none` (height-fit deliberately not default), `pager auto|never` (git-style `less -FRX`), and a global `--plain`/`--pretty` chrome override pair. **2026-07-14: examples delivered to Mike** (config file for persistent defaults; `--plain` = disable the whole TTY chrome stack in one flag — ANSI + width-fitting + gutter + preamble, i.e. the flag form of `NO_COLOR=1 THINGS_WIDTH=0`; `--pretty` = force chrome into a pipe; `--json` ignores all of it so agents stay unconfused). Awaiting his ruling. **Status check 2026-07-22: still open — none of the display knobs shipped** (no `--plain`/`--pretty`, no pager, no `limitMode`, no persisted display defaults). Note a config-file surface now EXISTS (`src/config.ts`, `things config set`, `~/.../config.json`) but carries policy keys only (`profile`, `maxDisruption`, `actor`, `auditEnabled`, `acceptedFingerprint`, `allowExperimental`, `uiEnabled`) — the §H decision now has a natural home waiting for it.
- **Productize the dedicated-Mac "ui" write vector?** — **RESOLVED 2026-07-14 (Mike ruled BUILD; AX-only; closet-mini target) — SHIPPED UNCERTIFIED.** Mike ratified the architecture: build the vector, driven by the **Accessibility API only** (semantic AXUIElement paths via `osascript` + System Events; NO coordinate clicks / screenshots — VNC was the research heritage but stays lab-only, being coordinate-based and geometry-fragile), on a dedicated always-on **closet Mac mini** that drives its own GUI locally. The seven ops shipped this pass — `todo.make-repeating`, `todo.reschedule-repeat`, `todo.pause-repeat`, `todo.resume-repeat`, `todo.stop-repeat`, `todo.convert-to-project`, `heading.convert-to-project` — fail-closed and two-key gated (`ui.enabled` config + `--dangerously-drive-gui` / `dangerouslyDriveGui`, hazard `H-UI-DRIVE`, disruption tier 3). Every op ships **UNCERTIFIED**: a per-op certification manifest (`src/write/vectors/ui-certification.ts`) records status + evidence, surfaced by `things capabilities` / `things doctor` and as a warning on every successful drive. **AXVM1 (PR #136) upgraded the certification story:** Accessibility is grantable in a Tart guest (SIP on; one-time user-path TCC toggle, persists reboot) and element presses steal NO focus and work under a locked session — so the certification suite is now **lab-runnable in a VM clone** per Things version, and the real-hardware sitting ([lab/ui-certification-runbook.md](lab/ui-certification-runbook.md)) becomes a final confirmation rather than the only path. The hazard copy is worded accordingly (drives the GUI, but no inherent focus-steal / unlocked-session requirement). **Immediate follow-up (§2): wire the in-VM certification run for the tier-1 ops** (AXVM1 grant recipe + the seven ops against the seeded library) and flip those manifest entries to lab-certified — the machinery ships now, all uncertified; the run did not block this PR. Architecture doc: [design/ui-vector.md](design/ui-vector.md). *Original exploration context retained below.*
  - ~~**EXPLORATION GREENLIT 2026-07-14** (Mike):~~ the ui vector is the FOURTH write vector (after URL/AppleScript/Shortcuts), **most-disruptive tier**, on the **hybrid create-quiet / transform-loud doctrine** (create+edit via quiet vectors; GUI-drive ONLY the transforms with no other spelling). UI1 (PR #90) proved feasibility (repeat create); **UI2 (2026-07-14, [ui-vector-research.md](lab/ui-vector-research.md))** mapped the transform set: make-an-existing-item-repeat (**identity replacement**), edit-rule (identity **preserved**), pause (`rt1_instanceCreationPaused`, reversible via Resume), **stop/un-repeat** (**UI2-i, 2026-07-14** — corrects UI2's "no Stop command exists": a **Stop** DOES exist, reachable ONLY from the open-card repeat-bar popover — absent from the Items menu and row context menu, a menu-parity oversight; it is a confirmation-guarded **identity-replacement un-repeat** — template uuid hard-deleted → NEW plain to-do with `rt1_recurrenceRule` cleared, spawned instance survives, terminal/no-Resume), convert-to-project on both to-do AND heading (identity replacement). The original UI2-e disruption verdict (every VNC drive ends with Things frontmost + focus-stolen) applies to the **VNC** research path only; **AXVM1 (2026-07-14) FALSIFIED the "AX is SIP-blocked in VMs" assumption** — System Events Accessibility scripting works in a stock Tart guest, needs no frontmost app, **steals no focus**, **works under a locked session**, and addresses menu items BY NAME (the shipped driver's basis). Row selection uses the URL-scheme handle (`things:///show?id=`) because Things does not expose list rows to AX. Full write-up: [axvm1-accessibility.md](lab/axvm1-accessibility.md).
- ~~**Glyph palette doc + `⧉`**~~ — **DROPPED 2026-07-14** (Mike: "forget about the ⧉ glyph for now — it's large and I'm not sure what we'd use it for"; it was just an example of unmined blocks). The curated glyph-inventory offer folds into the §H cross-terminal audit whenever that runs; no standing item.
- **When to send the Cultured Code report**: `docs/things-app-oddities.md` is effectively the draft (crashes §1/§6/§7, silent-failure families §2/§5, the double-trash black hole §6½, the completed-project black hole §6¾). Last evidence gaps below. **2026-07-14: Mike re-confirmed keep-flagged-but-deferred.**
  - **AX-era evidence banked 2026-07-15 (pointers, add to the report package):** the **stop-then-select project crash** (§7 **C5**, 2 `.ips` banked, deterministic 2/2); the **sidebar AX-mirror row blanking** after drag+scroll churn (§9); the **reminder-picker `AXDateTimeArea` ignoring committed AX writes** while the same-role "ends on date" picker honors them (§8l); the **context-dependent filter-bar tag inheritance** (§9a, opposite answers in two GUI surfaces); the **Stop-confirm sheet hardcoding "to-do" copy for projects** (§8j); and the **accessibility gaps** — no AX node for the `…` button (§8j), AXPress-inert custom `…`/repeat-bar popovers (§8j), AX-invisible right-click `NSMenu`s (§8k), and no AX names on Tags-window rows (§8i).

## 2. VM lab campaigns (autonomous; batch them — they share clone setup)

- ~~**In-VM certification of the ui vector's tier-1 ops.**~~ — **DONE — the AX initiative closed the whole in-VM certification arc.** Every ui-vector op is now **`lab-certified`** in `ui-certification.ts` against Things 3.22.11: UIC1 (to-do pause/resume/convert/make-repeating/reschedule), UIC3 (project reschedule/pause/resume), AXDRAG2 (`area.reorder`), UIC5/UIC6 (`project.make-repeating` + full rule vocabulary), and **HEADCERT1 (2026-07-17) certified `heading.convert-to-project`** — the LAST uncertified op — by reusing the row `select` action on the heading's parent-project view (a heading row IS selectable, addressed positionally by ordinal), DB-verified through the production CLI on both headings + gating ([headcert1-certification.md](lab/headcert1-certification.md)). `todo.stop-repeat` was **DROPPED** (never worked, card-only Stop). The background-AXPress-vs-activate question is answered (AX menu presses are background-capable, no focus steal; mouse-synthesis ops are foreground-bound). The remaining axis is the on-hardware `certified` confirmation (§5 of the runbook), not an in-VM task. Runbook: [ui-certification-runbook.md](lab/ui-certification-runbook.md).

- ~~**Upcoming membership matrix (UPC1)**~~ — **DONE 2026-07-13** (research-upc1.sh, run `things-run-upc1-20260713-172846`, PR mg/upc1-upcoming-membership): full deadline-forecast matrix mapped, blue-circle explained, upcoming fix UNBLOCKED (see §5). Verdicts: future-deadline **anytime AND someday to-dos AND someday projects** forecast into GUI Upcoming under the **deadline date** (richer than the prod anchor — not just someday projects); **Inbox (start=0) excluded** (but a *due* deadline still pulls Inbox into Today). when+deadline groups by the **WHEN-date**, one row, deadline as a flag; a deadline-before-start item still groups under its future when and is suppressed from Today by the future startDate (F-DL-FUTURE-START, supp stays NULL). **Blue Someday circle = a PROJECT with an OVERDUE deadline** (accent-coloured progress ring) — not "in Today" (prod rows are suppressed), not "suppressed per se" (the equally-suppressed to-do stays gray), coherent indicator not a bug. Two mechanism finds: `deadlineSuppressionDate` is stamped by **rescheduling an overdue item to Someday/Anytime** (no dismiss command exists; oddities §8e), and the "new to-dos" banner OK **materializes** deadline overlays `start→1, startDate:=deadline` (oddities §8d). Full write-up + ratified rule: [docs/lab/upcoming-research.md](lab/upcoming-research.md).
- ~~**Completed project + restored trashed child (PLOG1)**~~ — **DONE 2026-07-13** (research-plog1.sh, run `things-run-plog1-20260713-184245`, PR mg/plog1-completed-restore). Mike's live-GUI find, reproduced in-VM. Verdicts (a–d): the completion **modal ignores trashed children** — a project whose only unresolved child is trashed completes **silently, no sheet** (control-proven: a non-trashed open child DOES raise "There is still 1 to-do…"); the URL `update-project?completed=true` cascade shares the blind spot (§3). GUI **Put Back** restores the trashed child **into the original project with the project ref intact** and **does NOT reopen the completed parent** (contra §5b) → a completed project holding an OPEN to-do. The Logbook sweep is a **derived time-boundary** (`log completed now` advances `manualLogDate`, mutates zero task rows), so the open child rides into the Logbook still `status=0` and disappears from Anytime/Today/Inbox — reachable only by drilling into the logged project. Restore-before-sweep and restore-after-sweep converge. No crashes. Report-ready = oddities **§6¾**; write-up [docs/lab/plog1-research.md](lab/plog1-research.md); feeds the CC report (§3) and the surface-guard follow-up (§5). Note this used logInterval=Manually (settable via Settings → General; golden default is Immediately).
- ~~**Double-trash black hole screenshots (oddity 6½)**~~ — **DONE 2026-07-12** (run `things-run-r3-20260712-171142`, PR mg/lab-round3): 4-shot report-ready sequence banked at `lab/artifacts/things-run-r3-*/oddity-6half/`, referenced from oddities §6½. Ready for the CC report submission (§3).
- ~~**scf2 round-3 probes**~~ — **DONE 2026-07-12** (research-scf3.sh): P4/P3a/P2b all reconfirmed (Shortcuts backdating DEAD, AS WORKS, URL NO-OP, json WORKS; reminder SET DEAD; Parent = DETACH footgun). **P6 CLOSED** — sdef inventory = exactly one `_private_` command (no sidebar-reorder spelling; sidebar order stays UI-only), and the **Someday reorder convention is LOCKED** (reversed wire-list, matches Inbox A6) → ready to wire as a reorder scope. See §5 follow-ups.
- ~~**logInterval enum verification**~~ — **DONE 2026-07-12**: enum = `0=Immediately · 1=Daily · 4=Manually` (NO weekly/monthly in 3.22.11); `log completed now` advances `manualLogDate`. `src/read/log-boundary.ts` comment corrected.
- ~~**Deadline-less fixed-repeat encoding**~~ — **DONE 2026-07-12**: YES the GUI makes them (it's the default), and the plist is IDENTICAL to a deadlined ts=0 rule — deadline-ness is NOT in `rt1_recurrenceRule`, falsifying the fixed⇒deadlined law for the deadline-less case. `src/model/recurrence.ts` annotated. See §5 follow-up (nonzero-offset encoding + projection fix).
- ~~**Repeating-template dated-reminder clear**~~ — **DONE 2026-07-12**: no in-place automation clear exists (reminder is a rule property, not `reminderTime`; Shortcuts clear = safe no-op, AS move-to-Inbox = clean 301 refusal, both crash-free). `todo.clear-dated-reminder`'s repeating refusal is correct and stays.
- ~~**Lab runner Shortcuts arm**~~ — **DONE 2026-07-12** (`mg/lab-shortcuts-arm`): the DSL grew a `shortcut` step (guest input file + `shortcuts run --input-path/--output-path`) + a `group:interactive` skip for the delete-class proxies; `s-suite.json` is now recurring and wired into `lab:regress`, giving the shipped Shortcuts ops (`heading.add`, `todo.clear-dated-reminder`) recurring live coverage. The §C e2e reorder coverage was already present (commit `d38b05f`, v0.8.0 — inbox/someday/headings/projects-bounce) and re-confirmed in the same change.
- ~~**§E½ UI-vector feasibility probe**~~ — **DONE 2026-07-12 (UI1, PR mg/ui-vector-probe)**: VERDICT **FEASIBLE**. VNC-drove `File → New Repeating To-Do` end-to-end (menu → daily → OK → title) and verified an `rt1_recurrenceRule` row landed (tp=0 fixed, fu=16 daily, ts=0, rrv=4) plus a deadline-less spawned instance. Reliability notes: **Accessibility is NOT granted in the golden** (`osascript` System Events → −1719), so ~~there is no AS UI-scripting fallback~~ this run drove via VNC clicks + VNC keyboard (hardware HID, no TCC). **[AXVM1 correction 2026-07-14:** "not granted" ≠ "not grantable" — a one-time user-path TCC toggle makes AX UI-scripting work in-guest with SIP enabled; see [axvm1-accessibility.md](lab/axvm1-accessibility.md).**]** VNC framebuffer = 2048×1536 (physical px); menu/dialog layout is deterministic per resolution and clicked first-try; **VNC keyboard entry works** (title AND numeric fields) — the DLREPEAT "days earlier" failure is overcome (click field → 6×bsp + 6×del → type; note `vncdo type` sends letters lowercase and `super-a` types a literal 'a', so clear-by-delete, don't select-all). This green-lights a dedicated-Mac "ui" vector for the conclusively-UI-only surface (repeat rule create/edit, sidebar order, to-do↔project convert). Script: `lab/scripts/research-ui1.sh`.

### Queued 2026-07-13 (headless "closet Mac mini" research — feeds the ui-vector decision in §1)

- ~~**Locked-session vector probe (LOCK1)**~~ — **DONE 2026-07-13** (research-lock1.sh, PR mg/lab-lock1-sync1; run `things-run-lock1-20260713-091759`). Predictions CONFIRMED: over SSH under a real loginwindow lock, **(a) SQLite read, (b) `open -a Things3` launch, (c) `things:///` add, (d) AppleScript make, (e) `shortcuts run` heading.add ALL WORK**; **(f) VNC click hit the LOCK SCREEN** (woke the display to the Enter-Password prompt, never reached Things — screenshot `03-after-uiclick.png`). A locked session is a presentation/input barrier, not an execution barrier. **Lock-mechanism finding (deviation from the plan):** synthetic **ctrl+cmd+Q via VNC does NOT lock** (chord not honored), and the legacy `askForPassword` default is ignored on Sequoia — the working headless lock is `sysadminctl -screenLock immediate` + `SACLockScreenImmediate` via `launchctl asuser` (rc=0; a plain SSH shell gives rc=22/no-op). Unlock-over-VNC confirmed possible (typing the password into the lock field). ~~AX-under-lock still unprobeable (Accessibility SIP-blocked, per UI1).~~ **RESOLVED 2026-07-14 (AXVM1): AX-under-lock IS probeable and WORKS** — after the user-path grant, a System Events press of Items ▸ Repeat ▸ Pause succeeded under a genuine `sysadminctl -screenLock immediate` lock (flipped `rt1_instanceCreationPaused` 0→1), whereas the VNC click (arm f) hits the lock screen. So AX element press is a *lock-tolerant* driving path; see [axvm1-accessibility.md](lab/axvm1-accessibility.md). Full write-up: [docs/lab/headless-research.md](lab/headless-research.md).
- ~~**Sync freshness + last-sync signal investigation (SYNC1)**~~ — **DONE 2026-07-13** (same run). No authoritative "last synced" signal exists WITHOUT a cloud account: `BSSyncronyMetadata` (the Syncrony sync-state table) is **empty (0 rows)**; `Meta`/`TMSettings`/`TMMetaItem`/`TMTombstone` carry no sync cursor/timestamp; `defaults com.culturedcode.ThingsMac` has only Sparkle updater keys; the group-container plist has only "what's new"/foreground-check timestamps. **Buildable freshness proxies (all local):** app-running (`pgrep`), `main.sqlite-wal` mtime (confirmed to advance on writes), `MAX(TMTask.userModificationDate)` (last local edit), and plist `importantInformationLastForegroundDate` (last foreground). **Unified log yielded ZERO lines in the clone** (no queryable log store in the tart guest + clock-pin skew) — the sync log taxonomy is uncharted and needs SYNC2/real hardware. Signal inventory + unverifiable-without-account list: [docs/lab/headless-research.md](lab/headless-research.md).
- ~~**Sync conflict-semantics probe (SYNC2, networked)**~~ — **DONE 2026-07-13** (research-sync2.sh, run `things-run-sync2-20260713-124441`, PR mg/lab-sync2). Two clones on one throwaway account (mail.tm inbox + random pass, no Apple ID; VNC-created inside the VM). **Headline: the "last-writer-wins, silently drops one side" premise is FALSIFIED for text fields** — Things Cloud does a deterministic per-attribute **3-way TEXT MERGE** (notes AND title merge with a `--` separator, ordered by edit timestamp, arrival-order-independent; both devices converge identically). Different-field edits clean-merge; checklist items union-merge; delete-vs-edit merges per-attribute (item lands in Trash carrying the other side's note — no data loss). **`BSSyncronyMetadata` populates 0→11 rows once an account attaches**; the last-sync signal is key `GryCJ44xPcJG6go5KeTZp1` (see §5). Log taxonomy: NO `com.culturedcode.*` os_log subsystem (sync only visible via Apple TLS logs). Lab-infra gotcha: `route delete -inet default` leaves IPv6 up and Things syncs over it — true offline needs both families deleted + Things quit. Full write-up + verdict table: [docs/lab/headless-research.md](lab/headless-research.md) SYNC2. **Account is LIVE (burnable): creds in the run's `account-credentials.env`.**

## 3. Needs a human present (short sittings)

- ~~**P5**: Shortcuts delete of a NON-empty heading (delete-class consent re-prompts every run — no Always-Allow). Question: children re-parent, orphan, or cascade?~~ — **ANSWERED-BY-P12** (interactive delete-class batch, Mike present): a non-empty heading delete CASCADES — Trash: the heading row vanishes and its children are **reparented-to-project-root then trashed**; permanent: the whole subtree is **hard-deleted**. See [s-campaign-results.md](s-campaign-results.md) "Container delete cascades".
- **`.ips` capture** for the heading-schedule crash (§6) — nice-to-have for the CC report.
- **CC report submission itself** — once 6½ screenshots + (optionally) the §6 .ips land.

## 4. Calendar-pinned

- **macOS 27 public-beta regression VM** — ~late July 2026. Build a beta VM, run `lab:regress`, diff verdicts/tiers (the Things-update canary: rrv decode, fingerprint, doctor `repeats:`).
- **iOS 27 GA runbook** — ~Sept 14 2026: execute `docs/lab/things-update-runbook.md` + the Apple-Intelligence memo follow-ups (`docs/design/apple-intelligence-research.md`).

## 5. Smaller code items (unblocked, low effort)

### ~~Approved design 2026-07-13: undo actor-scoping~~ — DONE 2026-07-14 (PR mg/undo-actor-scoping)

Shipped exactly as ratified. `selectUndoTargets` now consults the recorded actor and supports exact-token selection instead of always picking the newest N globally. The three layers:

1. **`--by <actor>` selection filter** on `things undo` + a `by` param on the MCP undo tool. Exact match (or `*` for all); `--by mcp` never matches an `undo:mcp` record (inverse mutations are their own actor and are never undo targets). The existing `--actor`/`actor` flag (who the inverse is RECORDED as) is unchanged; the --help/description now contrast the two loudly.
2. **Asymmetric defaults**: MCP undo defaults to `by: "mcp"` (an agent must pass `by: "*"` to touch the owner's edits); CLI `things undo` stays GLOBAL, with `--by mcp` to clean up after an agent. Engine + unit + MCP + CLI tests cover the "newer human record skipped under mcp scoping" case.
3. **Undo tokens**: every mutation `ok` result (JSON envelope + MCP response, including compound summaries and bounce reorders) now carries an additive `undoToken`; `things undo --txn <token>` (MCP `txn`) inverts exactly that record. **Id mechanism chosen: a compound op's shared `txn.id`, or — for a single op — a content-addressed `m-<sha256(ts+op+actor+host+uuid)[..12]>`, derived (never stored) so the write path and the trail scan always agree** (`undoToken()` in `src/audit/schema.ts`). `--txn` is mutually exclusive with `--last`/`--by` (usage error, exit 2); an unknown or already-undone token fails loud and specific (the additive `undoOf` back-reference on inverse records distinguishes already-undone from nonexistent). Also fixed in passing: the bounce-reorder summary audit record now honors `options.actor` (previously always the config actor). Doctrine: [docs/design/architecture.md](design/architecture.md) (§ Undo selection).

Optional fourth layer, still parked: session-qualified actors (`mcp:<session>`) with prefix matching on `by:` — only if parallel agents become real.

### Other queued noodling (2026-07-13)

- ~~**Upcoming deadline-forecast fix (UNBLOCKED 2026-07-13 by UPC1; this is the "UPC1-driven upcoming deadline fix" the width-aware-TTY item waits on)**~~ — **DONE 2026-07-13** (PR mg/upcoming-deadline-forecast): `upcomingView` (`src/read/views.ts`) now unions the **deadline-forecast** cohort (`startDate IS NULL AND start IN (1,2) AND deadline > today AND (deadlineSuppressionDate IS NULL OR deadlineSuppressionDate < deadline)`, both to-dos and projects, **Inbox start=0 EXCLUDED**) into the scheduled stream, grouped/sorted on `COALESCE(startDate, deadline)` with the existing within-day `todayIndex` order. A when+deadline row stays ONE row under its when-date (forecast requires `startDate IS NULL`, so no double-emit). `--since`/`--until` clip forecast rows on their DEADLINE (their appearance date); occurrence-synthesis + `--repeats` untouched. Forecast rows keep `startDate: null` in JSON (no faked when-date); `renderUpcoming` groups them under the deadline header with NO date pill but the `⚑` flag (bare-flag anatomy, UPC1-A). View-doctrine header + fixtures (anytime/someday/someday-project present, inbox absent, suppressed absent, re-armed present, both-dates once, `--until`/`--since` by deadline, JSON honesty, repeats:false survival) landed same change. `capability-matrix.md` untouched (it tracks writes only — reads are vector-independent per its header). Live-verified against Mike's library: project `PjJB5B5i` (deadline 2026-08-28) appears under Aug 28; inbox `L4fSPxVN` (deadline Jul 31) stays absent. Ratified rule: [docs/lab/upcoming-research.md](lab/upcoming-research.md).
- ~~**Agent domain-model primer (HELD by Mike 2026-07-14)**~~ — **DONE (verified 2026-07-22): all five layers shipped.** (1) Canonical primer/handbook = the Agent Skill (`skills/things-cli/SKILL.md` + `references/model.md`/`contracts.md`/`gui.md` as the deep tier); (2) MCP `instructions` domain preamble in `buildInstructions()` (`src/mcp/server.ts`); (3) CLI page shipped as **`things help agent`** (not `help model`; advertised from the help index); (4) the skill itself, skill-v2 promoted/shipped in 0.11.0 (#246); (5) README "For agents" + "Agent skill" sections. Distribution landed too: `things install-skill` (#248) + publish-time version ratchet/drift notice (#249). Remaining tail (re-sweep + refinement round, then release) is tracked as roadmap **§N phase 4**, not here. *Original layered plan retained below.*
  - Original scope: an agent with no Things pretraining cannot intuit the domain from command enumeration alone (containment rules: area ⊃ {projects, loose to-dos}; project ⊃ headings ⊃ to-dos; projects can be area-less; headings exist ONLY inside projects; tag hierarchy + inheritance; completion ≠ logged (the sweep model); today/evening/anytime/someday/dated scheduling; repeating templates vs spawned instances; trash cascade). Layered plan: (1) ONE canonical primer (~60-80 lines, agent-optimized) as the single source; (2) condensed version prepended to the MCP server `instructions` (which already carry the live inventory); (3) a CLI page (`things help model` or similar) advertised in the --help epilog like `things legend`; (4) an **Agent Skill** (SKILL.md — an OPEN standard implemented across harnesses: Claude Code, Codex, Cursor, Pi, OpenCode, …; Mike's correction 2026-07-13, do not frame as Claude-only) as the DEEP tier: progressive disclosure lets a pretrained-on-Things model skip sections while an unfamiliar one self-guides through the full handbook; the basic primer in help/MCP instructions POINTS to it (e.g. suggesting `npx skills add mikegreiling/things-api` to go deeper); (5) extend the README "For agents" section. Two-tier model: primer = always-on and tight; skill = the handbook. Keep all surfaces derived from the one primer/handbook source so it can't drift.
- ~~**Consumer-timezone / pinned-clock views**~~ — **SHIPPED** (branch mg/consumer-timezone): `THINGS_TZ` (IANA zone) + `THINGS_NOW` (ISO instant) env knobs read by CLI and MCP, plus a per-call `tz` on the date-sensitive MCP tools (precedence per-call > `THINGS_TZ` > host; invalid = fail-closed usage error). All read boundaries thread a resolved `{ now, zone }` clock via `localToday(now, zone?)`; the period grammar (`3d`/`2w`/`1m`/`1y`, absolute) is zone-aware; additive `meta.clock { timezone, today }` is emitted only under a consumer zone/pinned now. Writes normalize-before-dispatch: consumer `when: today` → explicit consumer-zone date; `when: evening` refused fail-closed (`blocked:clock`) when consumer-today ≠ app-today (This Evening is app-machine-current-day only); reminder times untranslated; internal undo/reorder stay on the host clock. Deployment note in contracts.md: for a single-consumer host, aligning the system timezone (not the clock — sync ordering) is simpler. **Original feasibility verdict retained below.**
  - **Parked 2026-07-16 after a feasibility pass** — feasibility verdict: reads YES, cheap and coherent; writes MOSTLY, via normalize-before-dispatch; parked until the clock-threading fix landed. (a) *Reads*: Things view membership is DERIVED from stored calendar dates vs. an evaluation instant (this is why two synced devices in different timezones legitimately disagree about Today — the model is coherent under any evaluation clock). The read layer already half-supports injection (`now` params on today/anytime); the 2026-07-16 clock-threading fix (thread `now` through every view + `logBoundary` + `logbookView`) is the hard prerequisite and is in flight. After it, the feature is ~an env var (`THINGS_NOW` instant / `THINGS_TZ` IANA zone → compute `localToday` in that zone via `Intl`) plus an optional MCP arg. Doubles as a determinism primitive for tests/lab (aligns with the lab's clock-pinning doctrine). (b) *Writes*: relative keywords ("today", "tomorrow") sent raw to URL/AppleScript are interpreted by the APP's clock — un-normalizable there — but the CLI can resolve them to ABSOLUTE calendar dates in the consumer's zone before dispatch (both vectors accept explicit dates), so the app's clock never interprets a relative word; verify assertions then compare the stored packed date against the same precomputed absolute date, staying coherent. Known edge cases to spec before building: "evening" is a today-only bucket (inexpressible when consumer-today ≠ app-today); consumer-today may be app-yesterday (past start date → instant overdue semantics); reminder times are wall-clock and tz-less in Things' own model (document, don't translate). (c) MCP has no standard channel for a client to declare its timezone — needs an explicit tool arg, session config, or an instructions-level convention asking agents to pass one.
- **`--markdown` output mode (parked for a future design discussion — do NOT spec yet, Mike's instruction)**: a third render target alongside TTY and `--json` — YAML frontmatter for item/project metadata, `- [ ]`/`- [x]` checkbox lists for to-dos/checklists, `##`-level headings for Things headings, project title as `#`. Design cautions recorded now: markdown is a RENDERING of the same data, never a third contract (derive from the JSON-shaping layer, drift-tested); heading-level mapping needs care; NO markdown import/roundtrip in scope (that's a write-vector minefield to consider separately, if ever). Likely sweet spot: agent contexts (LLMs parse markdown cheaply) and piping into notes/docs.
- **Tag shorthand — `things errand` → `things anytime --tag errand` (Mike, 2026-08-02, parked; needs deliberate design, not scheduled)**: a bare noun that names a TAG expands to that tag's FILTERED VIEW. Concerns to resolve before any spec: (a) the bare-noun sugar already arbitrates command names, view keywords, areas, projects, and to-dos — adding tags gives one more thing to collide on, and a tag named like a project would silently change WHICH view renders (a project → a `project-view` card; a tag → a filtered list), i.e. a rename in the user's Things could flip the meaning of the same shorthand; (b) unlike every other bare-noun target (which SHOWS one resolved entity), a tag expands to a FILTERED VIEW rather than a show — a second semantic loaded into the sugar (which view does it filter — `anytime`? a merged all-tasks list? — and how does that compose with the existing view keywords?). Likely resolutions to weigh: an explicit sigil (`things @errand`) or explicit-only (no bare-noun tag routing), keeping the sugar's collision surface small. Not scheduled.

- **Resource-scoped verbs (cli-grammar Phase 2) go/no-go — HELD by Mike 2026-07-14, pinned**: (Fable's on-request honest take, 2026-07-14: not foolish, mildly in favor — it follows the grammar's namespace gravity and the closed verb enum avoids the rejected NL-parsing trap; the costs are surface bloat and doc/test duplication, so it's ergonomics rather than capability.) `things project <ref> add-heading "Groceries"` etc. — specced in [docs/design/cli-grammar.md](design/cli-grammar.md) (closed verb enum, fixed slots); Mike leans yes ("navigation" instinct, also the elegant home for heading ergonomics). Decide after the grammar/polish pass settles in daily use. **2026-07-22: reopened and reshaped by the §6 heading-demotion item** — Mike now leans toward project-scoped verbs as the *canonical* heading spelling (checklist model), not an additive alias; fold this go/no-go into that design round.
- ~~**Lint-warnings cleanup (64 pre-existing, ~30 files)**~~ — **DONE 2026-07-14** (PR mg/lint-zero-warnings): the pile had grown to 75 warnings across 41 files; one deliberate pass fixed 54 (all 31 `consistent-function-scoping` hoists, all 13 `no-array-sort`→`toSorted`, all 7 `no-shadow` renames, `prefer-set-has`, `prefer-array-find`, `preserve-caught-error`) and suppressed 21 with per-site reasons (all 20 `no-await-in-loop` sites — every one sequential-by-design: mutation-lock serialization, bounce leg ordering, polling loops, shared MCP test transport — plus `prefer-add-event-listener` on the MCP SDK's `onclose` property API). Enforcement lives in `.oxlintrc.json` `options`: `denyWarnings: true` (any warning now exits non-zero, so the existing `npm run check` lint step fails CI) + `reportUnusedDisableDirectives: "error"` (a stale suppression is itself a CI failure — the schema takes a severity, not the boolean). Ratified by Mike 2026-07-14: "we should not be merging anything that produces lint warnings."
- ~~**O-suite formal coverage of the someday reorder scope** (noted in PR #98)~~ — **DONE 2026-07-14** (this PR, UI2 fold-in): added **O15** (someday to-dos, P8b two-call anchor-stack) + **O16** (someday area-less projects, P9e inverted protocol) to `lab/suites/o-suite.json`, encoding the locked two-call verdicts per the shipped compiler. Verified GREEN in a fresh clone (run `o-20260714-065940`, all 15 o-suite probes `ok`). See [suite-audit.md](reference/suite-audit.md).

- ~~**`doctor` sync-health section**~~ — **DONE 2026-07-14** (PR mg/doctor-sync-health). New `── Sync health ──` section in `doctor` (+ additive `syncHealth` object in `--json` and the MCP `doctor` tool), built entirely on the SYNC1/SYNC2 LOCAL proxies + the cloud last-attempt signal. Signals: (a) **app running** (`pgrep -x Things3` via the one shared `isThingsRunning` helper reused from the automation probe — no second process-check shape); (b) **WAL freshness** (`main.sqlite-wal` mtime; flagged stale only when the app IS running and has not written in 24h — when the app is not running staleness is `null`/"cannot judge, DB frozen"); (c) **last local edit** (`MAX(TMTask.userModificationDate)`); (d) **last foreground** (group-container plist `importantInformationLastForegroundDate`, located relative to the resolved DB path, absence tolerated → null); (e) **cloud last-sync ATTEMPT** — read only when `BSSyncronyMetadata` has rows; 0 rows (or table absent on older schema) → "no Things Cloud account attached — no server-side sync signal exists without one". Both SYNC2 caveats are encoded: the value is worded as the last *attempt* (advances even offline; verdict says pair with reachability for confirmed sync), and the reader tries the account-specific key `GryCJ44xPcJG6go5KeTZp1` first then falls back to the bplist-double nearest-to-now (lease sentinel excluded by rejecting doubles >5yr future). **Implementation choices:** the `BSSyncronyMetadata` BLOBs are decoded by a minimal in-process bplist scalar-double reader (`decodeBplistScalarDouble` in `src/sync-health.ts` — unit-testable from raw bytes, no subprocess-per-row); the foreground plist is read via `plutil -extract … raw` on stdin (the same stable command shape `availability.ts` already uses). Tests: `test/unit/sync-health.test.ts` (bplist decode incl. lease-sentinel exclusion, staleness verdict with injected clock, cloud known-key + nearest-to-now paths), `test/cli/doctor.test.ts` (syncHealth JSON shape, empty-table no-account path, attached-account path), `test/cli/e2e.test.ts` (real-environment CLI path against the fixture DB — no-account path does not crash, human section renders). (d) unified-log scan deliberately NOT built (SYNC2: Things emits no `com.culturedcode.*` os_log subsystem, so no signal to key on). **Live note:** on the maintainer's real account the account-specific key was ABSENT and the value-based nearest-to-now fallback resolved the signal — real-hardware confirmation that the key is account-specific (SYNC2 one-account caveat) and that the fallback is the robust primary path.
  - **Follow-up (deliberately out of scope this pass):** the "data may be stale — Things is not running" one-line stderr warning on ordinary reads. `doctor` now answers it on demand; whether every read should nag is a separate UX call (overlaps the §H stateful-prefs / chrome decisions).
- ~~**PLOG1 surface-guard follow-up**~~ — **DONE 2026-07-14** (Mike's rulings applied; PR mg/plog1-surface-guards). PLOG1 (oddities §6¾) proved the app produces a *completed/logged project holding an OPEN, actionable to-do that vanishes from every live view*. Audited all three adjacent surfaces against `final.sqlite` semantics; only the read-side hint needed building.
  1. **`project.complete` / `project.cancel` cascade (write) — RULED: NO warn, NO block on trashed children.** Mike: completing a project with trashed children proceeds silently, mirroring the app. **No code change: already correct.** `projectChildren()` (`pre-state.ts`) is `trashed = 0`, so `H-PROJECT-COMPLETE-CHILDREN` never counts trashed children — our cascade already ignores them exactly like the GUI modal and URL cascade (PLOG1-a/d). Verified, ruling recorded.
  2. **Restore paths (write) — AUDITED: NO reachable restore-into-completed path exists → NO reopen op built.** Every un-trash / move-out-of-Trash surface was checked: **(a) `todo.restore`** (the only to-do un-trash op) lands the item in the **Inbox, de-scheduled, project ref lost** (E15 — `move to do … to list "Inbox"`), so it can NEVER strand an open child inside a completed parent — it targets a DIFFERENT container, which is exactly Mike's carve-out (relationship severed ⇒ no reopen, no warning). The in-place, project-preserving GUI **Put Back** that creates the PLOG1 state (PLOG1-b) is simply **not a surface we expose.** **(b) `project.restore`** un-trashes the PROJECT itself in place (P06) — not a child-into-completed path, not implicated. **(c) `todo.move` / `todo.add` into a completed project** is NOT a restore (it moves a live, untrashed to-do); the **app itself reopens the parent** (T19/U19), already guarded by `H-REOPEN-RESOLVED-PROJECT` + the `acknowledgeProjectReopen` ack — end-state is a reopened, consistent project, no stranded child. **(d) the URL-bounce family** (RC01/RC02) is a *dated-reminder* clear (re-date a live item today→back), un-trashes nothing. **Conclusion: our surfaces cannot reproduce the PLOG1 stranded-open-child state**, so per the follow-up's own fallback we built only the read-side hint (3). Reversibility matrix unchanged (no op added/altered).
  3. **Reads of logged projects (read) — BUILT.** `things project show <ref>` of a completed/canceled (incl. logged) project holding untrashed `status=0` children now renders a flush, dim advisory (`contains N unfinished to-do(s) — invisible in the app's live views`) — a deliberately distinct sibling of the disclosure-hint placeholder class (no `…`, no reveal command: the rows are already on screen; render-language.md § Disclosure hints). Additive JSON field `ProjectView.openChildrenWhileResolved` carries the count. Fixture tests in `test/cli/render.test.ts` (present for completed/canceled/logged-with-open-children incl. singular; absent for open parents and resolved parents whose only children are completed/trashed). This closes the discoverability gap the 2026-07-14 checked-unswept ruling (§1) left open: that work resurfaces a checked-but-UNSWEPT project row in Today/Anytime, but its OPEN child stays cascade-excluded and is fully buried again once the project is swept — the `project show` advisory is where it now surfaces.

- **TUI-idiom follow-ups (parked 2026-07-12, full context in roadmap §H)**: height-fit/pager display modes, the width-handling polish pass (GUI-parity `…` truncation vs hanging indent — Mike leans truncation), the `--plain`/`--pretty` chrome override, and the possible far-future `things ui` interactive subcommand. All deliberately deferred; pick up with the §H stateful-prefs decision. **Glyph/styling consistency itself LANDED 2026-07-13 (PR mg/render-design-language):** the canonical render design language — bold-white project titles (single-law `projectTitleAccent`), contextual resolved styling (Logbook resolved-is-normal), chip-form repeat states, and the per-view roles table — is ratified and shipped; doctrine in [docs/design/render-language.md](design/render-language.md). The cross-terminal glyph AUDIT (roadmap §H, how the chosen glyphs render across emulators/fonts) is still open and separate.
- ~~**Width-aware TTY rendering (parked 2026-07-13 after an ideation round — the concrete successor to the §H width-polish item above)**~~ — **SHIPPED 2026-07-13** (PR mg/width-aware-tty): segment-aware row fitting for list rows. `src/cli/width.ts` holds a vendored wcwidth (`visibleWidth`/`charWidth`/`clipPlain`) and `fitRow`, the ratified sacrifice order (both-shrink 4:1 → container-drop WHOLE → tags-bare); `formatItem` composes named segments and stays byte-identical when width is null. One DERIVED floor `MIN_FIT_WIDTH` (worst-case furniture + `TITLE_MIN` 16, recomputed by a drift-guard test) — effective width = `max(terminal, floor)`, below-floor wraps (no clip, never drops the deadline). Width resolved ONCE in `runCli` (`THINGS_WIDTH` override / `stdout.columns` on a TTY / null). GUI-measured collapse oracle + the CLI-only container rule ratified in [design/width-aware-tty.md](design/width-aware-tty.md). The right-pinned deadline gutter stays phase 2. The broader §H TUI-idiom umbrella (height-fit/pager modes, `--plain`/`--pretty`, `things ui`, the cross-terminal glyph audit) remains open.
- ~~**Suppress "(archived)"-titled areas?**~~ — **RULED NO 2026-07-14** (Mike: "too opinionated, and not mirroring the Things GUI"). They render as-is; decision closed.

### New follow-ups from lab round 3 (2026-07-12, PR mg/lab-round3)

- ~~**Wire the Someday reorder scope**~~ — **ALREADY SHIPPED (confirmed 2026-07-13, PR mg/someday-reorder)**: the "confirm same-vs-distinct code path" step this item asked for resolved the item itself — there is nothing left to wire. The someday reorder scope is a SINGLE code path shared by loose someday to-dos AND area-less someday projects (`reorder --scope someday`, one kind per call), discriminated by row type at compile time. Contrary to this item's assumption, the someday **TO-DO** arm shipped FIRST (commit `c5a41f4`, "reorder scopes headings/someday/projects"); area-less someday **PROJECTS** were ADDED to the same scope later (commit `dda2a19`, P9e). Full coverage already on `main`: catalog + `ReorderScope` (`operations.ts`), membership/wire-list (`pre-state.ts`), compile (`commands.ts`), guards + mixed-type refusal copy (`guards.ts`), CLI `--scope` help (`writes.ts`), capability-matrix line 103 (✅ shipped), reversibility matrix (`reorder` entry + exhaustiveness test), pre-rank undo, unit tests (`write-compile.test.ts` P8b two-call, `write-reorder.test.ts` to-dos/projects/mixed/in-area), and VM e2e smoke (§C tests 65/68/69 green, `list "Someday"` two-call audit records banked). PREMISE CORRECTION: the round-3 P6h 3-item probe (`[S3,S1,S2]`→`[S2,S1,S3]`) *looks* like a plain Inbox-style reversal, but the fuller lab evidence (P7e/P8b/P8c, o-suite-results §"Aggregate list scopes") established that Someday does NOT share Inbox's forward re-rank: it STACKS each sent id above the call's ORIGINAL top (anchor model), so the shipped compile uses a validated TWO-CALL protocol (push desired-bottom to top as anchor, then send the reversed wire list) — a single reversed call only coincidentally reproduces a 3-item reversal and does not generalize.
- ~~**Deadline-less fixed-repeat projection fix**~~ — **DONE 2026-07-12 (UI1, PR mg/ui-vector-probe)**: discriminator FOUND and fix SHIPPED. The discriminator is the **template row's own `deadline` COLUMN** (NULL = deadline-less, `4001-01-01` sentinel = deadlined) — universal across fixed AND after-completion, resolving it WITHOUT needing `t2_deadlineOffset` (which stays 0 in every case). A non-zero start offset also shows as `ts=−N` in the rule (3 days earlier → ts=−3), captured this round via a working VNC keyboard path. Fix: new `RepeatingInfo.deadlined` (mapper reads the column; the sentinel is nulled so it never leaks as a phantom deadline on template rows), and `src/read/views.ts` + `src/model/occurrences.ts` gate the deadline projection on it instead of `rule.type==="fixed"`. `src/model/recurrence.ts` CAVEAT rewritten; unit tests in `test/unit/occurrences.test.ts` + `recurrence.test.ts` cover deadline-less vs deadlined. Oddities §8a table updated. (This ALSO falsified the old after-completion `ts<0` heuristic — a deadlined ts=0 after-completion template exists.)
- **Repeating-template reminder storage** (oddities §8b, minor): where a repeating template's rule-level reminder time is actually stored (it is NOT in `reminderTime` and NOT in the decoded `rt1_recurrenceRule`) is unresolved — likely materialized only on freshly-spawned instances (the pre-spawned ones didn't carry it after a mid-cycle rule change). Not product-blocking (the reminder is UI-only to clear regardless); note only if a repeating-instance reminder feature is ever wanted.

### New follow-ups scoped by UIC1 (2026-07-14, PR mg/uic1-certification)

> **2026-07-15 — AX-INITIATIVE ROADMAP EXECUTED AND CLOSED (PRs #142–#153 merged); this pointer is now HISTORICAL.** The plan in [design/ax-initiative.md](design/ax-initiative.md) ran to completion — all three probe campaigns (UIC2, AXDRAG1, NATIVE1) done and all four build items landed (repeating-project ops + full repeat-rule vocabulary lab-certified; `area.reorder-sidebar` shipped; the stop family dropped). Read ax-initiative.md for the durable record. The two items below are kept only as the original scoping evidence that motivated those builds.

- **(1) Repeating-PROJECT ops + the full repeat-rule vocabulary.** UIC1 mapped the entire Repeat dialog ([uic1-certification.md](lab/uic1-certification.md) field map): weekly day-of-week (a `Sunday…Saturday` pop-up + a "+" for multiple days), monthly `on the [day|weekday] [last|1st…31st]` (so last-day and nth-weekday are expressible), yearly `[Month] in [day|weekday] [ordinal]`, the **Ends** pop-up (`never`/`after N`/`on date`), and the **Add reminders** / **Add deadlines (start N days earlier)** checkboxes — all reachable once the dialog is open (the `select-popup` primitive + the dialog `group` already handle pop-ups and the interval field). Two build items ride on this: (a) extend `make-repeating`/`reschedule-repeat` beyond the v1 frequency+interval subset to the full vocabulary; (b) **repeating-PROJECT** variants — the dialog is the same editor, but a project is a **sidebar** entity not selectable via `things:///show` as a to-do, so this needs a sidebar-selection reveal (see item 2's row-selection blocker). Cite the UIC1 field map + `_NS:` addressing notes.
- **(2) Unblock the two failed ops + new AX-only op candidates — needs a row-selection primitive.** [status 2026-07-22: HISTORICAL per the pointer above — retained as scoping evidence only] UIC1's two certification failures (`stop-repeat`, `heading.convert-to-project`) share one root cause: **Things list rows expose no AX/URL selection handle** — a to-do card opens only on a mouse double-click, and a heading/project/sidebar row is not `things:///show`-selectable. The deferred **native-AXUIElement helper** ([ui-vector.md](design/ui-vector.md) "Native AXUIElement bindings") is the unlock: a compiled helper can synthesize a double-click (opening the Stop card) and select rows by position, which would certify both failed ops. The same helper opens a class of **new AX-only candidates** the UIC1 surface dumps scoped: sidebar area/project reorder, tag-manager reorder, Settings-pane manipulation, and File ▸ New Repeating To-Do (create-from-scratch, distinct from make-repeating's identity replacement). Cite the UIC1 sidebar/File-menu/Settings dumps.

## 6. New feature items (added by Mike, 2026-07-22 triage onward)

Design-then-build items. None is specced yet — each wants an ideation/design round with Mike before code.

- ~~**Reorder/sort ergonomics + optimization (containers AND the Today/Anytime views).**~~ — **DESIGN RATIFIED 2026-07-27: this item FOLDED INTO [docs/design/heading-demotion-and-move.md](design/heading-demotion-and-move.md)** (the anchor `--first/--last/--before/--after` ergonomics for project/area/today/evening/someday containers are Phase A scope; the minimal-move planner is ratified as the compile layer onto the existing wire protocols; `sort --by <key>` is parked as an explicitly-unratified Phase B candidate; the Anytime semantics question was already settled by ANYORD, §0.4). *Original item:* The shipped surface is capable but agent-hostile in shape: `things reorder <uuids…> --scope <today|evening|project|area|inbox|headings|someday|projects>` demands the caller supply an explicit uuid permutation and know the right scope/container flags, and the per-scope wire protocols (anchor-stack two-call, bounce legs, ≤10/call caps) mean a naive "resend the whole list" costs many round-trips. Mike wants (a) **optimization** of the sorting operations themselves and (b) **friendlier ergonomics** on both the CLI and the MCP API for re-sorting within a container (project/heading/area) and within the Today and Anytime views. Design directions to bring to the ideation round, not yet ratified: relative single-item placement verbs (`--before <ref>/--after <ref>/--first/--last` — `things area reorder` already speaks this dialect, so extend the dialect rather than invent a second one); accept titles/refs not just uuids (reuse the existing ref-resolution layer); a `sort --by <key>` convenience (deadline/when/title/creation) that computes the permutation server-side; and a **minimal-move planner** — diff current order vs target and emit the fewest wire moves instead of resending full lists (this is the "optimize" half; it also shrinks undo records and stays inside the per-call caps). Open questions for the round: Anytime-view ordering semantics (Anytime is a grouped/derived view — what does "reorder within Anytime" even bind to per group? needs a semantics map first, possibly a small lab probe); whether MCP gets a dedicated `sort`/`place` tool or params on the existing reorder tool; interaction with the experimental-gated native strategy vs bounce defaults.
- **Change-watch / poll mode for agents (`things watch`, name TBD).** Motivating workflow (Mike's, verbatim in spirit): Claude Code hands Mike tasks — creates a little project, schedules it into Today — then kicks off a long-running `things` CLI invocation that **watches that project specifically** and reacts when Mike actually completes/adds/removes tasks in it, without firing on unrelated library changes. Shape: a read-only, long-running CLI command (and possibly an MCP story later) scoped to one container or view (`--project <ref>`, maybe `--today`), emitting structured events — likely NDJSON lines `{event: added|removed|completed|canceled|modified, uuid, title, …}` — plus a blocking `--once`/`--until <condition>` mode that exits on the first matching event (the natural fit for an agent's "wait for change" step and for `run_in_background` Bash tasks). Implementation raw material already banked: the SYNC1 freshness proxies — `main.sqlite-wal` mtime advances on every write, so wal-mtime is the cheap wake signal; on wake, diff a scoped member snapshot (uuid set + status/stopDate/index per member) against the last one so unrelated edits are filtered out (a pure poll-interval fallback also works where the mtime signal is unavailable). Constraints to honor in design: READS ONLY (never a write path); on the prod host, reads must keep the ONE stable command/binary shape (macOS consent re-triggers on new shapes — the watcher must ride the existing library read path, not invent a new binary); define behavior when Things isn't running (DB frozen — the doctor sync-health signals apply); bounded exit conditions (timeout, `--until-complete` when every member is closed) so agent tasks can't hang forever. Open questions: event granularity for `modified` (title/when/notes edits — all or opt-in?), whether completion-of-the-whole-project is a first-class terminal event, and how undo-driven reversals should read in the event stream.
- ~~**Container-scoped sandbox mode (added 2026-07-22).**~~ — **DONE (branch `mg/container-scope`, PR pending):** shipped per the ratified design in [docs/design/container-scope.md](design/container-scope.md). MCP `--scope` flag (the trust boundary) > `THINGS_API_SCOPE` env > stored `scope` config; enforcement lives once in the library (`src/read/scope.ts` predicate + `evaluateScope` pipeline gate); no-oracle guarantee via scope-aware resolution (out-of-scope == nonexistent, byte-identical); add-redirect, structural refusals, destination gate; additive `meta.scope` + TTY banner; `doctor` scope section; stored-config footgun warning. Deviations recorded in the design doc §8 (`todo.add-logged` refused not redirected; post-verify assertion not built). *Original item:* Hard-limit an entire process's Things access to one container — the motivating case is an AREA ("this coding agent may touch only the *Work* area"), but design it as a generic **"within this container"** scope so a single PROJECT works too. Semantics Mike sketched: **reads** — every view auto-applies the filter (`things today` shows only Today items in/under the scope container; same for `upcoming`, `anytime`, `show`, search, etc. — items outside the scope are invisible, not just de-emphasized); **writes** — create/complete/edit/move allowed only for items directly or transitively inside the container (projects in the area, headings/to-dos in those projects), and any operation that targets, or would RESULT IN, an item outside the scope is refused fail-closed (moving an item out of the area = blocked; creating in the Inbox or another area = blocked or explicitly out of scope). **Trust model is the crux — this is a process-level policy, not a per-call flag**: Mike's explicit framing is "I'm not going to trust the AI to include a `--area` filter at all times." Configuration lands where the disruption gates already live — at daemon/process spawn: a flag on `things mcp` (imposed for the life of that server process, like the existing two-key write gates) and/or an env var read at startup. Design cautions to bring to the round: enforcement must live in the LIBRARY layer (one choke point both CLI and MCP inherit), not be sprinkled per-command; the boundary is only as strong as who controls the environment — the MCP-daemon-flag form is a real boundary (the agent talks only through the tools), while a bare env var on CLI-in-Bash is advisory unless the harness pins the environment (document the difference honestly; possibly refuse to silently weaken — e.g. the scoped MCP server never exposes an unscope path); ref-resolution must not leak out-of-scope matches (title search, uuid lookups, and error copy all need to answer "not found" identically for hidden vs nonexistent, or the scope becomes an oracle); undo must only invert in-scope records (actor-scoping via `--by` already exists — scope compounds with it); interactions to spec: Inbox (outside every area — invisible? a curated allow?), area-less projects, the Logbook/Trash views, `heading.convert-to-project` and other identity-replacement ops (the successor must inherit scope membership), and the §6 watch mode (a scoped process naturally yields a scoped watcher). Aligns with the fail-closed doctrine and the existing `profile`/`maxDisruption` policy-key family in `src/config.ts` — likely a sibling policy key plus spawn-time override.
- **Notes are Markdown — VM probe (NOTESMD), then document across every consumer surface (added 2026-07-22).** — **STEP 1 DONE 2026-07-23 (`lab/scripts/research-notesmd.sh`, verdict + construct table [docs/lab/notesmd-results.md](lab/notesmd-results.md)).** Notes render markdown **FORMATTING but keep the syntax markers VISIBLE** (source-preserving live render). RENDERS: `#`/`##` headings, `_`/`*` italic, `**`/`__` bold, `` `code` ``, fenced code blocks, `-` bullets (nesting kept), `1.` ordered, bare URLs (auto-linkify), `<url>`. INERT: `[label](url)` (label stays literal, only the URL auto-links → confirms "don't wrap bare URLs"), `*`-bullets, `> blockquote`, `---` rule, `- [ ]`/`- [x]` checkboxes (with a minor quirk: `- [x]` renders DIMMED, `- [ ]` doesn't → oddities §9d). To-do and project render identically. **Step 2 (documentation sweep across skill/help/MCP copy) STAYS QUEUED** — not done here. Recommended phrasing + the checkbox-import precondition are in the results doc. *Original context below.* Mike's live-GUI observations: the notes field on to-dos AND projects renders markdown — `_underscores_` italicize, `**`/`__` bold; markdown links show label AND url (not collapsed, and possibly not clickable in that form); bullets maybe; `- [ ]` checkbox syntax presumably inert; **bare URLs are auto-linkified**, so markdown-link syntax around a URL may be pure noise. **Step 1 is a VM probe** — seed one note exercising the full plausible vocabulary (emphasis, links bare/markdown/angle, bullets ordered+unordered, nesting, headings, code spans/fences, blockquotes, hr, checkbox lines, hard breaks) and screenshot the rendered card, so the documented vocabulary is evidence not folklore. **Step 2: document the verdict everywhere agents form expectations** — the skill references, `things help` copy, and the MCP tool descriptions must say notes is a *markdown field with multi-line support*, not a text blob, plus the "don't wrap bare URLs" guidance (subject to surface-copy.md vocabulary rules). Feeds oddities if any construct renders bizarrely.
- **Multi-line notes as first-class CLI input (added 2026-07-22).** It already WORKS — `--notes` is a plain string and the shell passes real newlines (`--notes $'line1\nline2'`, or a quoted heredoc substitution) — but nothing advertises it, and there's no composable input path. Candidates: `--notes -` (read stdin; heredoc/pipe-friendly, the classic first-class spelling), maybe `--notes-file <path>`; document the `$'…\n…'` idiom in help/skill regardless. Deliberately NOT: silently interpreting literal `\n` escapes inside the plain flag (ambiguity footgun — a note may legitimately contain backslash-n); if escape interpretation is ever wanted, it's an opt-in flag. Cheap; could ride along with the NOTESMD documentation pass.
- **Markdown checkboxes → checklist import (ideation only, added 2026-07-22 — Mike: "maybe a silly idea, not fully thought through").** Let a `- [ ]`/`- [x]` block in supplied notes be converted into REAL checklist items — the use case is importing a run-list in one call. If built, it must be an explicit opt-in flag (e.g. `--checklist-from-notes`), never a silent transform of note text (surprise mutation of user content is a footgun); open questions: does the checkbox block get stripped from the stored note, does `[x]` map to checked, what's the roundtrip story (checklist→markdown on read is the `--markdown` output mode's problem, §5). Depends on NOTESMD confirming checkbox syntax is inert in the rendered note (if Things ever renders it, conversion gets more attractive).
- **Programmatic writes don't trigger a Things Cloud push — probe (SYNCLAT), then a sync-nudge mitigation (added 2026-07-23).** — **STEP 1 DONE 2026-07-23 (`lab/scripts/research-synclat.sh`, verdict [docs/lab/synclat-results.md](lab/synclat-results.md)): HYPOTHESIS FALSIFIED — the sender pushes promptly for EVERY vector, so the STEP-2 nudge build is NOT justified.** Every tested write vector (GUI create, URL `add` foreground AND background, URL into project + someday + 1.2 KB note = Mike's exact case, AppleScript `make`) advanced clone A's `BSSyncronyMetadata` last-attempt signal within **~2–3 s** (idle baseline: flat for 16 min+ with no writes → no periodic heartbeat; Things syncs ON CHANGE), and the pushed item genuinely reached the server (clone B pulled it). Destination/note-size/foreground-state irrelevant. The URL path (`open -g things:///add`) is byte-identical to the CLI's `todo add`. The minutes-of-latency Mike saw is **receiver-side**: idle clone B never naturally pulled in 5 min+ — APNs push is unavailable in the VM (SYNC2), so the receiver has no push-wake (the VM cannot measure real-phone pull latency). The settings-panel "Last Updated" **equals** the `BSSyncronyMetadata` attempt signal (both refresh together on a foreground sync) → `doctor` reports it honestly. **No "Sync Now" menu item exists** (UIC1 dumps). Shortcuts vector NOT measured (`shortcuts run` hung on in-clone consent). The lab did NOT reproduce Mike's *sender-side* GUI-vs-URL panel difference; leading unreproduced explanation = a **cold sync connection** on his long-idle Mac (fresh-login clones keep it hot). **NO oddity filed** (opposite of a no-push bug). **Remaining (needs Mike / on-hardware):** get an on-hardware repro of the sender-side stale-panel observation (cold-connection hypothesis) BEFORE building any nudge; document the sync-trigger model in the skill/contracts references (writes DO push ~2–3 s; cross-device latency is the receiver's pull/APNs cadence, not a sender delay). *Original hypothesis/plan retained below for context.* Mike's live observation (2026-07-23): an agent-created to-do (`things` CLI write, elaborate title + very long note, filed into Someday of a work area) appeared in the desktop GUI immediately but did NOT reach the phone for a couple of minutes; the Things Cloud settings panel showed the last update **15–20 minutes prior** — the programmatic write did not kick off a sync. Control, same sitting: manually creating a test to-do in the GUI advanced the panel's last-updated timestamp the moment editing finished. Working hypothesis: GUI edits trigger an immediate push, while URL-scheme/AppleScript mutations ride only the periodic sync timer — a minutes-wide drift window in which agent-created items are invisible on mobile (and stale-read/conflict exposure grows). **Step 1, probe (SYNCLAT):** VM lab campaign on the SYNC2 two-clone networked rig (throwaway account recipe already banked), reading the `BSSyncronyMetadata` last-attempt signal the doctor sync-health section already decodes plus arrival-on-the-second-clone as ground truth. Map: which write vectors trigger an immediate push (URL scheme vs AppleScript vs Shortcuts, with a GUI-edit control); whether destination/status matters (someday vs today/anytime/inbox — Mike suspects not, verify anyway); the periodic cadence when nothing nudges it; and whether the settings-panel "last updated" equals the `BSSyncronyMetadata` attempt signal (if so, `doctor` can report the drift window honestly). **Step 2, mitigation — find the cheapest coercion spelling** and test candidates in the same runs, ranked by disruption: a benign URL invocation (`things:///show` or similar); a no-op-adjacent AppleScript touch; app foreground/activate (focus-steal, last resort); an AX path (no Sync Now menu item is known — check the UIC1 menu dumps before assuming one exists). Wire the winner as a post-write sync nudge — opt-in flag or config key (e.g. a `sync.nudge` policy sibling in `src/config.ts`), possibly auto-appended after CLI/MCP mutations — so agent writes land on mobile promptly. Whatever the verdict, the sync-trigger model gets documented in the skill/contracts references (agents should know writes may sit local for minutes), and a confirmed no-push-on-URL-scheme finding is an oddities candidate for the CC report.
- **Per-app-context macOS permission (TCC) onboarding — one-swoop grant flow + staleness detection (added 2026-07-23).** Trigger observation (Mike, 2026-07-23): the first `things` CLI session spawned from the **T3 Code (nightly)** macOS app — a different host app than the previously-used Ghostty terminal — threw several macOS consent modals in quick succession mid-triage (access to Things data, presumably covering both reads and writes). Root mechanism: macOS TCC grants attach to the **responsible process** (the host app bundle that owns the process tree), not to the `things` binary or the user account — so every new agent-harness app, terminal emulator, or possibly even each rebuild of a nightly/ad-hoc-signed host starts from zero grants, and the modals then land one at a time, mid-workflow, exactly when an agent is driving and the human may not be watching. Four deliverables for the design/probe round: **(1) Permission-class inventory** — enumerate every TCC class our surfaces touch and keep it current: Apple Events/Automation per (host app → Things3) AND (host app → System Events) pair (AppleScript vector; the ui vector's osascript driver); the Sequoia-era "access data from other apps" group-container class (SQLite read of the Things container — likely the modal burst Mike saw); Accessibility (AX ui vector); Shortcuts (`shortcuts run` consent story, incl. the known no-Always-Allow delete-class prompts, §3/P5); whatever a new host needs for `things:///` URL dispatch (the auth token is per-database not per-app — verify whether any per-app "wants to open Things" confirmation exists at all); note Full Disk Access as the blunt umbrella alternative some users will prefer for the DB-read class. **(2) One-swoop onboarding flow** (`things onboard` / `things doctor --fix` / name TBD): a guided, user-present sweep that deliberately exercises each class in sequence so ALL the prompts fire NOW — narrating which modal is about to appear, what to click, and what denying breaks — then a verify pass reporting granted/denied per class. **(3) Prompt-free status detection + the help-surface warning**: each class needs a NON-TRIGGERING probe so we can warn without causing the very modals we're warning about (candidates: `AEDeterminePermissionToAutomateTarget` with askUserIfNeeded=false for Apple Events; `AXIsProcessTrusted` for Accessibility; the group-container class has no obvious query API — research needed; reading TCC.db directly requires FDA so is not the answer) plus identification of the current app context (resolve the responsible bundle id — process-tree walk / `launchctl procinfo`-style — with version and signing identity) checked against a recorded set of onboarded contexts in config/state; surface the verdict as a warning in **`things --help`** (Mike's specific ask: an agent reading help should learn "this app context hasn't been onboarded — permission prompts will interrupt commands; run `things onboard` now while the user can interact") and in `doctor`. **(4) Staleness matrix — when must onboarding re-run**: probe and verify every re-grant trigger we can construct: host app self-update (a signed stable app with stable bundle id + Team ID should keep grants; an ad-hoc/unsigned nightly whose cdhash changes every build may reset them — T3 Code *nightly* is exactly this risk class), bundle-id differences between release channels of the same app, macOS major upgrades, `tccutil reset` (per-class and blanket), Things-side app updates (the TARGET half of the AppleEvents pair), and manual revocation in System Settings; cover every corner case we can in the VM lab (AXVM1 precedent: TCC user-path toggles work in a Tart guest, SIP on) with real-hardware confirmation for the signed-update cases. Related doctrine to fold in: the `scripts/prod-read.sh` one-stable-command-shape rule is this same TCC mechanism seen edge-on (new command/binary shapes re-trigger consent) — the onboarding story should absorb/cross-link it rather than leave two separate consent doctrines; and remember the MCP server inherits the responsible-app attribution of whatever launched it, so onboarding must run inside (or explicitly cover) the context that will actually host the daemon, not just "a terminal".
- ~~**Demote headings to project-scoped sub-resources — the checklist model (added 2026-07-22).**~~ — **DESIGN ROUND COMPLETE 2026-07-27; ratified spec: [docs/design/heading-demotion-and-move.md](design/heading-demotion-and-move.md). Implementation Phase A IN PROGRESS (it gates the 1.0 contract shape break per D7): §2 project-scoped heading verbs (#291) + §3 bulk create (#289) + §4/§5/§7 the `move`/`reorder` vocabulary & detach family SHIPPED (this PR — variadic membership moves + in-place reorder over the existing wire protocols, `--detach` removed); remaining Phase A: none (the §4 anchor vocabulary compiles onto today's protocols — day-bucket/heading-bucket placement stays app-default pending the DAYORD/HEADORD/SOMEORD probes).** Rulings in brief: project-scoped heading verbs are the ONLY spelling (`project.add-heading`/`rename-heading`/`archive-heading`/`unarchive-heading`/`promote-heading`/`move-heading`; the `things heading` namespace is removed with NO aliases per ALPHA-CONTRACT); heading-sel = exact-title-or-uuid with NO index addressing (the study's unanimous reindex hazard — the checklist `--index` precedent deliberately does NOT transfer), duplicates + empty titles fail closed with uuid candidates, and **uuids STAY in read payloads** (dup disambiguation + script robustness + titleless records); variadic titles-only `todo add`/`--stdin`/`--id-only` compiled onto the batch machinery; `todo move`/`todo reorder`/`project move`/`project move-heading` gain the variadic anchor vocabulary with GUI-parity mixed-bucket semantics and the position-never-migrates rule; `--detach` is REMOVED for the one-spelling-per-kind detach family (`--no-heading`/`--loose` for to-dos, `--no-area` for projects); `project set-layout` is Phase B; the project-view heading sub-bucket FIDELITY FIX (scheduled/someday/repeating render under their headings — live-GUI confirmed) rides the 1.0 shape work; DAYORD + HEADXPROJ probes queued ([lab/probe-backlog.md](lab/probe-backlog.md) §C). *Original item + round inputs retained below.* Mike's framing: a heading can ONLY exist inside a project, every heading op is project-scoped anyway, so a first-class `things heading <verb>` namespace with exposed uuids never quite made sense. **Checklists already embody the target model** (`things todo checklist <uuid>` — namespaced under the owning to-do, items addressed by title or 1-based `--index`, "checklist items have no exposed uuid"): apply the same doctrine to headings — operations live under the project (`things project add-heading` / `promote-heading` / …), headings referenced by title or index within that project, uuid hidden as an implementation detail. This REOPENS AND RESHAPES the pinned cli-grammar Phase 2 go/no-go (§5): Phase 2 specced resource-scoped verbs as an ADDITIVE ergonomic layer; Mike now leans toward project-scoped being the *canonical* heading spelling, not an alias. Design round must settle: duplicate heading titles in one project (the checklist precedent is best-effort + `--index` escape hatch); whether `things heading …` verbs deprecate or vanish (breaking-change story pre-1.0); whether read-side JSON keeps emitting heading uuids (agents may hold them today); MCP tool reshaping; and whether checklist ops should ALSO gain the sibling `things todo <ref> check-item …`-style spellings for symmetry once the grammar is settled. **RATIFIED 2026-07-26 (verb harmonization, shipped): `add` is the canonical create-verb across the operation catalog** — `heading.create` → `heading.add` and `project.create-repeating` → `project.add-repeating` (op-kind string, CLI command, and MCP action value; old records stay undoable via a read-time alias map). Pressure-tested against the task-API landscape study (§ below — Todoist's sync-command vocabulary uses `add`) plus the CLI-tradition and rename-blast-radius arguments; `make-`/`convert-` stay DISTINCT as transform verbs (not folded into `add`), and the delete family is untouched. Deliberately deferred to THIS design round rather than renamed now: `heading.rename` (its promote/demote grammar is exactly what this item settles) and `todo.edit-checklist-item` (folded into the checklist state-token round below). **SEQUENCING (ruled 2026-07-27): this round now BLOCKS the ratified apiVersion-2 shape break (§0½ item 3, D7 — demotion first, then v2), so it is the next major design round.** New inputs queued for it (2026-07-27): (a) Mike's GPT-5.6 clean-room CLI thought experiment — headline vocabulary: **selection-based block `move <ids…> --before/--after/--first/--last`** where the source-list order becomes the destination order (reversal for free), container INFERRED from the anchor, membership+position in one verb; `reorder` reserved as a STRICT full-order container replacement (reconciliation tool), no partial form; bulk create via repeatable `--title`/stdin lines; project top-level order = unheaded to-dos + headings intermixed, keyed on DIRECT CONTAINMENT (headed tasks live only in their heading's sequence) — which matches both the Things GUI drag metaphor and (notably) our lab-locked anchor-based wire protocols better than the shipped full-permutation `reorder <uuids…> --scope` surface does. Heading-demotion tie-in: anchors and `--to-heading` targets must speak title/`--index` refs once heading uuids are hidden, and "un-head" (`--no-heading` = project root, unheaded section) must stay distinct from our `--detach` (which strips the PROJECT — different op, footgun if conflated). (b) The multi-model clean-room replication study — **DONE 2026-07-27, memo ratified as design-round input: [docs/design/cli-vocabulary-study.md](design/cli-vocabulary-study.md)** (GPT-5.6 via two scaffolds + Opus 4.8 + Sonnet 5). Headlines for the round: the move-vs-reorder split, the shared `--first/--last/--before/--after` anchor grammar, arg-order-as-destination-order (free reversal), bulk create, and container-inference-from-target-ids were UNANIMOUS (the last one contradicts our required `--scope` flags); the ChatGPT session's strict-full-order `reorder` did NOT replicate (both fresh GPT runs made reorder a partial block-mover — weight accordingly); and **all four runs independently warned AGAINST ordinal/index heading addressing** (a heading is itself a reorder target, so an index silently re-targets after any reorder — the "reindex hazard"), which directly challenges the checklist-model `--index` escape hatch this round planned to inherit; the strongest counter-position on record is the Claude camp's "keep heading uuids for scripting robustness" argument, which the round must answer if uuids are hidden. **Post-ratification addenda (2026-07-27):** bare-invocation semantics ruled (bare `todo move` = teaching error; bare `todo reorder` = in-place block at the earliest movee's slot, one container+bucket, `--first` NOT implied) — spec §4 updated, two refusal rows added. **PARKED REMINDER — agent-skill refresh:** the things-cli skill (SKILL.md + references) must be re-swept to teach the new ergonomics once Phase A lands — the move-vs-reorder distinction EXPLICITLY (ratified: it was not inferable from verb names), the project-scoped heading verbs, the detach family, bulk creation, and the project-view heading sub-bucket shape; fold into the roadmap §N phase-4 re-sweep rather than a separate pass. **PHASE A LANDED 2026-07-27 (PRs #289 bulk-create, #291 demotion verbs, #293 move/reorder + detach; REORDGAPS verdicts #290/#292; rule-5 reconciliation #294).** Phase A.1 follow-ups: **(1+2) the bounce-protocol EVIDENCE is now BANKED (BOUNCE2 campaign, 2026-07-27, [lab/reordgaps-results.md](lab/reordgaps-results.md) BOUNCE2 section; clone `bounce2-lab`, script `lab/scripts/research-bounce2.sh`)** — all four unknowns resolved so the A.1 BUILD can proceed non-blind: **BOUNCE2-h** settled the HEADORD-c anomaly (the bounce BACK-inserts a headed child — appends to heading-bucket end, heading FK preserved — so a FORWARD-order bounce places headed children in EXACT order; multi-item in-heading ORDER is now promisable, not best-effort); **SOMEBNC** proved the `when=` bounce reorders within-container someday members state-preservingly (AREA members FRONT-insert → reverse-order protocol, closing the §9f gap WITHOUT the destructive area command; PROJECT children BACK-insert → forward-order, a 2nd path beside native SOMEORD-b); **DAYORD-b + ANYBNC** re-confirmed for wiring (DAYORD-b banked as recurring o-suite **O17**, ran GREEN `o-20260728-001645`; ANYBNC is the loose-anytime reverse-order bounce); **BOUNCE2-t** measured ~110 ms/item (10/20/30 → 121/108/112 ms/item, 2 verified mutations/item). **The A.1 BUILD is BUILT (this PR):** the FORWARD/REVERSE-order bounce protocols + the DAYORD-b container `todayIndex` reorder are wired into the shared reorder op as new scopes `heading` / `area-someday` / `anytime` / `container-day` (+ the SOMEBNC-project bounce fallback on the `project` scope). The five placement classes flip to GUARANTEED; result payloads flip from app-default-note to guaranteed. **Config keys shipped:** `bounce-enabled` (default `true`; `false` → the planner REFUSES bounce-dependent placements naming the flag, never a destructive/unverified fallback) and `bounce-max-items` (default `30`, up from the hardcoded `BOUNCE_MAX_ITEMS=10`; BOUNCE2-t backs 30) — env overrides `THINGS_API_BOUNCE_ENABLED`/`THINGS_API_BOUNCE_MAX_ITEMS`, surfaced by `config get` with provenance. **Three ratified amendments also landed (Mike, 2026-07-27):** (1) `--before`/`--after` against an unmoved sibling is UN-REFUSED via an extended bounce (back-insert = forward-bounce the block-through-end suffix; front-insert = reverse-bounce the top-through-block prefix); the cap counts TOUCHED items, `--dry-run` reports the unnamed-sibling touch count, and the ok payload lists co-bounced siblings (`touched`) — the only residual anchor refusal is a no-protocol bucket. (2) Verify relaxed to ONE assertion per item round-trip (transient someday state not independently verified; fail-loud retained) via `WriteOptions.skipVerify`. (3) `allow-experimental` now DEFAULTS TO TRUE (the reorder command is the app's private/undocumented surface, not vendor-experimental; safe on by default given verify-per-write + fingerprint gate + o-suite/O17 canary); the planner picks the best ENABLED strategy per placement (native preferred, proven bounce fallback), refusing only where no enabled path exists. A `BOUNCEJSON` probe idea (can one `things:///json` array collapse an N-item bounce to 2 dispatches?) is filed in the probe backlog. **(3) fixture hygiene:** `test/fixtures/build-db.ts` never unlinks its per-test `.sqlite` files → repeated full-suite runs fill $TMPDIR to ENOSPC (found during #293); fix in `close()`/`cleanup()`. Phase B remains: `project set-layout`, retiring `reorder --scope`, the sort-by-key candidate. **Unlocked by the #298 campaign (2026-07-28), queued:** (1) **BOUNCEJSON collapse** — rewrite the BACK-insert bounce classes (within-heading order, project-someday fallback) in `src/write/reorder.ts` to compile ONE pre-validated `things:///json` array (exact array-order application proven, ~7× faster, validate-first FULL-ABORT on any bad element — strictly better atomicity than sequential legs); FRONT-insert classes stay on the URL bounce (json is index-inert there, oddities §9i). (2) **`project.move-heading-to-project`** — new ui-vector op on the lab-confirmed ellipsis-menu recipe (HID-click the heading `…` → Move… → keyboard-driven picker; DB oracle = single heading-row project-FK rewrite, children follow via heading FK) — needs the standard certification arm + two-key gating; closes the last heading capability gap.
- **Checklist state-token conditional replace — ETag/If-Match semantics for the whole-list rewrite (added 2026-07-23, part of the heading-demotion design round above).** Mike's idea: since every checklist write is under the hood a wholesale rewrite anyway, offer "produce the entire new state in one call" as a first-class path, guarded git-`--force-with-lease`-style by a token representing the state the caller last saw. Design round settled on the **HTTP ETag/If-Match precedent as the spec to crib** (same optimistic-concurrency mechanism as force-with-lease, but designed for a stateless request/response client like ours — git's lease leans on client-side remote-tracking state we don't have, and its implicit-default form has a known vacuous-pass footgun; the token here must always be explicit read-then-present). Sketch, pre-design-round: **(a) token = deterministic content hash**, canonical order-sensitive hash of `[{title, completed}…]`, version-prefixed (e.g. `cl1-<hash>`), opaque to callers, derivable from any read — no stored nonces (there is no server); item uuids can't participate (they regenerate on every rewrite; see `src/write/edit-checklist.ts` header). Every checklist-bearing read emits it; every successful checklist mutation returns the new one. **(b) The token subsumes `acknowledgeChecklistReset`** — presenting a fresh token proves the caller has SEEN the state being replaced, a strictly stronger acknowledgment than the boolean; bare (tokenless) replace keeps requiring the ack (the `If-Match: *` force spelling). **(c) Mismatch failure returns the CURRENT items + fresh token** in the error payload so an agent can rebase intent and retry without an extra read round-trip (the convergent-loop property). **(d) Granular intent ops stay** — `todo.edit-checklist-item` has undo semantics a replace can't reconstruct (targeted 3-way-merge inverse vs whole-snapshot restore that refuses on any out-of-band diff); token-gated replace is the bulk/agent path (N item changes in ONE rewrite instead of N — fewer URL dispatches and sync events), and granular ops also gain optional `--if-match` to harden their own documented read→rewrite race (the "HONEST LIMIT" in `edit-checklist.ts`). **Honest limits to document, not solve:** no atomic CAS exists at the app layer — the check is re-read-and-compare immediately before the `things:///json` dispatch, shrinking the clobber window from "since the agent's last read" to milliseconds, not zero; and edits made on another device but not yet synced down are invisible to any token. Generalizes to demoted headings later ("replace this project's heading structure under token" is the same primitive).

### From the task-API landscape study (2026-07-25 — ratified reference: [docs/reference/task-api-landscape.md](reference/task-api-landscape.md))

Mike's comparative-research experiment (Todoist v1 / MS Graph To Do / TickTick vs our DTO surface) produced three concrete build/queue outcomes; the full concordance table, ADOPT/ADAPT/SKIP verdicts, and the compat-layer feasibility verdict (real but niche — no daemon now; adapter-skill form works today; revisit on a named consumer) live in the reference doc.

- ~~**`changes --since` deletion visibility via `TMTombstone` (ADOPT — probe first, then build).**~~ — **STEP 1 DONE 2026-07-26 (TOMB1, `lab/scripts/research-tomb1.sh`, verdict [docs/lab/tomb1-results.md](lab/tomb1-results.md)): DON'T BUILD on `TMTombstone` — it is NOT a general deletion log.** A `TMTombstone` row is written iff the deleted row carried **`leavesTombstone=1`**, a flag set ONLY on repeating-template lineage (template + spawned instances), NEVER on ordinary user to-dos/projects/checklist items, and unchanged by attaching a Cloud account. Proven across an airgapped control, an account-attached single clone, AND a two-clone remote delete (the receiver applied the delete and removed the row **tombstone-lessly** — ordinary deletions propagate via the Syncrony change-log, not `TMTombstone`); the gate confirmed by deleting a `leavesTombstone=1` repeating instance (wrote a tombstone) alongside a plain control (wrote none). So a `changeKind:"deleted"` feed built on this table would surface only repeating-instance churn and **silently miss every ordinary user deletion** — worse than none. `deletionDate` itself is a trustworthy wall-clock epoch, but the row *population* is the wrong set. **If deletion tracking is ever needed:** a scoped member-set snapshot-diff (what the §6 watcher already needs — catches ALL deletions in scope) or the non-locally-queryable Syncrony server change-log; fold into the §6 watcher design. Follow-ups (TOMB2, non-blocking) in the results doc: what else (if anything) sets `leavesTombstone=1`; a READ-ONLY `scripts/prod-read.sh` check of the real `TMTombstone` population. *Original item retained below.* Today the changes feed reports `created|modified` only, so a mirror-keeping consumer (and the future §6 watcher) can never learn about deletions — the one real gap vs a delta API (Graph's `@removed` precedent). Things records deletions in `TMTombstone` (`deletedObjectUUID`, `deletionDate`; `leavesTombstone` on TMTask/TMChecklistItem opts rows in) — but **tombstones are a SYNC artifact**: the lab proved sync-off installs write NONE for any delete (A25/A27, a-suite-results §19), the Shortcuts "Delete Immediately" hard-delete leaves none even nominally (oddities, S-delperm), and the exact lifecycle (rows written at trash-time vs empty-trash vs sync push?) is the open atlas question (schema-v26.md §5). **Step 1, probe (TOMB1):** on the SYNC2 networked two-clone rig, map when tombstone rows appear per delete path (trash, empty-trash, area delete, Shortcuts hard-delete) and whether `deletionDate` is trustworthy as a `--since` cursor. **Step 2, build:** additive `changeKind: "deleted"` rows (uuid + deletionDate only — titles are unrecoverable, that's the point of a tombstone) with an honest degradation story: no account attached / empty table → the feed says deletions are untrackable rather than silently omitting them.
- ~~**Batch `temp_id` chaining + per-command idempotency ids (ADAPT — Todoist sync-command precedent).**~~ — **IMPLEMENTED 2026-07-26 (branch `mg/batch-temp-ids`).** Shipped on the JSONL `batch` and the MCP `batch` tool per the ratified design; full contract in [docs/design/contracts.md](design/contracts.md) (Batch chaining, idempotency, and undo). Rulings as built: **(1) tempId** is a per-line handle on any uuid-MINTING op (todo.add/add-logged, project.add/add-repeating, area.add, heading.add, todo.duplicate, todo/project.make-repeating, todo/heading.convert-to-project); `[A-Za-z0-9_-]{1,32}`, unique per batch; a duplicate / bad-charset / wrong-op declaration rejects the WHOLE batch pre-flight. **tag.add is excluded** (no uuid — usage error pointing at title refs). **(2) Binding** is to the leg's PRIMARY result uuid, ONLY on verified-ok; a failed leg (incl. the rule-mismatch that surfaces discovery uuids) binds nothing. **(3) `$refs`** resolve strictly/fail-closed per line as legs land — `unresolved-temp-ref` (undeclared), forward-ref, and unbound-leg refs fail that one line before dispatch; independent legs still run. Dotted `$x.instance`/`$x.replaced` reach a make-repeating's spawned/replaced uuids. **(4) opId** (`[A-Za-z0-9_-]{1,64}`) → recent-trail lookback (last 1000 records ∧ 7 days); a match skips the leg as `already-applied` with the original uuid and rebinds it. **(5) Undo:** all legs under one txn; a `batch` summary record's undo token undoes the whole submission in reverse via the EXISTING compound-summary machinery. **Review-settled amendments (2026-07-26):** (a) `project.duplicate` is INCLUDED in the mint-list — the ratified rule is "anything that mints a uuid"; the original table omission was an oversight (coordinator confirmed). (b) "Individual legs keep their own undoTokens as today" is intentionally NOT honored — grouping under the existing compound machinery makes a batch the single undoable unit (a role-"leg" record is excluded from independent undo and its per-line `undoToken` is suppressed); this is a BREAKING behavior change for prior batch users, labeled as such in the CHANGELOG (the compound precedent + the `--last` double-count argument won). (c) Added `dangerouslyDriveGui` to per-op batch options so the ui-drive mint ops (make-repeating, convert-to-project) can pass their per-leg two-key gate. The sibling checklist ETag/If-Match state-token item above remains queued.
- **apiVersion-2 rider: merge `error.detail` / `error.details`.** The error-envelope comparison surfaced our own wart: two near-identical field names with unrelated shapes (`detail` = expected/observed verify payloads; `details` = candidates/suggestions). Fold into ONE `detail` object when the v2 shape break lands (it's rule R5's natural companion; do not ship it as a lone break). Tracked with the §0½ item 3 shape-rework decision.

### 1.0-contract canon follow-ons (added 2026-07-29, PR `mg/contract-canon`)

The contract canon shipped as PR A of a two-PR arc: `docs/contract.md` (consumer-facing contract), `docs/design/api-doctrine.md` (the design canon), the `ErrorCode` registry narrowed in `src/contracts.ts` (the compiler IS the registry), and the README differentiators. ~~**PR B — the envelope JSON Schema generated from `src/contracts.ts` — is the immediate next step**~~ **PR B LANDED (branch `mg/envelope-schema`):** `schema/envelope.schema.json` is now generated from the `WireEnvelope` union in `src/contracts.ts` by `npm run schema:gen` (`scripts/generate-schema.ts`, `ts-json-schema-generator`), shipped in `package.json` `files`, and kept in three-way sync by `test/contract/envelope-schema.test.ts` (types↔schema deep-equal + schema↔runtime ajv validation of real emitted envelopes). Coverage boundary: the envelope layer (`apiVersion`/`ok`/`kind`/`meta`/`error`) is modeled exactly, the per-`kind` `data` payload is an open object (see `docs/contract.md` § The machine-readable schema). The canon documents the compatibility covenant that ACTIVATES at v1.0; it adds no compatibility machinery now (ALPHA-CONTRACT still governs). These are the shape/agent-ergonomics rounds the canon named as queued (R6/R7 are called out by name in api-doctrine §7):

- ~~**R6 — no-redundant-ancestry.**~~ **DONE (branch `mg/r6-r7-token-economy`, [src/read/shape.ts](../src/read/shape.ts)).** An item never states a fact its enclosing node already states: a project-view child drops `project`/`headingProject` + `area` (heading-group members additionally drop `heading`); an area-view child/project-card drops `area`; an `anytime`/`someday` section item drops `area`. **Mixed-provenance lists KEEP per-item refs** — search/today/upcoming/inbox/changes/projects. Deterministic BY VIEW KIND at the emit boundary (shared by CLI `--json` + MCP). Invariant verified: the dropped `area` is the EFFECTIVE area, and a project/heading child carries `area = NULL` in the DB, so the child's effective area resolves THROUGH its container to exactly the card's area — only ever a redundant fact is deleted. Rider that shipped with it: **bucket-implied lifecycle flags** — `logged`/`trashed` are dropped even when TRUE in the `trash`/`logbook` views and the card `logged`/`trashed` sections (the bucket states them), but SURVIVE (when true) on mixed surfaces so they still disambiguate. Teaching line landed in the contract glossary: **absence of a container field INSIDE a container view = inherited from the enclosing node**.
- ~~**R7 — named detail tiers, not a field-projection language.**~~ **DONE (same branch/module).** Two named tiers: **compact** (the default for every list context — `items`, `sections`, the collection arrays inside `view`) and **full** (`detail`/`show`, or a list forced with the `--full` flag / MCP `full: true` param). Compact = identity + structural facts + non-default facts: default-pruned `status`/`logged`/`trashed`, dropped `created`/`modified`, a ≤120-char first-line `notes` preview with a presence-keyed `notesTruncated` marker. `--full` restores per-row density but R6 still applies (ancestry redundancy is not tier-dependent). Two UNIVERSAL reshapes ride with the tiers (both tiers + `detail`): the flat `checklistItemsCount`/`openChecklistItemsCount` fold into one presence-keyed `checklist: {open, total}` (`.items` on detail), and the all-false `repeating` block is dropped (minimal truthful object when a template/instance). `--brief` (force compact on a detail) was NOT built — deferred; note it here if a caller ever needs it. Teaching line landed in the contract glossary: **compact absence = the default**.
- ~~**R8 — search treats headings as properties of projects and checklist items as properties of to-dos.**~~ **DONE 2026-07-29 (branch `mg/r8-search-provenance`).** The heading-only `matchedVia: {kind, title}` annotation is replaced by the converged **`match: {field, text}`** shape across all three arms (`src/read/views.ts` `searchView`): `field` ∈ `heading`|`notes`|`checklist`, `text` = the heading title / a ~80-char notes snippet centered on the hit / the matched checklist item's title. PRESENCE-KEYED — a title match carries NO annotation; precedence title (none) > heading > notes > checklist. The NEW checklist arm joins `TMChecklistItem`, surfacing the parent to-do (deduped — one to-do per match, credited to the first matching row by index); **checklist-item uuids appear on NO surface** (Mike's ruling — implementation detail like tag uuids; a regression test asserts none leak). Notes matches — previously unannotated — now carry `match: {field: "notes", …}`. The TTY render adds an indented muted `⤷ field: "…"` line under an annotated hit. Rides the compact tier. Docs: `contract.md` glossary `match`, `docs/design/contracts.md` (Search match provenance R8), api-doctrine §7. The earlier note about reconciling `matchedVia` is resolved by this PR.
- ~~**R9 — payload refinements.**~~ **DONE (branch `mg/r9-payload-refinements`, [src/read/shape.ts](../src/read/shape.ts)).** Five ratified wire/emission-layer changes riding the R6/R7 shaping boundary (DB mappers + internal types unchanged, minimal blast radius — the checklist-reshape pattern): (1) `tags`/`inheritedTags` fold from `{title}` objects to plain string-name arrays (`["errand","home"]`) on to-dos, projects, AND areas; (2) a project's flat `untrashedLeafActionsCount`/`openUntrashedLeafActionsCount` fold into a presence-keyed `todos: {open, total}` (same shape as `checklist`, omit when total 0) — the app-maintained leaf-action columns count leaf to-do children only (headings + checklist items excluded by construction, trashed excluded); (3) the compact `notes` preview + `notesTruncated` machinery is DELETED — compact drops the notes string for a presence-keyed `hasNotes: true`, full/detail keep the whole string; (4) `headingProject` is merged into the single `project` key (never a separate wire field); (5) the `heading` ref is dropped from the COMPACT tier everywhere (mixed lists included — the GUI shows the project, not the heading, outside a project view), full/detail keep it. Docs: `contract.md` glossary (`tags`/`todos`/`hasNotes` added, `notesTruncated` deleted), `docs/design/contracts.md` (R6/R7 + universal-reshapes + omit-empty table), api-doctrine §token-economy.
- ~~**R10 — stage taxonomy.**~~ **DONE (branch `mg/r10-stage-taxonomy`, [src/read/stage.ts](../src/read/stage.ts) + [src/read/shape.ts](../src/read/shape.ts)).** ONE derived per-item `stage` ∈ `inbox | upcoming | anytime | someday | logbook | trash` replaces the three wire fields `start` / `logged` / `trashed` (deleted, all tiers incl. detail). Derived by ONE pure function that REUSES the views' membership rules (trash > logbook > inbox > template/startDate-upcoming > someday > anytime; completed-not-yet-logged keeps its live stage). **[startDate→upcoming was CORRECTED by R10.2: only a STRICTLY-FUTURE `startDate` is `upcoming`; an arrived `startDate <= today` derives `anytime` + `today` marker.]** Today/evening are a SEPARATE presence-keyed axis (`today: true`/`evening: true`, evening ⇒ today) derived with the Today view's own two-arm predicate incl. the deadline-suppression guard (F-DL/UPC1, oddities §8d–8e): an undated deadline-today item = `anytime` + `today`. View cards + heading groups renamed/reshaped to the six words (`active`→`anytime`; `scheduled`+`repeating`→date-grouped `upcoming` with a trailing `{date:null}` resting-template group; `logged`→`logbook`; `trashed`→`trash`; heading groups `{heading, anytime, upcoming, someday}`); done at the WIRE (shape.ts) so the in-memory views + human render are untouched. `stage` dropped where a view/section states it, kept on search/changes/today/listings/card-node/detail; markers dropped inside the today sections, kept elsewhere. Tests: `test/unit/stage.test.ts` (exhaustive derivation matrix + GUI-verified marker corners + property-style view↔stage consistency). Docs: `contract.md` glossary (`stage`/`today`/`evening`; `start` deleted), `design/contracts.md` (R10 + view shapes + omit-empty), `design/heading-demotion-and-move.md` §9.
- ~~**R10.1 — stage-mixed catalogue correction + `todaySection` retirement.**~~ **DONE (branch `mg/r10-1-stage-followups`, [src/read/shape.ts](../src/read/shape.ts)).** Three ratified follow-ups to R10: (1) `stage` is KEPT on the flat `anytime` and `upcoming` catalogues — both were then believed stage-MIXED (Anytime carries arrived-dated `upcoming` rows; Upcoming carries deadline-forecast `anytime`/`someday` rows), so dropping it deleted non-redundant info; it is now dropped ONLY in the provably stage-pure `inbox`/`someday`/`logbook`/`trash` views + the card sub-buckets. **[CORRECTED by R10.2: the "Anytime carries arrived-dated `upcoming` rows" premise was WRONG — an arrived `startDate` derives `anytime`, so the Anytime catalogue is stage-PURE and now DROPS `stage`; only `upcoming` remains stage-mixed.]** The stage property test was tightened to strict equality (every emitted `stage` equals `deriveStage`, present exactly where the table says). (2) `todaySection` is RETIRED from the wire (redundant with the `evening` marker); it survives as an internal entity field for the render + write-verify paths. (3) The area-view block-count drift is documented honestly (below).
- ~~**R10.2 — arrived-dated items derive stage `anytime`; Upcoming is strictly future (UPC1).**~~ **DONE (branch `mg/r10-2-arrived-stage`, [src/read/stage.ts](../src/read/stage.ts) + [src/read/shape.ts](../src/read/shape.ts)).** Corrects the R10 derivation, which encoded "any `startDate` → `upcoming`" (a Today⊆Upcoming SUPERSET claim). The GUI corpus contradicts it: Upcoming membership is STRICTLY FUTURE (`COALESCE(startDate, deadline) > today`, UPC1, GUI-verified). `deriveStage` now splits the `startDate` branch on arrival, reading the presence-keyed `today` marker (its scheduled arm is exactly `startDate <= today`, computed with the response clock): a STRICTLY-FUTURE `startDate` (no marker) → `upcoming`; an ARRIVED `startDate <= today` (marker present) → `anytime` — a Today (star) + Anytime member, NOT Upcoming — covering active-arrived AND arrived someday-scheduled (`start=2`) rows. Repeating templates UNCHANGED (still `upcoming`). **Anytime is now provably stage-PURE** (every ANYTIME_SELF member derives `anytime`; templates excluded via `NOT_TEMPLATE`), so the `anytime` catalogue DROPS `stage` — reversing R10.1's "keep" for it. `upcoming` REMAINS stage-mixed (deadline-forecast `anytime`/`someday` rows) → keeps `stage`. Card/heading rebucketing falls out of rebucket-by-`deriveStage`: an arrived child rebuckets into the `anytime` bucket, and the `upcoming` date-groups now hold only future dates + the trailing `{date: null}` template group. The UPC1 §8d banner-materialized case (start=1, startDate := deadline = today) now derives `anytime` + `today` (was `upcoming`). Tests: `test/unit/stage.test.ts` (matrix arrived/overdue→anytime, future→upcoming, §8d pin; strict property test with `anytime` moved to the pure set + the R10.2 correction asserted load-bearing; card arrived-child rebucket), `test/unit/shape.test.ts` (anytime section drops stage). Docs: `contract.md` glossary (`stage`), `design/contracts.md` (R10 precedence + dropping), `design/heading-demotion-and-move.md` (R10 reshape note). Supersedes the SUPERSET wording in R10 + R10.1 above.
- ~~**R11 — `repeating` is template-only rule state; `instanceOf` on instances; `latestInstance` per SL1.**~~ **DONE (branch `mg/r11-template-instance-split`, [src/read/shape.ts](../src/read/shape.ts) + [src/read/detail.ts](../src/read/detail.ts) + [src/read/queries.ts](../src/read/queries.ts)).** The wire drops the `isTemplate`/`isInstance` discriminators — key PRESENCE carries the fact. A repeating TEMPLATE carries `repeating: {nextOccurrence, paused?, deadlined?}` (rule facts; plus `rule` on a detail) — presence of `repeating` MEANS template; `nextOccurrence` an EXPLICIT `null` when unprojected (the `area: null` precedent; omit-empty preserves it — the nested object is not a pruned entity); false booleans default-pruned. An INSTANCE carries a flat `instanceOf: <templateUuid>` and NO `repeating`. A plain row carries neither. A template DETAIL additionally carries a flat `latestInstance: <uuid>` — the GUI "Show Latest" pick, `max(creationDate)` among the template's instances, NO status filter (a completed newest-spawned instance is still latest — SL1 D1), blind to startDate/userMod/stopDate (SL1, [docs/lab/sl1-show-latest.md](lab/sl1-show-latest.md); `src/read/queries.ts` `latestInstanceUuid`); omitted when no instances, never on list/card rows. The in-memory `RepeatingInfo` KEEPS its flags (stage derivation, write hazards/verify, human render still read them); only the wire changes (minimal blast radius — R9/R10 precedent). Template-side CHILD template-membership marking (rsim-results P5's pending question) stays UNBUILT and queued — R11 only reshapes the template/instance rows themselves. Tests: `test/unit/shape.test.ts` (R11 split block), `test/cli/e2e.test.ts` (template/paused/instance/SL1-D1 through the full emit pipeline), `test/mcp/server.test.ts` (library-inheritance spot-check). Docs: `contract.md` glossary (`repeating`/`instanceOf`/`latestInstance`), `design/contracts.md` (reshape (5) + omit-empty tables + Booleans note).
- ~~**R12 — `when`, the derived TIME-AXIS position (replaces `startDate` + the `today`/`evening` markers).**~~ **DONE (branch `mg/r12-when-field`, [src/read/stage.ts](../src/read/stage.ts) `deriveWhen` + [src/read/shape.ts](../src/read/shape.ts)).** ONE derived, presence-keyed `when` ∈ `"today" | "evening" | <future ISO date>` carries an item's time position, complementary to `stage` — **doctrine: `stage` enumerates the sidebar BUCKETS, `when` enumerates the TIME POSITIONS (today | evening | a future date).** `deriveWhen` (beside `deriveStage`) REUSES the same `today`/`evening` markers the Today view derives — never re-derives membership — so `when` ∈ {today, evening} can never disagree with the star (property-tested). A FUTURE date is a strictly-future scheduled row (`startDate > today`) or a repeating TEMPLATE's projected next occurrence; absent = unscheduled + not in Today (an unprojected/paused template + every logged/trashed row carry none). Someday is deliberately NOT a `when` value (a bucket → `stage`); the write-side `--when someday` input is unchanged (input mirrors the app's When control, reads decompose onto the two axes). **Deleted from the wire:** the `today`/`evening` marker keys (all tiers), the raw `startDate` in COMPACT (FULL/DETAIL keep it beside `when` as the substrate — `startDate` = what is stored, `when` = where it sits), and a template's `repeating.nextOccurrence` (moved to `when`; a bare template now emits `repeating: {}`, which survives omit-empty). **Dropping (R6-consistent):** `when` drops inside the `today` sections and inside a card `upcoming` date-group keyed on it; KEPT on the flat `upcoming`/`anytime`/`inbox`/`someday`/search/changes surfaces (a deadline-pulled inbox/someday row reads `when: "today"` there while `stage` still reads inbox/someday — the informative cross-axis cases). In-memory entities + human render untouched (wire-only, minimal blast radius — R9/R10/R11 precedent). Tests: `test/unit/stage.test.ts` (exhaustive `deriveWhen` matrix + real-entity corners + the `when`⇔Today-membership property), `test/unit/shape.test.ts` (drop-table both directions + full-tier keeps `startDate`), `test/cli/e2e.test.ts` + `test/mcp/server.test.ts` (template-wire updates). Docs: `contract.md` glossary (`when`/`startDate` added, `today`/`evening` deleted, `repeating` updated), `design/contracts.md` (R12 section + item tables + view shapes + reshape (5) + omit-empty), CHANGELOG.
- ~~**R13 — provisional Today members + GUI-faithful someday/inbox/anytime membership for deadline-pulled rows (BANNER1/BANNER1b).**~~ **DONE (branch `mg/r13-provisional`, [src/read/views.ts](../src/read/views.ts) + [src/read/predicates.ts](../src/read/predicates.ts) + [src/read/stage.ts](../src/read/stage.ts) + [src/read/shape.ts](../src/read/shape.ts)).** Implements the GUI-verified banner laws ([docs/lab/banner1-research.md](lab/banner1-research.md), oddities §8s). (1) **`provisional: true`** — a presence-keyed marker on every Today member the GUI PIPS / counts in the "You have N new to-dos" banner: an autonomous entrant NOT yet materialized (`start != active OR startDate IS NULL`, BANNER1 L1; banner `N` = the provisional count, exactly). Reuses the SAME today/evening markers `when` does (never re-derived), on EVERY tier, NEVER dropped (the banner is not a section). Absent on a materialized member (user-placed `when=today` or a post-OK deadline-pull), a non-Today row, and a template. Read-only (the app clears it by mutating `start`/`startDate` on banner-OK — a GUI-only side effect that silently de-inboxes/de-somedays the row). (2) **Deadline pulls re-file to stage `anytime`; the `today` sections DROP `stage`** — `deriveStage` gains a step deriving `anytime` for a Today-marked undated row (BANNER1b L-A: the GUI files a pulled Inbox/Someday row under Anytime + Today at pull time), so EVERY Today member derives `anytime` and the today sections become stage-PURE (strict property-tested). (3) **GUI-faithful flat membership** — one shared `DEADLINE_PULLED` predicate (the exact Today deadline arm, reused verbatim): `someday`/`inbox` EXCLUDE pulled rows, `anytime` INCLUDES them (Anytime ⊇ Today's to-dos); a SUPPRESSED deadline is not a pull (stays in origin). Card someday buckets follow via rebucket-by-`deriveStage`. Ripple: `inbox --overdue`/`someday --overdue` now surface only a SUPPRESSED overdue capture (an unsuppressed one is pulled out); AND-composes with `--area`/`--tag`. Tests: `test/unit/views.test.ts` (membership both directions + filter compose), `test/unit/stage.test.ts` (provisional matrix + banner-count reconstruction + strict today-section purity property), `test/unit/shape.test.ts` (today-section stage drop), `test/mcp/server.test.ts` (spot-check), `test/cli/e2e.test.ts` (`--overdue` compose). Docs: `contract.md` glossary (`provisional` added; `stage` today-section note), `design/contracts.md` (R13 section + item tables + R10-dropping), CHANGELOG.
- ~~**Block-count ↔ stage-bucket reconciliation (queued, area/project cards).**~~ **RESOLVED for the area view by read-shape doctrine v2 PR 3.** The `meta.truncation.blocks[kind=area/projects]` sidecar is RETIRED for `area show` / `get_area`; each capped scope now carries its own INLINE `total` on the bucket record it describes — `children.anytime.total` for the `--area-limit` cap (the direct to-dos are all stage-`anytime`, since an arrived dated row derives `anytime` not `upcoming`, so the split-across-buckets ambiguity that motivated the honesty note no longer arises) and `projects.total` for the `--project-limit` cap. A consumer maps a hidden-count to the exact bucket it was hidden from directly. The project-view card is uncapped today (every `total` absent); the global inline-truncation sweep for any remaining grouped views is PR 5.
- ~~**Single-op idempotency design round (phase 1).**~~ **DONE 2026-07-29 (branch `mg/single-op-idempotency`).** Batch's `opId` machinery is generalized to single mutations: every single-invocation write command that runs the write pipeline takes `--op-id <key>` (MCP `op_id` on the single-mutation write tools), the shared charset/lookback machinery lifted into `src/write/opid.ts` (batch reuses it, not forked). The idempotency CHECK runs in the client's single-op entry (`run`): a match against a recent VERIFIED-OK record SKIPS execution and returns the command's normal success envelope with the ORIGINAL identity (`op`/`uuid`/`undoToken`/`title` from `requested.title`) + presence-keyed `alreadyApplied: true`; on no match the record stores the `opId` (reused batch recording). The variadic/compound verbs (multi-item `move`/`reorder`, granular checklist edits, clear-reminder, project make/add-repeating, archive/unarchive-heading, project reopen) REFUSE `--op-id` with a teaching usage error pointing at the batch per-line `op_id` (their idempotency is batch-shaped). Contract: `docs/contract.md` (idempotency promise + `opId`/`alreadyApplied` glossary), `docs/design/api-doctrine.md` §4, `docs/design/contracts.md` (single-op idempotency section). Sibling of the shipped batch idempotency (§6 above) and of the checklist ETag/If-Match state-token round.
- **Idempotency phase 2 — timeout reconciliation.** Phase 1 matches VERIFIED-OK records only. The open case is a resubmission whose ORIGINAL attempt is a `verify-failed:timeout` record (the write may or may not have landed — the expected delta never appeared before the deadline). Phase 2 reconciles this: on a resubmission carrying an `opId` that matches a `verify-failed:timeout` record, re-read the current state to decide whether the original change is now present (→ replay as already-applied) or genuinely absent (→ execute). Design the reconciliation read + the disposition it reports, for both the batch line and the single-op paths (they share the lookback). Sibling of the checklist ETag/If-Match state-token round.

## 7. Parked by Mike, 2026-08-05 (post-ratification follow-ups — do NOT start before the nine 0.14.0 migration PRs land)

1. ~~**One-vocabulary layer collapse (tech-debt audit).**~~ **DONE 2026-08-05 — all three batches shipped (Batches 1–2 PR #423; Batch 3 PR #424).** The ratified v2 read shapes + resolution-timestamp vocabulary are better abstractions than parts of the internal state model — Mike wants the internal entity/object schemas normalized to the SAME vocabulary at ingestion, cutting gratuitous projection layers ("sqlite → internal state → response formatting → CLI shaping"). Scope: audit every consumer key against its internal representation; classify same-name / renamed / computed-at-emission; migrate pure RENAMES inward to `src/model/entities.ts` (one name per concept, normalized at the DB boundary); keep genuinely emission-time concerns where they are (detail tiers compact/`--full`, ref round-trip promotion — depends on read-time namespace state, JSON serialization, TTY grouping projections) and keep write-substrate fields (`index`, `todayIndex`, `startBucket`, raw `status`/`start` enums, `startDate`) internal but clearly segregated. Deliverable: an audit table + rename PRs; success = `shape.ts` mostly drops/promotes attributes rather than translating vocabularies. Ruled worthwhile by Fable (maintainability win, low overcomplication risk) with the explicit caveat that layer DELETION (vs vocabulary unification) is not the goal — the TTY-projection layer and emission tiers earn their keep. **Audit LANDED (phase 1), execution pending — [docs/design/vocabulary-audit.md](design/vocabulary-audit.md):** the classification table + rename batches + substrate-segregation proposal. Headline: R1–R13 already unified nearly everything (DB→entity normalization complete, entity→wire divergences almost all legitimately computed-at-emission or SAME-NAME), so the rename surface is THIN — the ONE surviving `shape.ts` translation is the day-block `date`→`when` (mechanical, byte-identical wire), and the real structural win is segregating the seven entity-level internal fields (`start`/`logged`/`trashed`/`todaySection`/`today`/`evening`/`reminderLive`, intermixed on `TaskCommon`) into a nested `derived` bag + a wire-key-inventory lock test. The DB-row substrate (`index`/`todayIndex`/`startBucket`/raw enums) is ALREADY cleanly segregated in `TaskRow`. Adjacent finding (separate write-vocab ruling needed): the creation/completion-timestamp concept is spelled three ways (`createdAt`/`completedAt` on add, `creationDate`/`completionDate` on set-dates, `created`/`stopped` on reads). **EXECUTED — Batches 1–2 LANDED 2026-08-05 (PR #423):** R-1 (day-block `date`→`when`, `shape.ts` now translation-free) + R-1b (`IsoDateGroup<T>.date`→`when`) + the `derived` substrate bag (the seven entity fields nested off `TaskCommon`, `startDate` stays flat, `entityStage`/`entityWhen`/`entityProvisional` exported) + the wire-key-inventory lock test. `todaySection` was MOVED into `derived` (not deleted — the write-verify schedule delta depends on it). Wire byte-identical. **Batch 3 (R-3) SHIPPED 2026-08-05 (PR #424), maintainer-approved:** the `set-dates` op params `creationDate`/`completionDate` → `createdAt`/`completedAt`, unifying the write input surface on one spelling per concept (the add-path already used `createdAt`/`completedAt`). CLI flags (`--created-at`/`--completed-at`) and MCP params (`created_at`/`completed_at`) were already this spelling — wire byte-stable; reads keep `created`/`stopped`; DB column `creationDate` untouched. All three batches done — this item is CLOSED.

2. **Disruptiveness deployment profiles (configurable).** TIMEZ2 (#409) proved the pinned-zone quit/relaunch cycle works (dated-evening writes), and the ui-vector AX workflows are lab-certified — but app-restart and AX-driven mutations are untenable on a daily-driver workstation running Things alongside `things-api`, while perfectly acceptable on a dedicated headless Mac-mini automation host. Park: a configurable capability PROFILE (e.g. `workstation` vs `dedicated-host`) gating the disruptive mutation classes (app relaunch cycles, AX/ui-vector dialogs, anything that steals or kills the running app), with per-op capability docs stating which profile each op needs; refusals under the stricter profile name the profile knob. Documentation: the capability matrix / timezones doc gain a disruptiveness column keyed to the profiles.

3. ~~**Unswept-resolved reorder permit — LOGSORT.**~~ **LANDED (`mg/unswept-reorder-permit`).** The conditional permit is wired: `computeReorderPre` admits an UNSWEPT-resolved to-do movee (completed/canceled, `stopDate` ABOVE the live log boundary) as a member **iff** the compiled plan is the pure-native `index` wire — gated on `admitResolved && key === "index"`, passed true ONLY by the native `reorder` op's preRead (the sole caller of that wire; every bounce/move/day-axis orchestrator leaves it false). Swept movees stay refused (reactivate / `--completed-at`); an unswept movee on any uncertified protocol (bounce/move/`--in <date>` day-axis) is refused with a native-only-condition copy. Sweptness is derived at CALL time against `logBoundary` under the consumer zone (threaded through `preRead`). The LOGSORT byte-law is locked into the reorder `expectedDelta` via a `frozen` per-row assertion (status still closed + `stoppedDate` intact + `umd` unbumped — a reopen fails verify). Mixed open + unswept-resolved wires are permitted under the same condition; the O06 heading-child protection is unchanged and takes precedence. *Original item retained below.* — LOGSORT (#401) certifies that an UNSWEPT-resolved to-do (completed/canceled but still in its live container body, before the Logbook sweep boundary) re-ranks index-only via the native reorder verb — no reactivation needed. But PR D (#417) currently REFUSES unswept-resolved movees pending conditional native-only membership plumbing: the move/bounce fallback paths are uncertified for resolved rows (a fallback leg could reactivate or misfile them), so the engine can only safely serve them on a PURE-native index path. Wire the permit: allow an unswept-resolved movee when — and only when — the composed wire is pure-native index (no move/bounce fallback leg), so LOGSORT's certified path is reachable through `reorder` while the uncertified fallbacks stay refused with the existing reopen-first copy. (Refusal today lives in the resolved-movee guard, `src/write/{move,guards}.ts`; the permit is a conditional native-only membership relaxation, not a new op.)
