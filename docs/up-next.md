# Up-next queue — the short-horizon pick list

The working queue for the next session(s): **open items only**. An item is DELETED from this file the moment it lands — git is the archive; there are no strike-throughs, no DONE annotations, no "shipped-but-kept-for-context" tails. If you want to know what was already built, read `CHANGELOG.md` and the living docs. Settled *decisions* (what will NOT be built, which shape won) live in [design/decisions.md](design/decisions.md); long-horizon strategy lives in [roadmap.md](roadmap.md).

**One-home rule:** every open item lives in exactly ONE of {this file, roadmap.md}. This file holds the short-horizon queue; roadmap holds durable strategy. No item is restated across the two.

Sections are blocker-classes: **needs-Mike** (a decision, near-zero effort) · **VM-batchable** (autonomous lab campaigns) · **needs-human** (a short in-person sitting) · **calendar-pinned** (date-driven) · **small code** (unblocked, low effort) · **feature design** (wants an ideation round before code).

## POST-COMPACTION runway (Mike's rulings, 2026-08-28 — recorded pre-compaction, NOT yet acted on; execute in roughly this order)

**FIRST: the chord-reorder Q&A session.** Mike: the post-CHORD2 decision set "almost deserves its own session — I'd like to ask some pointed questions about the state of things after we probed its capabilities." The compact presentation (chord laws, shipped heading op, §8 replacement candidates, ORD-19, canary, recommendations on all four decisions) was DELIVERED 2026-08-28; awaiting his questions — the four chord decisions stay parked until that conversation concludes.

**NEEDS AN EXPLAINER before Mike can rule (explainers DELIVERED 2026-08-28 in the same walkthrough; awaiting his rulings):**
- **Deputy-side drive execution** (the #638 queue entry): "I need more context — I'm not sure what is being asked of me." Prepare a proper brief: the problem (client death strands GUI workflows; TORPH1 measured the un-reaped residue), the design (submit whole recipe to the launchd deputy, journaled, completes-or-cleans on client death, op-result as the polling surface), the costs (deputy version bump + reinstall ceremony, complexity), the relation to the agent-daemon phase plan.
- **Stateful display preferences**: he no longer remembers what it refers to — re-present the item (TTY toggle default family, `limitMode`/`pager`/`--plain`/`--pretty`, the config surface + precedence question; worked examples were delivered 2026-07-14).
- **`rt1_instanceCreationStartDate` wording**: show him the exact current sentences (the "app-materialized first occurrence" phrasing in src/write/promote-clone.ts + sibling docs) against the measured same-day law (the app materializes today's instance immediately and the column names the NEXT slot), plus the proposed replacement wording; he rules, then the sweep.
- **`certified-app-version` stamp**: explain what `things config set certified-app-version 3.23` does — it records the operator's sign-off on the 3.23 certification campaign and silences doctor's passive behavioral-drift notice (which fires while the running app version ≠ the certified one); note the M1 wrinkle (it runs 3.23.1 — decide whether the stamp should say 3.23.1 there or whether the notice is wanted).
- **The framebuffer/HID rig sitting**: what's actually needed from him — one in-person sitting at a real-display Mac (his own or a rigged one) to run the cells headless clones cannot: real pointer DRAGS (AXDRAG rung-2), the SESSGATE wrong-Space relocation + AX-blind-sheet recovery, long-title ellipsization cells, the promote-drive audible beep on real hardware, and (new this week) real-hardware certification of the GUI recipes generally — the 0.19.2 regression was invisible headless. Deliverable to prepare: a single scripted sitting he can execute in under an hour.
- **The free-Apple-ID amfid cell**: what it is — verifying that a FREE Apple Development certificate (any Apple ID, no paid membership) satisfies amfid for the SANDBOXED reader, which is the load-bearing assumption of the self-builder path (a stranger building the helpers without Mike's Developer ID). Needs him to sign into a throwaway/his Apple ID interactively inside a VM (Xcode ▸ Accounts) — credentials never touch the repo; the probe afterward is automated.

**PARKED by ruling:** the NL-adjacent `when=` curiosity → someday/maybe, revisit only if it converts a GUI-driven op to URL-driven or unlocks a real ability; MCP exposure of `rescue dismiss`/`relaunch` → later; golden-v5 mint → SKIPPED ("no reason to think the data model or API surface changed meaningfully between 3.23 and 3.23.1"; more point releases will come).


## RULED 2026-09-03 — staged pre-compaction, NOT yet acted on (execute after the #699 campaign lands)

- **#704 — raw `startDate` leaves default reads; detail tier may carry a clearly-labeled WEAK SIGNAL only (Mike's ruling).** `when` is the scheduling contract. Raw `startDate` invites the false inference "deferred since D" — STEV1's today-bounce rewrites it mechanically on a reorder. Ruling: keep something on the DETAIL tier only if its name makes over-reading impossible; otherwise omit ("if it came between a confusing output and no output, I would choose no output"). Orchestrator's recommendation, awaiting Mike's pick: replace raw `startDate` (detail tier, arrived rows in Today only) with a lower-bound field such as `inTodaySinceAtLeast: <date>`, documented as "in Today at least since this date; reorders can move it forward, never an exact tenure"; future-scheduled rows need nothing (`when` carries the date). Contract docs + read-shape doctrine + MCP parity in the same change.
- **#705 — successful mutation output gets concise (Mike's ruling: "definitely way too verbose still").** Default success output = the requested semantic change + the user-visible resulting state (+ undo token). Implementation narration moves to `--verbose`/trace: first casualty is STEV1's "row(s) whose day had already passed are now dated today — re-enters through This Evening" warning (a mechanical `startDate` rewrite on an already-arrived row changes nothing user-visible); sweep the rest with the #705 rule (a detail stays in default output only if the result differs materially from the request, the caller must act, a guarantee weakened, or additional public-domain state changed). Ride the #635 disclosure registry tiers.

## needs-Mike — decisions blocked on him, near-zero effort

- **Commission (or not) the raw-AX re-implementation of the Repeat drive (#687's ruling request).** After the observer rewrite, transport is nearly all of what remains: ~120 System Events round-trips ≈ 5.6 s on the M1 by itself; VOPAT1's ≈2.2 s figure assumed reads had left System Events entirely. Reaching it means re-implementing every certified address (HXPC1 label rows, the CGRD1 audit, focus guards, every refusal sentence) against raw AX — a second implementation to maintain. Decide AFTER the DEFAULTS2 minimal recipe lands and the M1 is re-measured: if the residual round-trip count falls with the actuation count, the rewrite may be unnecessary. Companion call: settle semantics — SOFT (a missed notification falls through to the step's certified gate; shipped) vs HARD-refusing (built, unwired). Recommendation: keep soft.

- **The two routed-latency levers DEPOBS2 left on the table (2026-09-03) — pick one, or neither.** Deputy-STEPPED scripts are ruled out on measurement: the deputy spawns a child `osascript` per script, so a step boundary costs a spawn (26.8 ms measured on the workstation, `S_field` ≈ 124 ms fitted on the M1) to remove waits worth 5–150 ms that sit inside retry loops — modelled **+248 ms** on the field's own `make-repeating` ([lab/depobs2-deputy-steps.md](lab/depobs2-deputy-steps.md)). What that leaves. **(A) Node-side settles at the three remaining CROSS-HOP boundaries** — the sheet appearing, the menu closing on an unchanged value (whose absence the next hop pays for in a swallowed click), the cadence group rebuilding. Node already marks every step and already owns `observerAwait` against the deputy-hosted ledger, so this needs no verb and no hop; what it needs is a RULING that a generator may emit its non-polling form when NODE did the waiting, i.e. a third injector state separating *the script may talk to a socket* (false on a routed host, permanently) from *the script must still poll* (false when node waited). Modelled 0.8–1.4 s of a 6.9 s routed drive; it changes routed script shapes, so it is a DEFAULTS3 quadrant campaign and must not resurrect #700. **(B) In-process script execution in the deputy** — `NSAppleScript`/OSAKit instead of a child process, worth the whole spawn term (13 × 124 ms ≈ 1.6 s) and the thing that would make stepping viable after all. It trades away the deadline-owned child, the fixed-argv shape and crash isolation, and brings AppleScript's thread/run-loop requirements into a process whose main thread sits in `dispatchMain()`. A helpers-architecture posture ruling, adjacent to the raw-AX commission above (which attacks the larger 4.1 s Apple-event term).

- **Should `make-repeating` on a to-do that ALREADY carries a deadline deadline the series? (DEFAULTS2 §6, 2026-09-02).** Today it lands a deadlined series with a BACK-SHIFTED first occurrence — exit 3, byte-identical with the pre-fill path on or off, so pre-existing rather than new. Either the promote should strip/ignore the source deadline (the series' deadline comes only from `--deadline`), or it should honor it as the rule's deadline offset and compute the first occurrence accordingly; a ruling, then a small fix. Also gated behind a ruling: the −62 % deadline-ON-the-seed shaping option (needs the DBLSPAWN1 matrix re-run through the CLI; the shipped recipe seeds ON the due date, deadline-free).

- **`doctor --ui-state`: keep or fold?** #644 deleted the top-level `ui-state` command per the ruling but kept the `doctor --ui-state` FLAG (a flag, not a namespace concern; `src/ui-state.ts` stays as its library surface). Keep the flag, or fold that render away too.

- **Stateful display preferences.** The growing TTY toggle family (`--show-later`, `--show-logged`, `--until`, `--all`, the limit-knob family `--limit`/`--area-limit`/`--project-limit`) wants user-configurable defaults, plus three deferred knobs: `limitMode fixed|fit|none` (height-fit deliberately NOT default — determinism beats cleverness), `pager auto|never` (git-style `less -FRX`), and a global `--plain`/`--pretty` chrome override pair (`--plain` = disable the whole TTY chrome stack in one flag: ANSI + width-fitting + gutter + preamble, i.e. the flag form of `NO_COLOR=1 THINGS_WIDTH=0`; `--pretty` = force chrome into a pipe; `--json` ignores all of it so agents stay unconfused). Examples were delivered to Mike 2026-07-14; awaiting his ruling on the config surface + precedence. A config-file surface now EXISTS (`src/config.ts`, `things config set`, `~/.../config.json`) but carries policy keys only — the decision has a natural home waiting for it. Wary of plist-driven defaults confusing agents whose output changes without a flag changing. *(Consolidated home for what was triplicated across roadmap §H + up-next §1/§5.)*

- **CHORD REORDER — the post-CHORD2 decision set (probe DONE [lab/chord2-reorder-laws.md](lab/chord2-reorder-laws.md); heading op SHIPPED #606/#608).** The ⌘-arrow chords are measured end to end: fully BACKGROUNDED (zero disruption events, twice-reproduced), block-multi-select law (non-contiguous coalesces; only unselected rows renumber), boundary laws (to-dos cross buckets silently with a umd stamp; headings decline), per-view column map (`index` vs `todayIndex`; Upcoming reorders, never reschedules; filtered views are VIEW-RELATIVE), templates immovable in project lists (§9e holds) but MOVABLE in Upcoming day-groups (**ORD-19 REOPENS**). Awaiting Mike, in rough value order: (1) **the to-do chord op** — build within-container to-do reorder on the chord vector? Shape question: hard fence at bucket edges, with the measured crossings (deport-to-root / adopt-by-heading) exposed only as named operations; the mixed-selection refusal and the view-relative filtered-view hazard are the other guards. (2) **the bounce-replacement program** — CHORD2 §8 recommends replacing the `area-someday` + `anytime` bounces (clearest wins), `today`/`evening` (also removes the evening leg's reminderTime stripping), `day` within-day order, and within-heading child order behind the fence; the front/back-insert landing taxonomy stops applying entirely. Migrate which, in what order? (3) **ORD-19 build** — one cell first (does a chord-set rank survive the next spawn?), then the day-group ordering op. (4) **the reorder canary's fate** — with heading order + (potentially) day-groups restored on chords, does [design/reorder-canary.md](design/reorder-canary.md) still earn its build, and its three ratification points.

- **Deputy-side drive execution — the completion guarantee for GUI-driven and bulk operations (2026-08-28, from the maintainer's timeout-orphan analysis).** Today a killed client (harness tool-timeout → SIGTERM/SIGKILL) strands a GUI workflow at an osascript hop boundary: dialog open (gating sync per oddities §24), no cleanup, no record — the deputy bounds the orphan to ONE hop but orchestration is client-side. The clean fix: the client SUBMITS the whole recipe/composite to the launchd-supervised deputy, which executes it journaled and, on client death, runs it to completion-or-clean-abort autonomously (FGRD2's proven Cancel as the epilogue); `op-result`/intent records become the polling surface, enabling a submit-and-poll async mode for long ops. Also collapses per-hop deputy IPC into one submission (the field-latency adder #636 could not reach). This is a deputy VERSION bump + the natural next phase of [design/agent-daemon.md](design/agent-daemon.md) (whose phase plan already awaits ratification) — decide the phase ordering and scope, then implement. The intent-record + lookback-race work (in flight 2026-08-28) is the prerequisite layer and lands independently.

## VM-batchable — autonomous lab campaigns

*(Remaining probes ride their parent feature items — the SYNCLAT on-hardware repro is needs-human, and the appliance-mode MASVM1 licensing probe lives in [roadmap.md](roadmap.md). The macOS 27 regression VM is calendar-pinned below. New probes get their own entry here when they're batchable and unblocked.)*


- **`area.reorder` after VOPAT2 PR 2 — the read is fixed, the CENSUS COUNT is what is left, and the M1 number is Mike's to take.** PR 2 shipped and is certified on both arms ([lab/vopat2-pr2-sparse-reads.md](lab/vopat2-pr2-sparse-reads.md)): the census realizes **16 rows instead of 190** and makes **373 round-trips instead of 888**, with the consumer output IDENTICAL in four sidebar states, the full sweep retained as the mismatch oracle (it fired twice in real drives and both moves landed), and every settle observed on the DEPUTY-ROUTED arm as well as the direct one. Measured A/B on one command: **2,664 round-trips / 570 rows realized → 1,177 / 54**, i.e. **6.2 s of field reads against 65.5 s**. Three things remain, in order. **(a) The maintainer's own measurement — SHIPPED and waiting on him.** The drive is published in **v0.20.10** (#726; gate GREEN both arms, Stage 5 routed guest 33 cells / 0 failures, including `--first`, `--last`, a mid-list `--before`, a move that needed the SBCOL1 fold and restored it, and the `SPARSE=0` A/B landing a byte-identical order) and the ask is filed on [#676](https://github.com/mikegreiling/things-api/issues/676) with the exact commands and the one-line trace extractor. `experimental-area-reorder` stays off until a one-wall move is timed on the M1 with helpers 1.4.0 installed; the drive now emits a `sidebar-move-cost` trace record (censuses sparse/sweep, escalations, round-trips, rows realized, gestures, settles observed/missed/timer) so the run reports its own cost rather than needing a model. Predicted from the lab counts at the field's rates: **~7.4 s for the common one-wall move, ~48 s for the worst multi-hop cell.** **#729 (2026-09-04) is folded into the same ask:** the to-last drop was landing second-to-last on his M1, the drop point is now closed-loop on the app's own landing slot (DRPLC1, certified direct + routed on 3.23 AND 3.23.3), and the drive emits a `sidebar-drop-target` record per gesture — so the re-run that takes the timing also reports whether the aim needed steering and by how much. The issue stays OPEN until he confirms on the machine that hit it. **(b) Carry the map through a FOLD.** The census that CONFIRMS a fold measures the row delta, so the ordinals below it could be shifted arithmetically instead of re-derived; today a fold invalidates the map and the next census pays the section-start read. Worth ~40 % of the remaining reads on the multi-hop cells, and it is the largest single lever left. **(c) Collapse-all is RULED OUT** (decisions.md, 2026-09-04) — re-priced at the field constants rather than re-argued: ~30 s modelled, because 31 actuations × ~640 ms of REPX1-certified in-script press/release/MOVED timers is ~20 s that no notification can remove. Do not re-propose it without new numbers for the actuation term.

- **The ~3 s the models cannot account for.** RDLAT2 fitted the M1 at 7.6 s, #687 predicts 7.5–8.0 s, the M1 measured 10.5 s on 0.20.6. The per-hop trace (durationMs + axOps + axElems, v0.20.6+) from the maintainer's next run is the instrument; the parity cell below is the fallback.

- **The 3.23.x parity clone is BUILT, and version is not the variable (DRPLC1, 2026-09-04).** The cell SBSCR1 deferred — an in-place upgrade of a scratch clone to the build the field host runs — now exists as a routine step of [`research-drplc1.sh`](../lab/scripts/research-drplc1.sh) (`--upgrade`: quit, `ditto` the banked installer over `/Applications/Things3.app`, warm, and prove the app still WRITES before believing anything, because the trial wall is silent). Both the direct and routed sittings ran every cell twice, once on 3.23 and once on **3.23.3**. Two things came out of it. **The app behaves identically on both builds** for everything the sidebar drive touches — the mid-drag layout law, the landing-gap size, the hit-test frame, the fold rung, the scroll unpin — so a field/lab difference is NOT a version difference, and #729's cause is a geometry margin rather than a 3.23.3 change ([lab/drplc1-drop-slot.md](lab/drplc1-drop-slot.md)). **And the helper grants survive an app upgrade inside a clone** — all four legs still `granted` with the deputy running 1.4.0 after `/Applications/Things3.app` was replaced — so a routed arm can be re-pointed at a new Things build without re-baking a golden. What is still NOT measured is the thing SBSCR1 actually wanted: the **15× field-vs-lab snapshot-cost gap**, which is a hardware/rendering term and cannot be produced in a clone at all. If the maintainer's next `sidebar-snapshot` trace records do not settle it, the instrument is his host, not another VM. See [lab/sbscr1-sidebar-scroll.md](lab/sbscr1-sidebar-scroll.md) §Open.

- **§9bb's ⌘⌫ artifact and the §8k crash-widening are the two lines the Cultured Code report gains from ODDS1** — no code change, just make sure they survive into the eventual submission: (a) `update-project?…&when=anytime|someday` on a repeating project template, recorded by UIC4 as a silent no-op on 3.22.11, now **kills the app** on 3.23 (a guard that got worse between builds is worth naming separately from §1); (b) the delete-confirmation half-fix — 3.23 added the repeating-template sheet §9bb asked for while a project full of open children still trashes with no prompt at all. Both are already appended to their entries; this item exists so the report drafter does not have to re-derive which changes are new. **REANCH1 (2026-08-23) adds three more lines and REWRITES one:** (c) [§1](things-app-oddities.md)'s crash boundary is FUTURE-vs-NOT, not bucket-vs-date — an ISO date equal to today is as fatal as the word `today`, so the same literal string is a safe write on one day and a process kill on another; and the whole dated branch is NEW in 3.23 (all four dated spellings kill 3.22.14), so §1's original blanket claim was correct for that line and this is an unannounced change in the *helpful* direction, which pairs interestingly with (a) above; (d) new [§15](things-app-oddities.md) — the same dated spelling still kills the app on an AFTER-COMPLETION template, i.e. the guard 3.23 added has nothing to test on a rule with no calendar anchor; (e) new [§16](things-app-oddities.md), the most user-facing of the set — re-anchoring a MULTI-weekday rule silently rewrites which weekdays the series fires on (`{Mon, Wed, Fri}` → `{Wed, Thu, Fri}`), and the GUI's own `Update Rule` button very likely shares the code path.

- **Things 3.23 certification — the last step, and the residue.** The campaign is otherwise DONE: golden-v4 minted and CERTIFIED, `lab:regress` GREEN end to end (all eight suites + the 132-step write-layer e2e), the assumption register walked row by row, the simulator/bench chain re-modelled to `databaseVersion` 27 with SIMFID re-certified host-side AND against a fresh golden-v4 clone (30 MATCH · 5 TOLERATED · 0 DIVERGENT). Evidence: [lab/gv4-323-certification.md](lab/gv4-323-certification.md) (+ [gv4-323-campaign.md](lab/gv4-323-campaign.md), [rdlg2-323-recipe-cert.md](lab/rdlg2-323-recipe-cert.md)).
  - **The one remaining step is Mike's, on the live host: `things config set certified-app-version 3.23`.** It is deliberately NOT run from a lab session — it stamps the operator's own config and silences doctor's passive behavioral-drift notice, so it waits on his review of the certification. The standing residue that makes the stamp honest-but-imperfect (the same shape 3.22.14 shipped with) is enumerated in the register's 3.23 audit block, item 3.
  - **Two ordering reaches have NO surface on 3.23 and now refuse** — heading order (`project move-heading`) and any day-group holding a repeating template (ORD-19). Both are app-side losses with no fallback protocol, not gaps in our code; they re-open the day Cultured Code fixes the private reorder command, which the o-suite will report (below).

- **The BEHAVIORAL canary for the private reorder command — design DRAFTED, awaiting Mike's ratification, then implement.** The full design (layered verdicts: declaration canary → measured-build seed table → cached runtime verdict → attempt-and-memoize on the first native-eligible write; positive-proof-to-enable fail direction; optional opt-in scratch-container doctor probe) is in [design/reorder-canary.md](design/reorder-canary.md) (2026-08-23). Three decision points for Mike are listed at its end; on ratification, implementation replaces the open ≥3.23 `privateReorderIsNoOp` range with the seed table + cache, and the gate lifts itself on the first proven success. The o-suite's ≥3.23 `deltaEmpty` rows remain the LAB-side canary that feeds the seed table.

- **3.23 repeat-instance follow-ups opened by REPX1** ([lab/repx1-instance-semantics.md](lab/repx1-instance-semantics.md) §7). (1) **`Resume` after a MULTI-DAY pause** — REPX1 §5.3 could not discriminate "restore the pre-pause cursor" from RD-15's "re-anchor to today+1" because at the probe's clock both answers were the same date; a pause held across several days separates them, and RD-15's re-anchor claim rests on a golden-v2 measurement. (2) **`Stop`'s two unmeasured halves** — the SINGULAR confirmation copy (only the plural `Stop Them` sheet was driven) and `Stop` on a repeating PROJECT (UIC2-c / oddities §7 C5 records a 3.22.11 crash when the demoted project is then selected; unre-probed on 3.23). Both are preconditions for ever reviving `todo.stop-repeat`, whose DROP rationale REPX1 falsified — see the capability matrix. (3) **oddity §9ff via the OTHER source-preserve trigger** — a terminal checklist element (SRCFATE SF-Tck). REPX1 §3.3 already answers the question this cell was carrying (the double-spawn reconciliation is NOT fixed on 3.23 — it is reachable from a bare instance re-date, [oddities §13](things-app-oddities.md)), so this drops from load-bearing to completeness: it would only establish whether the *preserve* path specifically still forms.

- **SIMFID coverage gaps opened by the v27 re-model** ([lab/simfid-results.md](lab/simfid-results.md)). (1) The appliers never maintain `untrashedLeafActionsCount`/`openUntrashedLeafActionsCount` on PROJECT rows (a minted instance project with N open leaves carries 0) — pre-existing and generation-independent, invisible today only because those goldens derive from the sim delta; it needs an APP measurement of the number Things actually stamps before it can be modelled, because a guessed value in the golden is worse than an honest gap. (2) SIMFID's recurrence/subtree half still certifies against ≤3.22-captured evidence (`rsim-evidence`) — the family is tier-3 ui-vector and the headless guest driver deliberately does not attempt it; an AX-capable clone driver is what would upgrade it, and it is also what would settle whether a freshly minted 3.23 instance still carries the `69760` junk sentinel (the retirement condition for the `instance-next-sentinel` tolerance). (3) `Items ▸ Repeat ▸ Create Next Copy` (RDLG2 §5.2 — materializes the pending instance, advances the cursor, bumps `icCount`) is fully specified but is NOT a covered write op; if a spawn applier ever lands, add it alongside the RSIM-S spawn case, with RDLG2 §5.3's negative (early completion leaves the template byte-unchanged) as an assertion.

- **SESSGATE wrong-Space relocation — END-TO-END VM certification (rides the next AX-vector run).** The session-reachability gate SHIPPED (2026-08-16, `session-reachability.ts`) and is lab-certified for the LOCKED-refusal branch (golden-v3: `1 0 0` → `blocked` exit 4, zero mutation) and the discriminator + recovery maneuver (`0 0 1` → `1 2 3`). Two residuals need a live app: (a) the **wrong-Space relocation branch** end-to-end — a genuine another-Space window (Things AX-0, others visible) driven through `make-repeating`, confirming the in-drive `close window 1`+`reopen`+`activate` relocates it and the drive then completes with the relocation warning; macOS exposes no scriptable Spaces API, so this needs a human sitting or a multi-display/Spaces rig (unit-level equivalence covers it for now). (b) the **AX-BLIND-sheet `close window 1`/`reopen` RECOVERY** — VMQ1 ([lab/vmq1-probe-closeout.md](lab/vmq1-probe-closeout.md) §5) CLOSED the AS-mutation-BLOCK half live in-lab (a Repeat sheet opened via the shell-`open` reveal blocks an AS `delete`, `trashed=0`, lock-independent), but the RECOVERY half did NOT reproduce: locking a session with a sheet already open keeps the sheet AX-REACHABLE (`1 1`, not the live-host `1 0 0`), so the AX-blind-sheet state can't be established headlessly, and `close window 1`+`reopen` left the delete still blocked (the close appears swallowed by the attached modal). Needs a real multi-Space/blind-window rig — same rig as (a). Also flag for the maintainer: does `close window 1` genuinely take down an ATTACHED modal sheet, or did the live-host recovery rely on the sheet sitting on a separate/blind window? Evidence: [lab/sessgate-session-reachability.md](lab/sessgate-session-reachability.md), [lab/vmq1-probe-closeout.md](lab/vmq1-probe-closeout.md) §5.

- **APDP2 — the dead-responsible-pid cell (optional; opened by APDP1 + the on-host tmux provenance check, 2026-08-25).** macOS responsibility is assigned at spawn and survives daemonization AND the responsible app quitting (measured on-host, prompt-free, via the kernel's responsibility API: a tmux server re-parented to launchd and every pane under it stay attributed to the terminal-app instance that first spawned the server — attaching from another terminal never re-keys it). UNMEASURED: what TCC does when a process whose responsible pid is DEAD provokes the app-data dialog (tmux outliving its terminal) — attribute the row to the requester, refuse, or resolve the stale identity? One clone cell: daemonize an intermediary from a terminal app, quit the app, provoke from a descendant, read the dialog owner + the TCC row. Affects only session-grant edge copy (the tmux caveat #588 added); not release-relevant.

- **The fixed-rule promote drive raises exactly ONE audible alert beep (CERTSWEEP1 2026-08-25, 3/3 across `project make-repeating`/`project add-repeating`/`todo add-repeating`; after-completion promotes are silent 2/2).** This closes the non-evidence gap BEEP1 §6.1 named and is a NEW beep source — BEEP1's ⌘A fix does not cover it. User-audible on the live host: the drive succeeds and the user hears an error tone. Bisect with the BEEPSEN1 sentinel per-step marks over the promote recipe's step list; also re-check CERTSWEEP1's paused-AC submenu observation while in the dialog (it read `… · Pause · Stop` where CNCAC1 §8 read `… · Resume · Stop` — state-dependence or drift?).

- **GUI text-addressed selectors vs TRUNCATED/long titles — long-title cells for the TWO remaining surfaces (Mike 2026-08-20; narrowed by HXPC1 2026-08-25).** The content-table `select-row` primitive reads back full model titles via the AS dictionary (safe); heading promote/demote selection is positional. The **Move… picker sub-item is CLOSED** — #589 replaced the blind Return with an exact-description row CLICK (the `New Project “…”` row is unreachable by construction — its description carries curly quotes) plus a `dest-not-open` pre-state refusal, HXPC1-certified in-guest including the exact-prefix-collision arm and the measured stray-project hazard (a completed/canceled uuid-ref destination used to mint `New Project`). Two surfaces still match AX-exposed text against the DB title and are UNVERIFIED for long/ellipsized titles: (1) **area drag-reorder** (`ui-drag.ts` `rowMatchesTitle`/`findAreaRow` — exact segment match on the harvested `AXValue`/`AXDescription`/`AXTitle`); (2) the **heading `…` button** description match (`"More. <title>"` exact `is`, U+200E prefix stripped — #589 fixed its RESOLUTION path (`whose` never searched below direct children; it now addresses `cell of row`, HXPC1-certified headless), so only the truncation question remains). AppKit normally reports the full `stringValue` as `AXValue` even when the cell draws ellipsized, so both are PROBABLY safe — but Things' sidebar/heading rows are custom views: measure, don't assume. VM cells: an area title and a heading title long enough to ellipsize in a NARROW sidebar/window (≥120 chars; one with emoji + a trailing `.`), then (a) dump row AX text vs DB title, (b) run `area reorder` / `project move-heading-to-project` / dissolve end-to-end; land the long-title cells as permanent regression coverage so a future Things release that changes AX exposure fails loudly.

- **The after-completion anchor is not in the depended-column manifest, and its SYNC behavior is unmeasured — plus the composite-sync cell (folded here from the retired CNC-residue entry).** `rt1_afterCompletionReferenceDate` is what an after-completion series' schedule derives from (CNCAC1 §1.1/§10) and the shipped composites move it on every `complete`/`cancel`; it is absent from `src/db/schema.ts`'s manifest and no two-device cell has exercised it (SYNCX1 covered the fixed-rule exception only). ALSO riding the same durable-account sitting: the CNC composite's sync behavior is inferred, not measured — it differs from the SYNCX1-certified chooser by one stamped `userModificationDate` on the minted row, which SYNCX1 measured to be the WINNING merge side, so the expectation is strictly safer than what was certified; one two-device cell confirms it. Both wait for the next durable-account attach, not before. RIDE-ALONG for the same sitting (2026-08-27): certify oddities §24 — an open Repeat sheet gates Things Cloud sync (field-measured A/B on 3.23.1; two-device confirmation + the exact release trigger). SECOND RIDE-ALONG (UMDZ1 §8, 2026-08-28): undo of a `--preserve-modified` write rewinds `umd` to a LOWER value — the one direction SYNCX1's `umd`-keyed arbitration says LOSES the merge — so the undo may not be timeline-silent on a second device; one two-device cell measures it.

## needs-human — short in-person sittings

- **Free-Apple-ID amfid probe cell — PARKED on Mike's credentials (the last open cell of the helpers distribution batch).** Confirm a FREE **Apple Development** certificate (any Apple ID, no paid membership) satisfies amfid for the sandboxed reader — the self-builder path's load-bearing assumption, asserted from cert-class reasoning and never measured. Not automatable: minting one requires an interactive Apple ID sign-in (Xcode ▸ Settings ▸ Accounts) inside the VM, and no credential may enter this repo (record anything ephemeral only in gitignored `lab/artifacts/`). Mike does the sign-in; the probe is then a `scripts/build-helpers.sh` run in the guest plus a reader handshake — a deputy-only bundle means amfid refused. The batch's other half shipped: release-time signing + notarization and the prebuilt bundle in the tarball ([design/release-signing.md](design/release-signing.md)); the notarization leg's first proof is the first tagged release.

- **The framebuffer / HID rig — three 3.23 cells are blocked on it, and it is the same rig SESSGATE and the long-title audit want.** A headless clone provides no real framebuffer, so a synthetic HID drag and a genuinely-blind window are both unreachable. **Scope correction (REPX1, 2026-08-22): synthetic CLICKS are NOT blocked.** `CGEventPost` at an AX-resolved frame actuates Things' custom-drawn content rows inside an ordinary headless clone under the AXVM1 grant — REPX1 used it to check off an Upcoming projection row and to build a three-row shift-click multi-selection — and it also established that `AXPress` on those same elements is DECORATIVE (`AXError=0`, zero DB delta, proven against two controls), which is why several past "AX press did nothing" negatives should be re-read as vector artifacts rather than app behavior. Only DRAGS remain genuinely rig-blocked. **Second scope correction (REPX2, 2026-08-22): the Make Exception / Update Rule chooser is CLOSED and never needed the rig** — it was a wrong-target problem, not a vector problem. Its trigger is a scheduling edit on the series' PROJECTION row (all prior vectors aimed at a materialized instance); menu + keystroke + a CGEvent row-click reach it, both branches drive, and the semantics are measured ([lab/repx2-exception-chooser.md](lab/repx2-exception-chooser.md) §1–§2). Blocked on the rig: (1) **a repeating PROJECT under 3.23** — `projectMakeRepeatingRecipe` drives the same dialog through a different reveal (the repeat-bar popover, HID-clicked), so the 3.23 dialog findings carry but the REVEAL is uncertified (§7.3); (2) the SESSGATE wrong-Space + AX-blind-sheet residuals and the long-title AX-truncation cells listed below. Worth one sitting that clears all of them rather than four separate ones.

- **Cultured Code report — submission.** [docs/things-app-oddities.md](things-app-oddities.md) is effectively the draft (crashes §1/§6/§7, silent-failure families §2/§5, the double-trash black hole §6½, the completed-project black hole §6¾; the AX-era evidence — stop-then-select crash C5, sidebar AX-mirror blanking, context-dependent filter-bar tag inheritance, the Stop-confirm "to-do" copy for projects, the AX-invisibility gaps). (The reminder-picker "write-inertness" was retracted — ANCH2 proved it drivable, oddities §8l.) **Timing is ruled: held until v1.0** (Mike, keep-flagged-but-deferred). This is the single home for the CC-report item (the former §1/§3 duplicate pair is folded here). Sub-line, optional: capture the `.ips` for the heading-schedule crash (oddities §6) to round out the package — ODDS1 banked a fresh one on 3.23.
  - **The FRESHNESS SOURCE is [lab/odds1-323-revalidation.md](lab/odds1-323-revalidation.md) (ODDS1, 2026-08-22).** Every entry now carries a dated 3.23 verdict, and the ledger is the index to read before drafting: which entries still reproduce, which four MOVED (§4b focus steal gone · §9bb template-delete confirmation added · §8g Stop menu-parity fixed · §8j `AXEnhancedUserInterface`), which nine are **MOOT rather than fixed** because the private reorder verb went inert (§12 — report those as "true as of 3.22.14", never as live 3.23 defects), and which 27 rows are honestly NOT re-validated and must keep their original version stamp. Re-run the ODDS1 batches (`lab/scripts/research-odds1{a..f}.sh`) after any future Things update before submitting.

## calendar-pinned — date-driven

- **macOS 27 regression VM — RE-SCOPED by Mike (2026-08-28): WAIT for macOS 27 GA + the Things release that targets it.** His reasoning: Cultured Code ships Things releases exploiting new OS features on day-one of macOS releases, so certifying Things 3.23 against a 27 BETA tests a version combination that will never exist in the wild. When 27 GA ships and Things follows, run the update runbook against that pairing instead.
- **iOS 27 GA runbook — ~Sept 14 2026.** Execute [docs/lab/things-update-runbook.md](lab/things-update-runbook.md) + the Apple-Intelligence memo follow-ups ([docs/design/apple-intelligence-research.md](design/apple-intelligence-research.md)) when Things likely ships its App Intents 2.0 release. *(Apple-Intelligence / macOS-27 strategic notes home HERE, not in roadmap — one-home.)*

## small code — unblocked, low effort

- **PTRGD1's identity leg compares GEOMETRY only, and the sidebar's grid defeats it (measured 2026-09-03, the §8 re-run).** Every sidebar row is `x = 40, w = 240, h = 24` on a 40 pt pitch, so the guard's frame-equality check against the planned row degenerates to a Y comparison with a 2 pt tolerance. Cell D — scroll the sidebar between the census and the gesture, then drag — REFUSED on three passes and PASSED on a fourth, because that scroll left a PROJECT row at y = 490 against the planned area row's y = 491. Nothing incorrect happened (the drag driver's order + assignment-digest asserts held), but the same-app stale-frame class the leg exists for is only caught by luck of the scroll offset. Fix: compare the row's harvested TEXT as well as its frame — the planned row's title is already in hand where `jxaSidebarDragScript` is built — and re-certify cell D across several scroll offsets, not one. The cross-application legs (frontmost, containment, occlusion) do not use frames and are unaffected. Evidence: [lab/ptrgd1-pointer-guards.md](lab/ptrgd1-pointer-guards.md) §4 "D again, under §8".


- **A fired reminder BANNER broke the Repeat-dialog shape probe once, with `-1700` — unmeasured (v0.20.9 release gate, 2026-09-03).** While the gate was driving the routed guest, a 12:00 reminder fired and the shape probe failed with `-1700` on that attempt; it is the DEFAULTS3 failure shape and it has not been measured. PTRGD1 §8 settles what a banner does to a POINTER gesture (it is not display-sized, so it is never exempt: the guard refuses and names it). What is open is the other half — whether a banner can break a dialog-shape READ, which is an AppleScript address rather than a screen coordinate, and if so how. One cell: seed a to-do with a reminder, let it fire, and drive `make-repeating` across the banner's lifetime (arriving, standing, dismissing), recording the shape probe's verdict at each phase. If it reproduces, the shape probe wants the same treatment the census got in FGRD2 — a named partial verdict instead of an opaque error. Evidence: [lab/ptrgd1-pointer-guards.md](lab/ptrgd1-pointer-guards.md) §8.


- **`lab/scripts/research-acfut1.sh` arms `trap cleanup EXIT` AFTER `lab_wait_for_ssh` (found by PROVREM1, 2026-09-03).** The same ordering bug orphaned a 50 GB scratch clone in PROVREM1's own driver when a boot outlasted the 360 s ssh wait (this host boots a clone in 6–7 min); the PROVREM1 driver was fixed in #708, ACFUT1's was not. Move the trap above the first wait and raise the ssh wait to 600 s; sweep every `lab/scripts/research-*.sh` for the same shape while there. Also PROVREM1 §8's open cells (someday-with-reminder branch — `H-REMINDER-SCOPE` still refuses `someday` unmeasured; the evening→evening corner; GUI reminder-only clear; sync) are VM-batchable follow-ons, filed there when a batch forms. Evidence: [lab/provrem1-provisional-reminder.md](lab/provrem1-provisional-reminder.md) §8.

- **Every after-completion promote emits a FALSE disclosure** (CNCAC2 §10, 2026-08-28): *"the series … already has a materialized occurrence, which this rule shape is not expected to produce yet"* — the citation (CNCAC1 §7.1) is right and the inference wrong: §7.1 says the series has no CURSOR, not no occurrence (it has exactly one, the preserved seed). Fix the #635 spawn-expectation map's cursor-vs-occurrence conflation. Riding the same doc section: the project-verb after-completion+deadline refusal question, and the promote-beep NON-reproduction (0/4 in CNCAC2 vs CERTSWEEP1's 3/3 on fixed-rule promotes — state-dependence to pin before the BEEPSEN1 bisect entry above spends effort on the wrong trigger).

- **Drive pre-flight refusals surface with the wrong error CLASS (AXDRAG5 residue, reconfirmed by SBRES1 2026-08-31).** A pre-flight refusal (tall-section wall, sidebar-normalization refusal) surfaces as `verify-failed:silent-noop` ("transport failed") — honest copy, wrong class: zero gestures ran and zero mutation landed, so it should be a `blocked`-family refusal; map the acting script's pre-flight refusal exit to the blocked shape instead of letting the pipeline wrap it as a transport failure. Evidence: [lab/axdrag5-field-stall.md](lab/axdrag5-field-stall.md) §"What remains", [lab/sbres1-sidebar-resolution.md](lab/sbres1-sidebar-resolution.md) §"What remains".

- **The banned-vocabulary regression tests do not scan `skills/**` (2026-08-31, #666's finding).** [design/surface-copy.md](design/surface-copy.md) rule 6 names the bundled skill as governed copy, but the enforcement tests scan only CLI `--help` and MCP descriptions — the shipped SKILL.md already uses vocabulary the doctrine bans. Either extend the scan to `skills/**` (and fix the existing violations) or amend rule 6 to match enforcement; don't leave the rule claiming coverage it doesn't have.

- **Deadline-bearing source promote: DO WHAT THE GUI DOES (ruled 2026-09-03).** `make-repeating` on a to-do that already carries a deadline currently lands the GUI's own default — a deadlined series anchored on the due date with the offset pre-filled (DEFAULTS1 §deadline) — and our expected-delta oracle calls it verify-failed (exit 3, DEFAULTS2 §6). Ruling: the pre-filled deadline IS the default unless `--deadline` overrides; fix the oracle/expected delta (and any seed shaping that fights it), certify the promote-a-deadlined-to-do arm, disclose the inherited deadline in the result. Small; waits behind the 0.20.8 hotfix for the VM.

- **The release workflow has no retry and `test/unit/bench-world.test.ts` blows its budgets on contended hosted runners (v0.20.8 cut, 2026-09-03).** The `publish` job failed TWICE on the same commit the PR's `check` job had passed — `bench-world` took 75 s on the release runner vs 4 s on the PR runner (~20× degradation, both `ubuntu-latest`), the suite 305–437 s vs 69 s; two `gh run rerun --failed` cycles before the tag published. The file's own header records two prior budget blowouts (#259, #281). A pushed tag blocked by a slow runner is the worst failure shape for a release path: either raise/scale the bench-world budgets (or mark it timing-fragile and exclude it from the release job's check), or give the release job a bounded retry. Small code.

- **WG-13 needs a LIVE lab lock — a suite row that writes 10,001 clusters and asserts the stored prefix (2026-08-27).** The content-field ceilings are unit-pinned only; the [assumption register](reference/assumption-register.md)'s WG-13 row carries the gap explicitly (`⚠ no live lock`). `test/unit/field-limits.test.ts`, `test/unit/param-schema.test.ts` and `test/engine/write-pipeline.test.ts` all encode the constants, and nothing writes an over-long payload through the wire, so the numbers could drift under a future Things without a single test noticing. Encode the row the register names: dispatch 10,001 grapheme clusters and assert the stored value is exactly the 10,000-cluster prefix. The 4,000-unit title cut and the 100-item checklist count are the same shape and ride along cheaply. The drift direction that matters is a ceiling FALLING — caught today only post-write, by `truncationDetail` naming the truncation rather than reporting a bare mismatch; a ceiling RISING is harmless (we would refuse a value the app had started accepting).

- **Truncated-write rollback — deliberately UNSHIPPED by NOTECAP1; build only if a ceiling ever moves (2026-08-27).** When the post-write backstop finds a prefix landing it now names the truncation precisely (how much of what was sent actually landed, and that the item is holding a partial value) but does not undo it — by then the previous value is already gone. A compensating write is composable from what exists: the promote-clone failure path is the shipped precedent for reasserting a captured pre-state after a leg lands wrong. It stays unbuilt because it is **unreachable by construction today** — the pre-dispatch refusal means nothing over a ceiling is ever sent, so the rollback would be dead code with no honest way to exercise it. The trigger to build it is a ceiling moving under a future Things, which `truncationDetail` will report. Recorded here so the omission reads as a decision rather than an oversight.

- **`rt1_instanceCreationStartDate` is documented wrong for a SAME-DAY rule — sweep the prose and correct it** ([lab/fgrd1-focus-guard.md](lab/fgrd1-focus-guard.md) §8, 2026-08-27; flagged there as wanting a maintainer's ruling). The codebase calls the column "the app-materialized first occurrence" (`src/write/promote-clone.ts`, plus the sibling "app-materialized next occurrence" phrasings in `src/read/`, `src/model/` and [design/read-shape-doctrine.md](design/read-shape-doctrine.md)). For a rule whose first occurrence is TODAY that is simply false: committing it makes the app materialize today's instance immediately and ADVANCE the cursor, so the column names the NEXT slot — measured `icStart = next` a week out, `icCount = 1`, with a live instance sitting on the requested day. The CODE is already correct (#625's fix accepts either oracle — the cursor naming the requested date, or a materialized instance on it); only the prose lags. Correct the doc comments and the reference docs to say what the column actually is: the spawn cursor, the next slot the app will materialize, which coincides with the first occurrence only while that occurrence has not already been created.

- **`update --completed-at` writes the ambiguous summary but its path never consults it (#602 flag, 2026-08-25).** `runUpdateDates` has no `replayIfApplied` gate of its own — `client.runUpdate` calls it directly with the caller's `opId`, and the mixed attribute+timestamp patch already records the key TWICE (attribute update + composite summary). Wants its own look: which record the key should ride, then the gate. Not a drive-by.

- **Composite results report the STATUS leg's vector/tier even when the mint ran at ui/3 (CERTSWEEP1, 2026-08-25).** The returned result says `url-scheme`/tier 0 while the audit summary correctly records ui/3 — the caller-facing disclosure understates disruption. Small honesty fix in the composite result assembly (src/write/template-mutation.ts).

- **`project.add` seeds children that NOTHING asserts — the one shallow spot the exhaustive-map sweep found (2026-08-23).** `project.add`'s `expectedDelta` asserts title/notes/when/deadline/area/timestamps but says nothing about `todos` or the structured `items` (headings + rich children), so a project born with an EMPTY body still verifies `ok` — the silent-drop class `things:///json`'s bare-string-array rejection (§9y) already produced once for checklists. A to-do's `checklistItems` IS asserted (`checklistTitles`); projects have no child-list field in the delta vocabulary to compare against. Fix = a `childTitles`/`childCount` getter in `src/write/verify/delta.ts` plus the assertion — but it needs PROBE EVIDENCE first that an imported child list is readable inside the verify window (poll timing; whether a heading-bearing `items` import lands atomically), because a premature assertion turns a good write into a false `verify-failed`. Marked in place at `projectAdd`'s delta in `src/write/commands.ts`.
- **Adopt the composite mutation lock in the REMAINING multi-leg orchestrators.** The mechanism shipped and the promote verbs (`make-repeating`, `add-repeating`, the template-direct clone) now hold ONE lock end-to-end: `withMutationLock` in `src/write/lock.ts` (async-context-scoped reentrancy) plus `runComposite` in `src/write/pipeline.ts` (wrap the mutating tail; the refusal is the pipeline's own `blocked`/`lock` shape). Still per-leg, and still interleavable with another writer at their leg boundaries, are the other read-modify-write composites: `clear-reminder` (reads the original date, clears, restores — an interleaved write to the same row restores the wrong value), `edit-checklist` (whole-checklist read-modify-write, so a lost update), `heading` archive/unarchive, and the two big ones, `move.ts` (variadic) and `reorder.ts` (whose bounce protocols park a row away and re-enter it, and whose scratch-container protocols create and delete a container mid-verb). Adoption is ~4 lines each — `return runComposite(deps, op, async () => { … })` around the mutating tail — but `move.ts`/`reorder.ts` are 3,000-line files whose entry points re-indent wholesale, so they want their own PR rather than a drive-by. Same test shape as the promote cell: every leg under one lockfile inode, gone afterwards.
- **Undo session-qualified actors (`mcp:<session>`).** The optional fourth layer of undo actor-scoping: session-qualified actors with prefix matching on `by:`. PARKED-CONDITIONAL — build only if parallel agents become real.
- **`--markdown` output mode.** A third render target alongside TTY and `--json`: YAML frontmatter for metadata, `- [ ]`/`- [x]` for to-dos/checklists, `##` for Things headings, project title as `#`. It is a RENDERING of the same data, never a third contract (derive from the JSON-shaping layer, drift-tested); NO markdown import/roundtrip in scope. Likely sweet spot: agent contexts + piping into notes. *(Mike: do NOT spec yet — a future design discussion.)*
- **Tag shorthand — `things @errand` (explicit-only).** Bare-noun routing of a tag NAME is rejected (collision surface; a rename could silently flip which view renders — see [design/decisions.md](design/decisions.md)). The open sliver is whether an explicit sigil (`things @errand` → `things anytime --tag errand`) is worth it at all, or whether tags stay explicit-only via `--tag`. Needs deliberate design; not scheduled.
- **Stale-DB stderr warning on ordinary reads.** `doctor` answers "data may be stale — Things is not running" on demand (sync-health section); whether every read should emit a one-line stderr nag is a separate UX call that overlaps the display-preferences / chrome decisions above.
- **CI actions v4 → v5 bump.** The release workflow emits Node 20 deprecation annotations on `actions/checkout@v4`, `upload-artifact@v4`, `download-artifact@v4`, `setup-node@v4` (forced onto Node 24). Non-blocking today; bump the four pins in one pass and watch one release-workflow run go green.
- **Cross-terminal glyph audit.** The CLI glyph language (`src/cli/glyphs.ts` — checkbox marks, pie quarters, chips, `★/⏾/≡`) was chosen on one macOS font stack. Audit rendering across emulators/fonts (Terminal.app, iTerm2, Ghostty, kitty, VS Code, Warp × SF Mono/Menlo/JetBrains Mono/a Nerd Font, plus a CJK-wide `ambiguous=double` config and a `NO_COLOR`/piped pass). Watch for tofu on `⏾` (U+23FE) and the pie quarters, double-width drift breaking column alignment, dim-on-light legibility. Every glyph is a `glyphs.ts` constant, so retuning is one file.

## feature design — wants an ideation round before code

- **Change-watch (`things watch`) + the agent daemon — design of record now lives in [design/agent-daemon.md](design/agent-daemon.md) (v1.0 GATE via roadmap).** The full watch design (snapshot-diff engine, four layers, level-based semantics, umd-SILENT constraint, WAL-wake trigger) was transcribed there 2026-08-19 and reshaped: phase α builds the engine + `things changes` in-process; the long-running `watch` loop hosts in the launchd helper (phase γ). **β1 — the permission deputy — SHIPPED 2026-08-19** (design §3a): the TCC-identity half of the daemon exists as a dumb privileged broker. NEXT STEP: the maintainer ratifies the remaining phase plan (α → β → γ → δ), then phase α. Open maintainer calls carried in the plan §5: `modified` field granularity (recommend all-fields w/ per-event list), `changes` as a first-class command (recommend yes), project-completion as a terminal event, undo-reversal reading.
- **Helpers — enable routing + residuals (ceremony DONE 2026-08-22: bundle installed, reader granted on the live host).** **Mike:** rebuild + reinstall after the bundle PR merges, then run the ceremony: `bash scripts/build-helpers.sh && things helpers setup` (install-or-update plus one sitting that settles the reader grant, both Automation prompts, and the Accessibility toggle; it exits nonzero until every leg lands), then `things config set helpers-enabled true`; afterwards optionally revoke Ghostty's FDA. Residuals: MCP server has env/config routing only (decide if it needs a per-call flag); composite SIGKILL-immunity end-to-end stays a β deliverable (the deputy bounds the orphan window to one osascript step). *(The statSync-gating residual RESOLVED as a measured NO 2026-08-31 — APDG1: the app-data class is operation-shaped, not path-shaped; a bare WAL `statSync` never prompts. See [lab/apdg1-container-touch-gating.md](lab/apdg1-container-touch-gating.md).)*
- **`--preserve-modified` follow-ups (the flag SHIPPED 2026-08-13 — universal write flag, best-effort restore, presence-keyed `preservedModified`/`preserveFailures`, captured pre-`umd` on the audit record).** The **SYNC-SAFETY GATE is RESOLVED** (SYNC2B, 2026-08-13, golden-v2/3.22.12): the flag is **sync-safe** — a hand-written past `umd` propagates to the peer device AND survives the sync round-trip in both directions (Things Cloud treats `umd` as ordinary per-attribute synced data, not a monotonic sync clock; the accompanying content edit rides the change-log independent of `umd`; a genuinely concurrent same-row edit elsewhere re-bumps `umd` via the per-attribute max-merge, which fails safe). The "UNSYNCED-only" caveat was **retired** from the `--help`/MCP/skill/`timestamps.md` copy. Evidence: [docs/lab/sync2b-durable-account.md](lab/sync2b-durable-account.md) (SY-2/SY-2M). The TAGMOD-arc symmetric-undo question is CLOSED: the restore shipped 2026-08-13 (#464, `undo.ts` keyed on `preModDates`) and UMDZ1 (#648, 2026-08-28) measured it as native parity (⌘Z restores the stored `umd` float digit for digit) and test-locked the never-irreversible-over-`umd` invariant. The one open sliver — undo across a sync boundary — rides the durable-account sitting (VM-batchable section).
- **Markdown checkboxes → checklist import (ideation only).** Let a `- [ ]`/`- [x]` block in supplied notes convert into REAL checklist items (import a run-list in one call). Must be an explicit opt-in flag (`--checklist-from-notes`), never a silent transform. Open questions: does the checkbox block get stripped from the stored note, does `[x]` map to checked, what's the roundtrip story. Depends on NOTESMD confirming checkbox syntax is inert in the rendered note (it is).
- **Heading-demotion Phase B.** Phase A shipped (project-scoped verbs, bulk create, move/reorder vocabulary, detach family). Phase B is `project set-layout` (unbuilt). **Re-verify what Phase B still means:** `reorder --scope` was already RETIRED in 0.14.0 (replaced by the kind-neutral `reorder … --in <target>`), so the old "retire reorder --scope" sub-goal is done; the `sort --by <key>` candidate is subsumed by the query-view ruling (see [design/decisions.md](design/decisions.md)). What remains: `project set-layout` + view-order polish. Two adjacent unlocks queued from the #298 campaign: **(a) BOUNCEJSON collapse** — CAUTION first (oddities §22, CHORD2 2026-08-25): a `things:///json` batch carrying `checklist-items` discards the WHOLE payload and exits 0, so the compiler must refuse-or-strip that key and the validate-first FULL-ABORT must treat §22 as a poison class; ALSO reconsider scope — the chord vector (CHORD2 §8) may retire several bounce classes outright before this collapse is worth building. Original item: rewrite the BACK-insert bounce classes (within-heading order, project-someday fallback) in `src/write/reorder.ts` to compile ONE pre-validated `things:///json` array (~7× faster, validate-first FULL-ABORT; FRONT-insert classes stay on the URL bounce, json is index-inert there per oddities §9i); **(b) `project.move-heading-to-project`** — a new ui-vector op on the lab-confirmed ellipsis-menu recipe (HID-click the heading `…` → Move… → keyboard picker; DB oracle = single heading-row project-FK rewrite, children follow via heading FK) — needs the standard certification arm + two-key gating; closes the last heading capability gap. Spec: [design/heading-demotion-and-move.md](design/heading-demotion-and-move.md).
- **CLI exposure of `project add-repeating` structured children (design note, maintainer to refine).** The LIBRARY `project.add-repeating` now threads the full project add vocabulary including the structured `items` field (#461 — headings + children + terminal-state), but the CLI `things project add-repeating` exposes only flat `--todo` (like `project add`'s flat `--todo`). Recommendation: a repeatable `--child <title>` for the flat case + `--items-json`/stdin for the full heading structure — mirroring whatever `project add` itself grows for structured children (they should share the flag surface). A design note, not a settled shape.
- **Checklist state-token conditional replace — ETag/If-Match.** Offer "produce the entire new checklist state in one call," guarded git-`--force-with-lease`-style by a token representing the state the caller last saw (HTTP ETag/If-Match is the spec to crib). Sketch: (a) token = deterministic content hash of `[{title, completed}…]`, version-prefixed, opaque, derivable from any read; (b) the token subsumes `acknowledgeChecklistReset` (presenting a fresh token proves the caller SAW the state being replaced); (c) a mismatch returns the CURRENT items + fresh token in the error payload so an agent rebases without an extra read; (d) granular intent ops stay (they have undo a replace can't reconstruct) and gain optional `--if-match`. Honest limit to document: no atomic CAS at the app layer — the check is re-read-and-compare immediately before dispatch (window shrinks to ms, not zero). Generalizes to demoted-heading structure later.
- **An AppleScript compile leg for notes-carrying ops — the >10k unlock** (opened by [lab/notecap1-notes-ceiling.md](lab/notecap1-notes-ceiling.md) §1, 2026-08-27). The notes ceiling shipped in #626 belongs to the **URL scheme**, not to Things: `set notes of to do id …` swallowed 15,000 digits, 15,000 × U+1F600 (60,013 bytes) and 15,000 combining pairs INTACT, and a 9,000→12,000 binary search found no cut in any of the three classes — the column has no measured limit and neither does the app's notes model. But every notes-carrying operation compiles url-scheme-only (eleven `if (vector !== "url-scheme") unsupportedVector(…)` sites in `src/write/commands.ts`), so the roomy vector is *declared* in the AppleScript matrix for `todo.add`/`todo.update` and unreachable in practice. An AS leg is therefore a real capability unlock rather than a refactor — it is the only way a caller writes a note the app is perfectly willing to hold. Shape questions before code: which ops gain it (creation-time `notes`, `update --notes`, and the two append/prepend spellings — whose URL semantics are per-PARAMETER, so an AS equivalent becomes a read-modify-write and wants the composite mutation lock); whether the vector escalates automatically above 10,000 clusters or has to be asked for; and what the pre-dispatch refusal becomes on a host where the AS leg cannot run. What does NOT move: the 4,000-unit title/name ceiling is identical through AppleScript, so that one is the app's own model and stays refused. Needs the standard certification arm — the 60 KB result is a read-back on one vector, and AS quoting for a body carrying CR LF and astral scalars is unprobed.

- **Disruptiveness deployment profiles — per-op capability gating + docs.** The `THINGS_API_PROFILE` knob EXISTS (`workstation` / `dedicated-server`, `src/config.ts`) — so the profile plumbing is not the open work. What remains: gate the disruptive mutation CLASSES per profile (app relaunch cycles, AX/ui-vector dialogs, anything that steals or kills the running app), have refusals under the stricter profile NAME the profile knob, and add a disruptiveness COLUMN to the capability matrix / timezones doc keyed to the profiles. Motivation: app-restart and AX-driven mutations are untenable on a daily-driver workstation but fine on a dedicated headless Mac-mini host. *(This item homes HERE only — roadmap points nowhere at it, per one-home.)* *(Partly advanced 2026-08-28: `rescue relaunch` is the first per-op consumer of `THINGS_API_PROFILE` beyond maxDisruption — the remaining work is the per-op gating sweep + the matrix disruptiveness column.)*
