# Up-next queue (parked 2026-07-12, post-v0.8.0)

The working queue for the next session(s). Everything here was triaged with Mike on 2026-07-12 after the v0.8.0 release (PRs #73–#85: Shortcuts write vector + live validation, reversible/guarded undo, checklist intent undo, reversibility matrix, trash-cascade fix, the `things projects` sidebar mirror, upcoming window, hidden-item hints, `--when` validation, capabilities undo column). Read `docs/roadmap.md` for the long-lived doctrine; THIS file is the short-horizon pick list. Remove items as they land.

## 1. Decisions Mike needs to make (blocked on him, near-zero effort)

- **Hide `── Repeating To-Dos ──` behind a flag in `upcoming`?** The dateless resting-template section (~15 rows in his library) always renders and survives every `--until` bound (a date bound can't apply to dateless rows — deliberate, PR #77). If maximum brevity wins, add `--repeats`/`--all`-style gating.
- **Today/anytime showing checked-unswept items?** Closed items the log-move sweep hasn't passed stay checked in place in project/area views (GUI parity, completion≠logged model). Whether the flat `today`/`anytime` lists should also show them was flagged ~2026-07-10 and never ruled.
- **Stateful display preferences** (roadmap §H): config-file/env defaults for the growing toggle family (`--show-later`, `--show-logged`, `--until`, `--all`, and the #89/#93 limit knobs). Decide the config surface + precedence; wary of plist-driven defaults confusing agents. Scope grew 2026-07-12 (see §H for full context): `limitMode fixed|fit|none` (height-fit deliberately not default), `pager auto|never` (git-style `less -FRX`), and a global `--plain`/`--pretty` chrome override pair.
- **Productize the dedicated-Mac "ui" write vector?** UI1 (PR #90) green-lit it: VNC synthetic input reliably drives real GUI dialogs (repeat create verified end-to-end), and it is the ONLY path for repeat rule create/edit, sidebar ordering (P6 closed automation conclusively), and to-do↔project convert. Build decision + hardware question (a dedicated always-on Mac, or lab-VM-only offering); Accessibility/AS UI-scripting is NOT a fallback (SIP-blocked in VMs).
- **Glyph palette doc + `⧉`**: Mike asked whether `⧉` (U+29C9) was ever considered (it wasn't — whole blocks like Misc Math Symbols-B were never mined). Open offer: a curated single-cell/color-safe candidate inventory under `docs/design/` to feed future picks and the §H cross-terminal audit. Also unanswered: did Mike have a slot in mind for `⧉`?
- **When to send the Cultured Code report**: `docs/things-app-oddities.md` is effectively the draft (crashes §1/§6/§7, silent-failure families §2/§5, the double-trash black hole §6½). Last evidence gaps below.

## 2. VM lab campaigns (autonomous; batch them — they share clone setup)

- ~~**Double-trash black hole screenshots (oddity 6½)**~~ — **DONE 2026-07-12** (run `things-run-r3-20260712-171142`, PR mg/lab-round3): 4-shot report-ready sequence banked at `lab/artifacts/things-run-r3-*/oddity-6half/`, referenced from oddities §6½. Ready for the CC report submission (§3).
- ~~**scf2 round-3 probes**~~ — **DONE 2026-07-12** (research-scf3.sh): P4/P3a/P2b all reconfirmed (Shortcuts backdating DEAD, AS WORKS, URL NO-OP, json WORKS; reminder SET DEAD; Parent = DETACH footgun). **P6 CLOSED** — sdef inventory = exactly one `_private_` command (no sidebar-reorder spelling; sidebar order stays UI-only), and the **Someday reorder convention is LOCKED** (reversed wire-list, matches Inbox A6) → ready to wire as a reorder scope. See §5 follow-ups.
- ~~**logInterval enum verification**~~ — **DONE 2026-07-12**: enum = `0=Immediately · 1=Daily · 4=Manually` (NO weekly/monthly in 3.22.11); `log completed now` advances `manualLogDate`. `src/read/log-boundary.ts` comment corrected.
- ~~**Deadline-less fixed-repeat encoding**~~ — **DONE 2026-07-12**: YES the GUI makes them (it's the default), and the plist is IDENTICAL to a deadlined ts=0 rule — deadline-ness is NOT in `rt1_recurrenceRule`, falsifying the fixed⇒deadlined law for the deadline-less case. `src/model/recurrence.ts` annotated. See §5 follow-up (nonzero-offset encoding + projection fix).
- ~~**Repeating-template dated-reminder clear**~~ — **DONE 2026-07-12**: no in-place automation clear exists (reminder is a rule property, not `reminderTime`; Shortcuts clear = safe no-op, AS move-to-Inbox = clean 301 refusal, both crash-free). `todo.clear-dated-reminder`'s repeating refusal is correct and stays.
- ~~**Lab runner Shortcuts arm**~~ — **DONE 2026-07-12** (`mg/lab-shortcuts-arm`): the DSL grew a `shortcut` step (guest input file + `shortcuts run --input-path/--output-path`) + a `group:interactive` skip for the delete-class proxies; `s-suite.json` is now recurring and wired into `lab:regress`, giving the shipped Shortcuts ops (`heading.create`, `todo.clear-dated-reminder`) recurring live coverage. The §C e2e reorder coverage was already present (commit `d38b05f`, v0.8.0 — inbox/someday/headings/projects-bounce) and re-confirmed in the same change.
- ~~**§E½ UI-vector feasibility probe**~~ — **DONE 2026-07-12 (UI1, PR mg/ui-vector-probe)**: VERDICT **FEASIBLE**. VNC-drove `File → New Repeating To-Do` end-to-end (menu → daily → OK → title) and verified an `rt1_recurrenceRule` row landed (tp=0 fixed, fu=16 daily, ts=0, rrv=4) plus a deadline-less spawned instance. Reliability notes: **Accessibility is NOT granted in the golden** (`osascript` System Events → −1719), so there is no AS UI-scripting fallback — all driving is VNC clicks + VNC keyboard (hardware HID, no TCC). VNC framebuffer = 2048×1536 (physical px); menu/dialog layout is deterministic per resolution and clicked first-try; **VNC keyboard entry works** (title AND numeric fields) — the DLREPEAT "days earlier" failure is overcome (click field → 6×bsp + 6×del → type; note `vncdo type` sends letters lowercase and `super-a` types a literal 'a', so clear-by-delete, don't select-all). This green-lights a dedicated-Mac "ui" vector for the conclusively-UI-only surface (repeat rule create/edit, sidebar order, to-do↔project convert). Script: `lab/scripts/research-ui1.sh`.

### Queued 2026-07-13 (headless "closet Mac mini" research — feeds the ui-vector decision in §1)

- ~~**Locked-session vector probe (LOCK1)**~~ — **DONE 2026-07-13** (research-lock1.sh, PR mg/lab-lock1-sync1; run `things-run-lock1-20260713-091759`). Predictions CONFIRMED: over SSH under a real loginwindow lock, **(a) SQLite read, (b) `open -a Things3` launch, (c) `things:///` add, (d) AppleScript make, (e) `shortcuts run` heading.create ALL WORK**; **(f) VNC click hit the LOCK SCREEN** (woke the display to the Enter-Password prompt, never reached Things — screenshot `03-after-uiclick.png`). A locked session is a presentation/input barrier, not an execution barrier. **Lock-mechanism finding (deviation from the plan):** synthetic **ctrl+cmd+Q via VNC does NOT lock** (chord not honored), and the legacy `askForPassword` default is ignored on Sequoia — the working headless lock is `sysadminctl -screenLock immediate` + `SACLockScreenImmediate` via `launchctl asuser` (rc=0; a plain SSH shell gives rc=22/no-op). Unlock-over-VNC confirmed possible (typing the password into the lock field). AX-under-lock still unprobeable (Accessibility SIP-blocked, per UI1). Full write-up: [docs/lab/headless-research.md](lab/headless-research.md).
- ~~**Sync freshness + last-sync signal investigation (SYNC1)**~~ — **DONE 2026-07-13** (same run). No authoritative "last synced" signal exists WITHOUT a cloud account: `BSSyncronyMetadata` (the Syncrony sync-state table) is **empty (0 rows)**; `Meta`/`TMSettings`/`TMMetaItem`/`TMTombstone` carry no sync cursor/timestamp; `defaults com.culturedcode.ThingsMac` has only Sparkle updater keys; the group-container plist has only "what's new"/foreground-check timestamps. **Buildable freshness proxies (all local):** app-running (`pgrep`), `main.sqlite-wal` mtime (confirmed to advance on writes), `MAX(TMTask.userModificationDate)` (last local edit), and plist `importantInformationLastForegroundDate` (last foreground). **Unified log yielded ZERO lines in the clone** (no queryable log store in the tart guest + clock-pin skew) — the sync log taxonomy is uncharted and needs SYNC2/real hardware. Signal inventory + unverifiable-without-account list: [docs/lab/headless-research.md](lab/headless-research.md).
- **Sync conflict-semantics probe (SYNC2, networked — breaks the airgap convention, needs its own safety design)**: two clones + a throwaway Things Cloud test account (no Apple ID required); induce same-field and different-field concurrent edits offline, reconnect, observe resolution (expected per public Cultured Code material: per-attribute delta sync, last-writer-wins per field, NO merge UI, NO duplication — same-field concurrent note edits should silently drop one side). Would turn my reasoned answer into a verdict and is CC-report-adjacent documentation. Low urgency; do only with a dedicated test account and never the real account.

## 3. Needs a human present (short sittings)

- **P5**: Shortcuts delete of a NON-empty heading (delete-class consent re-prompts every run — no Always-Allow). Question: children re-parent, orphan, or cascade?
- **`.ips` capture** for the heading-schedule crash (§6) — nice-to-have for the CC report.
- **CC report submission itself** — once 6½ screenshots + (optionally) the §6 .ips land.

## 4. Calendar-pinned

- **macOS 27 public-beta regression VM** — ~late July 2026. Build a beta VM, run `lab:regress`, diff verdicts/tiers (the Things-update canary: rrv decode, fingerprint, doctor `repeats:`).
- **iOS 27 GA runbook** — ~Sept 14 2026: execute `docs/lab/things-update-runbook.md` + the Apple-Intelligence memo follow-ups (`docs/design/apple-intelligence-research.md`).

## 5. Smaller code items (unblocked, low effort)

### Approved design 2026-07-13: undo actor-scoping (implement next; files disjoint from the polish pass)

Today the audit log (`~/.local/state/things-api/audit/YYYY-MM.jsonl`) records an `actor` per mutation (CLI = config actor, MCP = `"mcp"`, undos = `undo:<actor>`) but `selectUndoTargets` picks the newest N GLOBALLY — actor recorded, never consulted. Confirmed confusion case (Mike, 2026-07-13): human adds a project via CLI, agent then calls MCP undo → the HUMAN's project is reverted. The precondition guard prevents state clobber, not intent confusion. Approved three-layer fix (Mike ratified the asymmetric defaults):

1. **`--by <actor>` selection filter** on `things undo` + a `by` param on the MCP undo tool ("undo the last N changes made by X"). Note the existing `--actor` flag names the actor recorded FOR the undo — keep both, document the difference loudly.
2. **Asymmetric defaults**: MCP undo defaults to `by: "mcp"` (agents must not clobber human edits without an explicit `by: "*"`); CLI `things undo` stays GLOBAL (human owner's Cmd+Z), with `--by mcp` for cleaning up after an agent.
3. **Undo tokens (the strong fix)**: mutation results (JSON envelope + MCP tool responses) return the audit record/txn id; `things undo --txn <id>` (+ MCP param) undoes THAT record exactly — immune to interleaving by construction. Positional `--last N` stays as the human convenience. Implementation note: compound ops already carry `txn{id,role}`; single-op records need a stable id in the result path (record ts+hash or a real id — decide in-implementation, keep additive).

Optional fourth layer, parked: session-qualified actors (`mcp:<session>`) with prefix matching on `by:` — only if parallel agents become real.

### Other queued noodling (2026-07-13)

- **Agent domain-model primer (discussed 2026-07-13, recommendation made, scope needs Mike's greenlight)**: an agent with no Things pretraining cannot intuit the domain from command enumeration alone (containment rules: area ⊃ {projects, loose to-dos}; project ⊃ headings ⊃ to-dos; projects can be area-less; headings exist ONLY inside projects; tag hierarchy + inheritance; completion ≠ logged (the sweep model); today/evening/anytime/someday/dated scheduling; repeating templates vs spawned instances; trash cascade). Layered plan: (1) ONE canonical primer (~60-80 lines, agent-optimized) as the single source; (2) condensed version prepended to the MCP server `instructions` (which already carry the live inventory); (3) a CLI page (`things help model` or similar) advertised in the --help epilog like `things legend`; (4) an **Agent Skill** (SKILL.md — an OPEN standard implemented across harnesses: Claude Code, Codex, Cursor, Pi, OpenCode, …; Mike's correction 2026-07-13, do not frame as Claude-only) as the DEEP tier: progressive disclosure lets a pretrained-on-Things model skip sections while an unfamiliar one self-guides through the full handbook; the basic primer in help/MCP instructions POINTS to it (e.g. suggesting `npx skills add mikegreiling/things-api` to go deeper); (5) extend the README "For agents" section. Two-tier model: primer = always-on and tight; skill = the handbook. Keep all surfaces derived from the one primer/handbook source so it can't drift.
- **`--markdown` output mode (parked for a future design discussion — do NOT spec yet, Mike's instruction)**: a third render target alongside TTY and `--json` — YAML frontmatter for item/project metadata, `- [ ]`/`- [x]` checkbox lists for to-dos/checklists, `##`-level headings for Things headings, project title as `#`. Design cautions recorded now: markdown is a RENDERING of the same data, never a third contract (derive from the JSON-shaping layer, drift-tested); heading-level mapping needs care; NO markdown import/roundtrip in scope (that's a write-vector minefield to consider separately, if ever). Likely sweet spot: agent contexts (LLMs parse markdown cheaply) and piping into notes/docs.

- **Resource-scoped verbs (cli-grammar Phase 2) go/no-go**: `things project <ref> add-heading "Groceries"` etc. — specced in [docs/design/cli-grammar.md](design/cli-grammar.md) (closed verb enum, fixed slots); Mike leans yes ("navigation" instinct, also the elegant home for heading ergonomics). Decide after the grammar/polish pass settles in daily use.
- **Lint-warnings cleanup (64 pre-existing, ~30 files)**: unrelated rules (`no-await-in-loop`, `consistent-function-scoping`, `no-array-sort`, …) discovered during the #100 hygiene sweep; `no-map-spread` already zeroed. One deliberate pass (fix or suppress-with-reason each) so warnings become signal.
- **O-suite formal coverage of the someday reorder scope** (noted in PR #98): scope is s-suite/e2e-locked already; adding o-suite rows is consistency polish only, needs a real VM run — fold into the next lab-touching change.

- **`doctor` sync-health section (queued 2026-07-13, design sketch; SYNC1 signals landed 2026-07-13)**: for long-running headless operation — check (a) Things app RUNNING (no app = frozen DB, there is no background sync daemon), (b) WAL activity as a freshness proxy (`main.sqlite-wal` mtime — SYNC1 CONFIRMED it advances on every app write), (c) the SYNC1 last-sync signal, with a staleness threshold callout, (d) optionally a unified-log error scan for sync failures. CLI reads could also surface a one-line warning when the app is not running ("data may be stale — Things is not running"). **SYNC1 verdict (see [docs/lab/headless-research.md](../lab/headless-research.md)):** there is **no authoritative "last synced" signal without a cloud account** — `BSSyncronyMetadata` is empty. Use the buildable-today LOCAL proxies: (a) `pgrep -x Things3`; (b) `main.sqlite-wal` mtime; plus `MAX(TMTask.userModificationDate)` (last local edit) and group-container plist `importantInformationLastForegroundDate` (last foreground). (d) is BLOCKED in-lab (log store empty in the clone). The true server-sync timestamp, if it exists, lives in `BSSyncronyMetadata` and is SYNC2/account-gated.
- **TUI-idiom follow-ups (parked 2026-07-12, full context in roadmap §H)**: height-fit/pager display modes, the width-handling polish pass (GUI-parity `…` truncation vs hanging indent — Mike leans truncation), the `--plain`/`--pretty` chrome override, and the possible far-future `things ui` interactive subcommand. All deliberately deferred; pick up with the §H stateful-prefs decision.
- **Suppress "(archived)"-titled areas?** Mike's three empty areas render `(no projects)` (correct sidebar mirroring — Things has no area-archive feature; "(archived)" is his naming). If he wants them hidden it's a deliberate divergence (config/pattern filter) — needs his call first (overlaps §1 preferences item).

### New follow-ups from lab round 3 (2026-07-12, PR mg/lab-round3)

- ~~**Wire the Someday reorder scope**~~ — **ALREADY SHIPPED (confirmed 2026-07-13, PR mg/someday-reorder)**: the "confirm same-vs-distinct code path" step this item asked for resolved the item itself — there is nothing left to wire. The someday reorder scope is a SINGLE code path shared by loose someday to-dos AND area-less someday projects (`reorder --scope someday`, one kind per call), discriminated by row type at compile time. Contrary to this item's assumption, the someday **TO-DO** arm shipped FIRST (commit `c5a41f4`, "reorder scopes headings/someday/projects"); area-less someday **PROJECTS** were ADDED to the same scope later (commit `dda2a19`, P9e). Full coverage already on `main`: catalog + `ReorderScope` (`operations.ts`), membership/wire-list (`pre-state.ts`), compile (`commands.ts`), guards + mixed-type refusal copy (`guards.ts`), CLI `--scope` help (`writes.ts`), capability-matrix line 103 (✅ shipped), reversibility matrix (`reorder` entry + exhaustiveness test), pre-rank undo, unit tests (`write-compile.test.ts` P8b two-call, `write-reorder.test.ts` to-dos/projects/mixed/in-area), and VM e2e smoke (§C tests 65/68/69 green, `list "Someday"` two-call audit records banked). PREMISE CORRECTION: the round-3 P6h 3-item probe (`[S3,S1,S2]`→`[S2,S1,S3]`) *looks* like a plain Inbox-style reversal, but the fuller lab evidence (P7e/P8b/P8c, o-suite-results §"Aggregate list scopes") established that Someday does NOT share Inbox's forward re-rank: it STACKS each sent id above the call's ORIGINAL top (anchor model), so the shipped compile uses a validated TWO-CALL protocol (push desired-bottom to top as anchor, then send the reversed wire list) — a single reversed call only coincidentally reproduces a 3-item reversal and does not generalize.
- ~~**Deadline-less fixed-repeat projection fix**~~ — **DONE 2026-07-12 (UI1, PR mg/ui-vector-probe)**: discriminator FOUND and fix SHIPPED. The discriminator is the **template row's own `deadline` COLUMN** (NULL = deadline-less, `4001-01-01` sentinel = deadlined) — universal across fixed AND after-completion, resolving it WITHOUT needing `t2_deadlineOffset` (which stays 0 in every case). A non-zero start offset also shows as `ts=−N` in the rule (3 days earlier → ts=−3), captured this round via a working VNC keyboard path. Fix: new `RepeatingInfo.deadlined` (mapper reads the column; the sentinel is nulled so it never leaks as a phantom deadline on template rows), and `src/read/views.ts` + `src/model/occurrences.ts` gate the deadline projection on it instead of `rule.type==="fixed"`. `src/model/recurrence.ts` CAVEAT rewritten; unit tests in `test/unit/occurrences.test.ts` + `recurrence.test.ts` cover deadline-less vs deadlined. Oddities §8a table updated. (This ALSO falsified the old after-completion `ts<0` heuristic — a deadlined ts=0 after-completion template exists.)
- **Repeating-template reminder storage** (oddities §8b, minor): where a repeating template's rule-level reminder time is actually stored (it is NOT in `reminderTime` and NOT in the decoded `rt1_recurrenceRule`) is unresolved — likely materialized only on freshly-spawned instances (the pre-spawned ones didn't carry it after a mid-cycle rule change). Not product-blocking (the reminder is UI-only to clear regardless); note only if a repeating-instance reminder feature is ever wanted.
