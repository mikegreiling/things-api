# AGENTBENCH roadmap & state

Living state of the bench mini-project. Update in the same change as any bench work (root AGENTS.md rule). Doctrine lives in [CONSTITUTION.md](CONSTITUTION.md) — do not restate it here.

## ⟹ RESUME HERE

**Rounds 1 AND 2 COMPLETE and integrated (2026-07-17→18, PRs #196–#220).** Everything is on main: harness, corpus v2 (27 tasks), evergreen world, three refinement rounds' accepted copy (skill v0.2, MCP conventions, CLI `.data`/`.uuid` signposts), the consolidated 31-tool MCP catalog (#219), eight bench-caught tool-defect fixes, and per-arm [ledgers](ledger/README.md) + scorecards in results/ (round0, round2-base, mcp-consolidated, transfer-gpt54). Current levels (gpt-5.4-mini): skill ~89% dev (plateaued), cli ~89% dev, mcp 84%; gpt-5.4 transfer: 96% all arms, ergonomic friction-ranking preserved. **Next candidates (none blocking, pick by appetite):** RSIM probe (deferred on machine memory; unblocks recurrence corpus + tier-3 catalog drops); cli charter one-liner (state the 58-char help-index budget — two gate-reverts wasted on it); runner fail-fast on uniform provider errors; messy-world profile for truncation/recovery families; sandbox stdin bridge for `things batch` coverage; a genuinely-small second subject model when one is servable. Ops rules: run loop batches DETACHED (nohup + watcher, never harness background tasks); prefix `export PATH="/opt/homebrew/bin:$PATH"` for bench/git commands. Subject auth: openai-codex OAuth (`npm run bench:login`), subject gpt-5.4-mini, refiner gpt-5.6-sol (`gpt-5.3-codex-spark` NOT servable on ChatGPT-account auth).

Phase 0 checklist:
- [x] Simulator write vector + fence + fixture marker + unit tests (fe27cf1; adversarially reviewed, findings fixed in ebbb1ff — fail-closed fence, `--db` split-brain, host-escape gating, applier-branch tests)
- [x] Bench scaffolding: runner/sandbox/arms/grade/report + pseudo-agent smoke — types/fixture/sandbox/arms/grade/report/runner + prompts/ + two sample tasks; `npm run bench -- --pseudo` exercises seed→sandbox→grade→report. Pi/just-bash API facts in [NOTES.md](NOTES.md).
- [x] Task corpus v1 (90659a3 — 12 tasks, 7 families, 2 validation + 2 holdout; encodings verified in tasks/AUTHORING.md)
- [x] Skill v0 (SKILL.md preamble + references skeleton; gui.md curated facts awaiting Mike's review)
- [x] Living docs (this file, CONSTITUTION, README) + root docs (roadmap §K pointer, AGENTS.md living-docs entry, CHANGELOG)
- [ ] PR for `mg/bench-harness`
- [x] Evergreen world profile (`bench/world.ts`, branch `mg/bench-world`) — lived-in library layered under task seeds; on by default (`--world-seed`, `--no-world`); worldSeed recorded per run

## Phase ladder

0. **Harness** (current) — everything above, merged as one PR.
1. **Baselines** — three arms × pinned subject panel, all splits; failure-classification report identifies leverage before any copy edits. Subjects: small pinned OpenAI models (see constitution's roles; Anthropic-in-Pi is blocked by the third-party-harness policy — optional sanctioned `claude -p --model haiku` cell, low volume). Two auth paths: `--provider openai` (OPENAI_API_KEY in env) or `--provider openai-codex` (ChatGPT-subscription OAuth via `npm run bench:login`). Candidate codex-OAuth cells: subjects `gpt-5.4-mini` / `gpt-5.3-codex-spark`, refiner `gpt-5.6-sol` — all reachable under one ChatGPT subscription (mind the subscription rate caps + softer model pinning noted in the README before running large sweeps this way).
2. **Refinement loops** — worktrees `mg/loop-cli-help` / `mg/loop-skill` / `mg/loop-mcp`, each round based on the same pinned integration commit; checkpoint digest to Mike per batch (≤5 iterations or 2 consecutive no-accepts). **Driver built** (`bench/loop.ts` + `bench/loop-core.ts` + `bench/refiner.ts`, driver merged #201, ledger follow-up on `mg/bench-ledger`, not yet run live): digest → refiner → allowlist → fmt/check gate → re-bench → accept/revert-by-metric-ladder → commit; per-arm file allowlist enforced; gui-semantic skill edits parked as `needs-mike.patch`; usage fail-safe (`--token-budget` default 12M — 60% warning, clean resumable abort at 100% exit 8 or 5 consecutive provider errors exit 9); emits `bench/loop-state.json` (append log) + per-batch `checkpoint.md`. **Post-hoc debriefs + per-arm knowledge ledgers** ([`bench/ledger/`](ledger/README.md)): after every re-bench a second refiner call attributes the delta and distills one transferable lesson; each candidate (accepted/reverted/parked) becomes an append-only entry in its arm's ledger (committed so it rides the arm's PR), and each refiner charter is seeded with that arm's most recent lessons (own-arm only; cross-arm transfer is human-mediated at integration). Decision math, abort paths, debrief, and ledger unit-tested with a fake refiner (`test/unit/bench-loop.test.ts`). **Edit contract** (`mg/bench-edit-contract`, from loop-skill batch 1 where `gpt-5.6-sol`'s unified diffs died at `git apply`): the refiner now emits exact `edits: [{file, find, replace}]` (+ optional `creates`) validated (allowlist + unique find) and applied atomically, with one feedback retry and an `apply-failed` ledger outcome on giving up; a real `git diff` is computed post-apply for artifacts.
3. **Integrate & merge** — promotion per constitution; A and C file-disjoint (parallel PRs), B rebases on integrated CLI; final cross-arm report.
4. **Parked** — CI smoke subset; Tart-VM replay validating simulator fidelity; VM-graded long-tail pack (ops the simulator can't apply — recurrence is the expected first tenant); open-weight subject panel; second model family for transfer testing; skill distribution in the npm package; `things help views` topic if the GUI-perception family shows bare-CLI need.

## Round history

- **Skill round 3 — the recurrence round (2026-07-18, batch loop-skill-r3).** Two-step: (1) **curated `references/recurrence.md` v1** (RSIM-grounded rule vocabulary incl. the marquee example) lifted marquee-yearly from 1/9 (round-0 sweep) to 2/3 on the skill arm before any loop iteration; (2) the loop then **accepted a one-line contract fix** (required `--interval`, `--allow-disruptive` gating incl. dry runs, add-then-convert chain, multi-weekday-in-one-rule worked example): **dev 52/63→59/63 (94%), validation 15/24→20/24 (83%), multi-weekday 1/3→3/3** — the decomposition failure mode eliminated. Iter 2 reverted (dense-bundle lesson holds at two sentences). **Batch ended in a crash, not a stop**: the 33-task corpus outgrew the 12M default budget and the runner's exit-8 cap surfaced as an unhandled subprocess error instead of the loop's clean budget abort — ledger entries reconstructed from loop-state (provenance-marked), checkpoint absent. **Queued fixes: ~~loop treats subprocess exit 8 as the clean budget-abort path~~ LANDED (`mg/loop-budget-abort`) — a runner exit-8 (its `--max-total-tokens` cap, sized to the loop's remaining budget) now unwinds as the loop's own token-budget `LoopAbort` (revert unapplied patch → ledger + checkpoint with holdout SKIPPED → exit 8 + resume message) instead of crashing; the same change adds a post-baseline cost projection that WARNS loudly when `--token-budget` is below 1.5× the projected 2-iteration cost (default budget left unchanged). corpus-v3 batches still need `--token-budget ≥ 20M`.** Ledger lesson: capability facts alone don't execute — state invocation PRECONDITIONS (required flags, gates, chains) at the verb's entry point.

- **Recurrence family round 0 (2026-07-18, first sweep on corpus v3).** gpt-5.4-mini, 6 tasks × 3 arms × 3 reps: **skill 14/18 · mcp 12/18 · cli 11/18** — the hardest family yet, as designed. The marquee (last-Sunday-of-December) is the corpus's hardest task: **1/9 across all arms**; multi-weekday discovery is partial (5/9 — decomposition-instead-of-`--weekdays` is the observed failure mode, confirming the task's design). Friction inverts the usual order: **mcp 0.67 ≪ skill 2.00 / cli 1.64** — the consolidated `repeat` tool's schema *guides* rule construction where CLI flags must be discovered by trial. **Tier-3 evidence delivered: the merged `repeat` tool is now exercised and load-bearing (12/18 through it) — RETAIN; the catalog-drop question is closed.** Obvious next lever: a recurrence-targeted refinement round (rule-flag vocabulary in skill/help copy; the marquee failures are class-(d) argument construction). Scorecard: [results/recurrence-r0/](results/recurrence-r0/).

- **Corpus v3 — recurrence family (2026-07-18, link two of the recurrence chain).** 6 tasks (33 total): weekly-fixed identity replacement, after-completion identity preservation (pinned-uuid assertion), the MARQUEE last-Sunday-of-December yearly shape, multi-weekday discovery ("every Thu and Fri" — the CLI expresses it in ONE rule via `--weekdays`, so decomposition is the graded failure mode, inverting the original two-rules hypothesis), repeating project (validation), and template rediscovery + reschedule (holdout; via `show`'s `.data.detail.repeating.templateUuid` chain). Splits: +2 validation (now 8 tasks/24 gate runs), +1 holdout (now 4/12). Seed layer gained pinned uuids, declarative `repeat` templates, and `instanceOf` instances. Pseudo smoke 33/33. Next: three-arm sweep to exercise the family live (first real traffic for the MCP `repeat` tool — tier-3 evidence).

- **Round 2 close (2026-07-18): consolidation validated + transfer verdict.** (1) **MCP tool consolidation (#219, Mike-approved)** measured on the bench: mini success **61/81→68/81 (+9 pts)**, friction **0.41→0.12**, median context **−21%**, static init **16.6k→12.8k** — the leaner 31-tool catalog improved every metric at once (small models choose better from fewer tools); scorecards in [results/mcp-consolidated/](results/mcp-consolidated/). Tier-3 drops still deferred pending RSIM. (2) **Transfer baseline (gpt-5.4)**: **96% on ALL three arms** (78/81 each) with friction preserving the arms' ergonomic ranking (mcp 0.04 < skill 0.36 < cli 0.88) — the refined surfaces generalize up-tier with no arm-specific overfit; at this tier the skill's success edge ceilings out but its friction edge persists (stronger models infer the data model; procedural clarity still cuts retries); [results/transfer-gpt54/](results/transfer-gpt54/). (3) Findings: `gpt-5.3-codex-spark` is NOT served to ChatGPT-account codex auth (catalog lists it; backend refuses — zero-cost discovery); the bare runner should FAIL FAST when every run errors identically (the loop has a circuit breaker, the runner doesn't — small fix queued). (4) RSIM remains deferred on machine memory headroom (~60MB free at check time).

- **CLI round 3 (2026-07-18 overnight, batch loop-cli-r3, post-defect-batch stack).** 4 iterations, **1 accepted** (`.uuid` field clarification at the discovery surface — agents assumed a conventional `.id`, extracted nothing, and cascaded): **dev 45→48/54, validation 12→14/18, holdout 4/9→7/9** — bare CLI now near the skill arm's levels. Iterations 3–4 reverted at the GATE (fmt/check — the refiner repeatedly overflowed the ≤58-char help-index budget; charter-ergonomics note: the cli charter should state the index line-length contract). Combined with the defect batch (#216) and answer fairness (#217), the cli arm's overnight arc: 56/81 → ~89% dev / 78% validation / 78% holdout.

- **CLI defect batch 2 (2026-07-18, from loop-cli-r2 tool-defect leads).** The four leads resolved to **3 fixed + 1 refuted**, each reproduced before any change; re-run of the three affected tasks (cli × 3 reps, `bench/artifacts/defect-batch-2/`) below.
  - **Granular checklist silent no-op — FIXED (real, the worst class).** Not an applier gap (the `--check`/`--add`/`--remove` ops deliver a `todo.replace-checklist` leg the simulator DOES apply) — the model emitted `--check` and `--add` as TWO tool calls in one turn, and the agent driver ran them concurrently against the single shared sandbox DB. Two checklist full-rewrites (Things has no item-level surface) then raced: both read the same pre-state, last writer silently clobbered the other, both reported "verified." Reproduced deterministically (concurrent = 20/20 fail, serial = pass); fixed by serializing `bench/sandbox.ts` exec (a shell runs one command at a time). `longtail-checklist-edit` **1/3 (pre-fix) → 3/3 (post-fix)**. `fix(bench)` e1ac361, regression test `test/unit/bench-sandbox.test.ts`.
  - **Project-children read/write count disagreement — FIXED.** Fixture builder left projects' `openUntrashedLeafActionsCount` at 0, so `things projects --json` read 0 open children while the complete-project guard (live count) saw them. `buildBenchFixture` now reconciles the counters. `recovery-project-children-policy` **3/3 → 3/3** (the read is now honest rather than misleading mid-run). `fix(bench)` c195ec5.
  - **Invalid bare-verb `move` hint — FIXED.** `things move <ref> <dest>` suggested `things todo move <ref> <dest>`, itself a usage error (move takes `--area`/`--project`/`--heading`). The hint rewrites the positional destination behind the flag matching its kind. `fix(cli)` 8df61ad.
  - **Residual evening-verify mismatch (post-#214) — REFUTED.** The failing `gui-evening-tonight` runs wrote correctly (`--when evening` → `observed.todaySection=evening`, exit 0, SQL passes, single add, NO duplicate/verification failure). The only red was `answer[section]: got undefined` — the subject replied with BARE `{"section":"evening"}` instead of the system-prompt-mandated fenced ```json block (agent-side final-answer formatting variance, not a clock/verification/duplicate defect; #214 already fixed the verification clock). `gui-evening-tonight` **2/3 → 3/3**.

- **CLI round 2 (2026-07-18 overnight, batch loop-cli-r2).** 3 iterations, **1 accepted**: the `.data`-envelope clarification in `src/cli/help.ts` — the same IDEA that failed in round 1 succeeded with better placement (ledger feed-forward visibly steering) — **dev 38→44/54 (+6), validation 7→9/18**, friction 0.66→0.59; 2 further candidates correctly reverted. Holdout 4/9 (weakest arm on holdout — bare CLI remains the hardest surface). Digest surfaced 4 distinct tool-defect candidates (residual evening-verify mismatch post-#214, granular checklist silent no-op, project-children read/write count disagreement, invalid bare-verb move hint) — overnight defect batch dispatched; fixes land before any further cli iterations.

- **Skill round 2 (2026-07-18, batch loop-skill-r2, corpus v2 + honest fixtures).** 2 iterations, **0 accepted** (both narrow candidates regressed validation — reverted; ledger lessons banked). Corrected-fixture baseline is strong: **dev 48/54 (89%), validation 14/18, holdout 7/9, holdout friction 0.14** — the skill arm is approaching its plateau for gpt-5.4-mini; remaining failure mass is argument-construction (8) + two tool-defect classifications. **The refiner's (f) classification caught a REAL regression**: correct `--when evening` writes verify-fail under a future pinned clock (suspected #211 verify-reader clock divergence; fix in flight) — the charter's "file a defect, don't paper over it with copy" rule proving out. Per stopping rules, further skill iterations are deprioritized until (a) the verify-clock fix lands, (b) long-tail corpus growth, or (c) a second subject model changes the failure surface.

- **Round 2 baseline (2026-07-18, corpus v2, 27 tasks × 3 reps × 3 arms).** cli 56/81 · skill 62/81 · mcp 61/81; friction 0.84 / 0.66 / 0.41. MCP nearly closed the gap with the skill arm on the harder corpus (round-1 conventions holding); these numbers are the round-2 promotion reference (round-1 numbers are NOT comparable — different corpus). Four tasks scored 0/3 on ALL arms (reasoning-effective-tags, reasoning-standalone, validation-reasoning-heading-inheritance, gui-placement-room214) — triaged before any loop runs (universal zeros are as likely matcher defects as subject failures); verdicts recorded in the next entry. Scorecards: [results/round2-base/](results/round2-base/).

- **Round 2 universal-zero triage (2026-07-18).** The four all-arm zeros resolved to **2 seed-defect false zeros, 1 product read-path bug CAUGHT BY THE BENCH, and 1 genuine subject failure kept** — matchers were correct in every case, so none were loosened:
  - **reasoning-effective-tags** and **validation-reasoning-heading-inheritance** — SEED DEFECT (unsatisfiable). The bench fixture never applied AREA tags: `bench/fixture.ts applySeeds` `case "area"` seeded the area but ignored `s.tags`, and `attachTags` used `tagTask` (TMTaskTag — the wrong table for areas). So `outdoor`/`community` never reached `TMAreaTag`, capping `inheritedTagsFor` at the project tag; `count===3` was unreachable. Fixed by seeding area tags via `tagArea` (TMAreaTag). Now satisfiable — verified: `TMAreaTag` carries Garden→outdoor and a reading rep passes each task.
  - **reasoning-standalone** — PRODUCT READ-PATH BUG the bench caught. The read decode emitted `todaySection:"today"` for undated `start=active` items (prod stores `startBucket=0` on every undated Anytime to-do), contradicting Today-view membership; skill+mcp subjects keyed on that field and answered `view:"today"`. Fixed in `src/model/mappers.ts mapTodaySection` (gated to Today members only — start=active, dated, not future; field omitted otherwise) with the injected clock threaded through the read layer. Flipped standalone skill+mcp from 0/3 to 3/3.
  - **gui-placement-room214** — GENUINE subject failure, LEFT ALONE. The subject put the room number in notes, not the glanceable title the prompt asks for; a fair `gui-perception` discriminator.
  - **Corrected per-task numbers** (gpt-5.4-mini, openai-codex, 3 reps, `bench/artifacts/triage-verify/`): reasoning-effective-tags cli 0/3 · skill 0/3 · mcp 1/3; reasoning-standalone cli 0/3 · skill 3/3 · mcp 3/3; validation-reasoning-heading-inheritance cli 2/3 · skill 0/3 · mcp 1/3. All three are now SATISFIABLE (≥1 arm/rep passes each); the remaining zeros are subject reasoning variance (stopping at `search` instead of reading detail; cli-standalone is the separate cli no-parseable-JSON format issue), not harness defects — the legitimate discriminators the loop should measure.

- **MCP round 1 (2026-07-17/18, batches loop-mcp-1/-2/-3).** **1 accepted** (batch 1: operational conventions in `buildInstructions()` — resolve-then-pass dates vs the exposed clock, dry-run-creates-no-state, direct-vs-effective tags, todaySection/view-state semantics, limit-vs-all rule) — dev 21→25/33, validation 2→3/6; then 2 reverted candidates (batch 3, no dev improvement) and **holdout 5/6** on the final state. Ops saga: batches 1 and 2 were killed externally mid-iteration — diagnosed as the session harness's background-task layer (a fully detached `nohup` run completed on the same memory-starved machine); batch 1's accept predated its kill, so its ledger entry is reconstructed (see ledger/mcp.md provenance note). **Standing ops rule: run loop batches detached (`nohup` + watcher), never as harness background tasks.** Cross-arm round-1 synthesis: loaded reference material (the skill) is the strong lever for mini-class subjects; copy nudges in CLI help-index and MCP per-tool descriptions were weak levers (0 accepts in cli, both mcp accepts came from INSTRUCTIONS-level conventions); with wording plateauing on the mcp arm, structural candidates (tool consolidation, trimmed default exposure — constitution's phase-2 lever) are on deck for mcp round 2; honest accounting shows MCP is the costliest arm per task (med dev tokIn ~36k vs skill ~15k, cli ~7.5k).

- **CLI round 1 (2026-07-18, batch loop-cli-1).** 2 iterations, **0 accepted** — both candidates targeted the `.items`-vs-`.data` envelope confusion via `src/cli/help.ts` index copy and produced no dev improvement (correctly reverted; ledger debriefs banked). Finding: the signpost index may be a weak lever for output-interpretation failures — candidates for the next cli round: the `help output` topic, per-command `--json` descriptions, or error-message remediation. Validation 0/6 is genuine subject performance (bare CLI fails both validation tasks that the skill arm passes), NOT a harness fault — initially misread as a metrics bug; the investigation instead shipped the loud sweep-parse guard (#205). Holdout 2/6. Artifacts: bench/artifacts/loop-cli-1/, [ledger/cli.md](ledger/cli.md).

- **Skill round 1 (2026-07-18, batch loop-skill-2b, Mike-approved at checkpoint).** First live refinement batch (find/replace edit contract; batch loop-skill-1 was void — the old unified-diff contract corrupted both candidates, fixed in #203). 3 iterations: **1 accepted** (JSON `.data` envelope + truncation check, summary-vs-`show` tag evidence, `start`→view semantics, weekday→ISO-before-invoke, explicit area-move, outside-in nested creation with UUID chaining) — dev 23→27/33, friction 0.74→0.22, med tokens ↓2.5k, validation 3→4/6, **holdout 6/6**; 2 reverted (validation regressions). Ledger lesson (fed forward): dense prescriptive bundles raise selection cost — prefer narrowly-scoped verifiable rules. Budget: ~6.4M/12M. Artifacts: bench/artifacts/loop-skill-2b/, [ledger/skill.md](ledger/skill.md).

- **Round 0 — Phase 1 baseline (2026-07-17).** Subject `gpt-5.4-mini` via openai-codex OAuth; all 14 tasks × all splits × 3 reps × 3 arms. Scorecards: [results/round0/](results/round0/). Headline: **skill 35/42 · mcp 26/42 · cli 25/42**; friction (successful runs, mean errors) skill 0.46 < mcp 0.62 < cli 0.80; median static context cli 0.3k / skill 3.7k / mcp 16.6k. Signals: bare-CLI failures concentrate in the understanding family (reasoning-effective-tags 0/3, reads-inbox-count 0/3) + compound builds; the skill fixes most understanding failures BUT reasoning-effective-tags went 0/3 in-baseline (3/3 variance-prone — shakeout passed it; digest before copy edits); MCP's distinct weakness is write construction (writes-schedule-wednesday 0/3, gui-placement-room214 0/3) — tool-description copy candidates. Directional only (N=3). Immediately after baseline: **skill v0.1** landed (Mike curation: standalone to-dos exist; Inbox reframed as untriaged STATE — no container + no schedule, move-to-inbox un-files and un-schedules) — the loops' first A/B is v0 (this baseline) vs v0.1.

## Known limits & decisions log

- 2026-07-18: **Recurrence simulator appliers LANDED** (`mg/sim-recurrence`) — the RSIM shapes are now executable through the fenced simulator. New appliers in `src/write/vectors/simulator.ts`: `todo.make-repeating` (fixed = delete source + hidden template + one instance; after-completion = preserve source relinked as the instance, tp=1 template, no fresh row), `project.make-repeating` (same, area preserved / start→someday), `todo`/`project` `reschedule-repeat` (in-place rule replace, identity kept, creation date advanced), plus `todo.complete` extended to stamp an after-completion template's reference + next-start dates WITHOUT materializing the next instance. `project.create-repeating` is covered transitively (the `runCreateRepeatingProject` orchestrator over `project.add` + `project.make-repeating`), so it needs no direct applier or matrix entry. Rule blobs come from a SHARED decoder-validated composer lifted into `src/write/recurrence-rule-blob.ts` (bench/world.ts now imports it — one source for both bench templates and simulated writes). `planner.ts` now lets a simulating vector satisfy any `forcedVector` so the orchestrators' forced-`ui`/`url-scheme` legs resolve to the simulator under the fence. pause/resume/stop-repeat stay OUT of the matrix (no RSIM-proven delta). Tests: per-op RSIM shape assertions via SQL + the real `decodeRecurrenceRule`, incl. the marquee last-Sunday-of-December (mo/wd/wdo) rule, all end-to-end through `runMutation` (`test/engine/write-simulator.test.ts`). A repeat-heavy corpus family can now be authored.

- 2026-07-18: **RSIM answered — recurrence simulator appliers + corpus tasks unblocked; tier-3 catalog decision now evidence-capable** ([docs/lab/rsim-results.md](../docs/lab/rsim-results.md), PR mg/rsim-probe). Recurrence-creation row shapes characterized end-to-end in a clone: creation spawns exactly ONE instance (fixed = delete source + fresh template/instance; after-completion = reuse source as the instance); completion schedules the next occurrence without materializing it; reschedule is in-place. The simulator may now grow recurrence appliers to these rules, a repeat-heavy corpus family can earn real MCP tier-3 usage evidence, and the deferred tier-3 catalog drop is now decidable on data instead of the circular "never called" signal.

- 2026-07-18 (Mike): **MCP tool-catalog consolidation landed** (structural round-2 MCP lever — the "tool consolidation vs the ~36k/task cost" candidate flagged in the RESUME HERE block and the round-1 synthesis). Grounded in the tool-usage mining of the 350 MCP-arm runs (30/48 tools never called; 61% of the catalog paid for unused surface; the 7-tool recurrence cluster = 24% of the catalog at zero calls). Twenty-two singletons folded into five verb-parameterized tools (`repeat`/`update`/`heading`/`delete`/`set_status`) plus `reorder` absorbing the sidebar-area reorder — zero functionality loss, every op still reachable behind a `scope`/`kind`/`action` discriminator. Measured static init (same `bench/arms.ts` method + fixture): **16,680 → 12,594 tokens** (catalog −4,086 / −25%); realized under the report's modeled ~31% because faithful union-of-params entries run larger than its "1.3×-largest-member" heuristic. **Tier-3 deferred deliberately**: the never-called escape hatches (`run_operation`, `batch`, `undo`, `changes_since`, `capabilities`, `doctor`) are untouched — the recurrence "never called" signal is circular (no repeat-heavy family exists in the corpus yet), so dropping/gating tier-3 waits on the RSIM recurrence-simulator probe (docs/lab/probe-backlog.md §C) landing a repeat-heavy task family to earn real usage evidence. Next: re-bench the MCP arm on the consolidated surface to measure the effect on success/friction, not just static cost.

- 2026-07-18: **Corpus v2 landed** (round 2 step 1): 15 → 27 tasks; validation split 2 → 6 tasks (gate now rests on 18 runs), holdout 3 → 4; long-tail families within simulator coverage (checklist granular edits, cancel-vs-complete, trash restore, tag nesting, project-complete children policy, undo) + 2 gui-perception tasks (evening bucket; upcoming locate). Batch task dropped — sandbox forwards neither VFS files nor stdin to the `things` child (coverage backlog: stdin/tempfile bridge). All 27 pass the pseudo smoke on the world profile.

- 2026-07-17: **Metrics anomaly — RESOLVED (round-1 prep, `mg/bench-round1-prep`).** The hypothesis was correct and is now confirmed against pinned source. pi-ai's Responses backend (`node_modules/@earendil-works/pi-ai/dist/api/openai-responses-shared.js`, shared by the `openai-codex-responses` api) builds `usage.input` as `max(0, input_tokens − cached_tokens − cache_write_tokens)` — i.e. **cache-DISCOUNTED**. The old runner recorded that discounted number as `tokensIn`, so cache-friendly arms (MCP's 16.6k static catalog is cached hard after turn 1) under-counted true context — the 2.3k vs 16.6k gap. **The split is genuinely reported, so no estimate is needed:** the `Usage` type (`dist/types.d.ts`) carries `input`, `output`, `cacheRead` (= `cached_tokens`), `cacheWrite` (= `cache_write_tokens`), `reasoning?` (subset of `output`), and `totalTokens` (raw provider total). Fix: `runner.ts` now records `tokensIn = input + cacheRead + cacheWrite` (the honest total the model processed) and a new `tokensInCached` (= `cacheRead`); `report.ts` shows `tok_in` (total) plus a `cached` column; `types.ts` documents both. Cross-arm token comparisons can now use `tokensIn` directly. Findings + verbatim code path recorded in [NOTES.md](NOTES.md) ("Token accounting"). Round-1 also lands here: the standalone-to-do corpus task (`tasks/reasoning-standalone.json`) and standalone to-dos in the world profile (`world.ts` `STANDALONE_TODOS`, tested in `test/unit/bench-world.test.ts`).



- 2026-07-17: **HOST-ESCAPE INCIDENT + quadruple-fence response.** During Phase 0, a scaffolding-worktree smoke ran the write sample on a branch that PREDATED the fence: `THINGS_SIM_WRITES=1` was inert there, `defaultVectors` returned real transports, and 4 real `things:///add?title=Water%20the%20plants` dispatches reached the operator's live app (each verify-failed against the fixture, masking the escape as a benign FAIL); a fixture-uuid reveal also popped a modal, and bench audit records polluted the real audit trail via wrong env names (`THINGS_STATE_DIR` vs the real `THINGS_API_STATE_DIR`). Cleanup: audit scrubbed with backup; the 4 synthetic todos left for Mike's manual deletion (prod writes are forbidden even for cleanup). Hardening (all shipped, all tested): (1) `defaultVectors` marker backstop — a `benchFixture`-marked DB (env or client-opened) can NEVER be paired with real transports, throws without the full fence; marker made opt-in in `buildFixtureDb` so ordinary unit fixtures still exercise real-vector seams; (2) fence requires scratch `THINGS_API_STATE_DIR`/`THINGS_API_CONFIG_DIR`; (3) reveal gate keys on the opened DB's marker, not just env; (4) runner zero-dispatch preflight — a `--dry-run` canary must compile to `simulated:todo.add` before any task runs; (5) schema tripwire — `SIMULATED_DATABASE_VERSION` pin refuses drifted fixtures until the modeling chain is re-verified (drift-runbook step 5). Standing lesson: never run bench smokes from a worktree whose base predates the current fence — the preflight now makes that impossible to miss.

- 2026-07-17 (updated 2026-07-18): Simulator presents as the `url-scheme` vector; ops outside its coverage still read as "unsupported" to the agent even where real Things supports them — corpus must stay within coverage until the VM pack exists. **Recurrence is now WITHIN coverage** (2026-07-18, RSIM appliers): `todo`/`project` make-repeating (both fixed and after-completion), reschedule-repeat (both scopes), the orchestrated `project.create-repeating`, and the completion-side after-completion scheduling all apply RSIM-derived row shapes, so a repeat-heavy task family can now be added. Still OUT (no RSIM-proven delta): pause/resume/stop-repeat — deliberately absent from the simulator matrix (unsupported beats guessed). The forced-`--vector` fidelity gap is now BRIDGED for the simulator specifically: a leg that forces a vector (the repeating-project orchestrators force `ui`, coercion legs force `url-scheme`) resolves to the simulator under the fence (`planner.ts` treats a simulating vector as satisfying any `forcedVector`); a forced `--vector` against the REAL transports still reports unsupported where the CLI would succeed.
- 2026-07-17: Corpus assertions are SQL + structured-final-answer matchers only (no library imports in grading), so grading stays decoupled from the surfaces under test.
- 2026-07-17: Adversarial review of fe27cf1 — CLEAN on the big fidelity questions (guards/when-validation/hazard gates all run on the simulator path; evening/someday encodings agree with the read path; undo round-trips; audit marker is display-only). Fixed in follow-up: fail-closed fence (half-set env no longer falls through to real transports), env-vs-`--db` split-brain, host-escape gating (`things open`, setup/doctor probes) under the sim fence. Open question parked: real-app behavior on duplicate `tag.add` titles (simulator inserts; app merge behavior unverified).
- 2026-07-17: Harness requirement (from the same review): the sandbox's `things` command must construct the child env from run config alone — agent-visible sandbox env state must not be able to unset `THINGS_DB`/`THINGS_SIM_WRITES`.
- 2026-07-17: **World profile SHIPPED** (`bench/world.ts`): ~8 areas / ~20 mixed-state projects / ~450 rows incl. ~3y logbook, 9 recurrence templates (all four frequencies, fixed + after-completion, nth-weekday incl. last-Sunday-of-December) as decoder-validated XML plist blobs + linked instances; enforced invariants: zero Today/overdue contribution from world rows (danger-zone read tasks stay exact), corpus-collision fence (normalized-equality + LIKE-pattern), deterministic (seed, clock) via rng-derived uuids. Sample task `reads-inbox-count` redesigned from a global inbox count (incompatible with a rotating world) to a scoped inbox lookup. Polish parked: verb-object pools can produce goofy combos ("Draft the bike tires") — harmless distractors.
- 2026-07-17 (Mike): **Evergreen world profile** — fixtures are regenerated per run from a declarative lived-in library (engineer-life areas/projects/years of logbook/checklists/reminders/recurring templates), all dates as OFFSETS from the pinned task clock (never absolute → never stale), seeded-PRNG inventory rotation, layered UNDER per-task seeds with a title-collision check and declared world invariants (v1 world is well-groomed: no stray open-overdue items, so global-count task assertions stay valid; a "messy world" profile is future work for truncation/recovery families). Prod DB consulted for SHAPE STATISTICS ONLY via scripts/prod-read.sh (survey in session scratchpad, never committed); zero content crosses over.
